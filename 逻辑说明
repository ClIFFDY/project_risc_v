# 逻辑说明 —— RISC-V 6 级流水：指令 data 在流水线中的逐拍位置

> 参考文档。所有时序均对照 RTL（sources_1/new/core/*.v）并用 iverilog 探针 tb 逐拍核对
> （探针记录见工程历史，不再保留；下文中“clk0..clkN”即该逐拍记法：clk0 = 该指令被 itcm 锁存的那一拍）。
> 格式约定见 §0。写日期 2026-09-04。

---

## §0 读法与约定

### 0.1 时间轴记法

CPU 全部为上升沿触发。本文档称 **“clkN 拍”** 为第 N 个时钟窗口，即第 N 个上升沿之后的
半个周期（与探针 tb 在下降沿采样到的值一致）。“某一拍的上升沿”= 该窗口开头的那个上升沿。

每类指令的表格都以**它自己的取指拍为 clk0**（各表互相独立，方便单看一类）：

- **clk0 取指沿**：该上升沿 icache 按 pc 取指，`inst_out` 锁存本指令；同一沿 pc 已更新为
  本指令地址 +4（顺序下一条地址）。
- clk0 拍内：`pre_decoder` 的**纯组合译码**（jal / br_en / jalr_pre / 各 offset）看到的就是本指令
  （它此刻在 itcm 输出上）→ 决定 clk1 上升沿 pc / 取指是否重定向。

### 0.2 模块边界与位置说法

- 本工程用“**指令在 XX 与 YY 之间**”表示位置 = “指令数据已被 XX 锁存（在其输出寄存器里），
  YY 将在下一上升沿采样它”。下文表格里直接写成“XX 锁存/未锁存”或“在 XX↔YY 之间”。

模块链与锁存边界（一条普通指令经过的全部边界，+1 表示比前一边界晚 1 拍）：

| 边界（模块） | 相对取指 | 该处锁存的内容 | 备注 |
| --- | --- | --- | --- |
| icache（取指） | +0 (clk0) | inst_out = 指令字（唯一取指源） | 同一沿 pc/aux 更新；缺失时 busy 按住流水线 |
| pre_decoder | +1 | rs1/rs2/rd、opcode_dec、func10、imm、各 offset、dec/lsu 标志、aux_addr_out、pc_operand | 它的**组合**输出(jal/br_en)在 clk0 就被本指令驱动 |
| mem_buf | +2 | 上述全部字段的纯透传缓冲 | STALL/FLUSH 时冻结或清零 |
| decoder（ID/EX） | +3 | r1/r2_data_out(操作数)、rd_out、we、alu_func4、csr_* 、jalr_target_q、success/br_fail/irq_ret/trap、jalr 等 | **操作数在此拍定型**；本拍组合出 alu 结果 |
| wb_reg | +4 | result、rd、we | alu 无寄存器，wb_reg 是 EX 结果寄存器 |
| regfile 写 | +5 | regs[rd] ← wdata | we/loaded 门控，x0 不写 |

即对无冲突指令：**取指 clk0 → 预译码 clk1 → 缓冲 clk2 → 译码/EX clk3 → wb_reg clk4 → 写回 clk5**。

寄存器堆读口在 pre_decoder 侧（r1/r2 = rs 字段）；regfile 读 + forw 前递产生的
`r1/r2_data_final` 供给 decoder（及 lsu），decoder 在 clk3 才把它们锁进自己的输出寄存器。
因此两条相邻 ALU 指令（后一条的 rs=前一条的 rd）靠 forw 直接解决，见 §2。

### 0.3 stage 状态机（controller，组合输出，不是寄存器）

| stage | 产生条件 | 行为要点 |
| --- | --- | --- |
| EXE | 默认 | 各模块正常锁存 |
| FLUSH | `flush_irq`（irq / irq_ret / irq_act）或 `flush_jump`（jalr_fail / br2 / br3）或 `exc`（异常，见 §29）任一 | 该沿 pc/itcm 重定向取指；pre_decoder/mem_buf/decoder 清零（冲刷未提交指令）；irq_bubble 计数 |
| STALL | stall（lsu load-use）或 i_busy | pc/itcm/pre_decoder/mem_buf 冻结；decoder/lsu 保持；forw `stalled` 拍后一级前递 |

> **2026-09-25·异常独立成位（`flag_bus[9]`）**：`trap`（ecall/ebreak）原来并进 `flush_irq`，
> 而 `flush_irq` 自己又是"被冲刷"的条件之一 ⇒ **经 `flag_bus[7]` 自门控，冲不掉自己**。
> 现在所有机器模式异常（trap / 非法指令 / 三种指令地址非对齐 / 访存非对齐）在 `trap_unit` 里
> 仲裁成一个寄存器源 `exc`，占 `flag_bus[9]`，与 `flush_irq`（bit7）、`flush_jump`（bit6）
> 并列成为第三条冲刷源。低 6 位（停顿/在途位）位序未动。

> IDLE(2'd0) 实际上**没有定义**：controller 的组合输出只可能取 EXE/STALL/FLUSH，从不置 IDLE；
> 各模块 localparam 与分支里的 IDLE 是历史遗留的死代码，当前不会触发。
>
> **2026-09-06·取指去 stage 化**：`fetch_addr` 的组合选择不再按 `case(stage)`
> （EXE,STALL / FLUSH / IDLE）分类，改为纯事件信号优先级 mux：
> `irq > irq_ret > br2 > br3 > jalr_fail > br1 > jal > jalr > 顺序 pc_addr>>2`。
> 依据：stage 是这些信号（+stall）的函数（见上表），pre_decoder 组合块又已按
> `stage∈{EXE,STALL}` 把 jal/jalr/br_en 关零、controller 保证权威冲刷信号同拍必 FLUSH，
> 故两组信号永不同拍互斥，拼成单级优先级后在可达状态上与旧 case 严格等价；
> STALL 冻结下无事件信号 → 落回 `pc_addr>>2`，同旧 EXE/STALL 分支。
> 附带作用：切断综合 time-loop 里 `stage→fetch_addr→icache.busy→stall→stage`
> 的 `stage→fetch_addr` 直连边。
>
> **2026-09-19·该 mux 已从 itcm 移入 icache.v**（itcm 退居后备存储器），优先级不变，见 §23。

### 0.4 本文五类

按与数据路径的关系分五节：ALU 运算(§2)、分支预测(§3)、跳转 JAL/JALR(§4)、
访存 load/store(§5)、CSR/中断(§6)。每节：先给典型程序，再给逐拍表（行=流水位，
列=clk），关键控制信号单独成行。clk 用 `c0 c1 …` 缩写表头。

---

## §1 流水线总览（一段混合代码的背靠背位置）

例：5 条不同类指令连续取指时，各自相对取指拍的位置（假设无冲突/无预测错误）：

```
c0:   addi 取指
c1:   addi→predec      jalr 取指(若 B 是分支则本拍被 br1 重定向)
c2:   addi→mem_buf     (上条)→predec     br 取指
c3:   addi→decoder     (上条)→mem_buf    (上条)→predec     ld 取指
c4:   addi→wb_reg      …                                     (上条)→predec  st 取指
c5:   addi→regs写回    …                                                  st→mem_buf(lw→lsu)
...
```

即任意时刻流水里最多 5 条未提交指令，指令按“取指→…→写回”每拍推进一位。
下面各节单独看每类的边界与冲突。

---

## §2 ALU 运算类（R 型 / I 型 / LUI / AUIPC）

例：
```
0x00 addi x1,x0,7    00700093
0x04 add  x2,x1,x1   00108133   ← 后一条的 rs1=x1 依赖前一条 rd
0x08 addi x3,x1,1    ...
```

### 2.1 addi x1,x0,7 逐拍位置

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 | c5 | c6 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| pc | 0x04 | 0x08 | 0x0c | 0x10 | 0x14 | 0x18 | … |
| itcm 锁存 | addi(本指令) | add | addi x3 | … | … | … | … |
| pre_decoder | — | addi（itcm↔predec 之间已无）→ **本拍 predec 锁存** | add | … |  |  |  |
| mem_buf |  |  | addi 锁存 | add |  |  |  |
| decoder |  |  |  | **addi 锁存**：rs=x0，imm=7 → alu 组合 result=7 | add |  |  |
| wb_reg |  |  |  |  | **addi：rd=1 result=7** | add |  |
| regs[1] | 0 | 0 | 0 | 0 | 0 | **写 7** | 7 |

要点：decoder 输出寄存器就是 EX 级操作数寄存器；alu 是纯组合，结果在 decoder 锁存后的
**同一拍**（c3）就有效，wb_reg 于 c4 沿收下。regs 写回在 c5 沿。

### 2.2 相邻依赖 add x2,x1,x1（x1 由上一条 addi 产生）

add 取指 c0=addi 取指后 1 拍。add 在 decoder 锁存时（=addi decoder 锁存的下一拍），
addi 的 alu 结果已经组合有效并挂在 `result_back1`。forw 依 `rd_back1(=1) == rs1_1` 把
`result_back1` 送进 add 的 r1_data_final → **相邻 ALU 依赖 0 停顿**。

前递优先级（forw）—— **三条独立比较支路，`loaded` 不遮蔽 back2**：
1. back1：`!stalled && rd_back1!=0 && r==rd_back1` → result_back1
2. `loaded`：`loaded && rd_load!=0 && r==rd_load` → ld_data（§5）
3. back2：`rd_back2!=0 && r==rd_back2` → result_back2
4. 否则用 regfile 读值 r1_data_in_dec

（2026-09-18 修：原为 `rd_back2_sel = loaded ? rd_load : rd_back2` 合并成一个比较器，
`loaded` 一高 back2 整个不可用。基线单拍 `loaded` 时只错 1 拍尚能过 CoreMark；
§5.3 的 `ld_hold` 把 `loaded` 拉成 2 拍后，落在 back2 槽的消费者彻底取不到值，
CoreMark 从 10.06M 拍退化到 20M 拍跑不出结果。拆成独立支路后与 09-11 基线逐拍一致。）

另外 regfile 读口自身带写回旁路：`(we||loaded) && rd!=0 && rs==rd ? wdata_final : regs[rs]`
（rd==x0 绝不旁路，这是修过的 bug，见工程记忆）。

### 2.3 AUIPC / LUI 特殊点

- AUIPC：pre_decoder 把 `pc_operand = aux_addr − 4` 传给 decoder，当作 r1 走 alu ADD。
  （aux_addr 始终 = 顺序后继 pc；减 4 得本指令自身地址。）
- LUI：直接 immU 作 r2，alu ADD(r1=0)。

---

## §3 分支 branch（含预测 / 冲刷）

例（分支在 0x1c，目标 0x18，计数 3 次，第三轮不跳）：
```
0x14 addi x5,x0,3          # 计数
0x18 addi x5,x5,-1         # loop 体
0x1c bne  x5,x0,0x18       # 实际跳(前2轮) / 不跳(第3轮)
0x20 …                     # 出口
```

### 3.1 预测器怎么工作（信号）

- BHT 64×2bit，复位=1。`predict_en = bht[pc 低位索引] > 1`：初值 1 ⇒ **首轮不预测跳**。
- 在 itcm 取到 branch 的那一拍（c0），bra_predict 组合输出：`br1 = br_en & predict_en`。
  - `br1=1`：预判跳 → c1 上升沿 pc/itcm 直接按 offset 取目标（**提前一拍**，见 3.3）。
  - `br1=0`：预判不跳 → 照常顺序取。
- branch 进入 decoder 时（c3，取指+3）才算真结果：`success`（真跳）/`br_fail`（真不跳）。
  bra_predict 再组合：`br2 = success & !br_pred_taken_in`（判跳但预判不跳→冲刷取目标）；
  `br3 = br_fail & br_pred_taken_in`（判不跳但预判跳→冲刷取顺序下一条）。
  `br_pred_taken_in` 是**随指令流水的载荷**：取指拍 `br1 = br_en & predict_en` 算出后，
  经 pre_decoder → mem_buf → decoder 三级寄存器跟着这条分支一路带到 c3，与 `success`/`br_fail`
  同拍呈现。**不再有 pv 两级记录槽**（旧方案按索引猜"这颗是不是当初预测跳的那颗"，载荷是把它
  焊在这条指令上）。
- 命中正确则 br2/br3=0，不冲刷。

### 3.2 情形 A：预判不跳 + 实际不跳（首轮且真不跳）＝ 无惩罚

分支照常流过（decoder c3 判 br_fail，但 pv=0 ⇒ br3=0），不需要表。

### 3.3 情形 B：预判跳 + 实际跳（BHT 饱和后稳态）＝ 零惩罚（br1 提前跳）

bne 取指拍=c0 时 `br1=1`：

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 |
| --- | --- | --- | --- | --- | --- |
| pc | 0x20 | **0x1c**（目标+4） | 0x20 | … | … |
| itcm | bne@1c | **0x18(loop体) 已被 br1 重定向取到** | bne@1c(下一轮) | … |  |
| pre_decoder | — | bne 锁存（pv=1 记录） | 0x18 |  |  |
| mem_buf |  |  | bne | 0x18 |  |
| decoder |  |  |  | bne 判决：x5≠0 success=1；pv=1 ⇒ br2=0 ⇒ 不冲刷 | 0x18 |
| stage | EXE | EXE | EXE | EXE | … |

即稳态循环每轮 1 条指令/IPC=1，预测跳正确时目标指令在 **c1** 就进入 itcm（与顺序流同速），
decoder 判决只是确认、不打断。

### 3.4 情形 C：预判不跳 + 实际跳（br2 冲刷，取目标）

bne 首轮（BHT=1，br1=0）实际却跳：顺序取的下一条及其后指令全是错误路径。

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 | c5 |
| --- | --- | --- | --- | --- | --- | --- |
| pc | 0x20 | 0x24 | 0x28 | 0x2c | **0x1c**（目标 0x18+4） | 0x20 |
| itcm | bne@1c | lui@0x20(错) | addi@0x24(错) | sw@0x28(错) | **addi@0x18=目标**（br2 重定向取到） | bne@1c(下一轮) |
| pre_decoder | — | bne（记 pv=0） | lui(错) | addi(错) | **清零** | addi@0x18 |
| mem_buf |  |  | bne | lui(错) | **清零** | （恢复填充） |
| decoder |  |  |  | **bne 判决 success=1；br2=success&!pv=1** | 清零 |  |
| stage | EXE | EXE | EXE | **FLUSH**（组合） | 正常 | EXE |
| regs[x5] | … | … | … | 写 2 |  |  |

要点：
- 真结果在 **c3**（decoder 拍）出来 → 该拍 stage=FLUSH；错误路径指令被冲掉；
  **c4** 上升沿重定向取目标（目标在 itcm 出现于 c4）。
- 相对“预判跳且真跳”（目标 c1 到），此路径目标晚 **3 拍**：净损失 ≈3 拍。
- 注意冲刷后第一拍取到的目标指令，其流水推进比稳态慢 1 拍（FLUSH 沿后各缓冲重排），
  表中 c5 标“恢复填充”；此后回到每拍一条。

### 3.5 情形 D：预判跳 + 实际不跳（br3 冲刷，取顺序下一条）

循环末轮（BHT=2，br1=1）实际不跳：c1 错误地重定向去取 0x18 目标路径。

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 | c5 |
| --- | --- | --- | --- | --- | --- | --- |
| pc | 0x20 | 0x1c | 0x20 | 0x1c | **0x24**（出口） | 0x28 |
| itcm | bne@1c | addi@0x18(错,目标路径) | bne@1c(错,再取) | addi@0x18(错) | **lui@0x20=出口**（br3 重定向） | addi x7 |
| pre_decoder | — | bne（pv=1） | addi(错) | bne(错) | **清零** | lui@0x20 |
| decoder |  |  |  | **bne 判决 br_fail=1；br3=br_fail&pv=1** | 清零 |  |
| stage | EXE | EXE | EXE | **FLUSH** | 正常 | EXE |

要点：预判跳会连续“自我循环”多取 1~2 条目标路径指令（上表 c1/c2 都是错误取的），
c3 判决不跳 → FLUSH，c4 重定向取真出口 0x20。净损失同样 ≈3 拍。

### 3.6 小结（分支代价）

| 情形 | 何时知道 | 冲刷? | 目标到 itcm 拍 | 相对理想损失 |
| --- | --- | --- | --- | --- |
| 预判不跳·真不跳 | c3 | 无 | — | 0 |
| 预判跳·真跳（br1） | c3 | 无 | c1 | 0 |
| 预判不跳·真跳（br2） | c3 | 有 | c4 | ≈3 拍 |
| 预判跳·真不跳（br3） | c3 | 有 | c4 | ≈3 拍 |

冲刷细节：冲刷沿 FLUSH，pc 按 bra_predict 的 br_addr2/beq_off 计算、itcm 按
br_addr1+beq_off_q2 取指（pc 与 itcm 差 +4，见 §7 公式表）；被冲的是
“在 decoder 之后尚未锁存的”所有指令——decoder 已锁存的（含 branch 自己）属已提交不冲。

---

## §4 跳转 JAL / JALR

### 4.1 JAL（直接跳，永不预测失败）

```
0x08 jal x3, fn     008001ef   # rd=x3, link=pc+4=0x0c, 目标 fn=0x10
0x0c nop                       # 死顺延（永远不会被取到）
0x10 fn: addi x4,x0,1
```

JAL 在取指拍 c0 就被 pre_decoder 组合译出（jal=1），**c1 上升沿 pc/itcm 即重定向到目标**，
中间不插入任何 fall-through 指令——nop@0x0c 从未被取（目标指令 c1 进 itcm）。

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 | c5 | c6 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| pc | 0x0c | **0x14**（目标 0x10+4） | 0x18 | … |  |  |  |
| itcm | jal@0x08 | **addi x4@0x10(目标)** | addi x5 |  |  |  |  |
| pre_decoder | —(组合 jal=1) | jal 锁存（aux=0x0c 即 link 值） | addi x4 |  |  |  |  |
| mem_buf |  |  | jal |  |  |  |  |
| decoder |  |  |  | jal：jal_flag=1，alu result=aux=**0x0c** | addi x4 |  |  |
| wb_reg |  |  |  |  | jal：rd=x3 |  |  |
| regs[x3] |  |  |  |  |  | **写 0x0c** |  |

要点：JAL 不像分支要等 decoder 判决——**跳转在 c1 生效**（比分支 br1 还提前一拍的原因：
JAL offset 是立即数，无需预测）。link=pc+4 用 pre_decoder 锁存时的 aux_addr（=c0 拍 pc，
即 jal+4）。JAL 不触发 FLUSH（stage 保持 EXE）。

**link 值的前递（2026-09-11 修，详见 §10）**：`decoder` 的 `OPCODE_JAL` / `OPCODE_JALR`
分支必须置 `rd_back1 <= rd_in`。否则 forw 的 back1 判据 `rd_back1 != 5'd0 && r1 == rd_back1`
不成立，link 值送不到**紧邻的下一条**——back2（`wb_reg.rd_back2`）和 regfile 的读旁路
（`rd_alu`，取自 wb_reg）都比 back1 晚一拍，够不着"紧挨着"的消费者（典型形状：
`jal f` 下一条就是被调用者的 `mv t0,ra`）。

### 4.2 JALR（寄存器间接，BTB 预测）

```
0x04 jalr x2,0(x1)     # 目标=rs1(x1)+imm0，link=pc+4=0x08
```

- **预测命中（BTB 有表项）**：c0 取指拍 `jalr_pred = pre_jalr & btb_hit` 组合为 1 ⇒
  与 br1/情形 3.3 相同的提前重定向（pc/itcm 按 BTB 存的预测偏移走）。c3 decoder 算真目标
  `jalr_target_q = r1_data_final + offset`；相等 ⇒ 不冲刷，零惩罚。
- **预测未命中 / 预测错误（BTB 空或目标变了）**：不提前跳，顺序取指推进（同分支 br2 流程），
  decoder 拍 c3 `jalr_fail=1` → FLUSH，c4 重定向取 `jalr_target_q`（真目标），pc 置目标+4。

> **校验判据（重要）**：c3 校验用的“预测值”必须是**取指那一刻**的 BTB 值，而不是 decode 时刻
> 按已漂移的 pc 重采样 `btb`。现在由**载荷**实现：取指拍 `jalr_predict_offset = btb[取指pc]`
> 随指令经 pre_decoder → mem_buf → decoder 一路带到 c3（`jalr_pred_addr_in`），
> `jalr_fail = (jalr_target_q != 32'd0) & (jalr_target_q != jalr_pred_addr_in)`。
> 旧方案用 bra_predict 里的 **2 项环形队列 `jt_q`**（入队 = pre_jalr，出队 = decoder 出现真目标），
> 已随载荷化删除。若改回按 decode 时刻的 pc 重采样，pc 逐拍 +4，读到的可能是**别的槽**里第一次
> 执行留下的旧目标（G 场景里 isr 紧贴 ret，旧值 0x38 撞上真值 0x38 → jalr_fail 误判 0 → 错误
> 路径 isr 代码被当真指令执行）。此改动即修 G。

未命中情形（BTB 空，探针实测）逐拍：

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 |
| --- | --- | --- | --- | --- | --- |
| pc | 0x08 | 0x0c | 0x10 | 0x14 | 0x14（=目标 0x10+4） |
| itcm | jalr@0x04 | nop@0x08(错) | addi@0x0c(错) | addi@0x10(错) | **目标 addi@0x10 重取** |
| pre_decoder | — | jalr | nop(错) | addi(错) | 清零 |
| decoder |  |  |  | **jalr 锁存：jalr_target_q=0x10，jalr_fail=1** | 清零 |
| wb_reg |  |  |  |  | jalr：rd=x2，result=aux=**0x08**(link) |
| stage | EXE | EXE | EXE | FLUSH | 正常 |

要点：JALR 真目标来自**寄存器操作数**（c3 才由 forw/regfile 定型），所以即使 BTB 未命中
也必须把已顺序取错的指令冲掉——未命中惩罚与分支错判同量级（≈3 拍）。link 值走 jalr_flag
路径（同 JAL，aux=pc+4），rd 写回仍对齐 regs 拍。

---

## §5 访存 load / store（lsu，字节使能）

lsu 与 decoder 同拍（都读 mem_buf 输出）。总线侧信号全部经 lsu 锁存后输出：

| 信号 | 含义 |
| --- | --- |
| bus_addr_out | **字地址**（lsu 里 `(基址+imm)>>2`；dtcm/外设按 [23:20] 译码） |
| bus_be_out | 字节使能：sb=`0001<<a[1:0]`，sh=`0011<<(2*a[1])`，sw=`1111` |
| bus_data_out | 写数据（sh/sb 已按 a[1:0] 移到目标字节槽） |
| bus_we_out | 1=写 |
| ready_in | 读响应有效，来自 cpu_top 组合：`dtcm_ready | tim_ready | bus_loaded_in` |
| loaded | 车3 里那条 load 的响应到达那拍有效；`rd_load`/`ld_data` 与它同源（见 §5.3） |
| ld_we | load 写回寄存器堆使能，只在响应那拍置起（保持拍不重复写） |
| ld_data | 回读数据（按 func3 符号/零扩展；字节槽从地址低位取） |
| we/rd_out | lsu 要写回的 rd 使能/号（喂 alu 的 we_in/rd_in 合并线） |
| stall | `use_hit | waw_hit | (车3 占用) | (访存指令进不了车)`，见 §5.3 |

地址译码（2026-09-19 起，dtcm/dcache 已不在总线上，见 §23）：
- **dcache**：字地址 `[31:24]==8'd0 && [23:13]==11'h100`（字节 0x0080_0000 区，32 KB），在 `cpu_top` 内，
  **不经过 bus_arb**。命中 1 拍应答；缺失按住流水线回填。`dtcm` 是它的后备存储器（流式读出/单拍写入）。
- **tim_in**：`[23:20]==1` 且 `[3:0]==1(cnt_set 读写)/2(cnt 读)/3(清中断)`，仍搭在 lsu 出口总线上（在 `cpu_top` 内）。
  它的窗口 `[23:20]==1` 与 dcache 的 `[23:13]==0x100` **严格互斥**，两者按位或合流成 `bus_data_in_final`。
- **bus_arb 外设槽**（`bus_addr[31:24]`）：1=UART 0x0400_0000、2=PLIC 0x0800_0000、
  4=gpio_group 0x1000_0000、5=i2c 0x1400_0000，由 bus_arb 按该字段选通（1..31 全归它管，加外设不用改 bus_arb）。见 bus_arb.v。
  槽 3/6/7（tim_p、dcache、flash_spi）**已停用**。
  **槽 3 = tim_p（2026-09-19 删）**：`mcu_top` 里的例化、`bus_ready[PER_TIM]`、`bus_we_tim_p`、
  `plic.irq_sources` 的源 2（tim_p_irq）、`pwm1~pwm4` 连线全部删除；`gpio_group.pwm1~pwm4` 改接 `1'b0`
  （gpio 的 PWM1~PWM4 模式仍保留，只是输出恒 0）。
  `peripherals/tim_p.v` **文件保留**，模块级 `tim_p_tb` 仍可单跑；闭环 `tim_p_irq_tb` 因层次引用 `u_tim_p` 已失效。

### 5.1 store sw（写，不写回 rd）

```
0x20 li x6,0x00800000    lui x6,0x800   00800337
0x24 addi x7,x0,0x123
0x28 sw x7,0(x6)                        # dtcm[0] = 0x123
```

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 | c5 |
| --- | --- | --- | --- | --- | --- | --- |
| itcm | sw | (下条) |  |  |  |  |
| pre_decoder | — | sw（lsu 标志=1） |  |  |  |  |
| mem_buf |  |  | sw（opcode_lsu=STORE 挂上） |  |  |  |
| **lsu**（=decoder 拍） |  |  |  | **锁存总线写请求**：bus_addr_out=0x00800000>>2=0x00200000；be=1111；we=1；data=0x123（x7 经 forw 到） |  |  |
| dcache→dtcm |  |  |  |  | **写透**：dcache 当拍把写打到 mem 侧，**dtcm 于本沿写** dtcm[0]←0x123（同旧 dtcm 直挂总线时的拍数） |  |
| regs |  |  |  |  |  | （无写回） |

要点：store 的基址/数据在 c3 前由 regfile+forw 定型（基址 x6 由 2 条前的 lui 来，走 back1/back2
前递）。lsu 于 c3 锁存总线请求，dtcm 在 c4 沿把写数据 merge 进 RAM（字节使能只改目标字节）。
store 不产生 we，不写 regs。无访存依赖时不 stall。

### 5.2 load lw + 紧邻使用（load-use，1 拍 stall）

```
0x2c lw x8,0(x6)        # 读 dtcm[0]=0x123
0x30 add x9,x8,x8       # 紧邻使用 x8 —— 触发 stall
```

| 位置 \ 拍 | c0 | c1 | c2 | c3 | c4 |
| --- | --- | --- | --- | --- | --- |
| itcm/pc | lw 取指 | add 取指 | (冻结) |  |  |
| pre_decoder |  | lw | add | （STALL 冻结） |  |
| mem_buf |  |  | lw | lw(冻结) |  |
| **lsu** |  |  |  | c2 锁存读请求（bus_addr），c3 dtcm 返回 loaded=1 | c4 释放 |
| decoder |  |  |  | add 被挡（rd==rs ⇒ STALL） | c4 恢复 EXE |
| regs[x8] |  |  |  |  | **c4 沿写 0x123**（loaded 旁路 ld_data） |

要点（对照探针实测）：
- 紧邻 load 的指令若用其 rd，**STALL 1 拍**。stall 由 `(opcode_post==LOAD)&&(rd_1==r1_post|rd_1==r2_post)`
  （lsu）判；`loaded` 返回即释放（`rd_1` 是派发拍锁存的 load 目的 rd，`:loaded` 表示该 load
  数据回到，`rd_load=rd_2` 与 data 同拍同源旁路给 forw/regfile）。
- 数据回读：总线读请求于 c2 锁存，**dcache 于下一沿 c3 返回**（命中 1 拍；缺失则由 dcache 按住
  流水线、从 dtcm 回填整行后再出数，见 §23.4），
  regfile 写对齐到 c4 沿——`loaded` 把 `ld_data` 选成写回数据，无需额外气泡。
- **中间隔 1 条无关指令再用**（如 lw; nop; add）不触发 stall：此时 forw 用
  `loaded`（`rd_load` 支路）前递即可（旁路命中）；该拍 back2 支路仍独立可比，见 §2.2。
- 外部总线（SoC 外设）：数据返回由 `bus_loaded_in` 驱动，`ready_in = dcache_ld_ready|tim_ready|bus_loaded_in`；
  i_busy 走 controller 的 `stall | i_busy`（冻结流水线）——时序同上，只是 `loaded` 的拍数随外部等待延后。

字节扩展：lb/lbu/lh/lhu 的符号/零扩展与字节槽选择都由 `ld_size/ld_off`（func10 + 地址低 2 位）
在 lsu 组合完成；数据回到 `ld_data_out` 已按 32 位对齐。

### 5.3 两节车厢（车2 发送 / 车3 写回）

load 与 store 走**同一条**在途队列，只有两节在用（"三节车厢"里的第一节常态空着）：

| 车厢 | 存什么 | 干什么 |
| --- | --- | --- |
| 车2（发送） | kind / rd / size / off / 载荷（addr、wdat、be）+ `s2_sent` | 常态下 ld/st **直接进这节**，进来那拍就把总线摆上（与 alu 写回同拍）；下一拍进车3 |
| 车3（写回） | kind / rd / size / off | 与 dcache 里锁住的那条**是同一条**（请求已发），只等 `ready_in`；store 进这节即算提交（下一拍让位） |

| 事件 | 条件 | 动作 |
| --- | --- | --- |
| 进车2 | `new_in & new_go`（见下） | 锁 kind/rd/size/off 与载荷；`s2_sent <= send_new`（**没真发出去就保持 0**） |
| 发送 | 进车2 那拍（`send_new`），或 `s2_send`（miss 期间进来的、或等在途读应答的，条件够了补发） | 摆一拍总线（`bus_go`，只摆一拍） |
| 车2→车3 | `s2_v & s2_sent & s3_ok` | **没发过的车不许前移**——否则请求丢失、后续数据全错配 |
| 出车3 | `s3_done = s3_v & (s3_kind | ready_in)` | store 下一拍让位；load 等到响应 |
| 写回 | `ld_we = s3_v & ~s3_kind & ready_in` | `rd_load`/`ld_data_out` 同拍给出；`ld_hold` 再保持一拍给 back2 槽的消费者 |

**入口门（`new_in`）**：`访存 && !flush_w && !(stall_m|stall_v|icache_busy) && !use_hit && !waw_hit`
—— **不看 dcache 忙**：miss 当拍照样进车2 把操作数锁好，等 hold 落下再发（这就是"miss 当拍锁操作数、回填完发送拍释放"）。
**`new_go`**：`s3_ok & s2_ok`（车3、车2 都要腾得出来），**只看车厢位置**。

**stall（两节车厢的四条来源）**：
```
stall = use_hit | waw_hit | (s3_v & ~s3_done) | (访存指令 && !new_go)
```
- **车3 有人 ⇒ 组合拉高整个 stall（不分访存/非访存）**：车3 里那条结果没回来时，后面**任何**指令都不许越过它去读寄存器。
  正因为这条，能进来的新指令只可能撞上**车2** ⇒ **hazard 只扫车2 就够**（不必扫车3）。
- **`full_stall` 只按"车厢有没有位置"算，不能把"被上游 stall 挡住"也算进去**：本模块的 stall 经 `flag_bus[1]`
  回灌 mulu 的 `pipe_stall`，混进来会与 `stall_m` 构成组合环（死锁）。
- **入口门里也不能带 `~miss`**：那样 miss 期间整条流水被停，而 dcache 的 miss 要靠流水推进才收得了尾 ⇒ 环。
- **hazard 扫描必须排除 x0**：store 车厢的 `rd=0` 与 `sw zero,…` 的 `rs2=x0` 会误命中 ⇒ 无谓停顿（甚至卡住）。

**在途读只许一笔（空总线 `blank_bus`）**：应答**只能按顺序归属** —— 车3 里那条在等哪笔应答，就只能收哪笔。
慢设备（外设读）与 dcache 的应答拍数不同，若允许两笔读同时在途，**先回来的那笔会落到前一条头上**。
```
ld_out    = (车2 里刚摆上、下一拍才进车3 的读) | (车3 里还没等到应答的读)
blank_bus = miss | (ld_out & ~ready_in)        // 这一拍总线上不摆
```
`blank_bus` 为 1 时，新指令**照样进车2**（锁操作数），只是 `s2_sent=0`，等不 blank 了由 `s2_put` 补摆。
- **为什么不能只把总线空一下、`s2_sent` 照旧置 1**：车厢会把"没真摆出去的请求"当发过了前移到车3 ⇒
  **请求凭空消失**，后续应答全部错配（实测：总线流里少一条读）。所以 `s2_sent <= new_put` 必须说实话。
- **为什么 blank 那拍必须把总线清零**（`else` 分支不能省）：从设备是**电平**判据，残留地址会被当成新请求**连答**
  （HEAD 注释里的"读地址只摆一拍"就是这个道理）。实测：漏掉清零 ⇒ 同一笔读被应答多次 ⇒ 数据全错。
- **为什么不能只"锁存应答"**：锁存只能知道"有一笔应答到了"，不知道是哪一笔 —— 归属问题没解决。
- **实测（`prog/ls_mix2.s`）**：`lw t2,0(外设)` 紧跟 `lw t3,4(dtcm)`，dcache 的应答先回、被前一条收走 ⇒
  **t2/t3 两条读的结果互换**（signature 第 1/2 字对调）。加门后与 ISS 参考模型逐字一致。
- **不减速**：dcache 1 拍应答 ⇒ 需要摆下一笔的那拍 `ready_in` 恰好为 1 ⇒ `blank_bus=0`；
  只有"应答确实还没到"时（外设慢读）才等 —— 那时本来就该等。

**为什么保持一拍**：load 往后第 2 条指令在 lsu 拍靠 `forw` 的 `loaded` 支路（`rd_load` 比对）取数，
那一拍正好落在响应**下一拍**；只摆一拍它会取不到（实测该拍 `loaded=0`）。
**写回另用 `ld_we`**：直接用 `loaded` 当 `we_ld` 会在保持拍把同 rd 再写一次，盖掉同拍 ALU 的写回。


---

## §6 CSR / 中断（system）

### 6.1 CSR 读写（CSRRW/CSRRS/CSRRC/I 类）

CSR 走“decoder 出地址和 r1(zimm)，CSR 读数据直接进 alu，alu 按 func3 算结果”的结构：

| 数据路径 | 拍 | 说明 |
| --- | --- | --- |
| 取指 | c0 | 同普通指令 |
| pre_decoder | c1 | 锁存 imm12_csr(地址) / imm5_csr(zimm, I 类) / rs1 |
| mem_buf | c2 | 透传 |
| decoder | c3 | 锁存 csr_addr、csr_wr_en、func3（csr_func3），r1_data_out=rs1 或 zimm；**同一拍**组合：alu 收到 csr 读数据 cs_data(=csr_data_out 按 csr_addr) → rd 值=cs_data，写回值 result_csr 按语义（CSRRW:r1；CSRRS:r1\ | cs；CSRRC:cs&~r1 …） |
| csr 寄存器 | **c4 沿** | csr.v 在 c3 拍的 csr_wr_en 下写（300/304/305/341/342/344），data=alu.result_csr（组合直达 u_csr.csr_data_in） |
| rd 写回 | c5 | 与 ALU 相同（rd=csr 旧值） |

注意：forw **没有** CSR 前递。背靠背 `csrw mstatus; csrr …` 时，csrr 的 decoder 拍 = csrw
decoder 拍 +1，而 csr 写沿 = csrw decoder 拍 +1 —— csrr 读到的已是**已提交新值**（这正是之前
修 RMW 陈旧读问题的结构，见工程记忆）。

### 6.2 中断（exti/timi/softi → 取指改向 isr / 返回 mret）

信号链（csr.v）：pend 为**电平**（eirq_pend=exti, tirq_pend=timi）→
`global_pend = (eirq_en&eirq_pend)|(tirq_en&tirq_pend)|(sirq_en&sirq_pend)`
→ `irq_act = MIE(irq_en_reg) && global_pend && !irq_process`。
门控：`irq = !(irq_ret|trap|jalr_fail|br2|br3) && irq_act` —— 跳转冲刷拍不响应中断。

受理（irq_act 且非 process）当拍：
- stage→FLUSH；mstatus.MIE 清 0、MPIE=旧 MIE、irq_process=1；
- `iret_addr1 = iret_addr2 = pc_addr_in − irq_bubble`（**两个寄存器同值**；稳态 bubble=12 →
  pc−12，即最老未提交指令；bubble 语义见下）；
- **pc→`isr_addr2 + 4`，icache 取指→`isr_addr1 >> 2`**（即 icache 实际取 mtvec 首条、
  pc 续 mtvec+4）；cause 写 mcause（11/7/3）。

返回 mret（decoder irq_ret）：
- stage→FLUSH；MIE←MPIE、irq_process=0；
- **pc→`iret_addr2 + 4`，icache 取指→`iret_addr1 >> 2`**（取 iret_addr1 执行、
  pc 续 iret_addr1+4）。**pc/icache 都不再减 bubble**（2026-09-05 定稿）。
- `-4/+4` 的来源见 §7 末段：pc 存的是"顺序后继地址"，icache 取指存的是"本指令地址"，两者恒差 4。

> **2026-09-19 补记**：icache 单通道重构时 `pc.v` 的 `ext_target` 被整个删掉，
> 连带把 `jalr_fail`/`irq`/`irq_ret` 三路的 `+4` 也删了 —— 后果是这三路重定向**全体早 4 字节**
> （重执行上一条指令）。已按基线 `ext_target=0` 分支补回。见 §23。

**受理时机控制（2026-09-05，controller/csr，G 与嵌套/紧贴双中断全绿的机制）**：
- **irq_bubble（controller，4bit）**：复位=12；`stage==FLUSH` 时清 0；否则每拍 +4 直到 12。
  它是"取指 pc − 提交边界"的偏移代理：稳态=12；FLUSH 置 4（冲刷目标刚被取、pc=目标+4、边界=目标 → 受理应读 pc−4），此后每拍 +4 到 12。回填任意拍受理 pc−bubble 都等于边界。
- **ird_tmr（controller，2bit）**：`jal | jalr_pred | br1`（**预判型**早跳：目标可能错、且边界还停在跳转指令上，bubble 盖不住远端）
  置 3，否则每拍 −1；非 0 输出 `irq_gate`。br2/br3/jalr_fail/irq_ret 是**权威冲刷**，靠 FLUSH 置 4 的 bubble 即可在回填中正确受理，不再进 tmr。
- **csr 受理三重门控**：`irq_act = MIE && global_pend && !irq_process && !irq_gate
  && !(iret|trap|jalr_fail|br2|br3)`；controller 的 `irq = irq_act`（不再单独再门控）。
  含义：① 定时器非 0（刚发生预判型早跳）不响应；② 控制转移解析同拍（jalr_fail/br2/br3/mret/trap）不响应——否则会按投机 pc 采 iret。
- 这样 iret_addr1 只在"稳定拍"采，pc−12 一定落在最老未提交指令上。G(单)、双中断(分开/紧贴 mret)、
  带访存 ISR 均过，coremark 不变。

中断流程（无重叠、单 exti）粗拍：

| 拍 | 事件 |
| --- | --- |
| cX | 普通指令流；exti 有效但受 ird_tmr/门控，等稳定拍才 act=1 |
| cX+1(FLUSH) | pc→isr+4，itcm 取 mtvec 首条；MIE 清；iret_addr1=pc_addr_in−bubble(12) |
| 之后 | ISR 按普通指令流跑（嵌套由 MIE/MPIE 管理） |
| ISR 尾 mret | FLUSH；MIE 还原；pc/itcm 用 iret_addr1/2 固定回跳；ird_tmr 置 3 抑制紧贴的二次受理 |

---

## §7 附：pc / itcm 取指重定向公式对照（写错落点排查用）

| 事件 | pc 模块（pc.v, 本拍沿） | icache 取指（icache.v, 本拍沿） | 触发条件 |
| --- | --- | --- | --- |
| 顺序 | pc_addr = pc_addr+4 | pc_addr>>2 | EXE 无跳转 |
| jal | pc_addr + offset_jal2 | (pc_addr + offset_jal1)>>2 | predec 组合 jal（c0→c1 生效） |
| jalr 预测 | offset_jalr2(=预测+4) | offset_jalr1(=预测)>>2 | jalr_pred（c0→c1 生效） |
| br1 预测跳 | pc_addr+offset_beq2 | (pc_addr+offset_beq1)>>2 | br1（c0→c1 生效） |
| br2(br_fail 错) | br_addr2+beq_off_q2 | (br_addr1+beq_off_q2)>>2 | FLUSH 且 br2 |
| br3(br_fail 对错) | br_addr2 | br_addr1>>2 | FLUSH 且 br3 |
| jalr_fail | **jalr_target_q+4** | jalr_target_q>>2 | FLUSH 且 jalr_fail |
| 中断 | **isr_addr2+4**（=mtvec+4） | isr_addr1(=mtvec)>>2 | FLUSH 且 irq |
| mret | **iret_addr2+4** | iret_addr1>>2 | FLUSH 且 irq_ret |

公式里的 +4/−4 差：pc 寄存器保存的是“顺序后继地址”（下一条），icache 取指保存的是
“本指令地址”，两者恒差 4；pre_decoder 的 offset_jal1/beq1 已预减 4（imm−4），pc 用未减的
offset_jal2/beq2，配合实现单拍重定向时 pc/icache 各自落点一致。

**排查跳转落点错误时**（先看 §8，再回来对表）：
1. 先分清“pc 侧落点”与“icache 取指侧落点”各是多少、是否差 4/8/12，再看是 br2/br3/
   jalr_fail/irq/irq_ret 中的哪一路。
2. **`pc_addr` 不能直接当"正在执行的指令地址"读** —— 它恒等于「正在执行的指令地址 + 4」
   （icache 读出打一拍，见 §10 末段）。用 pc 直方图判"跳转落在哪"会**整体偏 +4**，别误判成 bug。
3. **判定"某路要不要 +4"的规则**：目标信号是不是**真实指令地址**。
   `jalr_target_q`(decoder 算的 rs1+imm)、`isr_addr1/2`(mtvec)、`iret_addr1/2`(pc−bubble) 都是
   真实指令地址 → **要 +4**；`br_addr2` 是从流水线里取的、已是 pc 约定 → **不加**。
   旁证：预测路径 `cpu_top.v` 给 pc 的 `.offset_jalr2(jalr_predict_offset + 4'd4)`。

> **2026-09-19**：`ext_target` 已整个去掉（icache 单通道后不存在"外区取指"，原公式里
> `ext_target ? 4'd0 : 4'd4` 恒取内区分支）。上表就是内区分支的真值。

---

## §8 关键信号归属速查

| 信号 | 归属模块 | 是寄存器还是组合 | 一拍含义 |
| --- | --- | --- | --- |
| pc_addr / aux_addr | pc | reg | 顺序后继地址（= 正在执行的指令地址 +4）/ 同步镜像 |
| inst_out | icache | reg | 当前已取指令（**唯一取指源**，缺失期由 busy 按住） |
| dec / lsu | pre_decoder | reg | 该指令走 decoder 侧还是 lsu 侧 |
| jal / br_en / jalr_pre(预测) | pre_decoder 组合 | comb | 基于 icache 当前指令 |
| br1/br2/br3 / jalr_fail | bra_predict | comb | 分支/跳转预测与纠错 |
| success / br_fail | decoder | reg | c3 分支真结果 |
| r1/r2_data_final_dec/lsu | forw | comb | 给 decoder/lsu 的已前递操作数 |
| jalr_target_q | decoder | reg | c3 真目标 |
| bus_addr/be/data/we | lsu | reg | c3 锁存的总线请求 |
| loaded / ld_data | lsu/dcache/dtcm/tim_in | reg/comb | 读数据有效/数据（dcache 命中 1 拍；缺失回填后由 dtcm 供数） |
| dcache_busy / dcache_busy_d1 | dcache / cpu_top | comb/reg | 数据侧缺失停顿；`d_hold_int` 复刻原 `bus_arb.d_hold_ext` 的 1 拍延拓 |
| icache_busy | icache | comb | 取指缺失/自举期按住整条流水线 |
| flush | controller | comb | 7 个冲刷事件的 OR（§22.4 从 `stage` 里拆出来，降扇出） |
| we_3/rd_3, we_4/rd_4, we_5/rd_5 | decoder/alu 输出/wb_reg | reg | 各写回级 |
| rd_back1 | decoder | reg | ALU 级指令的 rd，供 forw back1 前递；**JAL/JALR 分支也必须置**，否则 link 值送不到紧邻下一条（§10） |
| csr_wr_en/csr_addr/csr_data_in | decoder / alu.result_csr | reg/comb | CSR 写请求与数据 |
| irq_act/irq_processing/irq | csr.v / controller | comb | 中断请求 |
| irq_bubble | controller | reg | mret 回绕气泡计数 |

（RTL 以 `RTL/core/` + `RTL/soc/` + `RTL/peripherals/` 下文件为准；本文如与代码冲突，以代码为准。）

---

## §9 icache：fill_end 链式再回填的同一行排除（2026-09-10）

**问题**：`icache.v` 在 `stage` 由 1'd1 回 1'd0 的那一拍（`fill_end` 有效）判断是否链式发起下一次回填，
判据原为 `!cache_hit && ibus_re_in`。`cache_hit` 是由 `valid`/`tag_ram` 组合算出的，
而这一行刚填好的 `valid`/`tag_ram` 要到同一时钟沿才写入 —— 即在本 `fill_end` 有效期间，
`cache_hit` 取到的仍是写入之前的值，于是"刚填完的同一行"被判成 miss，对同一行**再回填一次**。

**后果**：① 每次 miss 多等一整行回填时间（探针观测：同一行出现两次 `fill_cnt` 0→15）；
② 第二次 `fill_end` 有效时，`ibus_data_out` 仍按 `fill_buf[miss_word]` 输出该行首字，
而此时 `ibus_addr_in`（即取指地址）已前推到下一个字，于是同一个指令字被送进
pre_decoder 两次 —— 微测 `alu_refill` 的观察即为 `addi t0,t0,1` 被 `li t0,0` 覆盖，累加结果少 2。

**修法**：判据加"排除刚填完的同一行"：
`if (!cache_hit && ibus_re_in && !(fill_idx == idx && fill_tag == tag))`。
即只有本拍取指不在刚填的这一行时才链式再发起回填；同一行填完直接按命中路径供字。

**验证**：`alu_refill` 结果 0x000000fd、`addi t0` 退休 24 次、耗时 99→67 拍；
`icache_mt` inst=64 err=0；`mini` marker=1、中断受理 1 次、75→41 拍；`mtirq2d` 0x0000600d（纯 itcm，不受影响）；
`mcu_bus_tb` 0x0000600d。

---

## §10 decoder：JAL / JALR 漏置 rd_back1（2026-09-11）

**问题**：`decoder.v` EXE 块开头把 `rd_back1` 清 0，只有 `OPCODE_OP` / `OPCODE_OP_IMM` /
`OPCODE_LUI` / `OPCODE_AUIPC` / `OPCODE_SYSTEM` 五个分支重新赋 `rd_back1 <= rd_in`，
**`OPCODE_JAL` 与 `OPCODE_JALR` 没赋**。这两类正是写 link 值（`pc+4`）的指令。

**后果**：`forw` 的 back1 前递判据是 `rd_back1 != 5'd0 && r1 == rd_back1`，`rd_back1=0`
直接不成立 ⇒ **link 值永远走不了 back1**。而 back2（`wb_reg.rd_back2`）和 regfile 读旁路
（`rd_alu`，取自 wb_reg）都比 back1 晚一拍，只够着"隔一条"的消费者。

于是**紧邻 `jal` 的下一条若用 `ra`，读到的是寄存器堆里的陈旧值**。典型形状即被调用者第一条
`mv t0,ra`（`addi t0,ra,0`）：

- 微测 `mt_jalra`（8 条指令）：`jal callee` → `mv t0,ra` 拿到的 `r1_3 = 0x28`（寄存器堆残值），
  而正确值应是 `jal callee` 刚写回的 `0x0c`；同形状的 `ret` 也因拿不到刚写的 `ra` 而不跳。
- CoreMark：横幅那条 `ee_printf` 无 `%`，不调 `number`；下一条 `ee_printf("CoreMark Size : %lu\n")`
  一进来就 `number` → `jal __umodsi3`，而 `__umodsi3` 的第一条正是 `mv t0,ra`。
  `t0` 拿到旧 `ra`(0xc5c) ⇒ `jr t0` 从 `number` 中间飞回 `ee_printf` 的拷贝循环 ⇒
  `number` 的收尾 `addi sp,sp,160` 永不执行，sp 停在 0x803AB0 ⇒ `ee_printf` 用错的 sp 走收尾，
  `lw ra,1148(sp)` 读到 0 ⇒ `ret` 目标 0，被 `jalr_fail = (jalr_target_q != 0) & …` 的直接否掉
  ⇒ 落到 `d28/d2c j ce4` ⇒ 收尾段无限重复、sp 每轮 +1184，直到某次"恢复 ra"的地址撞进 `.rodata`，
  `ra` 被字符串覆盖，pc 彻底跑飞。

**修法**：两处各加一行

```
OPCODE_JAL:  begin ... rd_back1 <= rd_in; ... end
OPCODE_JALR: begin ... rd_back1 <= rd_in; ... end
```

**验证**：`mt_jalra` 判据翻绿（`dtcm[0x801000]=0x0000000c`、`jr t0` 执行=2；
改前为 `0x28` / 0）；`mt_number` 输出 `666\n`、`number` 收尾 `lw_ra/addi_sp160/ret` 全执行；
`mt_banner` 92 字节三条打印全出、跑到自锁、sp 回归 0x803ff0；全回归 9/9 绿；
**CoreMark 跑完 10 次迭代并打印完整结果，`crclist/crcmatrix/crcstate` = 0xe714 / 0x1fd7 / 0x8e3a，
与 `core_main.c` 官方期望表 `[3]`（seedcrc=0xe9f5 → known_id=3）逐一对上**。
唯一一条 `Errors detected` 来自 `Total time (secs): 0` 触发的"必须跑满 10 秒"报告规则
（`CLOCKS_PER_SEC=50000000`，10M 拍 = 0.2 s 整数除法得 0），非功能错误。

**排查工具（可重跑）**：`tools/mt_jalra.S` + `project_risc_v.sim/mt_jalra_tb.v`（8 条指令、
40 拍密集探针 + 全量写口事件）；`tools/mt_number/`、`tools/mt_banner/`（同一份 `ee_printf.c`/
`link.ld`/`start.S`，`-O1`）。判读 `pc_addr` 时注意：**`pc_addr` 是送 itcm 的地址寄存器，
恒等于「正在执行的指令地址 + 4」**（itcm 读出打一拍），直接按它读落点会全体偏 4。

---

## §11 plic 读不通 + tim_p 外设化 + lsu 在途 load 队列（2026-09-12）

### 11.1 plic：ld_ready 只写在复位分支

**问题**：`plic.v` 的 `ld_ready` 只在 `if (rst)` 分支里赋值（且第 73 行覆盖第 72 行），
非复位分支从不赋值。

**后果**：读 PLIC 永远等不到应答 ⇒ 该读指令在 lsu 里永久停在 STALL ⇒ 仿真/上板一律挂死。
这也是此前全工程没有任何程序访问过 PLIC 的原因。

**修法**：复位分支删掉那行；非复位分支先 `ld_ready <= 1'b0;` 打底，
5 个读命中分支（priority / enable / threshold / pending / claim）各置 `ld_ready <= 1'b1;`，
与 `bus_data_out` 同沿。

**验证**：`plic_tb` 8 项全过（复位后读 `PRIO(2)` 必须 `ld_ready==1`，修复前必挂）；
`tim_p_irq` 闭环里 claim 读通、源号=2。

### 11.2 tim_p：按规范改成合规外设，挂索引 3

移植来的 `tim_p.v` 原是 16 位地址 / 8 位数据 / `bus_sig_in` 的自定义接口，
**读是组合透传**、**中断会自动清 0** —— 两条都违反《总线说明规范》§6。

按 §5 接口重写：`bus_addr_in[31:0]` / `bus_data_in[31:0]` / `bus_be_in[3:0]` / `bus_we_in` /
`bus_data_out` / `ld_ready`（后两者均为 reg）。**读改寄存输出**（与 uart / plic / dtcm 同形）；
**中断只置 1**，只有写 `IRQ_CLR` 才清（原 `irq_mode` 位重定义为 0=单次 / 1=周期）；
4 个 8 位级联计数器拼回单个 32 位 `cnt`；PWM 改寄存输出消毛刺。

寄存器映射（字偏移 = `bus_addr_in[3:0]`，字节基址 0x0C00_0000）：

| 偏移 | 名称 | 访问 | 语义 |
| --- | --- | --- | --- |
| +0x00 | CNT_SET | RW | 目标值，逐字节受 `bus_be_in` 保护 |
| +0x04 | CNT | RO | 当前计数值 |
| +0x08 | MODE | RW | bit0：0=单次、1=周期 |
| +0x0C | IRQ_CLR | W | 任意写清中断（唯一清中断途径） |
| +0x10 / +0x14 / +0x18 / +0x1C | PWM_DUTY1~4 | RW | 四路门限，逐字节受 `bus_be_in` 保护 |
| +0x20 | PWM_EN | RW | bit0~bit3 = 四路使能 |
| +0x24 | IRQ_STAT | RO | bit0=中断、bit1=停止 |

未实现地址仍返回 `ld_ready=1`（读不挂死）。接入：`mcu_top.v` 加 `PER_TIM=3`、`bus_ready[3]`、
`bus_we_tim_p`、`pwm1~pwm4`；`plic` 的 `irq_sources` 改成 `{29'd0, tim_p_irq, uart_rx_irq, 1'b0}`
（tim_p = 源 2）。`bus_arb.v` 不用动。

**验证**：`tim_p_tb` 11 项全过（含"中断粘滞性"——置位后不写就应一直为 1，原代码必挂）；
`tim_p_irq` 闭环 PASS @cyc=479：ISR 4 次、mcause 0x8000000B、claim 源号 2、
CNT_SET 回读 200、pwm1 高低都出现过。

### 11.3 lsu：在途 load 队列

见 §5.3 的机制描述。**问题**：原 lsu 用 `rd_1`/`rd_2` 两级移位 + `loaded = ready_in`（电平）
凑对齐 —— `loaded` 是电平、`rd_2` 是自由移位，二者靠拍数凑；背靠背 load 时会把
**上一笔应答配到下一笔的 rd 上**（FreeRTOS 里 `lw ra,off(sp)` 拿到别的 load 的数据
⇒ `ret` 飞向 DTCM ⇒ 重跑 main）。根因即"rd/size/off 没有和应答绑在一起，靠延迟对齐"。

**验证**：
- 微测 `mt_ldfifo`（`tools/mt_ldfifo.S` + `project_risc_v.sim/mt_ldfifo_tb.v`，判据写 dtcm 0xFFC~0xFFF）：
  ①连发 27 条 load；②load-use 停顿后紧邻 load；③store 紧邻 load 读写同址；
  ④32 轮 `jalr` 冲刷窗口里塞死槽 load —— **4/4 PASS**，入队=出队=157、溢出/孤儿/越界全 0。
- 新旧 RTL 对拍：FreeRTOS 全跑 9000 拍，DTCM 写口 **1645 笔逐字节完全一致**
  （改前第 1156 笔即分叉）。
- FreeRTOS 菜单恢复 **225 字节**（Function1 / Function2 都能进、都能回主菜单）。
- 全回归 **13/13 绿**。

---

## §12 gpio_group：32 脚 GPIO 外设化 + 中断聚合（2026-09-12）

### 12.1 模块功能

32 根双向脚，**每脚一个 4 位 mode**决定这脚接什么：

| mode | 值 | 脚方向 | 说明 |
| --- | --- | --- | --- |
| UNUSE | 0 | 高阻 | 默认 |
| OUT | 1 | 输出 | 值取 `gpio_output[i]` |
| IN | 2 | 输入 | 电平进 GPIO_IN |
| IRQ | 3 | 输入 | 边沿/电平检测进中断状态位 |
| TX | 5 | 输出 | 透传模块的 `tx` 输入 |
| RX | 6 | 输入 | 电平透到模块的 `rx` 输出 |
| PWM1 / PWM2 / PWM3 / PWM4 | 7 / 8 / 11 / 12 | 输出 | 透传对应的 `pwm1~pwm4` 输入（tim_p 已删，2026-09-19 起 `mcu_top` 接 `1'b0`，输出恒 0） |
| SCL | 9 | 输出 | 透传 `scl` 输入（I2C 预留） |
| SDA | 10 | 开漏双向 | `sda_oe` 时拉低、否则高阻；电平回读进 `sda_in` |

**`mcu_top` 顶层只留 `clk` / `rst` / `gpio_pin_bus[31:0]`** —— 所有需要出去的外设信号都经
gpio 引脚复用，不再有独立顶层端口：

| 外设信号 | 接到哪 | 怎么出到引脚 |
| --- | --- | --- |
| UART `tx` | `gpio_group.tx` | 某脚配成 TX 模式即把串口引出去 |
| UART `rx` | ← `gpio_group.rx` | 某脚配成 RX 模式，该脚电平进 UART 接收端；无 RX 脚时 `rx` 恒 1（空闲） |
| tim_p `pwm1`~`pwm4` | `gpio_group.pwm1`~`pwm4` | 某脚配成 PWM1~PWM4 模式 |
| I2C `scl`/`sda_oe`/`sda_in` | 暂时接 `1'b1` / `1'b0` / 悬空 | I2C 空闲态（SCL 高、SDA 释放）；`i2c.v` 移植进来后按同样方式接上 |

`tx`/`rx`/`scl`/`sda_oe`/`sda_in` 这几个 `gpio_group` 端口名和语义按移植来的原样没动。

### 12.2 总线适配与寄存器映射

按规范 §5 换接口：`bus_addr_in[31:0]`（索引 `[31:24]==4`，内部偏移 `[5:0]`）/ `bus_data_in[31:0]` /
`bus_be_in[3:0]` / `bus_we_in` / `bus_data_out[31:0]`(reg) / `ld_ready`(reg) / `gpio_irq`(reg)。
**读全走时序逻辑**、与 data 同沿出 `ld_ready`；写严格 1 拍；未实现偏移读仍返回 `ld_ready=1` 不挂死。

| 字偏移 | 名称 | 访问 | 说明 |
| --- | --- | --- | --- |
| 0x00 | GPIO_OUT | RW | 32 位输出值，逐字节受 `bus_be_in`，只对 OUT 模式脚生效 |
| 0x04 | GPIO_IN | RO | **32 脚实际电平全回**（含 OUT 脚驱动出去的值；mode 不限制回读范围） |
| 0x08/0x0C/0x10/0x14 | GPIO_MODE0..3 | RW | 脚 0..7 / 8..15 / 16..23 / 24..31，每脚 4 位 |
| 0x18 | GPIO_IRQ_STATUS | RW | 每脚 1 位，**sticky，写 1 清** |
| 0x1C/0x20 | GPIO_IRQ_TYPE0/1 | RW | 脚 0..15 / 16..31，每脚 2 位：0=下降沿 1=上升沿 2=任意边沿 3=低电平 |

字节地址 = 字偏移 `<< 2`（CPU 发字节地址，lsu 右移 2 位后才是这里的 `bus_addr_in`）。
例：MODE0 是字节 `0x1000_0020`。

### 12.3 中断聚合（按 PLIC 能力定的方案）

PLIC 内部只认**源 1..31 共 31 个**（源 0 保留），已占 1=UART rx、2=tim_p，本次 **gpio 占源 3**。
32 根脚**聚合成 1 根 `gpio_irq`**（`gpio_irq = |gpio_irq_status`），具体是哪几根脚触发由软件读
`GPIO_IRQ_STATUS` 定位 —— 不用把 29 个剩余源全吃光，留给后面 I2C/SPI/DMA。

**必须是 sticky、只能写清**，不能让引脚直接驱动 irq 线，两个原因：
① 规范 §6 规定外设中断只能置 1、只能由写操作清 0；
② PLIC 是**每拍从电平重新锁存** pending 的（`if (irq_sources[i]) pending[i] <= 1'b1;`），
电平线一撤就丢中断、一保持就无限重入。

对应的 ISR 顺序（本工程闭环程序 `tools/gpio_test.S` 就是这么写的）：
**① 写 `GPIO_IRQ_STATUS` 清状态（源撤掉）→ ② 读 PLIC claim → ③ 写回 complete → ④ mret**。
顺序反了（先 claim 后清）会出现：claim 清的 pending 在下一拍被仍然为高的源重新锁存 → mret 后立刻重入。

低电平触发（TYPE=3）是特例：写清时若源仍为低，下一拍会重新置位 —— 这是电平语义本身，软件得先消掉源。

### 12.4 顺带修掉的原代码问题

移植来的 `gpio_group.v` 是 8 脚、`bus_addr_in[15:0]`/`bus_data_in[7:0]`/`bus_sig_in[3:0]` 的旧接口：

1. **脚 0 是死的** —— 数据寄存器那三段循环全写 `for (i = 1; i < 8; ...)`，跳过 i=0，脚 0 设 OUT 写不进输出值、设 IN 读不回来。
2. **中断是电平、每拍刷新**（`gpio_irq <= 2'b0;` 再条件置 1）—— 违反规范 §6，上板必丢中断。
3. **读是组合透传**（`always @(*)` 直接给 `bus_data_out`）—— 违反规范 §6。
4. **`sda_in` 在 for 循环里反复赋值** —— 多驱动、最后一个 i 赢。
5. **`UNUSE = 4'bz`** —— z 参与 `==` 比较恒为假；原来只当默认值用侥幸没炸，现在方向判断要比较 mode，改成 `4'b0000`。
6. **`initial` 初始化** —— 改走复位分支。
7. **`GPIO_IN` 只回 IN 模式脚** —— 改成 32 脚实际电平全回：32 根脚本来就该都能读回来，
   而且 OUT 脚能读到自己驱动的值（回读校验用）。

### 12.5 验证

- 模块级 `gpio_group_tb.v`（51 项）：寄存器读写、字节使能、8 种复用透传、SDA 开漏、
  四种触发类型各自正反例、**中断粘滞性**、写 1 清、**低电平写清后源仍在会重新置位**、
  非 IRQ 模式脚不产生中断、高 16 脚、未映射偏移读有 ready 且回 0、空闲期 `ld_ready` 恒 0。
- 闭环 `gpio_irq_tb.v` + `tools/gpio_test.S`：脚0=OUT(输出高)、脚1=IN、脚2=IRQ(下降沿)，
  tb 把脚2 拉低 → `gpio_irq` → PLIC 源3 → CPU ISR。
  **PASS @cyc=74**：ISR 次数=1、mcause=0x8000000B、claim 源号=3、清后状态回读=0、
  IN 回读=7（脚0 OUT 驱动高 + 脚1/脚2 外部高）、OUT 回读=1、MODE0 回读=0x321、脚0 实测高。
- 全回归 **15/15 绿**。

### 12.6 顶层收敛：只留 clk / rst / 32 位 GPIO

`mcu_top` 的端口从 `clk/rst/rx/tx/pwm1/pwm2/...` 收敛成 **`clk` + `rst` + `gpio_pin_bus[31:0]`**，
外设信号全部内部互连后经 gpio 复用出脚（接法见 §12.1 的表）。改动只有两条内部线：

```
uart_top   .rx(uart_rx_w)  .tx(uart_tx_w)
tim_p      .pwm1(pwm1_w)   .pwm2(pwm2_w)
gpio_group .tx(uart_tx_w)  .pwm1(pwm1_w)  .pwm2(pwm2_w)  .rx(uart_rx_w)
           .sda_oe(1'b0)   .scl(1'b1)     .sda_in()
```

**影响面**：例化 `mcu_top` 的 5 个 tb 都要跟着改端口（`mcu_bus_tb` / `tim_p_irq_tb` /
`mt_ldfifo_tb` / `gpio_irq_tb` / `uart_addr_probe`）。其中 **`tim_p_irq_tb` 的 PWM 观测点从
顶层 `pwm1` 端口改成了 GPIO 引脚**：程序 `tools/tim_p_test.S` 开头加一句把脚0/脚1 配成
PWM1/PWM2 模式（`li t1, 0x87; sw t1, 0x20(gpio基址)`），tb 改为看 `gpio_pin[0]` 的高低。
这样 PWM 的观测变成端到端（tim_p → gpio 复用 → 引脚），比原来的顶层直连更有意义。

`rtos_menu_tb` 不受影响 —— 它自己拼 SoC（cpu_top + uart + plic），不例化 `mcu_top`。

**验证**：全回归 **15/15 绿**；`tim_p_irq` **PASS @cyc=482**（pwm1 高/低都从引脚上看到）。

---

## §13 i2c：总线规范适配 + 从机 + 开漏 SCL（2026-09-12）

移植来的 `i2c.v` 只有主机发送、且接口是旧式（`bus_addr_in[15:0]`/`bus_data_in[7:0]`/`bus_sig_in[3:0]`）
外加一套 DMA 并口仲裁。本轮按"代码风格 / 资源优化 / 功能"三项要求重做。

### 13.1 总线接口与寄存器

外设索引 **5**（字节基址 **0x1400_0000**），按规范 §5：`bus_addr_in[31:0]` / `bus_data_in[31:0]` /
`bus_be_in[3:0]` / `bus_we_in` / `bus_data_out`(reg) / `ld_ready`(reg)。**原模块完全没有读通路**
（无 `bus_data_out` 也无 `ld_ready`），本轮补齐。

| 偏移 | 名称 | 访问 | 说明 |
| --- | --- | --- | --- |
| 0x00 | DATA | RW | 写=入 TX FIFO；**读=出 RX FIFO**（读操作自己弹队） |
| 0x04 | CTRL | RW | bit0 start、bit1 stop、bit2 清中断、**bit3 主/从** |
| 0x08 | CFG | RW | [2:1] `frq_mode`(100k/400k/1M)、[0] `ack_mode`、[3] 拉伸使能 |
| 0x0c | SLV_ADDR | RW | [6:0] 从机地址、[14:8] **地址掩码**、[15] 从机使能 |
| 0x10 | STATUS | RO | bit0 busy、bit1 err、bit2 RX 非空、bit3 TX 满、bit4 RX 满、bit5 被寻址 |
| 0x14 | IRQ_STAT | RW | 写 1 清：bit0 err、bit1 rx_ready、bit2 tx_done、bit3 stop_det |

中断出 1 根 `i2c_irq` → PLIC **源 4**（`irq_sources[4]`）。中断只置 1、只由写清 ✓。

### 13.2 去掉 DMA 仲裁

删掉 `bus_addr_dma`/`bus_data_dma`/`bus_sig_dma` 三个端口、`dma_oc` 判据和
`bus_addr_final`/`bus_data_final`/`bus_sig_final` 三根仲裁 mux，内部直接用 `bus_*_in`。

### 13.3 从机

从机不能和主机共用一套状态机：**主机自计时推进**（`cnt` 数到 `cnt_l`/`cnt_h` 才走一步），
**从机必须由外部 SCL 边沿驱动**。所以是两套 FSM，用 `mode` 位切换，共用同一对开漏引脚：

```
S_IDLE --(起始位)--> S_ADDR --(8 个上升沿移位)--> S_ADDR_ACK
   |                                                   |
   |                           匹配且 R/W=1 --> S_TX --(8 位)--> S_TX_ACK
   |                           匹配且 R/W=0 --> S_RX --(8 位)--> S_RX_ACK
   |                           不匹配        --> S_DONE
   +--(停止位)-------------------------------------------------+
```

三条关键设计：

1. **ACK 必须在整个 SCL 高电平期保持**。第一版把 ACK 写在 `scl_rise` 分支里，结果是
   SCL 一升高就把 SDA 放开 —— 从机收不到自己的 ACK，主机采到的是 NACK。改成用 `ack_ph`
   两相：**第 1 个下降沿拉低、第 2 个下降沿才释放**，状态切换放在释放那一拍。
2. **从机自己释放 SDA 会伪造成停止位**。从机放开 ACK 时 SDA 上升，若此时 SCL 仍高，
   全局的 `stop_det` 就会命中，从机刚进 S_RX 就被清回 IDLE。修 ACK 时序（在下降沿释放）后
   这个窗口自然消失 —— 释放发生在 SCL 低电平期，后面那个高电平期里 `sda_q2` 早已跟上。
   （第一版曾用 4 位"刚驱动过"计时器回避，修好时序后已删除，省 4 个 FF。）
3. **收到的字节要在第 8 个上升沿当场锁存**（`s_rx_byte <= slv_recv`）。若等到 `S_RX_ACK`
   再取 `slv_recv`，那时移位寄存器已经移完、取到的是错位值。

**地址比较**：`slv_match = ((recv[7:1] ^ slv_reg[6:0]) & slv_reg[14:8]) == 0`，
掩码位写 0 表示"该位不比较"。掩码全 0 就是完全匹配。

**时钟拉伸**：`CFG[3]=1` 时，`S_RX_ACK` 若遇上 RX FIFO 满，就拉低 SCL 不放，等 CPU 把
FIFO 读空了再放并回 ACK；关掉拉伸时直接回 NACK。

### 13.4 代码风格与资源

- 全部改成 `reg` + `always @(*)` 阻塞赋值（见 [[rtl-code-style]]）。
  原来 `wr_ptr = wr_ptr + 1;` 用阻塞赋值混在时钟块里、复位段 `cnt` 被赋值两次（9 位和 1 位并存）
  这类问题一并清掉。
- **FIFO 指针与中断状态都收成单一写口**：`tx_rd` 由 `m_pop || s_pop` 单独一块写，
  `irq_stat` 单独一块写，`irq_clr` 做成组合掩码（写清赢）。
- **资源**：分频计数 25→16 位（最大只用到 300）；`cfg_reg` 32→4 位；TX/RX FIFO 深度
  16→8（指针 5→4 位、索引 `[2:0]`）；删掉 `s_drv_tmr`。合计省约 **190 个 FF**。
- `$readmemh` 之外没有任何 `initial`，全部走复位分支。

### 13.5 顶层接线

`mcu_top` 例化 `u_i2c`（`PER_I2C=5`、`bus_we_i2c`、`bus_ready[5]`），
`i2c` 的 `scl_oe`/`sda_oe`/`scl_in`/`sda_in` **直接接 `gpio_group` 的同名端口** ——
这四个端口当初就是为它预留的。`gpio_group` 的 SCL 模式相应从"纯输出"改成
**开漏双向**（`scl` 输入换成 `scl_oe` 输入 + 新增 `scl_in` 输出），SDA 本来就是开漏。
顶层仍然只有 `clk` / `rst` / `gpio_pin_bus[31:0]`。

### 13.6 验证

`project_risc_v.sim/i2c_tb.v`（已并入 `tools/run_reg.sh`），**28 项全过**：

| 段 | 覆盖 |
| --- | --- |
| 1 寄存器 | 读写、字节使能（SLV_ADDR 需两个字节使能）、未映射偏移读有 ready 且回 0 |
| 2 主机发送 | 外部从机模型收 2 字节 0xa5/0x5a、ACK 相位、TX FIFO 排空、busy 落下 |
| 3 从机接收 | 地址 0x50 匹配回 ACK、两字节 0x11/0x22 进 RX FIFO、读 DATA 弹队、rx_ready/stop_det 中断、写 1 清 |
| 4 从机发送 | 地址 0x51(R) 回 ACK、两次读事务分别取回 0xc3 / 0xd4、发完后 TX FIFO 空 |
| 5 地址掩码 | mask=0x7E 时同地址仍 ACK、地址不符则不 ACK |
| 6 ld_ready | 空闲期恒 0 |

**外部设备模型全部用 50MHz 时钟驱动（不用 `#delay`）**，SCL 半周期 = `HALF` 拍，
并且严格遵守"**SDA 只在 SCL 低电平期改变**"。这一点踩了很深的坑：早先的 tb 在 SCL 还高的
时候就改 SDA，等效于在错误时刻摆 Start/Stop；而 `wr`/`rd` 任务在任意时刻进入、与时钟沿竞争，
地址有时整整一个周期都没被 DUT 采到 —— 表现成"随机几项不过"。**改成先 `@(negedge clk)` 对齐、
再摆信号、撤信号也在 negedge**，全部确定。

**全回归 16/16 绿**。

### 13.8 闭环：i2c -> gpio 引脚 -> PLIC 源 4 -> CPU ISR

`tools/i2c_test.S` + `project_risc_v.sim/i2c_irq_tb.v`，**例化真正的 `mcu_top`**
（含 bus_arb / uart / plic / tim_p / gpio_group / i2c），tb 在引脚上挂一个外部 I2C 从机模型。

程序流程：把脚0 配成 SDA、脚1 配成 SCL（`MODE0 = 0x9A`）→ i2c 配主机（CFG=0x3：400kHz、
ack_mode=1）→ PLIC `prio(4)=1`/`en(4)=1`/`thresh=0` → 往 TX FIFO 压 `0xa5`、`0x5a` →
写 `CTRL.start` → 自旋等 ISR。ISR 里先写 `IRQ_STAT` 清 i2c 中断（源撤掉 PLIC 才不会重新锁存），
再 claim、读 STATUS、complete。

**tb 侧引脚用"谁拉低谁赢"建模**：SDA/SCL 两边都是开漏，tb 把引脚驱到
`dut_low | tb_low ? 0 : 1` 的结果 —— 既给 DUT 正确电平，又不会和 DUT 的驱动力打架
（若 tb 硬驱高、DUT 拉低就会冲突成 X）。

**结果**：`PASS @cyc=2556`，ISR 1 次、mcause=0x8000000B、清后 IRQ_STAT=0、
claim 源号=**4**、外部从机收到 **a5 5a**。

两个踩到的坑：

1. **`gpio_group` 在没有脚配成 SDA/SCL 模式时，把 `sda_in`/`scl_in` 驱成 `1'bz`** ⇒
   i2c 的同步寄存器收进 X ⇒ sticky 的 `irq_stat` 把 X 锁死（表现为 `irq_stat=x100`）。
   改成默认 `1'b1`（I2C 空闲电平）。**上板同样会中招**：CPU 还没配引脚之前总线就是 z。
2. **从机必须在整个 ACK 高电平期保持 SDA 低**。主机是在 M_ACK1 之后的 `cnt` 滚回处
   （远晚于上升沿）才采 `sda_q1`。tb 若在上升沿就释放 SDA，主机读到的是 NACK ⇒
   第 2 字节发不出去（实测从机只收到 1 个字节）。

**全回归 17/17 绿**。

---

## §14 tim_p PWM 扩到 4 路（2026-09-12）

原来是 2 路（`PWM_DUTY1/2`、`PWM_EN` bit0/bit1）。扩到 4 路时顺便把 duty 寄存器排成连续的
一段，代码里就能用一个循环代替四段重复：

| 字偏移 | 名称 | 说明 |
| --- | --- | --- |
| +0x00 / +0x04 / +0x08 / +0x0C | CNT_SET / CNT / MODE / IRQ_CLR | **不变** |
| +0x10 / +0x14 / +0x18 / +0x1C | PWM_DUTY1 ~ PWM_DUTY4 | 新增 3/4 路，1/2 路地址不变 |
| +0x20 | PWM_EN | bit0~bit3（**从 +0x18 顺延**） |
| +0x24 | IRQ_STAT | **从 +0x1C 顺延** |

只有 PWM_EN 和 IRQ_STAT 挪了位置（给 duty 让出连续的一段），`tools/tim_p_test.S` 跟着改。
内部改成数组：`pwm_duty[0:3]`、`pwm_en[3:0]`，写口用一个 4 次循环按
`bus_addr_in[3:0] == R_DUTY1 + i` 命中，读口同理。

**gpio 侧**：mode 枚举空着的位置补上 `PWM3 = 4'b1011`、`PWM4 = 4'b1100`，
`gpio_group` 加 `pwm3`/`pwm4` 输入并透传；`mcu_top` 把 tim_p 的四路接到 gpio 四路。

**复用脚电平镜像**：`gpio_group` 的顺序块把 **TX / PWM1~4** 这五类复用脚的电平镜像进
`gpio_output`，供 `R_OUT` 回读（只影响回读，引脚驱动走组合块直接透传）。
补 PWM3/PWM4 时漏了这处，导致这两种模式下 `R_OUT` 读回恒 0，而同为 PWM 的 PWM1/PWM2
能读回实际电平 —— 已补齐，`gpio_group_tb` 加"PWM3/PWM4 电平镜像进 R_OUT 回读"一项守住。

**验证**：
- `tim_p_tb` **15 项全过**（新增：pwm3 高低电平都出现过、未使能的 pwm2/pwm4 恒低、
  PWM_EN 回读=0x5、DUTY3 回读=2）
- `tim_p_irq` 闭环 **PASS @cyc=484**：程序把脚0/脚1/脚2 配成 PWM1/PWM2/PWM3，
  tb 在引脚上同时观测到 **pwm1 与 pwm3 的高、低电平**，ISR 4 次、claim 源号 2 不变
- 全回归 **20/20 绿**（§15 起回归项由 17 增至 20）

---

## §15 测试资产清理（2026-09-12）

一次性的探针 tb 和被取代的调试程序清掉：`project_risc_v.sim/*.v` 71 → **28**、
`tools/*.S` 63 → **32**，共删 74 个。删的是三类：定位期抓事件的探针（`cm_*.v`、`*_probe.v`）、
早已语法不通的旧 tb（`coremark_tb`、`mtb`、`mtp`、`mtirq*`）、被后续版本取代的调试程序
（`post_flush*`、`t2/t3/t5_*`、`ic_*`、`*_flash`、`fwd_h4` 等）。

**三组"钉住过真实根因"的微测保留并并进 `tools/run_reg.sh`**（回归 17 → 20 项）：

| 新增项 | 它钉住的形状 | 出处 |
| --- | --- | --- |
| `mt_jalra` | `jal` 后紧邻用 `ra` —— decoder 漏置 `rd_back1` | §10 |
| `itcm_rd` | 数据通路读不到 ITCM —— `.data` 必须镜像 | §6 |
| `mt_ldst_stall` | load-use 停顿 + 紧邻 store | §5.3 |

后两项原先只 `$display` 不判定，各补了 PASS/FAIL 行。
`boundary.S` / `bytetest.S` / `hello.S` / `uart_test.S` 无人引用但保留（边界、字节使能、SoC UART 用例）。

---

## §16 dcache 挂总线（2026-09-12）

> **⚠ 已被 §23（2026-09-19）取代**：dcache 现在在 `cpu_top` 核内、**不经过 bus_arb**，
> 译码窗口也从总线槽 6 改成 dtcm 窗口。本节保留为历史记录。

队友移植来一份 `soc/dcache.v`，原本面向另一套 valid/ready 握手总线、mem 侧是整套 AXI4 式带通道。
按 §5 改造成本工程的总线外设，**结构上与 `icache` 同构**，挂在 `bus_arb` 后面。

### 16.1 拓扑与槽位

`svg_graph/source/connected-edges.json` 里本来就画好了：`arb → dcache`（d_addr/d_data）、
`dcache → ram`（dmem_addr/dmem_wdata/dmem_rdata/dmem_len/dmem_be）、`arb → ctrl`（d_hold）。
即 dcache 是**挂在仲裁器后面的一台总线设备**，和 `arb → uartplic` 并列。

- 槽位 **6 = 0x1800_0000（字节）→ 字地址 0x0600_0000**（1..5 已占）。`bus_arb.v` 的 `[31:24]` 1..31 是通用解码，**加设备不用改解码**。
- `dcache.busy` → `bus_arb.d_hold` → `cpu_top.bus_hold_in` → `controller.stall`（框图里的 `d_hold` 线，本次补上）。
- mem 侧下位是 **flash 控制器**（`peripherals/flash_spi.v` 目前还是空壳），`mcu_top` 里先按 `icache` mem 侧的惯例接常量
  （`mem_rdata=0`、`mem_valid=0`）。**没实现 flash 控制器之前，访问槽 6 会挂在缺失上** —— 与 icache 外部取指同样的情况。

### 16.2 结构（照 icache 重写）

上一版是 6 态状态机（IDLE/LOOKUP/READ_CMD/REFILL/WRITE_CMD/RELEASE）+ 锁存地址 + 寄存器读数组，
命中要走 3 拍。现在按 `icache.v` 的形状来：

| 项 | icache | dcache（现在） |
| --- | --- | --- |
| 状态 | 1 位 `stage`（0=查/应答、1=回填） | **同** |
| 地址 | 不锁存，`tag/idx/word` 组合取自 `ibus_addr_in` | **同**（取自 `bus_addr_in`） |
| 阵列 | `tag_ram[0:1][0:127]`、`iram[0:1][0:127][0:15]`、`valid`/`lru` | `tag_ram[0:1][0:127]`、`data_ram[0:1][0:127][0:3]`、`valid`/`lru` |
| 回填 | `fill_buf[0:15]` 收齐后整行写入 | `fill_buf[0:3]` 收齐后整行写入 |
| 命中出数 | `ibus_data_out <= iram[way][idx][word]`，**1 拍** | `bus_data_out <= data_ram[way][idx][word]`，**1 拍** |

几何：**2 路 × 256 组 × 16 字 × 4B = 32 KB，行 64 B**（2026-09-14 扩到 32 KB，原 2 路 × 128 组 × 4 字 = 4 KB，
见 §21）。地址约定不变：CPU 侧是**字地址**（`[11:4]`=组号、`[31:12]`=tag、`[3:0]`=行内字），
mem 侧是**字节地址**（`bus_addr_in << 2`）。

**命中延迟从 3 拍降到 1 拍** —— 和 DTCM 一样：地址脉冲那一拍采到，下一拍 `ld_ready` + 数据，
正好是 `lsu` 期望的「push 后第 2 拍 ready」。所以**命中时 `busy` 不抬**，CPU 不为此停顿。

### 16.3 请求与写通路

- 本工程总线没有 valid 线，时钟上只有 **1 拍的地址脉冲**（`lsu.v:110-113` 打完就清零）。
  请求判据按 §5「外设无需识别 `[31:24]` 但需定义」：`bus_addr_in[31:24] == 8'd6` 且 `stage == 0`。
- **写**：不等状态机，直接由请求组合产生 `mem_req && mem_we`，**当拍打到 mem 侧，零额外周期**；
  同时若命中就按 `bus_be_in` 逐 lane 更新缓存行（写透 + 写命中更新，无脏位无写回）。
  写不需要 CPU 应答 —— 本工程 `ready` 只在读分支抬（`dtcm.v:56-67`），`bus_arb` 对写的 `per_sel` 恒 0。
- **读缺失**：锁 `miss_word/fill_addr/fill_tag/fill_idx/fill_way`，`stage<=1`，4 拍 burst 收进 `fill_buf`，
  `fill_end` 整行写入并当拍出数。替换先挑无效路，两路都有效才按 `lru`。
- `busy = (stage==1 && !fill_end) || (rd_req && !cache_hit)` —— **缺失从检出那一拍就把 CPU 按住**。

### 16.4 反压通路（本次唯一动到已验核的地方）

多周期设备必须有停顿线：访存队列若满却不反压，背靠背的 load 就会丢掉。
**2026-09-24 起 lsu 改成两节车厢（§5.3）后有了结构性反压**：车3 占用就组合拉高 `stall`，
入口进不来时也拉 `stall`（`full_stall`）⇒ 不再有"丢第 N 条"的问题。

- `controller.stall = stall | i_busy | bus_hold_in`
- `lsu` 的入队门 `new_in = 访存 && !flush_w && !(stall_m|stall_v|icache_busy) && !use_hit && !waw_hit`

第二条是必需的：停顿期间若 lsu 级停着一条访存指令，入队信号仍为 1 ⇒
地址脉冲**每拍重复摆**、队列每拍重复入队（对 dtcm 无害是因为它每拍都答），
对多周期的 dcache 就变成「入队 N 次、只应答 1 次」⇒ 队列失步挂死。门控后停顿期不发，停机解除再按 EXE 发一次。
（store 不用管：store 的 `we/be/data` 本来就在停顿分支里清零。）

**验证没伤到核**：把 HEAD 的 4 个文件（`cpu_top`/`lsu`/`bus_arb`/`mcu_top`）取出来建基线树，同一套 tb 跑一遍，
**回归输出与改动后逐字一致**（只多出新增的 dcache 项）。`bus_hold_in` 恒 0 时核的行为严格不变。

### 16.5 验证

- `dcache_tb.v` **32 项全过**：读缺失回填 / 读命中 / 同行其余字 / 跨行 / 缺失落在行内第 3 字 /
  写透 + 写命中更新 / 字节使能写 / 写缺失不分配 / way1 命中取数 / 2 路替换与 LRU（白盒查 `tag_ram`/`valid`）/
  `ld_ready` 纪律 / `busy` 在 data 前 1 拍落 / 命中延迟 = 1 拍 / 看门狗
- **变异测试：11 个变异全部被抓**（way1 取数取错、命中不更 LRU、替换恒选 way0、写命中不更新、burst 少一拍、
  缺失回错字、命中不抬 ld_ready、缺失不抬 busy、写不打到 mem、tag/valid 不写回），无一漏网
- **回归 21 项**（新增 dcache_tb），与基线逐字一致

### 16.6 `bus_arb` 的 `per_sel` 改成撑得多周期外设（2026-09-12，已应用）

原实现：

```verilog
else if (!bus_loaded_out) per_sel <= !bus_we_f_cpu ? per_decode : 5'd0;
```

`per_decode` 取自 `bus_addr_f_cpu[31:24]`，而 `lsu` 的地址**只摆 1 拍就清零** ⇒ 下一拍 `per_decode = 0`
⇒ `per_sel <= 0`。于是 `per_sel` 只挂 1 拍就掉 0，**多周期外设抬的 `ld_ready` 再也送不到 CPU**。
对 1 周期的 dtcm/uart 无影响（它们本来就 1 拍应答），只挡住多周期设备。

微测 `project_risc_v.sim/arb_hold_tb.v`（外设模型照 dcache 的形状，第 4 拍抬 1 拍 `ld_ready`）实测：
**外设抬了 1 次 `ld_ready`，`bus_loaded_out` 抬了 0 次**。

改成「按请求锁存、按住直到应答」：

```verilog
if (rst) per_sel <= 5'd0;
else if (per_decode != 5'd0) per_sel <= bus_we_f_cpu ? 5'd0 : per_decode;
else if (bus_loaded_out)     per_sel <= 5'd0;
```

- 请求拍：`per_decode != 0` ⇒ 读锁进 `per_sel`，写恒 0（写不应答，与原来一致）
- 等待期：`per_decode = 0` 且没应答 ⇒ **保持**，不再掉 0
- 应答拍：`bus_loaded_out = 1` ⇒ 清 0，下一拍回到空闲

验证：微测 `PASS`（`per_sel` 保持 6、`bus_loaded_out` 抬起、`bus_data_b_cpu` 出槽 6 数据、应答后回 0）；
**全回归 22 项与改这一行之前逐字一致** —— 对所有 1 周期外设行为不变。

---

## §17 flash_spi：外置 flash 控制器（2026-09-12）

> **⚠ 已被 §23（2026-09-19）停用**：`mcu_top` 不再例化 `u_flash_spi`，4 个顶层 SPI 端口
> 与对应 xdc 约束已删，**文件保留**。本节（含 §17.9 的 `busy` 早放一拍）保留为历史记录 ——
> 接回 flash 时这些坑仍然成立。

外置 SPI flash 控制器。它**有三个上游口**：
- **dbus 控制口**（§5 总线外设，槽 **7**）—— 只管**擦除和配置**，**读不到也写不到 flash 数据**
- **icache 的 mem 侧**（只读）
- **dcache 的 mem 侧**（读 + 写）

SPI 四根线从 `mcu_top` 直出，不经 gpio、不可配置。

### 17.1 统一的 mem 侧接口（8 根）

以 `icache` 原有形状为基准，把两边做成**完全同形**：

```
output reg        mem_req;      // 请求（读/写），电平保持到被接受
output reg        mem_we;
output reg [31:0] mem_addr;     // 字节地址
output reg [31:0] mem_wdata;
output reg [3:0]  mem_be;
input             mem_ready;    // 从机已接受本次命令
input      [31:0] mem_data;
input             mem_valid;    // 读数据有效，每字一拍
```

`icache.v` 补 `mem_we`/`mem_wdata`/`mem_be` 三个输出（恒 0，只读缓存用不到；纯增输出，4 个 tb 零改动）；
`dcache.v` 的 `mem_rdata` 改名 `mem_data`、新增 `mem_ready`。

### 17.2 flash_spi 本体

- **SPI Mode 0**（SCK 空闲低、上升沿采、下降沿移），`SCK = clk/(2*(SPI_DIV+1))`，`SPI_DIV` 来自 CONFIG 寄存器。
- 命令：`0x03` 读、`0x02` 页编程、`0x06` WREN、`0x05` RDSR、`0xD8` 扇区擦除。
- **读**：`CS↓` → `0x03` + 24 位地址 → 之后随 `mem_req` 流式移出，每 32 位抬一拍 `mem_valid`；
  先收到的字节落在**最低位**（小端，与写路径自洽）。`mem_req` 落就收尾 —— **不需要事先知道 burst 长度**。
- **写**：`[WREN]` → `[0x02][地址][数据字节]`，`CS↑` 后置 `prog_busy`。
- **擦除**：`[WREN]` → `[0xD8][扇区地址]`，`CS↑` 后置 `era_busy`。
- **轮询**：`[0x05]` 收 1 字节状态，WIP 仍为 1 就再来一轮；WIP 落则清 `prog_busy`/`era_busy`。
- **子事务之间必须留 CS 间隔**（`S_GAP` 状态）：WREN 和主命令是**两段独立事务**，CS 不抬起来 flash 不会认第二个命令字节。
- **轮询不占住 FSM**：一次扇区擦除在真 flash 上是几十毫秒。若主 FSM 一直卡在轮询里，icache 缺失就无法取指。
  所以轮询是**后台最低优先级**、夹在缓存事务之间跑：`rd_pick`（缓存读）永远优先，写完/擦完就回 IDLE，
  靠 `poll_pick` 慢慢确认 WIP 落。`STATUS.busy` 覆盖**整个未完成操作**（含 `prog_busy`/`era_busy`/`era_pend`），
  不能只看 FSM 状态 —— 否则软件会在两次轮询的间隙误判"擦完了"。

### 17.3 地址映射

**不能用 `mem_addr[23:0]` 当 flash 地址**：dcache 槽 6 首字节 `0x1800_0000` 的低 24 位是 **0**（`8` 在 bit27），
整个 4MB 窗口会塌到 flash `[0,4MB)`，正好盖住 icache 的代码区。改成每个上游各减区基址、再加各自 flash 基址：

| 上游 | SoC 区首字节 | flash 基址 | 结果 |
| --- | --- | --- | --- |
| icache | `0x0000_8000` | `0x000000` | 代码从 **flash 0** 起 |
| dcache | `0x1800_0000`（低 24 位为 0） | `0x400000` | 数据从 **flash 4MB** 起 |

### 17.4 总线寄存器（槽 7 = 字地址 0x0700_0000 / 字节 0x1C00_0000）

| 偏移(字) | 名称 | 访问 | 语义 |
| --- | --- | --- | --- |
| 0 | `ERASE_ADDR` | RW | 写 = **要擦的扇区在 dcache 侧的字节地址**（低 24 位有效，控制器往下取整到 4KB）→ 下发一次 `[0x06][0xD8]`。已有擦除在跑时写入被忽略 |
| 4 | `STATUS` | RO | bit0=busy（整个操作未完） bit1=flash_wip bit2=prog_busy bit3=era_busy bit4=fifo_full bit5=fifo_empty |
| 8 | `CONFIG` | RW | bit[1:0] = SPI 分频（SCK = clk/(2*(div+1))），复位值 1 → 12.5MHz@50MHz |
| 0x0C | `TIMEOUT` | RW | WIP 等待上限（**周期数**，26 位）。复位默认 `26'h3FF_FFFF` ≈ 1.34s@50MHz。**写 0 = 关闭超时** |
| 0x10 | `IRQ_STAT` | RW(写1清) | bit0 = 超时标志（sticky，只能写清） |
| 其它 | — | RO | 返回 0 且 `ld_ready` 仍抬（读不挂死） |

`STATUS` bit6 也镜像这个超时标志（只读）。

### 17.4b 超时与中断（走 PLIC 源 5）

真 flash 万一不清 WIP，控制器会一直轮询下去、写 FIFO 填满、CPU 被 `d_hold` 永久挂住 —— 没有自救途径。
所以加一个**看门狗**：

- 每次下发编程/擦除时把 `tout_cnt` 重装为 `TIMEOUT`；只要还有未完操作（`prog_busy || era_busy`）就每拍减一。
- 减到 0 → **中止本次操作**（清 `prog_busy`/`era_busy`/`era_pend`）并置 `irq_err`。CPU 因此解锁。
- `flash_spi_irq = irq_err`，接到 **PLIC 源 5**（源 1=UART 2=tim_p 3=gpio 4=i2c）。
  按 §6「中断只能置 1」的要求，它是 sticky 的，**只能由写 `IRQ_STAT` 清**。
- 软件流程：`STATUS.busy` 等 0 或等中断 → ISR 里**先写 `IRQ_STAT` 撤源、再 claim/complete**（顺序同 gpio/i2c）。

**总线碰不到 flash 数据**：总线上只有这三个寄存器。数据读只走 icache/dcache 两条 mem 通路，
数据写只走 dcache（写透 → `0x02`）。写 `ERASE_ADDR` 之外的总线写不产生任何 SPI 事务。

### 17.5 写反压

SPI 编程一个字要几百拍，而 dcache 写是 1 拍发完不管。解法 = **控制器写 FIFO 深 4** + **dcache 把写保持到被接受**：

- dcache：请求拍锁 `wr_pend`/`wr_addr`/`wr_data`/`wr_be`，`mem_req`/`mem_we` 保持到 `mem_ready`；
  `busy` 含 `wr_pend && !mem_ready`。
- flash_spi：`dcache_mem_ready = !FIFO 满`。
- 于是 **FIFO 不满时写仍 1 拍被接受、CPU 不停**；只有真写不过来才拉 busy → `d_hold` → CPU 停顿，**一笔不丢**。

### 17.6 接入 mcu_top

顶层加 4 个口 `spi_cs_n/spi_sck/spi_mosi/spi_miso`（推挽）；`PER_FLASH = 7`；例化 `u_flash_spi`。

**xdc 已按开发板排针分配**（Microphase Z7-Lite，`constrs_1/new/cpu.xdc`）：

| 信号 | 排针 | Bank / 电平 |
| --- | --- | --- |
| `gpio_pin_bus[31:0]` | **JP1** 前 32 个信号脚（JP1-1..36，跳开 11/12/29/30 电源） | Bank34 / LVCMOS33 |
| `spi_cs_n / spi_sck / spi_mosi / spi_miso` | **JP2-1..4** | Bank35 / LVCMOS33 |
| `clk` | N18（板上 50MHz 晶振，不在排针上） | Bank34 / LVCMOS33 |
| `rst` | T12 = JP1-39 `GPIO1_17P`，即板上 **PL KEY2** | Bank34 / LVCMOS33 |

`gpio_pin_bus[i]` 按 JP1 排针顺序取：bit0=JP1-1(N17)、bit1=JP1-2(P18)、…、bit31=JP1-36(U15)。
JP1 共 36 个信号脚，用掉 33 个（32 GPIO + rst），空出 JP1-37(U13)、38(V13)、40(U12)。
原先指向已不存在端口 `rx`/`tx` 的两行死约束已删除。

### 17.7 验证

- **`flash_spi_tb.v` 65 项全过**：tb 侧是**带 NOR 语义的 flash 从机模型**（WEL 只由 0x06 置、没 WEL 就忽略 program、
  program 按位与、0xD8 擦 4KB 扇区）。覆盖读写命令/地址/数据流、字节序、字节使能区间、burst 自然收尾、
  三种并发、FIFO 满→不丢、**总线擦除**、**擦除非对齐地址取整**、**擦除中 STATUS.busy=1**、
  **擦除中再写 ERASE 被忽略**、CONFIG 分频真的改变 SCK 周期、以及**总线访问不产生 SPI 事务（碰不到 flash）**、
  **擦除进行中缓存读仍然通**。
- **`flash_soc_tb.v` 闭环 PASS @cyc=9859**：真 `mcu_top`，程序放 flash 里。icache 经 SPI 取指跑通；
  再经 dcache 读两行、写一个字（核对真的落进 flash 模型）；**最后经 dbus 写 ERASE 寄存器下发扇区擦除、
  轮询 STATUS.busy、读回被擦扇区应为全 1** —— 整条控制通路走通。
- **超时链路**：`flash_spi_tb` 里把从机的 WIP 挂住 → 断言超时后 `flash_spi_irq` 拉高、`era_busy` 清、`STATUS` 镜像位起、写 `IRQ_STAT` 能清；
  `flash_soc_tb` 闭环里程序把超时调小、使能 PLIC 源 5、下发一次"擦不完"的擦除 → **ISR 恰好 1 次、mcause=0x8000000B、claim 源号=5**。
- **变异测试 19 个全被抓**（命令字节 ×4、地址高字节、读写字节序、FIFO 不满、写长度、CS 间隔、
  轮询不清忙、busy 不含擦除、擦除不看忙、擦除地址不对齐、两个区基址、dcache 不映射、CONFIG 写死、仲裁）。
  头几轮有漏网，是靠**给 tb 补覆盖**抓住的（例如"擦除地址取整"要断言发出的地址 A11:A0 全 0，
  而不是只看被擦的内容 —— 从机模型自己也会取整，看不出来）。
- **全回归 26 项**，与加 flash 之前逐字一致（只多出 4 个新项）。

### 17.8 已知未尽

- **icache 陈旧**：CPU 改写了 flash 里的代码区后，icache 没有 invalidate，仍按老内容取指。本次不解决。
- **`mem_ready` 对 icache 是死输入**（icache 自己把 `mem_req` 保持到 fill 完），统一接口的代价。
- **不做 Fast Read / 双线四线**：需要把顶层四根线改成 IO0~IO3 双向，本次没做（用户明确不做 QSPI）。

### 17.9 icache 链式再回填时 `busy` 早放一拍 → 丢一条指令（已修，2026-09-13）

**现象**：写 `flash_soc_tb` 的 ISR 时，`flash_spi` 超时中断进来后，ISR 里靠前的一条 `sw` 静默丢失。
ISR 是 `csrr mcause; sw t0,0x144(s1); ...`，`dtcm[0x144]` 一直保持旧值 0，看起来像"读到了错的中断原因"。

**定位过程**：先做了 4 个微测。前 3 个都是负结果（`mt_csrr` 直线间隔 0/1/2/3 全对 ⇒ 不是 `csrr` 前递缺口；
`mt_csrr2` ITCM 里的 ISR 入口同形状对 ⇒ 不是陷阱入口；`mt_csrr3` ISR 在外部取指区 + 存储器每 64 拍一拍也对
⇒ 不是"回填慢"）。第 4 个 `mt_csrr4` 定点命中：**tb 在 icache 正在回填 0x8080 那一行时拉中断**。

**逐拍现场**（干净构建）：

| 拍 | ic.stage | fill_end | fill_addr | ia 字号 | stage | 下一拍 `ibus_data_out` |
| --- | --- | --- | --- | --- | --- | --- |
| c01 | 1 | 1 | 0x8080（**旧行，CPU 已不要**） | w0 | **EXE**（放行） | 0（新行 `valid` 还是 0） |
| c02 | 1 | 1 | 0x8000（真正要的行） | **w1** | EXE | `fill_buf[miss_word=0]` = **w0** |
| c03 | 0 | 0 | — | **w2** | EXE | `iram[..][2]` = **w2** |

实测 `inst_effective` 序列 = `w0, w2, w3, …` —— **跳过了 w1**。
（ISR 开头是 `csrr,sw,csrr,sw`，`w0`/`w2` 的 opcode 都是 `0x73`，所以看起来像"第 1 条跑了两遍"，其实是**跳过一个**。）

**根因**（`soc/icache.v` 的 `busy` 组合表达式）：

```verilog
busy = (stage == 1'd1 && !fill_end) || (stage == 1'd0 && !cache_hit && ibus_re_in);
```

`fill_end` 那个沿上，icache 顺序块同时做两件事：① 把刚填好的行写进 `valid`/`tag_ram`/`iram`
（**这些值这一拍还读不到**，`valid` 仍是 0，所以命中路径这一拍出 0）；② 链式条件成立时 `stage` 保持 1、
**启动下一行**的回填。而 `busy` 只看到 `fill_end=1` 就落 0，**看不见"这一拍还在启动新一行、新行一个字都还没出来"**。

于是流水线提前一拍放行、`ia` 前进一位；等真正那行的 `fill_end` 到来时，`fill_buf[miss_word]` 与 `ia` 已经错开一位，
下一拍命中路径又按**当时**的 `ia` 取字 —— **中间那个字谁也没给**。

**触发条件 = 链式再回填**（两个背靠背的 `fill_end`），正是 §9 引入的路径。普通单次缺失不错位：
那时 `ia` 在整个填充分冻结在 `miss_word` 上，`fill_end` 补发的就是 CPU 要的那个字。

**修法**：把 `busy` 的填充项从「只看 `fill_end`」改成「`fill_end` 且**链式条件仍成立**时也算忙」——
第三项就是 §9 那个链式再回填的判据本身：

```verilog
busy = (stage == 1'd1 && (!fill_end || (!cache_hit && ibus_re_in &&
                                         !(fill_idx == idx && fill_tag == tag)))) ||
       (stage == 1'd0 && !cache_hit && ibus_re_in);
```

**验证**：
- `mt_csrr4`：改前 `dtcm[0]=00000000` FAIL，改后 `8000000b` PASS
- `flash_soc_tb` 闭环：**`flash_app.S` 里的 3 条 `nop` 已去掉**，闭环仍 PASS（@cyc=19583 → 17404）
- 回归 **28 项**，与改前逐字一致（唯一差异是 `flash_soc_tb` 的拍数与链接告警地址）
- 顺带：`tools/build_ic.sh` 里 app 的链接地址从陈旧的 `0x4400` 改成 `0x8000`；
  `mt_csrr4` 用**专用 runner** `tools/run_mt4.sh`（每次都重建 hex，避免被 `run_reg.sh` 覆盖）

---

## §18 多驱动清理：i2c / flash_spi（2026-09-13）

用户指出 i2c 有多个位置存在多驱动。全工程扫了一遍，**i2c 三处、flash_spi 两处**（flash_spi 是同批写的，同类问题）。

### 18.1 问题

同一个 `reg` 被两个 `always` 块赋值。Verilog 语义**不确定**（同一时间步内按块执行顺序，后者覆盖前者），综合会报 multiple driver。
仿真里 iverilog 按源码顺序跑，所以 `i2c_tb` 28 项照样过 —— **是碰巧过的**。这两个模块都是 2026-09-12 写的、**从没综合过**，一上综合就会暴露。

| 模块 | 信号 | 写者 A | 写者 B |
| --- | --- | --- | --- |
| i2c | `bit_cnt` | 主机 FSM | 从机 FSM |
| i2c | `ctrl_start` | 总线写块（rst 清 + 写 1 置位） | 主机 FSM（`M_IDLE` 里清） |
| i2c | `ctrl_stop` | 总线写块（rst 清 + 写 1 置位） | 主机 FSM（进 `M_STOP` 时清） |
| flash_spi | `era_pend` | 总线块（rst 清 + 写置位） | FSM（擦除开始时清） |
| flash_spi | `irq_err` | 总线块（rst 清 + 写 1 清） | FSM（超时置位） |

`gpio_group` 的 `i` 也被扫描器报出来，但那是**三个块共用同一个 for 循环下标**，综合会展开，无副作用 —— 不是这类问题。

### 18.2 修法：每个信号收敛到一个写者

- **`bit_cnt`（i2c）**：从机 FSM 改用自己的 `s_bit_cnt`（15 处）。两个 FSM 本来就各跑各的，只是共用了变量名。
- **`ctrl_start` / `ctrl_stop`（i2c）**：**总线写块当唯一写者**。主机 FSM 只输出一拍脉冲 `m_start_clr` / `m_stop_clr`，
  总线写块在 `reg_sel` 的 case 之后拿它来清。
  - 语义不变：以前"消费的那一拍就清"，现在"下一拍由总线块清"。FSM 在中间那一拍已经进了 `M_START`/`M_STOP`，
    不再读这两个位，所以**不会重复触发**。
- **`era_pend`（flash_spi）**：总线块当唯一写者；FSM 输出 `era_taken` 脉冲。
  FSM 超时分支里原来那句 `era_pend <= 1'b0` 是**冗余**（擦除开始时就已经清了，且总线写被 `!era_pend && !era_busy`
  门控住，期间不会再挂）→ 直接删掉。
  - 语义不变：`era_pend` 多保持一拍，但那一拍 `era_busy` 已经是 1，`era_pick` 和总线写的门控都挡住了。
- **`irq_err`（flash_spi）**：总线块当唯一写者；FSM 输出 `err_set` 脉冲。

### 18.3 验证

- 重扫多驱动：**i2c / flash_spi 均为「无多驱动」**（只剩 `gpio_group` 的 benign for 下标）
- **全回归 28 项输出与改动前逐字一致** —— 这是"纯收敛写者、行为不变"的最强证据
  （`i2c_tb` 28 项、`i2c_irq` 闭环 @cyc=2556、`flash_spi_tb` 71 项、`flash_soc_tb` @cyc=17404 全部照旧）

---

## §19 条件分支跳到外区丢一条指令：`pc.v` 的 `br2`/`br3` 缺 `ext_target` 调整（2026-09-14）

**现象**：把 dcache 的实验版（写命中失效）接上去跑 `flash_soc_tb`，程序判据从 `0x600D` 变成 `0x0000F000`。
反汇编对位：`0x8110 lui t1,0xf` 执行了，**紧邻的 `0x8114 addi t1,t1,13` 整条没执行**，
于是 `t1=0xF000` 被原样存进判据槽。同一组合只要换回原 dcache（写命中更新）就 PASS —— 说明触发点由 dcache 决定，
故障本身不在 dcache。

**定位**：`0x8110` 是 `bne t5,t4,8110` 的落点，也是新缓存行的**行首**。给闭环加取指探针（`project_risc_v.sim/flash_trace_tb.v`），
抓到触发那两拍：

| 拍 | pc_addr | itcm.fetch_addr | icache 看到的 | word | miss_word | ctl |
| --- | --- | --- | --- | --- | --- | --- |
| 分支重定向拍 | 0x804C | **0x2044**（目标，组合直通） | 0x2044 | 4 | 0（旧值） | FLUSH |
| 下一拍起 | **0x8114** | **0x2045** | 0x2045 | **5** | **4** | STALL |

`miss_word` 是**分支重定向那一拍**锁的（当时 `ibus_addr_in` 是分支目标本身的字地址 0x2044）；
流水线稳定后 `pc_addr = 0x8114`，`fetch_addr` 变成 0x2045。
回填结束那一拍 icache 按 `miss_word` 出数（0x8110 那条），而 CPU 要的是 `fetch_addr` 那个字（0x8114 那条）—— 错开一个字。

**根因**（`core/pc.v` 的 FLUSH 分支）：`jalr_fail` / `irq` / `irq_ret` 都按 `ext_target` 做了 ±4 调整，**`br2` / `br3` 漏了**：

```verilog
else if (jalr_fail) pc_addr <= jalr_target_q + (ext_target ? 4'd0 : 4'd4);   // 有
else if (irq)       pc_addr <= isr_addr2      + (ext_target ? 4'd0 : 4'd4);   // 有
if (br2)            pc_addr <= br_addr2 + beq_off_q2;                        // 漏
else if (br3)       pc_addr <= br_addr2;                                     // 漏
```

内区目标要 `+4`（`pc_addr` 相对取指地址多出来的那个字节偏移），外区目标要 `+0`。
这正是"进外区走 `jalr` 是好的、走条件分支就丢字"的原因：`jalr` 那条调整后 `pc_addr` 正好落在目标本身，
填充期间 `fetch_addr` 一直等于 `miss_word`。

**修法**：`br2`/`br3` 补上同样的调整。内区 `ext_target=0` 时表达式不变，**itcm 路径零影响**。

**验证**：
- 新定点微测 `mt_brext`（`tools/ic_boot_bext.S` + `tools/ic_app_bext.S` + `project_risc_v.sim/mt_brext_tb.v`）：
  起跳点在外区已缓存行、目标是一个**全新的行**，行首起连续 4 条 `addi` 各写一个 dtcm 槽。
  改前 `dtcm[0..3] = 11 00 33 44` FAIL，并打印 `回填结束：miss_word=0 但当前 word=1`；改后 `11 22 33 44` PASS。窗口约 210 拍。
- **全回归：与改前逐字一致**，只多出 `mt_brext` 一项 PASS；唯一差异是 `flash_soc_tb` 的拍数 17404 → 17406
  （分支重定向差 4 字节，后续时序整体挪 2 拍）。
- 之前必 FAIL 的「dcache 写命中失效 + flash_spi」组合不再丢指令（该 dcache 版本是 BRAM 面积实验，未进树）。

**补充**：§17.9 里"普通单次缺失不错位，因为 `ia` 在整个填充期冻结在 `miss_word` 上"这条前提，
**只在取指路径同源时成立**。条件分支重定向那一拍，`itcm.fetch_addr`（组合）与 `pc_addr`（寄存）本来就差一个字 ——
这条路径原先没有任何回归覆盖，所以一直潜伏，直到 dcache 改动换了分支方向才暴露。

---

## §20 dcache 数据阵列进 BRAM：写命中改「失效」＋ `bus_arb.d_hold` 补一拍（2026-09-14）

### 20.1 目的

`dcache` 的 `data`（1K×32）综合出来是**分布式 RAM**：`RAM64X1D × 64 + RAM64M × 320`。
整体占 `Slice LUT 2798`（其中 `LUT as Memory 1536`）。目标片 xc7z010 只有 6000 LUTRAM / 17600 LUT，这块占比过高。

### 20.2 原来的写法为什么推不成 BRAM

BRAM 只有 **1 个写口**、一拍只能做**一次**写操作。原写法「写透 + 写命中逐 lane 更新缓存行」在 `sb`/`sh` 时会变成最多 4 次独立写操作 → 推不出来。
试过且都失败的形状：整字单写口 + 字节使能写、4 个子字分别赋值、4 个并行 8 位阵列、`dtcm` 式 `merge_word` 自读改写（组合读又会挡住 BRAM 的同步读规则）。

### 20.3 采用的形状：写命中**失效**，不更新缓存行

写口只留「回填整字写」一路 → 单写口、单操作 → 推成 BRAM。
写命中时把命中那一路的 `valid` 清 0；写本身照旧写透到 flash。
下次读该行会缺失、从 flash 回填 —— flash 里已经是新值，取到的仍然正确。

**代价**：每个 `sw` 之后的首次读都要重新缺失回填（一次 4 字 SPI 事务）。功能正确，**性能有明确损失**。

### 20.4 顺带找出并修掉的核缺陷：dcache 缺失 load 紧邻使用

改完 dcache 后闭环挂在 `pc=0x8128 / 判据=0xF00D`。隔离实验证明与 dcache 无关：
把 app 里那条「读未缓存行（dcache 缺失）」的 load 的消费者从隔 2 条改成**紧邻**（`tools/flash_app_lduse.S`），
**用未改之前的 RTL** 跑同样挂。逐拍现场（未修，c=8471/8472）：

| 拍 | ctl | `d_hold` | dcache | `ld_ready` | `ld_we` | 结果 |
| --- | --- | --- | --- | --- | --- | --- |
| c8471 | EXE | 0 | `fill_end` 那拍，`busy` 已落 | 0 | 0 | `ld_data` 还是上一条的旧值 |
| c8472 | FLUSH | 0 | 数据这拍才出 | **1** | **1** | `br2=1` —— 分支与写回同拍，用旧值判的 |

**冲突本身**：`dcache.busy` 在 `fill_end` 那一拍就落（「busy 在 data 前 1 拍落」是规范 §6 的要求，`dcache_tb` 也断言这条），
而 `ld_ready` 下一拍才抬。`bus_arb` 原来是 `d_hold = |bus_busy`，跟着 `busy` 一起落
→ `bus_hold_in` 比 `bus_loaded_in` **早一拍释放** → 流水线提前一拍前进，紧邻消费者锁到的是 load 落盘前的操作数。
隔 2 条以上就没事 —— 基线里那三次缺失读恰好都隔 ≥2 条，所以一直潜伏。

**修法**：`bus_arb` 的 `d_hold` 在 `|bus_busy` 落下后再撑一拍（`d_hold_ext` 寄存 `|bus_busy`）。
单周期外设从不拉 `busy` → 对它**零影响**；只有多周期外设（dcache）会多持一拍。

### 20.5 综合对比（`synth_design -top dcache -part xc7z010clg400-1`）

| | 原 dcache | 写命中失效版 |
| --- | --- | --- |
| Slice LUT | 2798 (15.90%) | **1119 (6.36%)** |
| LUT as Memory | 1536 (25.60%) | **128 (2.13%)** |
| Slice Registers | 525 | 491 |
| Block RAM Tile | 0 | **1 × RAMB36E1** |

`data_reg`（1K×32）：原版 `RAM64X1D × 64 + RAM64M × 320`；新版 **`1 K x 32 READ_FIRST / WRITE_FIRST` → 1 块 RAMB36**。
`tag0`/`tag1`（128×23）两个版本都还是分布式 RAM —— 太小、而且命中判定要两个异步读，不值得改。

### 20.6 验证

- `dcache_tb` 模块级 **40/40 全过**（该 tb 的写路径判据对两种语义都成立）
- 闭环 `flash_soc_tb` **PASS @cyc=18054**
- 新增回归项 **`flash_soc_lduse`**（`tools/flash_app_lduse.S`）：**PASS**；
  把 `bus_arb` 的撑拍撤掉后该项立刻 **FAIL（0xF00D @cyc=9619）** → 守卫确实咬得住
- **全回归 30 项无 FAIL**

---

## §21 icache / dcache 容量都扩到 32 KB（2026-09-14）

> **⚠ 几何已被 §23（2026-09-19）改掉**：icache 现为 **3 路 × 128 组 × 32 字 = 48KB**
> （way0 锁定 16KB + way1/way2 各 16KB），dcache 现为 **2 路 × 256 组 × 8 字 = 16KB**。
> 本节的"选加长行而不是加组数"结论仍成立，保留为历史记录。

### 21.1 做了什么

两个 cache 现在都是 **2 路 × 256 组 × 16 字（64 B 行）= 32 KB**。
（**注**：icache 的几何当天晚些又改过一次 —— 256 组 × 16 字 → **128 组 × 32 字（128 B 行）**，容量仍是 32 KB，
原因与时序有关，见 §22.7。）

| | 原 | §21 当时 | **最终** |
| --- | --- | --- | --- |
| icache | 2 路 × 128 组 × 16 字 = 16 KB | 2 路 × 256 组 × 16 字 = 32 KB | 2 路 × **128 组 × 32 字** = **32 KB** |
| dcache | 2 路 × 128 组 × 4 字 = 4 KB | 2 路 × 256 组 × **16 字** = 32 KB | 2 路 × 256 组 × 16 字 = **32 KB** |

dcache 的**行从 4 字改成 16 字**（与 icache 同形）—— 这一条是关键。

### 21.2 为什么 dcache 选「加长行」而不是「加组数」

加组数 = 加索引位宽 → `valid0`/`valid1`/`lru` 这些 256 位向量、以及 tag 阵列的读选择从 128:1 变 256:1，
**全是纯 LUT**。实测（隔离对照）：dcache 只加组数（128→256 组、行仍 4 字）要多花 **890 个 LUT** 才换到 **1 块** BRAM。

改成长行则 `idx` 保持 8 位不动、tag 反而从 22 位降到 20 位，数据阵列多出来的部分全落在 BRAM 里 ——
**多出的 24 KB 基本白送**（实测比「只加 dcache 组数」那版还少 22 个 LUT）。

### 21.3 综合对比（`synth_design -top mcu_top -part xc7z010clg400-1`）

| 配置 | Slice LUT | LUTRAM | FF | RAMB36 |
| --- | --- | --- | --- | --- |
| A 原样（16 KB + 4 KB） | 11003 (62.5%) | 300 | 7709 | 17 (28.3%) |
| C icache 32 KB + dcache 8 KB（行 4 字） | 12624 (71.7%) | 516 | 8524 | 22 (36.7%) |
| **D icache 32 KB + dcache 32 KB（行 16 字）** | **12602 (71.6%)** | **484** | 8536 | **28 (46.7%)** |

28 块 = itcm 8 + dtcm 4 + icache 8 + dcache 8。报告存 `tools/_syn_32k.rpt`（另有 A / C 两份对照）。

### 21.4 代价

- **LUT 62.5% → 71.6%**（+1599）。索引位宽 7→8 位带来的读选择变贵，这部分**无法避免**。
- **dcache 一次缺失的回填从 4 字变 16 字**（SPI 事务约 4 倍长）。闭环 `flash_soc_tb` 拍数 18054 → 22020（+22%）。
  容量大 8 倍、缺失更少，净效果取决于工作负载的局部性。
- **32 位数据装不满 36 位的 RAMB36**（每块上限 89%），这是结构性的。

### 21.5 验证

- `dcache_tb` **40/40**（替换测试的地址与 tag 期望已按 16 字行更新）
- `icache_mt` PASS
- **全回归 30 项无 FAIL**，除闭环拍数外与改前逐字一致

---

## §22 时序优化：WNS -3.236ns → -0.089ns（2026-09-14）

### 22.1 起点与约束

约束 `create_clock -period 20.000`（50 MHz）。cache 扩到 32 KB 后 WNS = **-3.236ns**。
最差路径 `u_plic/irq_en_reg[2]` → `u_cpu_top/u_regfile/r1_data_dec_reg[13]`，
26 级、23.134ns（逻辑 5.465ns / **布线 17.669ns = 76%**）。**最差 9 条路径起点全是同一个 PLIC 寄存器。**

### 22.2 诊断：这不是「plic → regfile 的通路」

它俩只是同一条**跨 6 个模块、横贯全芯片的组合锥**的两个端点。链条：

```
plic 读数据 mux → decoder 操作数选择 → itcm/u_pc 取指地址加法（CARRY4 × 3）
→ decoder fetch_addr → icache tag 阵列读地址 → tag 的 LUTRAM 读出（cache_hit）
→ icache busy → cpu_top stall → decoder stage → regfile 读数据 mux
```

高扇出网（非时钟）：`icache_mem_addr[6]/[7]`、`u_icache/fill_tag_reg[9]_0[*]` **扇出 1110**、
`u_dcache/lru[255]_i_2_n_0` 772、`u_dcache/fill_idx[4]/[5]` 307。

### 22.3 修法①：PLIC 的 `exti` 打一拍（收益最大，**已应用**）

`mcu_top` 里 `plic.exti` **直连** `cpu_top.exti`，中间无寄存器；`csr.v:164` 的 `eirq_pend = exti` 也是组合的。
改成在 PLIC 内寄存：`irq_valid` 保持组合，`exti` 打一拍输出。

**中断受理晚一拍，但 `mepc` 一字未变** —— §6.2 那套 `irq_bubble`（FLUSH 置 4、之后 +4/拍到 12）
本来就是为「受理发生在冲刷后的任意拍」设计的，一拍偏移被它吸收。
全回归除 `tim_p_irq` 拍数 484→485 外**逐字一致**，全部陷阱微测（mt_csrr2/3/4、mtirq2d、mt_brext）未变。

**WNS -3.236 → -0.298ns。**

### 22.4 修法②：`flush` 从 `stage` 里拆出来（**已应用**）

`stage` 编码 EXE=01 / FLUSH=10 / STALL=11，所以

```
stage[1] = flush_events | stall
stage[0] = stall
```

**`stage[1]` 把「冲刷锥」和「停顿锥」OR 在了一起**，这是扇出 200+ 宽网的根源。
但 `(stage == FLUSH) ≡ flush_events`（冲刷优先级高于停顿）是**可证等价**的 ——
只关心冲刷的消费点根本不必经过 `stage[1]`。

改动：`controller.v` 新增 `output reg flush`（那 7 个事件的 OR，`rst` 时恒 0），`stage` 的选择与
`irq_bubble` 的 FLUSH 判据改用它；`mem_buf.v` 的清零判据 `stage == FLUSH` → `flush`；
`lsu.v` 的 `ld_enq` 里 `(stage != FLUSH)` → `!flush`。

**WNS -0.298 → -0.089ns**，LUT 12579 → 12551。

### 22.5 试过但失败的（**别重走**）

| 做法 | 结果 |
| --- | --- |
| 给 `stage` / `fetch_addr` / `idx` / `fill_idx` / `rd_addr` 加 `(* max_fanout *)` | **-3.236 → -6.017ns**。`max_fanout` 落在**组合网**上是复制整条逻辑锥（级数 26→30、布线 17.7→20.5ns），不是复制寄存器 |
| 给 `pc_addr` 加 `max_fanout`（它是真寄存器）+ `wb_reg` 换 `!flush && pipe_stall` 判据 | **-0.089 → -0.330ns**，LUT 还涨 150。两个改动捆在一起分不清谁的锅，但净效果为负，已撤 |

**结论：在只剩 0.1ns 余量时，任何改动都会重新洗牌布局、收益不可预测 —— 一次只做一个、每个都实测。**

### 22.6 顺带修掉 flash_spi 一个真 bug：轮询饿死读（**已应用**）

**现象**：icache 行从 16 字改到 32 字后，闭环 `flash_soc_tb` 超时 ——
`icache: stage=1 fill_cnt=0 busy=1`，ISR 那行的回填永远起不来。

**根因**：`flash_spi` 的状态在 `S_CMD → S_DATA → S_GAP → S_CMD` 里**无限循环、从不回 S_IDLE**：

```verilog
S_END: ... else if (xact == X_POLL) begin
    flash_wip <= status_rx[0];
    if (status_rx[0]) begin                  // 只看器件的 WIP
        tx_sh <= CMD_RDSR; state <= S_GAP;   // 直接跳 S_GAP，绕过 S_IDLE
    end
```

而 `rd_pick` 的条件是 `(state == S_IDLE) && ...` —— **只要擦除的 WIP 挂住、轮询循环一进，
icache/dcache 的读就被永久饿死**；超时清了 `era_busy` 也救不回来，因为这个分支根本不看它。

**为什么以前没炸**：16 字行时是**时间巧合** —— ISR 的缺失落在轮询开始之前。行加长后每次回填约 2 倍，
那个缺失正好落进轮询窗口。（与 §19 同一模式：改动只是把潜伏的 bug 换个方向暴露出来。）

**修法**（一行条件）——让轮询循环让位给挂起的读与超时中止：

```verilog
if (status_rx[0] && (prog_busy || era_busy) && !rd_req) begin
```

既修掉饿死，又符合原本的设计意图（「读可以穿过擦除」），顺带改善长擦除期间的读延迟。

### 22.7 icache 几何：256 组 × 16 字 → **128 组 × 32 字**（**已应用**）

容量不变（32 KB），但 **tag 阵列 256 项 → 128 项**（LUTRAM bank 4 → 2）→
关键路径中段那跳的地址译码扇出 93 → ~46。tag 位宽不变（20 位）：`word` 4→5 位、`idx` 8→7 位正好抵消；
数据阵列仍是 8192 字（13 位地址）→ BRAM 占用不变。

依据：指令是整块搬的，行大命中率更好；而且这条路径中段最贵的一跳（1.538ns、扇出 93）
正是 §21 扩容到 256 组直接造成的。

代价：行 64 B → 128 B，**每次缺失回填从 16 字变 32 字**，闭环拍数 22020 → 23144。

### 22.8 现状

| 版本 | WNS | 布线 | 级数 | LUT |
| --- | --- | --- | --- | --- |
| 起点（32 KB 后） | -3.236ns | 17.669 | 26 | 12579 |
| ① `exti` 打拍 | -0.298ns | 13.899 | 16 | 12579 |
| ② `flush` 拆分 | **-0.089ns** | 16.198 | 16 | 12551 |
| ③ 试错（已撤） | -0.330ns | 14.238 | 24 | 12701 |

**② 那版 P&R 跑过两次，结果完全一致、可复现。**

**未测**：§22.6 + §22.7 之后的 WNS。

**已知的病根（未解决）**：最差路径是
`pc_addr → itcm.fetch_addr → icache 的 idx/tag → tag 阵列读出 → icache.busy → i_busy → stall → stage → wb_reg.CE`
—— 这正是 §0.3 记的那个环的**残留**（当年只切断了 `stage → fetch_addr` 的**直连边**）。
这条锥**功能必需**（取指地址命中与否决定整条流水线停不停），而且**不能寄存**
（§17.9 / §19 已证明寄存 `busy` 就会早放/晚放一拍、丢指令）。

**下一步可考虑**：把 §22.4 的等价替换推广到其余 `stage == STALL` 消费点
（`icache` / `pre_decoder` / `decoder` / `regfile` / `lsu` / `mem_buf`），或做布局引导
（这条锥 16 级逻辑只花 3.5ns、布线花 16.2ns，瓶颈是**摆位不是逻辑**）。

---

## §23 取指单通道化 + 数据侧对称重构 + 全面缩容（2026-09-19）

### 23.1 拓扑变化（前后对照）

| | 之前 | 现在 |
| --- | --- | --- |
| 取指源 | itcm（低位 32KB）+ icache（外部取指，挂总线） | **icache 一个**（移入 `RTL/core/`，由 `cpu_top` 直接例化） |
| 取指地址生成 | 在 `itcm.v` 里 | **移到 `icache.v`**，与 §0.3 的优先级 mux 一起 |
| 数据侧 | lsu → bus_arb → dtcm（槽 6 是 dcache → flash_spi） | **lsu → dcache（在核内）→ dtcm**；dcache **不经过 bus_arb** |
| `bus_arb` | 7 个槽（含 dcache/flash） | **只服务 5 个外设**（uart/plic/tim_p/gpio/i2c） |
| `flash_spi` | 例化在 `mcu_top`，4 个顶层 SPI 端口 | **只保留文件，不例化**；顶层端口与 8 行 xdc 约束一并删除 |

`itcm` / `dtcm` 后续会按实际情况酌情换成 DDR / flash 的 IP core，所以本轮把它们都收成
"**流式后备存储器**"的统一形状（见 23.3 / 23.5），换 IP 时只动读流长度。

### 23.2 icache：唯一取指源（48KB，3 路组相联）

- 几何 **128 组 × 3 路 × 32 字 = 48KB**，每路 16KB。`iram[0:12287]`，
  `rd_addr = {way[1:0], idx[6:0], word[4:0]}`（14 位，路步长 4096 字）。
- **way0 锁定**：自举把 itcm 前 128 行（字节 `0x0000–0x3FFF`）装进 way0，`tag0` 恒 `19'd0`，
  正常路径的 `fill_way` 是常数 `2'd1`、tag 只写 `tag1/valid1` —— **way0 的写通路被物理取消**，
  不是靠 lock 位仲裁。
- **way1/way2 两路 LRU**（每组 1 位，`lru[idx]==1` 表示 way1 是 MRU）：
  失效优先填空位，两路都有效时按 `lru` 淘汰；命中/回填都翻转 `lru`。
  > 之前只有一路可写（way0 锁死、way1 独占），**等于直接映射**，组相联是坏的 —— 本轮的改动点。
- **自举（bootloader）**：复位释放后 `busy` 持续拉高，从 itcm 顺序填 128 行 × 32 字
  （约 4100 拍），填满后 `busy` 落下。功能上等价于"itcm + icache + bootloader"三者。
- 回填接口接了 `mem_req/mem_addr/mem_wdata/mem_be/mem_valid`，`mem_we` 恒 0。
- **`is_ibus` 输出已删**（单通道后没有"内/外区"之分，该线本来就悬空无人消费）。
- `pc.v` 的取指窗口随之从"32KB 内区"放宽到 **64KB**（与 itcm 同尺寸）——
  否则 tag 只有 {0,1} 两个值，way2 永远空转，3 路白加。

### 23.3 itcm：退居后备存储器（64KB）

不再承担取指地址生成，也不再是"取指源"：去掉 `is_ibus_q` 与写口，只剩

```
mem_req 上升沿锁行首 → 每拍流出一个字（共 32 拍）→ mem_valid
```

索引从 `mem_addr[14:2]` 改为 `[15:2]`（64KB）。`$readmemh` 路径仍走
`Tools/hexpath.sh local|commit` 的双机切换。

### 23.4 dcache：移入 `cpu_top`（16KB）

- **文件从 `RTL/soc/` 移到 `RTL/core/`**（`Tools/test/run.sh` 只通配 `RTL/core/*.v`）。
- 几何 **256 组 × 2 路 × 8 字 = 16KB**（行从 16 字缩到 8 字）。
- **译码窗口从"总线槽 6"改成 dtcm 的窗口**：`bus_addr_in[31:24]==8'd0 && bus_addr_in[23:13]==11'h100`。
  **不能放宽成 `[31:24]==0`** —— 会吞掉 `tim_in` 的 `[23:20]==1`，破坏
  `bus_data_in_final` 的按位或合流（见 §5 地址译码）。
- 保持 §20.3 的「**写穿 + 写命中作废**」：单写口（只有回填整字写）才推得出 BRAM；
  写穿保证 `dtcm` 永远是最新的。**不做 lock**，两路 LRU。

### 23.5 dtcm：退居 dcache 的后备存储器（32KB）

接口从「总线从设备」改成「流式后备」，与 icache↔itcm 同形：

- **读**：`mem_req & ~mem_we` 上升沿锁行首，连吐 **8 字**（= dcache 的行字数）
- **写**：`mem_req & mem_we` **电平**判据，单拍完成，按 `mem_be` 分字节落进 BRAM
- `mem_ready` 恒 1（写永远被接受，store 保持零停顿）

> **写必须用电平判据**：原 `flash_spi` 有写 FIFO 兜着，照抄 icache 的"上升沿启动"形状会让
> **背靠背两条 store 只落一条**（实测 `sw s3,0(s2)` / `sw s4,4(s2)` 丢第二条）。

### 23.6 pc.v：去掉 `ext_target`

icache 单通道后不存在"外区取指"，`ext_target` 恒为内区，于是整个删掉。基线里

```verilog
pc_addr <= jalr_target_q + (ext_target ? 4'd0 : 4'd4);   // 内区(ext_target=0) → +4
pc_addr <= isr_addr2     + (ext_target ? 4'd0 : 4'd4);   // 同上
pc_addr <= isr_ret_addr2 + (ext_target ? 4'd0 : 4'd4);   // 同上
```

内区分支的 `+4` **必须保留**（理由见 §7 末段）。本轮删除时三处 `+4` 一并丢失，
症状是 `jalr`/中断重定向**全体早 4 字节**（重执行上一条指令）—— 已补回。
`br2`/`br3` 的 `−(ext_target ? 4 : 0)` 内区分支本来就是 0，删掉无影响。

### 23.7 三个必须保住的隐性时序契约

1. **`dcache` 的 `fill_end` 拍要再撑一拍 hold**——`busy` 在 `fill_end` 就落，而
   `ld_ready` 下一拍才抬（§20.4）。原来这 1 拍由 `bus_arb.d_hold_ext` 兜住，
   dcache 搬进核内后必须在 `cpu_top` 里**复刻**（`dcache_busy_d1` → `d_hold_int`）。
2. **`dtcm` 的写要用电平判据**（见 23.5）。
3. **`dcache` 的译码窗口不能放宽**（见 23.4）。

### 23.8 实现过程中挖出并修掉的缺陷

| # | 缺陷 | 症状 | 修法 |
| --- | --- | --- | --- |
| ① | `dcache` 的 `rd_en` 在 miss 拍仍拉高 | 未初始化的 `data[]`（x）经**按位或合流**污染整个读回数据 | `rd_en = fill_end \|\| (rd_req && cache_hit)` + `data[]` 零初始化 |
| ② | `fill_cnt` 位宽没跟着行字数改（行 16→8 字只改了比较值 `4'd15→4'd7`） | 写入口 13 位 `{way,idx,fill_cnt}`、读口 12 位 `{way,idx,miss_word}` → **读写落在不同地址**，load 全 0 | `fill_cnt` 改 `[2:0]` |
| ③ | `dtcm` 的写用了上升沿判据 | 背靠背 store 丢第二条 | 改电平判据 |
| ④ | `pc.v` 丢 `ext_target` 时连带丢了 `+4`（见 23.6） | `jalr`/中断重定向早 4 字节 | 补回 |
| ⑤ | **`.data` 的 LMA 仍在 itcm**（`AT(__data_lma)`） | 数据侧读不到 itcm ⇒ `start.s` 的拷贝循环第一条 `lw` 就**永久 STALL**（`ready_in` 永不抬）。**板子上同样挂** | 改 LMA=VMA=DTCM 首址，由 `data.hex` 预装（见 23.11） |
| ⑥ | **`dcache.bus_data_out` 不每拍清零** | `cpu_top` 把 dcache/tim_in/dtcm 的读数据**按位或**合流；dcache 残留值被或进每一次外部总线读（实测读 UART 状态口拿到上次 dtcm 读的值 → `ee_printf` 轮询死循环） | 每拍先清零（对齐基线 dtcm / `tim_in.v:46` 的形状） |
| ⑦ | **`dtcm` 的读/写索引用了 `mem_addr[13:2]`（12 位）** | 数组是 `[0:8191]` = 32KB（8192 字，需要 13 位 = `[14:2]`），12 位只有 4096 字 = **16KB 可寻址**，字节 `0x00804000–0x00807FFF` 全别名到低半区。CoreMark 的 `.data` 只 2.4KB，所以一直没暴露 | 改 `mem_addr[14:2]`（读 `raddr` 1 处 + 写口 4 处）。**对照 `itcm` 是 `[15:2]`（64KB）**，两块的"尺寸↔切片"关系要一致 |

> ⑤⑥ 的共性：**`cpu_top` 内那三个读源是"按位或"合流，不是按槽位 mux**。
> 基线的 dcache 挂在 `bus_arb` 后面（mux），所以它不守"非本窗口输出 0"的规矩也没事；
> 搬进核内就必须守。新增/搬入核内的总线从设备都要检查这一条。

①的隐蔽性最高：`dcache` 搬进核内后第一次进了 `bus_data_in_final` 的按位或，
x 会顺着 OR 爬满整条读回路径（`ra` 变 x）。

### 23.9 验证

- **回归 15 项全绿**。新增两项：
  - `icache/tb_icache_3way.v` —— 四条 tag 全落在**同一组的同一条行**上
    （`0x0000`/`0x4000`/`0x8000`/`0xC000`），强制 way1/way2 互踢。
    判据：三处累加值正确 + `way1/way2 都被回填到` + 终态 tag 组合符合 LRU。
  - `system/tb_isr_ret.v` —— **26 探针**，白盒比对每次 `irq_ret` 的落点是否 `iret_addr2+4`，
    黑盒查 ISR 标记。**这一项才咬得住 23.6 的缺陷**（`tb_isr` 只查 `dtcm[0]/[1]`，
    重定向早一条时那两个值照样成立）。
- **基线 A/B**：`Tools/test/system/tb_base.v` 直接对基线 RTL 跑同类检查。基线用
  `git archive a7f5eb8^ | tar -x -C <目录>` 导出（单通道化之前那份，端口表含 `ibus_*`，
  布局为 `soc/peripherals/core`；原先的平铺副本 `RTL/_bak_20260915/` 与之逐字节相同、
  仅差 `itcm/dtcm.v` 的 `$readmemh` 路径，2026-09-20 已删）。
  基线 mret 全部落在 `iret_addr2+4`（偏离 0 次）—— 这是"+4 必须保留"的实证。
  **注**：该基线里 `pc.v` 带 `ext_target` 端口 —— 就是"重定向 +4 该不该减 4"的原始开关，
  2026-09-20 推导同一规则时参考过；要做同类改动先看 `git show a7f5eb8^:core/pc.v`。
- 资源：itcm 64KB + icache 48KB + dtcm 32KB + dcache 16KB = **160KB**（§21 那版是 224KB）。

### 23.9b 综合 / 实现实测（Vivado 2019.1，xc7z010clg400-1）

| | §21.3 基线 | 本轮第一版 | **本轮定版** |
| --- | --- | --- | --- |
| Slice LUTs | 12602 (71.6%) | 27065 (153.8%) ✗ 放不下 | **11511 (65.4%)** |
| LUT as Logic | — | 22717 | **11143** |
| LUT as Memory | 484 | 4348 | **368** |
| Slice Registers | 8536 | 12708 | **8019** |
| RAMB36 | 28 (46.7%) | 20 | **42 (70.0%)** |
| **WNS** | **−0.089ns**（违例） | 未能布局 | **+0.314ns（MET，0 违例）** |
| WHS | — | — | +0.012ns（MET） |

**从 −0.089ns 违例变成 +0.314ns 正裕量**，且 LUT/FF 都比 §21.3 那版更低。

三步修正的贡献（按顺序）：
1. **itcm / dtcm 改成 BRAM 可推断模板** —— 这是决定性的一步。
   改前：64KB itcm 塌成 **8536 LUT**、32KB dtcm 塌成 **4096 LUTRAM**，`Place 30-640` 直接报
   "requires 22698 LUT as Logic but only 17600 available"。改后两块各只要 ~20-40 LUT。
2. **去掉 icache 的 tag 阵列复位** —— 之前被综合成带复位的 FF（3×128×19 位），
   导致 `Place 30-487` slice 打包失败（需 4834 slice / 可用 3998）。
   去掉后 Slice Registers 12708 → **8019**（比基线 8536 还低），LUT 13397 → **11511**。
3. 地址寄存器直接驱动 BRAM，取指/访存的组合锥变短，WNS 顺带转正。

**判读提醒**：`Place 30-640`（LUT 超资源）和 `Place 30-487`（slice 打包失败）是两个不同的错，
前者是 LUT 数，后者还要看 FF 与控制集 —— 加存储器时两种都要防。

### 23.10 已知未尽

- **`itcm` / `dtcm` 换 DDR / flash IP core**：两者已收成统一的流式接口，换的时候只动
  读流长度与地址窗口。
- **`flash_spi` 未接回**：文件保留、例化与顶层端口已删。接回时要同时恢复
  `bus_arb` 的槽 7、4 个顶层端口、xdc 的 8 行约束，以及 `plic.irq_sources` 的 bit5。
- **icache 无 invalidate**（同 §17.8）。
- **取指窗口放宽到 64KB 后，超出 itcm 的取指不再有"外区"通路**：程序必须整体落在 itcm 内。

### 23.11 CoreMark 低迭代仿真（2026-09-19）

**目的**：在 iverilog 里跑通 CoreMark 的**功能**（不是性能），验证整条 SoC 路径。

**构建**（`ITERATIONS` 可覆盖，见 `Tools/coremark/Makefile.coremark`）：

```bash
ITERATIONS=1 bash Tools/coremark/Makefile.coremark   # → Data/ins.hex + Data/data.hex
iverilog -g2001 -o cm.vvp Tools/test/coremark/tb_coremark.v RTL/core/*.v
vvp cm.vvp
```

- **`ITERATIONS=0` 不能用于仿真**：它是 CoreMark 的自动标定，要求跑满 10 秒才定迭代数 ⇒ 永不终止。
- **生成两个 hex**：`ins.hex`（`--only-section=.text` → itcm）与 `data.hex`（`--only-section=.data` → dtcm）。
  **必须按段分别导出**——`.data` 的 LMA=VMA=0x0080_0000，裸 `-O binary` 会跨约 8MB。
- **tb 自带最小 UART 模型**：见 23.8 ⑤ 的说明。判据是"**凡 `bus_addr_out[31:24] != 0` 的读都回 0 并抬 `bus_loaded_in`**"
  —— dcache（`[31:24]==0 && [23:13]==0x100`）与 tim_in（`[31:24]==0 && [23:20]==1`）都不在此判据内，
  不会被误应答；写不需要应答。

**结果**（`ITERATIONS=1`，`cyc≈1.56M`，含 4100 拍自举）：

```
2K performance run parameters for coremark.
CoreMark Size    : 666
Iterations       : 1
seedcrc          : 0xe9f5
[0]crclist       : 0xe714
[0]crcmatrix     : 0x1fd7
[0]crcstate      : 0x8e3a
[0]crcfinal      : 0xe714
ERROR! Must execute for at least 10 secs for a valid result!
Errors detected
```

- **三个 CRC 与 §10 记的 10 轮跑出来的值逐位相同** —— 印证 CoreMark 的 CRC **只由 seed 和
  `TOTAL_DATA_SIZE` 决定，与迭代次数无关**，所以 1 轮也能验功能。
  `seedcrc=0xe9f5` → `known_id=3`（`core_main.c:308`），官方表逐一对上。
- `ERROR! Must execute for at least 10 secs` 与随之而来的 `Errors detected` 来自
  `core_main.c:373-378` 的"跑满 10 秒"规则（`Total ticks=24` ⇒ 整数除法得 0），**非功能错误**。
- 本次只跑到"打完即停"，**没有做性能计数**。

**顺带确认**：`.text` = 12328 字节，正好落在 icache **way0 的 16KB 锁定段**内 ⇒
自举一次之后全程序常驻，**取指零缺失**。

## §24 系统总线换 AHB-Lite + APB（2026-09-19）

### 24.1 做了什么

`soc/bus_arb.v` **退役**，换成三个新模块：

| 文件 | 职责 |
| --- | --- |
| `soc/lsu_ahb_master.v` | lsu 的"1 拍脉冲"→ AHB 地址相位；读应答转发 |
| `soc/ahb_interconnect.v` | 仲裁（**按 2 master 设计**，第 2 端口本轮接常量，给 DMA 预留）+ 回程广播 |
| `soc/ahb_apb_bridge.v` | 槽号译码 + 读数据 mux + 驱动 4 个外设的**现有端口** |

**4 个外设（uart/plic/gpio/i2c）一个信号都没改。**

### 24.2 为什么可行：请求拍 == 地址相位

AHB-Lite 的一次传输：master 在 T 拍驱 `HADDR/HTRANS`（地址相位），从设备在 T→T+1 沿采样，
`HREADY=1` 时**数据相位**落在 T+1。
现有外设（`uart_top` 为代表）：T 拍看 `bus_addr_in/bus_we_in/bus_be_in`，T+1 拍输出寄存的
`bus_data_out` 并抬 1 拍 `ld_ready`。**完全重合。**
而 `bus_arb.v` 原来的 `per_sel` 锁存（请求拍锁、应答拍清）本来就是这个相位锁存。

### 24.3 三根私有信号（对 ARM AHB-Lite 的显式偏离）

1. **`hwstrb[3:0]`**：AHB-Lite 规范里字节使能是 AHB5 才有的 `HWSTRB`。本工程直接用私有信号
   透传给外设的 `bus_be_in`，省掉从 `HSIZE+HADDR[1:0]` 反推。
2. **`rd_valid`**：**只有已映射从设备的读应答才算数**。这是本轮最大的坑，见 24.4。
3. **`bus_valid_out`**（`lsu` 新增，`cpu_top` 透传）：不能用 `bus_addr_out != 0` 推"有没有请求" ——
   地址 0 的合法访问、store 到槽 0、未来"地址暂为 0 但请求有效"三类情况会静默失效。

### 24.4 修掉的两个真缺陷（都是本轮自己引入、被 tb 咬出来的）

| # | 缺陷 | 症状 | 修法 |
| --- | --- | --- | --- |
| ① | 桥把 `hready` 直接接成**所选中从设备的 `ld_ready`** | `ld_ready` 是只在数据相位有效的一拍脉冲，写事务与空闲期恒 0 ⇒ `hready=0` ⇒ `bus_hold_out` 拉高 ⇒ **CPU 被无端冻住、同一条指令反复重发**。现场：`gpio_irq` 的 `irq_en[3]=0`（PLIC 写没落到）、`mt_ldfifo` 出现 6 个孤儿 | `hready` 在无事务完成时恒 1；只有读的数据相位才由从设备决定 |
| ② | 桥对**未映射槽/核内窗口**也返回 `hready=1` | `lsu` 会对**所有**访存地址发总线请求，包括 `0x00800000`（dcache 窗口）与 `0x00400000`（tim_in）。原 `bus_arb` 在 `per_decode==0` 时**不产生任何应答**；新桥却照样抬 `bus_ready_out` ⇒ 每次核内访问多出一个**假 ready** ⇒ `ld_pop` 空弹出队 ⇒ **孤儿=6** | 加私有 `rd_valid`：只有 `sel_pend` 是映射槽（1/2/4/5）时才产生读应答 |

> ②的判据已固化进 `project_risc_v.sim/ahb_interconnect_tb.v`：
> **映射槽读恰好 1 个 `rd_valid`；核内窗口与未映射槽恒 0；写恒 0；一个写脉冲恰好 1 个地址相位。**

### 24.5 `bus_hold_in` 的处置

**恒 0** —— 与原来 `mcu_top` 把 `bus_hold_in` 接常量 `1'b0` 一致。
因为当前 `hready` 恒 1（所有从设备都是 1 拍应答），主机侧是组合透传，**没有可被反压的队列**，
`bus_valid_in` 的每一拍当拍就变成地址相位，不存在丢请求。

> **加第二个 master（DMA）时必须补命令锁存**：仲裁一旦可能让 `hready` 拉低，
> 就必须先把地址相位锁住再等应答（AHB 要求地址相位于 `HREADY=0` 期间保持）。

### 24.6 验证：逐拍等价

| 项 | 基线 | 换总线后 |
| --- | --- | --- |
| 回归 9 项 | 全绿 | **全绿，且输出逐字一致** |
| `mt_ldfifo` | `@cyc=6046 入队=157 出队=157 溢出=0 孤儿=0` | **完全相同** |
| `gpio_irq` / `i2c_irq` | `@cyc=97` / `@cyc=7085` | **完全相同** |
| CoreMark `-O2 / ITERATIONS=1` | `Total ticks 1049534`、CRC `0xe714/0x1fd7/0x8e3a`、自锁 `@cyc=3179509` | **完全相同** |

**连周期数都一个不差** —— 说明这是**逐拍等价**的替换，没有引入任何延迟回归
（方案原本留的余量是"+15% 以内"）。

### 24.7 未做（保留为规划）

- **DMA**：`ahb_interconnect` 已留第 2 master 端口（接常量）、槽 8、PLIC 位 6
- **NPU（队友的任务）**：留槽 9、PLIC 位 7。接入时直接挂 `ahb_apb_bridge`
- **DDR / flash 控制器**：**不挂这条系统总线**，将来与 `icache`/`dcache` 的 mem 侧直连（相当于第二套总线）
- 地址映射**未变**：1=UART / 2=PLIC / 4=gpio / 5=i2c；槽 3/6/7 保留

### 24.8 层次重梳理（2026-09-19，纯结构、零逻辑改动）

`mcu_top` 从例化 7 个模块收敛到 **3 个**：

```
mcu_top
 ├── cpu_top                      （core/，未动）
 ├── bus_con_top                      （soc/，新增）
 │    ├── rst_buf                 系统复位发生器（原来直接在 mcu_top 里）
 │    ├── lsu_ahb_master
 │    ├── ahb_interconnect
 │    └── ahb_apb_bridge
 └── io_block_top                     （IO_block/，新增）
      ├── uart_top（+ uart_rx / uart_tx）
      ├── plic
      ├── gpio_group
      └── i2c
```

- **`peripherals/` 文件夹已改名为 `IO_block/`**（8 个文件）。`flash_spi.v` 与 `tim_p.v` **保留文件但不例化**
  （与重梳理前一致 —— 这两个此前就已被停用）。
- **`rst_buf` 归入 `bus_con_top`**：`bus_con_top` 吃顶层 `rst`，内部产生 `rst_cpu` 往上输出给 `cpu_top` 与 `io_block_top`。
- **`bus_con_top ↔ io_block_top` 的外设侧接口**与原来 `mcu_top` 里那套完全同形：
  下行 `bus_addr_out` / `bus_data_out` / `bus_be_out` / `bus_we_out` / `bus_sel_out`（32 位独热），
  上行 4 组 `(模块)_data` + `(模块)_ready`；`io_block_top` 内部把 4 个外设的 irq 接到 `plic`，
  `plic_exti` 往上输出给 `cpu_top.exti`。**uart/gpio/i2c 之间的引脚复用连线现在收在 `io_block_top` 内部**。

- **白盒 tb 的层次路径随之变化**（这些都是 tb 侧的引用，不是 RTL 行为）：

| 原路径 | 新路径 | 涉及的 tb |
| --- | --- | --- |
| `dut.u_rst_buf` | `dut.u_bus_con.u_rst_buf` | `gpio_irq_tb` / `i2c_irq_tb` / `mt_ldfifo_tb` |
| `dut.u_gpio` | `dut.u_io_block.u_gpio` | `gpio_irq_tb` / `i2c_irq_tb` |
| `dut.u_plic` | `dut.u_io_block.u_plic` | `gpio_irq_tb` / `i2c_irq_tb` |
| `dut.u_i2c` | `dut.u_io_block.u_i2c` | `i2c_irq_tb` |
| `dut.u_cpu_top.*` | **不变** | 全部 |

- **验证**：回归 9 项输出与重梳理**前逐字一致**（含 `mt_ldfifo` 的 `@cyc=6046`、`gpio_irq @cyc=97`、
  `i2c_irq @cyc=7085`）；CoreMark 同样一数不差。
- `tools/run_reg.sh` 的通配路径已同步为 `$RTL/core/ $RTL/IO_block/ $RTL/soc/`。

### 24.9 AHB-Lite 相对原简单总线的优势（理论分析，2026-09-19）

先把"原总线"的规格说准 —— 它不是"低配 AHB"，而是一套**私有非流水协议**：

| 维度 | 原 `bus_arb` | AHB-Lite |
| --- | --- | --- |
| 地址相位 | **1 拍脉冲**（地址只存在 1 拍） | 电平，保持到 `HREADY=1` |
| 与数据相位的关系 | 请求拍 → 下一拍应答，**单未完成事务** | 地址相位与上一笔数据相位**重叠**（流水） |
| 突发 | **无**。每笔访问 = 1 个地址 + 1 个应答 | `HBURST` INCR4/8/16、WRAP |
| 多主机 | 无仲裁器（单 `bus_arb` 输入） | `HREADY` 全局广播 + 每从机仲裁（多层）或 `HBUSREQ/HGRANT`（全 AHB） |
| 从机等待 | `bus_busy` → `d_hold`（本项目里**已空接**） | `HREADY` 拉低，协议规定 |
| 错误响应 | **无**（未映射地址静默挂死） | `HRESP` = ERROR |
| 字节使能 | `bus_be_in[3:0]`（自定义） | 规范内是 AHB5 的 `HWSTRB`（本工程仍用私有 `hwstrb`） |
| 第三方 IP 对接 | 不能，私有 | 可以 |

**核心量化：突发的收益全在"地址相位被摊薄"。**

设一笔事务 = 1 个地址拍 + N 个数据拍。搬运 N 个字：

| 方式 | 拍数 | 效率 |
| --- | --- | --- |
| 原总线（逐字） | 每个字 1 地址 + 1 应答 = **2N** | 50% |
| AHB-Lite 单拍 `SINGLE` | 同样 **2N** | 50% |
| AHB-Lite `INCR16` 突发 | 1 地址 + 16 数据 = **N+1**（每 16 拍多 1 个地址） | **94%** |

以 icache 的 128B 行为例（32 个字）：
- 原总线：32 字 × 2 拍 = **64 拍**
- AHB-Lite：`HBURST` 上限 16 beat ⇒ 拆成 2 个 `INCR16` ⇒ **34 拍** —— **约 1.9×**

若再把 DDR 侧数据通路加宽到 64 位（`mem_ctrl` 的齿轮箱），128B 行变成 16 个 64bit beat ⇒ **17 拍** —— **约 3.7×**。

> **⚠️ 但必须诚实说清：这个收益今天拿不到。**
> 原因有二：
> ① **现在挂在系统总线上的 4 个外设全是"单字寄存器访问"**（uart/plic/gpio/i2c），本来就 1 拍一笔，
>    突发无从施展 ⇒ **本轮换总线对吞吐的净收益是 0**。这一点被实测证实了：CoreMark 的
>    `Total ticks` 换总线前后**一个数都没变**（`1049534`）。
> ② **`icache`/`dcache` 的填充不走这条总线**（它们在 `cpu_top` 核内直连 `itcm`/`dtcm`）。
>    而突发收益最大的恰恰是缓存行填充。按 §24.7 的规划，DDR/flash 控制器将来也与
>    **cache 的 mem 侧直连**（第二套总线），**不进 `ahb_interconnect`**。
>
> ⇒ **AHB-Lite 的真正价值是"接口标准化"而非"当前提速"**：将来 DMA / NPU / 第三方 IP
> 可以按规范直接挂上来，且 `HRESP` 给了未映射地址一个明确结局（协议规定回 ERROR，
> 而不是像原总线那样让 CPU 停在 `STALL` 永不退出）。**突发能力是为那一步预留的。**

**AHB 的另一个硬限制（选型时要知道）**：`HBURST` 单次**最多 16 beat**。
32 位总线下一次突发最多搬 64B，所以 1KB 地址边界每 16 笔就要重新发一次地址；
这也是 §24.7 里"内存侧不能靠 AHB、必须给 cache 一条独立通路"的判断依据之一
（AXI 的突发上限是 256 beat，32 位下 1KB、64 位下 2KB）。

## §25 RV32M 乘除法扩展（2026-09-19）

### 25.1 为什么做

CoreMark `-O2` 剖析（`Tools/test/coremark/tb_cm_prof.v`，ITERATIONS=1，总 1,391,245 拍）：

| 项 | 拍数 | 占比 |
|---|---|---|
| `__mulsi3`（libgcc 移位-加法软乘法） | 560,778 | 40.3%（9,492 次，约 59 拍/次） |
| dcache 缺失等待 | 158,652 | 11.4% |

取指**零缺失**、IPC 0.833，所以 CoreMark/MHz 只有 0.952 的**唯一主因就是核里没有硬件乘法器**。

### 25.2 方案：仿 lsu 的独立 mulu + regfile 第三写口

不做"组合乘法塞进 alu"，而是新增 `RTL/core/mulu.v`，**与 `lsu` 流水线行为同步**；
`regfile` 由 2 写口扩到 3 写口（`we_alu` / `we_ld` / `we_mul`）。

**乘法两级流水**（对齐 lsu 的提交沿；`opcode`/`func10` 取自 `mem_buf` 输出，与 lsu 同源）：

| 拍 | 动作 |
|---|---|
| T | 指令在 mulu 级，组合判 `is_mul` |
| T→T+1 | 锁操作数 `m_a`/`m_b`/`m_op`/`m_rd` |
| T+1 | 33×33 有符号乘法（`a_ext`/`b_ext` 按 funct3 补符号位或补 0） |
| T+1→T+2 | 锁乘积 `m_p` |
| T+2 | `mul_we` / `mul_loaded` 拉高（= lsu 的写沿） |
| T+3 | `hold`（`mul_loaded` 第二拍，对应 lsu 的 `ld_hold`） |

`MULH/MULHSU/MULHU` 只是取 `m_p[63:32]`，靠两个 33 位扩展实现全部 4 条：

- `a_ext = (funct3==3'b011) ? {1'b0, a} : {a[31], a}` —— 011=MULHU 才无符号
- `b_ext = (funct3[1])       ? {1'b0, b} : {b[31], b}` —— 010/011=MULHSU/MULHU 无符号

**除法是 32 拍移位-相减迭代 + FSM**（组合除法会直接成为新的最差路径），期间用一个 stall
冻住取指/预解码/decoder/wb_reg；边界值在 RTL 里**显式**处理：
除零 → 商全 1、余数=被除数；`INT_MIN/-1` → 商 `INT_MIN`、余数 0。

**`decoder` 必须抑制 M 指令的 ALU 写回**：M 与普通 ALU 共用 `OPCODE_OP`，而
`alu_func4 = {inst[30], funct3}` 的 8 个码与 M 的 funct3 **全部撞车** ⇒
`func10[9:3]==7'b0000001` 命中时 `we<=0` 且 `rd_back1<=0`（否则 ALU 的垃圾结果
会被 back1 前递出去污染紧邻的下一条）。

**独立第三份读数据通路**：`pre_decoder` 出 `mul` 标志，`regfile` 用它单独填
`r1_data_mul`/`r2_data_mul`（与 `dec`/`lsu` 用**独立 `if`**，因为 M 指令的 `dec` 与 `mul` 同为 1），
`forw` 复制出第三份镜像链 `r1_data_final_mul`。目的是照 `pc.v` 的 `pc_addr`/`aux_addr`
手法**按消费者拆扇出**，**不是**给组合网加 `max_fanout`（§22.5 的教训）。

### 25.3 踩到的两个真缺陷

#### 25.3a 除法写口比"程序序在它前面的指令"早（`mulu.v`）

触发形态（`core_main.c` 里的 `TOTAL_DATA_SIZE / 核数`）：

```asm
2edc:  li   a1,2000        # ALU 指令，程序序在前
2ee0:  divu a1,a1,a4       # 34 拍长指令
```

实测写口时序（`$time`，时钟周期 10ns）：

```
t=47805  mul_we=1  mul_d=0000029a   ← divu 写 x11 = 666
t=47825  alu_we=1  alu_d=000007d0   ← li  的写，晚 2 拍
```

`li` 被同一个 `mul_stall` 冻在 c3/c4 共 34 拍；除法却在 `d_done` 当拍**直接写口**，
解冻后 `li` 才走完 c4→写口 ⇒ 同 rd 时**晚写的赢**，`a1` 最终留下 2000 而不是 666。
传导下去就是 `results[i].memblock` 全偏 → 越界 load → 无从设备应答 → lsu 在途队列
指针失步（`wrt=0/rdp=2`，`fifo_full=1` 却无真实请求）→ **PC 永久冻结**。

**修法**：给除法结果加一条 3 拍提交延迟链 `d_cmt_q`，写口脉冲落在 `d_done+3`
（即 c2 → c3 → c4 → 写口 的正常级数），`mul_loaded` 的前递窗口同步覆盖 `d_done+1..+3`。
`d_cmt_q` **只由 rst 清、不判 flush**：`d_done` 一旦发出说明这条 div 已即将离级，
被冲刷不会重执行，结果必须照样提交（与乘法第二级不判 flush 同理）。

#### 25.3b `wb_reg` 在 STALL 期间反复写同一个值（`wb_reg.v`）★

这个更隐蔽，而且是**25.3a 修好之后才暴露出来的**。

形态（rv32im 的 `ee_printf.number()` 十六进制转换循环）：

```asm
2450: remu a5,a1,a2      ; a5 = num % base
2464: add  a5,t5,a5      ; a5 = dig + index    ← ALU 写 a5
2468: lbu  a5,0(a5)      ; a5 = 表里的字节      ← load 写同一个 a5
246c: divu a1,a1,a2      ; ★ 34 拍长指令夹在中间
2470: sb   a5,-1(t3)     ; 消费者
```

`divu` 把 `decoder`/`wb_reg` 连同 `add a5` 的结果一起冻住 34 拍。而 `wb_reg` 在
`stage == STALL` 时**保持 `we_out` 不放** ⇒ 这条被冻住的 ALU 指令**每拍都往 regfile
重复写一次**，把期间 load 已经写进去的字节又盖回成**地址**（`dig + index` 的低字节）。
消费者拿到的是地址 ⇒ 打印出来每个字符都是 `0x54+digit`，而 `0x54` 正是 `digits`
表的地址低字节。**注意 `stalled` 抑制 back1 那一层本来是对的**（stall 期间确实压住了），
真正的漏点是 regfile 被反复写。

**修法**：`wb_reg` 的 STALL 分支把 `we_out` 清 0（只保持数据，保持写使能 = 反复写）。
写使能那一拍已在进入本级那拍完成，所以清零不会丢写。

**为什么微测没抓到**：45 条 RV32M 微测里没有「长指令夹在 load 和它的消费者之间」这一形态。
已在 `Tools/test/mul/mul.s` 补上 26–44 共 19 条：`div/divu/rem/mul` 前紧邻写同一 rd 的
ALU 指令、前置写换成 `load`、`add`+`lbu` 同 rd、divu 结果参与地址计算、以及**反面对照**
（前置无同 rd 写时本就不该出错）。

### 25.4 验证

| 项 | 结果 |
|---|---|
| RV32M 微测 `Tools/test/mul/tb_mul.v` | **45/45** |
| 全量回归 `Tools/test/regression.sh` | **16/16 全绿** |
| CoreMark rv32im CRC | **与 rv32i 逐位相同**：`seedcrc 0xe9f5` / `crclist 0xe714` / `crcmatrix 0x1fd7` / `crcstate 0x8e3a` |
| CoreMark 拍数（ITERATIONS=1，含一次性开销） | 1,391,245 → **816,039** |
| **CoreMark 稳态每迭代拍数**（(总(4次)−总(1次))/3） | 1,050,263 → **482,802** |
| **CoreMark/MHz** | **0.952 → 2.07**（2.18×） |

> **口径必须用稳态，不能用 `ITERATIONS=1` 的拍数** —— 后者含约 33 万拍一次性开销
> （`portable_init` / `core_list_init` / `core_init_matrix` / 打印），会把它当成 41% 的每迭代成本，
> 把结论压到 1.62。正确做法：跑 `ITERATIONS=4` 与 `ITERATIONS=1` 各一次，
> `每迭代 = (总拍数(4) − 总拍数(1)) / 3`。两个自洽校验：
> `1e6/1,050,263 = 0.952`（正好是既有基线值）；`1,050,263 − 482,802 = 567,461 ≈ __mulsi3 的 560,778`。

### 25.4b 综合 / 实现实测（Vivado 2019.1，xc7z010clg400-1，50MHz）

| 项 | 基线（无 M） | 加 M 后 |
|---|---|---|
| WNS | +0.439 ns | **+0.353 ns（MET，全约束满足）** |
| 最差路径起点 | `jalr_target_q1_reg` | `u_decoder/jalr_pred_addr_out_reg[15]` |
| Slice LUTs | 11,505（65.4%） | **12,756（72.5%）** |
| Slice Registers | 8,054 | 8,257 |
| Block RAM Tile | 42.5 / 60 | 43.5 / 60 |
| DSP48E1 | 0 | **4**（33×33 有符号乘法 → 4 个 DSP，Block Arithmetic） |

**关键路径没有被乘法器抢走**：起点仍是 §22.8 那条 jalr 预测地址的组合环，与 `mulu` 无关。
WNS 相比基线掉 0.086 ns，来源是多出的 1,251 个 LUT 带来的拥塞，不是 DSP 布局竞争。

> **加 `mulu.v` 时必须同步往 `project_risc.xpr` 的 `sources_1` 文件表里加一条**，否则综合报
> `[Synth 8-439] module mulu not found`。另注意：若同时有另一个 Vivado 进程开着该工程，
> 它退出时会用自己内存里的旧文件表覆盖 `.xpr`，把手改的条目冲掉。

> **`ITERATIONS=1` 时 CoreMark 必然打印 `Errors detected`** —— 它要求跑满 10 秒才算合法结果，
> **这不是 CRC 校验失败**：rv32i 版输出同样的行，且两者 `crclist/crcmatrix/crcstate` 完全一致。

### 25.5 已知未尽

- **除法 32 拍全阻塞**：CoreMark 只有 2 次除法，影响可忽略。若将来除法密集，可改成
  "只冻消费者"（照 lsu 在途队列的形态），而不是冻整条流水线。
- **`Tools/test/run.sh` 用 `RTL/core/*.v` 通配**，新增 `mulu.v` 自动收编，无需改脚本。
- **`-march` 已统一为 `rv32im_zicsr`**：`Tools/coremark/Makefile.coremark`、`Tools/test/run.sh`、
  `Tools/build.sh` 三处。

## §26 dcache：整字 store 命中不再写失效（2026-09-19）

### 26.1 为什么改

M 扩展做完后（§25），CoreMark 稳态每迭代 482,802 拍，其中 **dcache 缺失等待 152,943 拍（31.7%）**，
是最大单项。加探针量缺失来源：

```
读缺失        = 13,167 次
写总数        = 15,325（命中 12,779 / 未命中 2,546）
其中整字(BE=1111) = 11,311 / 子字 = 1,468
-> 写命中失效  = 12,779 次
```

**`写命中失效 12,779 ≈ 读缺失 13,167` —— 97% 的读缺失是写失效策略自己制造的。**
每次 store 命中都 `valid <= 0`，而 CoreMark 的状态机/矩阵全是原地更新，
于是「写→失效→下次读又缺失」一直循环。

### 26.2 改法：整字 store 命中走【行内更新】，不失效

关键观察：**写命中里 88.5%（11,311 / 12,779）是整字 store（`sw`，BE=1111）** ——
整字覆盖整个字，**不需要读-改-写**，直接用现成的单写口写进阵列即可。
子字 store（`sb`/`sh`，1,468 次）覆盖不了整字，仍走写失效。

写穿照做、缓存与 dtcm 始终一致，所以**不需要脏位与换出**。

### 26.3 坑：两路写必须合成【一条写语句】，否则 BRAM 推断会掉

第一版写成 `if (fill...) data[...] <= mem_data; else if (wr_full_hit) data[...] <= bus_data_in;`
—— **同一进程内对 `data` 写了两次**，Vivado 判成「同进程内多端口写」：

```
WARNING: [Synth 8-6849] Infeasible attribute ram_style="block" set for RAM
         "u_cpu_topi_2/u_dcache/data_reg", trying to implement using LUTRAM
```

16KB 阵列掉进 LUTRAM ⇒ `Place 30-640`：需要 20,330 个 LUT 位点、器件只有 17,600 ⇒ 布局失败。

**修法**：地址 / 数据 / 使能各算一个表达式，最后只有**一条**写语句：

```verilog
always @(*) begin
    is_fill_wr = (stage == 1'd1) && !fill_end && mem_valid;
    ram_we     = is_fill_wr || wr_full_hit;
    if (is_fill_wr) begin ram_waddr = {fill_way, fill_idx, fill_cnt}; ram_wdata = mem_data;     end
    else            begin ram_waddr = {hit_way[1], idx, word};         ram_wdata = bus_data_in; end
end
always @(posedge clk) if (ram_we) data[ram_waddr] <= ram_wdata;
```

两路天然互斥（`wr_req` 要求 `stage==0`，填充时 `stage==1`），写口 mux 无冲突。

> **这条坑与 `regfile` 同源**：Vivado 报 `regs_reg` 时给的理由就是
> "RAM has multiple writes via different ports in same process"。

### 26.4 验证

| 项 | 结果 |
|---|---|
| 新微测 `ldst/tb_ldst_wrhit.v` | **13/13**（同行 store→load、行内 8 字全写全读、跨组跨路、子字 store、子字后整字覆盖；探针确认整字命中 11 次 / 子字 3 次） |
| 全量回归 | **17/17 全绿**（新增这条已登记进 `Tools/test/regression.sh`） |
| CoreMark CRC | 仍是 `0xe714 / 0x1fd7 / 0x8e3a` ✓ |
| CoreMark 拍数（ITER=1） | 816,039 → **678,649** |
| dcache 缺失（ITER=1） | 13,167 → **1,583**（−88%） |
| IPC | 0.761 → **0.9155** |
| **CoreMark 稳态每迭代拍数** | 482,802 → **347,812** |
| **CoreMark/MHz** | 2.07 → **2.88**（对 rv32i 基线 0.952 共 **3.02×**） |
| WNS | +0.353 → **+0.581 ns（MET）** |
| Slice LUTs | 12,756 → 13,129（74.6%） |
| Block RAM Tile | 43.5 / 60（**不变**，推断保住） |

### 26.5 已知未尽

- **子字 store 仍写失效**：1,468 次/迭代，是剩余缺失的大头。要做得多一次「读-改-写」（读口+写口两拍），收益递减。
- **写未命中不回填（no-write-allocate）**：保持现状，回填要引入脏数据管理。
- **未做 write-back**：写穿已保证 dtcm 始终最新，换 write-back 只省 dtcm 写次数，收益小。

## §27 写口合并与 mulu 的 stall 门控（2026-09-19）

### 27.1 起因：regfile 是全设计最大的 LUT 大户

加完 M 扩展后跑 `report_utilization -hierarchical`（实现后）：

| 模块 | Logic LUTs | 占比 |
|---|---|---|
| **u_regfile** | **4,223** | **33.1%** |
| u_icache | 1,357 | 10.6% |
| u_dcache | 1,325 | 10.4% |
| u_decoder | 1,167 | 9.1% |
| u_lsu | 943 | 7.4% |
| u_uart | 823 | 6.4% |
| u_bra_predict | 721 | 5.6% |
| u_mem_buf | 628 | 4.9% |
| u_mulu | 572 | 4.5% |

`regs` 声明了 `(* ram_style = "block" *)`，但综合明确拒绝：

```
WARNING: [Synth 8-4767] Trying to implement RAM 'regs_reg' in registers.
	1: RAM has multiple writes via different ports in same process.
```

⇒ 32×32 全散成 FF，于是每个写口都要一份 32×32 地址译码、`bypass()` 也要按读侧镜像复制。

### 27.2 一条接口契约：在途单元看到的冻结信号要「减掉自己」

写口能合并的前提是**同拍不撞**。这里踩到一个真缺陷，值得立成契约：

**`mulu` 原来只判自己内部的三类冒险**（div 未发起 / div 进行 / mul-use），
**看不到 controller 的合成 stall**，也没有 `stage` 输入：

```verilog
m_push = is_mul && !stall && !flush && !bus_hold_in;   // stall = mulu 自己的输出
```

所以只要 icache / dcache 有多拍事务把一条 mul 冻在 c2，`m_push` 就**逐拍重发**
→ `m_v` 恒 1 → `m_pv` 连续拉高 → `we_mul` 连续多拍，写口沿与 alu 彻底错开。

**为什么 CoreMark 测不出来**：它的 `.text` 只有 13,672B，落在 icache way0 锁定的 16KB 内，
**icache 缺失恰好是 0**。为此专门建了 `Tools/test/mul/tb_icmiss.v`：程序 24.8KB、跨过
0x4000、6000 条 nop 夹 180 条 mul。实测：

```
icache 缺失                                   = 67 次
危险窗口（icache 填充时 M 指令正停在 mulu 级） = 70 拍    ← 就是这条通路
```

**修复**：`mulu` 增加 `pipe_stall` 输入，把**乘法两级流水、除法提交链 `d_cmt_q`、输出保持 `hold`
全部按它冻结** —— 与 alu 的写回级同呼吸，写口沿恢复同偏移。

> **坑（自锁）**：喂给 mulu 的信号**必须排掉 `mul_stall`** —— 那正是 mulu 自己的输出
> （div 进行 / mul-use 冒险）。第一版没排，一冻结就永远等不到 `m_pv`，微测当场挂掉 36 条。
> 故 `cpu_top` 里分两根：
> ```verilog
> wire pipe_stall_w = stall | mul_stall | icache_busy_w | bus_hold_in | d_hold_int;  // 给 controller 定 stage
> wire mul_freeze_w = stall | icache_busy_w | bus_hold_in | d_hold_int;              // 给 mulu，排掉自己
> ```

> **另一个同形状的坑（2026-09-24 修，CoreMark 挂死根因）**：`stall_m` 的**释放**条件写成
> `(mstalled && m_pv)` —— 等的是**结果脉冲**；而 `mstalled` 在任何 `stall` 拍都会置起。
> 一旦那个脉冲在 `mstalled` 置起**之前**拍到（相位错开一次），之后就再无脉冲 ⇒ 永久等 ⇒
> `stall_m` 常 1 ⇒ `m_push` 里的 `!stall` 又把这条 mul 自己挡死 ⇒ 沿程序序向后传染 = 死锁。
> **改成看"乘法流水还有没有在途"**：`stall_m = (m_v | m_pv | hold) ? 1'b1 : 1'b0`（流水空 ⇒ 结果早已落地 ⇒ 放行）。
> 实测：CoreMark 从"351 字符挂死"变成 777 字符且 CRC 全对，周期与基线一致。
> **为什么不能简单删 `!stall`**：`stall_m` 同时承担"本条 mul 的操作数还没到"这一正当等待，
> 删掉会让乘法拿陈旧操作数推进（实测 CoreMark CRC 算错）。

**契约表述**：*已发射的事务（mulu 的乘除、lsu 的在途 load）一旦发起就必须跑完 —— 冻它只会自锁；
外部停顿必须先冻住它们，否则写口沿会与 alu 错开。所以每个「有在途事务」的单元，
看到的冻结信号 = 全流水线 stall 减去它自己。*

（`stage` 本身**保留**：它是 `(flush, stall)` 的纯组合函数、无状态，分发与集中信息等价；
它的价值是「八个流水级同一拍冻结」这个单一广播不变式，改成各模块自判会把它降成八处约定。）

### 27.3 写口合并：3 → 2

| 口 | 承载 | 依据 |
|---|---|---|
| **A** | `alu` \| `mul` | 两者写沿都由**流水线偏移**决定（c2+3），且 mulu 的级已按 `mul_freeze_w` 冻结 ⇒ 严格同偏移、永不同拍（三个微测实测 **0 次同拍**） |
| **B** | `ld` | load 的写沿由**总线事务长度**决定（多拍），与流水线偏移不绑定，**与谁合并都可能撞**，故独占一口，保持「撞上也是两条独立写」 |

**`bypass()` 与写口统一成同一优先级（alu > mul > ld）**：改前 bypass 是 `alu>ld>mul`、
写口是 `mul>ld>alu`，方向正好相反（§2.2 记的老隐患）。

> **曾经试过 3→1**（三口全合）：LUT 9,622 / WNS +0.751，看着更漂亮，但**同拍时会丢写**
> —— 靠的是「实测没撞过」这个统计，硬件里没有兜底。**已放弃。**

### 27.4 验证

| 项 | 结果 |
|---|---|
| 回归 | **18/18 全绿**（新增 `mul/tb_icmiss.v`） |
| `mul/tb_mul.v` | **60/60**，含 alu/mul 同拍检测器、`m_push` 重复推送检测器 |
| CoreMark CRC | `0xe714 / 0x1fd7 / 0x8e3a` ✓ |
| CoreMark 拍数 | 678,649 —— **与改动前逐拍一致**（无性能损失） |
| alu/mul 同拍共写口 | **0 次**（三个测试全程） |
| Slice LUTs | 13,129 → **11,487（−1,642，−12.5%）** |
| Slice Registers | 8,238 → **7,730（−508）** |
| WNS | +0.581 → **+0.505 ns（MET）** |
| Block RAM / DSP | 43.5 / 4（不变） |

### 27.5 已知未尽

- ~~**`u_uart` FIFO 64 → 32**~~：已做（`rx_buf[0:31]` / `tx_buf[0:31]`，指针 6→5 位，零头清零循环 64→32）。
  **位宽必须跟着一起收**，否则回绕位与满空判定不符。省 512 个 FF；CoreMark 输出与拍数均不变。
- **`u_mem_buf` 628 LUTs** 对纯流水寄存器偏多，值得单独看。
- **`icache` 缺失通路现在有测试守着**，但只覆盖了「缺失 + mul 在途」；缺失与其他在途单元的
  交互（将来若加 FPU/DMA）需照 §27.2 的契约重新评审。

## §28 板级实测：CoreMark 上板（2026-09-19，xc7z010clg400-1 @ 50MHz）

### 28.1 流程

```bash
ITERATIONS=0 bash Tools/coremark/Makefile.coremark     # 自动标定版 -> Data/ins.hex + data.hex
vivado -mode batch -source run_board.tcl               # synth -> impl -> write_bitstream
# 产物: project_risc.runs/impl_1/mcu_top.bit
```

- **`ITERATIONS=0` 是自动标定**：先跑满 10 秒再定迭代数，**所以上电后有一段完全没有输出的静默期**。
  为此在 `barebones/core_portme.c` 的 `portable_init()` 里（UART 初始化之后、`main` 任何输出之前）
  加了一段 banner 打印，避免看起来像卡死。
- 比特流里的程序来自综合时读的 `Data/ins.hex`（`itcm.v` / `dtcm.v` 的 `$readmemh`）。
  **重出比特流前必须先重编 CoreMark** —— `Tools/test/run.sh` / `regression.sh` 会覆盖 `Data/ins.hex`。

### 28.2 实测输出

```
CoreMark Size    : 666
Total ticks      : 13915
Total time (secs): 13
Iterations/Sec   : 153
Iterations       : 2000
Compiler version : GCC15.2.0
Compiler flags   : -O2-HAS_FLOAT=0
Memory location  : STACK
seedcrc          : 0xe9f5
[0]crclist       : 0xe714
[0]crcmatrix     : 0x1fd7
[0]crcstate      : 0x8e3a
[0]crcfinal      : 0x4983
Correct operation validated.
```

### 28.3 解读

**① 功能：CoreMark 自校验通过**（`Correct operation validated`）——
它用 `seedcrc` 从内建表选期望值自查，是最强判据。三个分量与 rv32i 基线**逐位相同**
（`0xe714 / 0x1fd7 / 0x8e3a`）。

> **`crcfinal = 0x4983` 与仿真里的 `0xe714` 不同是正常的**：`crcfinal` 是 **2000 次迭代的累计** CRC
> （每次迭代把 list/matrix/state 串进去）；仿真那次只有 1 次迭代，累计值恰好等于 crclist。
> CoreMark 的校验已覆盖该项，不需要另行解释。

**② 性能：与仿真吻合到 0.02%**

```
13915 ms / 2000 次 = 6.9575 ms/迭代
6.9575 ms × 50MHz  = 347,875 拍/迭代
仿真实测            = 347,812 拍/迭代     ← 差 0.02%
```

这条吻合同时验证了两件事：**50MHz 时钟假设成立**；**仿真拍数可以直接当板级拍数用**。

**③ 分数**

| 口径 | 值 |
|---|---|
| CoreMark 打印的 `Iterations/Sec` | **153**（它用截断的整数秒 13 算） |
| 按真实 13.915 s | **143.7** |
| **CoreMark/MHz** | **2.87** |

`Total ticks` 单位是毫秒（`CLOCKS_PER_SEC=1000`）；13.9 s 满足 CoreMark「≥10 秒」的合法性要求。

**④ 对基线的总加速**

| 版本 | 50MHz 得分 | CoreMark/MHz |
|---|---|---|
| rv32i（无 M、写失效 dcache、3 写口） | 47.6 | 0.95 |
| **本版（RV32IM + dcache 写命中修复 + 2 写口）** | **~144** | **2.87** |

**3.0×**。各步贡献：§25 的 M 扩展 2.18×；§26 的 dcache 写命中修复到 2.88×；
§27 的写口合并 / mulu stall 门控**把 LUT 降了 12.5% 而性能一拍未变**。

### 28.4 本版最终时序与资源（实现后，50MHz，period=20ns）

| 项 | 值 |
|---|---|
| **WNS** | **+0.505 ns（MET，全约束满足）** |
| Slice LUTs | 11,487 / 17,600（65.27%） |
| Slice Registers | 7,730 / 35,200（21.96%） |
| Block RAM Tile | 43.5 / 60（72.50%） |
| DSP48E1 | 4 / 80 |
| 比特流 | `project_risc.runs/impl_1/mcu_top.bit` |

---

## §29 机器模式异常处理（2026-09-25）

### 29.1 目标口径

只做 M 模式、单 hart。实现的异常清单（cause 号一律按规范）：

| 异常 | cause | 判定在哪一级 | mepc | mtval |
| --- | --- | --- | --- | --- |
| 指令地址非对齐（br 目标） | 0 | bju 判定延迟拍（E5） | 分支自身地址 | 目标地址 |
| 指令地址非对齐（jalr 目标） | 0 | 同上（E5） | jalr 自身地址 | 目标地址 |
| 指令地址非对齐（jal 目标） | 0 | post_decoder 组合判、寄存到 E5 | jal 自身地址 | 目标地址 |
| 非法指令 | 2 | post_decoder（E4） | 该指令地址 | 0 |
| 断点 ebreak | 3 | post_decoder（E4） | 该指令地址 | 0 |
| load 地址非对齐 | 4 | lsu 组合判 → 寄存一拍（E4） | 该指令地址 | 出错地址 |
| store 地址非对齐 | 6 | 同上 | 该指令地址 | 出错地址 |
| M 模式环境调用 ecall | 11 | post_decoder（E4） | 该指令地址 | 0 |

**mepc 一律 = 故障指令自身地址**（规范口径），`mret` 后由 handler 自己 `+4` 回退。
未做：取指/访存访问错（1/5/7，无 PMP、地址全映射）、U 模式相关、PMP、mcountinhibit。

### 29.2 ★ 判定相位：异常源必须落在 E4/E5

`flag_bus[9]` 驱动的冲刷会清掉 c1..c4 与 wb 共 5 级。流水里 **E1 最年轻、E5 最老**
（E1 = icache 输出、E2 = pre_decoder Q、E3 = mid_decoder Q、E4 = post_decoder Q、E5 = wb_reg Q）。
若异常源在 E1 判定，被清掉的 E2/E3/E4 是**比它更老**的指令 ⇒ 好指令被误杀。

> **不变式：异常源必须落在 E4/E5**（被清的必然比它年轻）。

这条决定了三处实现：
- br / jalr 的判定本来就在 E4 组合、E5 寄存 ⇒ 天然合规；
- jal 的非对齐虽在 E1 就能静态判（`inst[21] = immJ[1]`，分支地址恒 4 对齐），也必须寄存到 E5；
- lsu 的访存非对齐在 E3 组合判出，**多寄存一拍**对齐到 E4。

### 29.3 逐项实现

**（1）新增 `trap_unit.v`：异常仲裁。** `csr.v` 只做架构状态，不参与仲裁。
优先级 **E5 > E4**（同拍多个源时程序序最老者胜）；`exc` 是寄存器源，占 `flag_bus[9]`。
`trap`（ecall/ebreak）从 `flush_irq` 里**摘出来**并入 `exc` —— 原来并入 `flush_irq` 会经
`flag_bus[7]` 自门控，冲不掉自己（§0.3 的注）。

**（2）指令地址非对齐（cause 0）+ mtval。**
- br：目标恒 = `aux_addr_in + beq_off_in`，非对齐判据 `success_now & beq_off_in[1]`（**零加法器**）；
- jalr：`jalr_low2 = r1[1:0] + r2[1:0]` 的 2 位和 —— **不能分别看两个操作数**，LSB 会往 bit1 进位；
- jal：`inst[21]` 静态可判，但目标（`pc + immJ`）**不在流水里**（`mid_decoder` 不为 JAL 置 `imm_c`）
  ⇒ 由 `post_decoder` 就地从同拍指令字解 `immJ`、配一个加法器算出、寄存一拍；
- mtval = 出错的目标地址，**复用 bju 的落点寄存器** `jp_target`：非对齐拍 `exc` 会压掉 jump 重定向
  （`flush_jump_w = bit6 & ~bit9`）⇒ 那一拍它只被 mtval 消费，不重复寄存一份；jal 那一支由
  单铺的一路载荷 `jal_target_in` 送进同一个寄存器。

**（3）非法指令（cause 2）：`post_decoder` 里一张 RV32IM+Zicsr 白名单**，按**指令字的原始字段**
（opcode/funct3/funct7）判。三处必须记住：
- **全零字必须合法**：本核拿它当流水线气泡/NOP（icache 取指门注入 0、各级冲刷清 0、复位 `inst_out=0`、
  链接脚本 `.fill`）—— 否则每次跳转冲刷的气泡都会变成陷阱；
- OP-IMM 的**非移位**指令 `inst[31:25]` 是**立即数**不是 funct7，只有 funct3 ∈ {001,101} 才约束它；
- `sub`（OP，funct3=000，funct7=0100000）必须合法。
另有 LOAD/STORE 的**宽度**合法性（funct3 白名单见 §29.3(6)）。

**（4）ecall / ebreak 改规范口径**：原来 `mcause` 被写成 `0x8000_000B / 0x8000_0003`（**带 bit31
中断位、且 11/3 是中断码**），现在异常一律写不带 bit31 的 `3 / 11`；`mepc` 也从"回返地址"改成
"故障指令自身"。另修 `mret` 少置的 `MPIE <- 1`。

**（5）`fence` / `fence.i`：不需要任何 RTL 改动。** 本核 itcm **没有写端口** ⇒ 代码物理上不可变
⇒ fence 语义天然成立、NOP 即完整实现。但必须在白名单里显式声明合法（否则会被 (3) 误判）。

**（6）访存地址非对齐（cause 4/6）：按【访问宽度】判。**
`lsu` 里用 `func10[1:0]`（funct3 的宽度编码）一次覆盖 LB/LH/LW/LBU/LHU 与 SB/SH/SW：
`[1:0]==01`（半字）看 `bit0`、`==10`（字）看 `bit1:0`；字节没有对齐概念。
- 判定**当拍就进 `new_in` 的门** ⇒ 故障指令不进车厢、不拉总线；
- 判定结果**寄存一拍**对齐到 E4，与其它异常共用同一条交付通路；
- **★ 判定必须与入口条件 `new_in_pre` 相与**：被冲刷/停顿/冒险挡住的指令本来就进不了 lsu，
  它那一拍的 `byte_addr` 是错路指令的垃圾操作数（实测 CoreMark：一个被冲刷的错路 `lh` 判出
  `0xffffffff`；load-use 停顿中 `r1` 是转发未完成的中间值）⇒ 对它抛异常会把好程序打断。
- ★ 另外在 lsu 入口加了一道**宽度白名单**挡住非法宽度：**lsu 的两级车厢在冲刷拍是不清的**，
  非法宽度会先上总线（非法 store 以 `be=0000` 上线、非法 load 读回垃圾还写回 `rd`）。
  这张白名单必须与 `post_decoder` 的那张**保持一致**。

**（7）CSR 侧（§7 四项）**
- **合法性检查**：访问不存在的 CSR（读或写）、访问只读 CSR（地址 `bit[11:10]==11`）且**真的要写**
  ⇒ cause 2。"真的要写" = `inst[19:15] != 0`（`csrw mscratch, x0` 是**空操作**，这是 RISC-V 著名陷阱）。
- **`misa`(0x301) = `0x4000_1100`**（MXL=01 + I[8] + M[12]，只读常量；读回 0 是不合规的，
  因为 MXL=00 是保留编码）；**`mhartid`(0xF14) = 0**（单 hart 必须为 0，只读）。
- **`mtvec` 的 MODE 是 WARL**：本核只实现 Direct ⇒ 写入时把低 2 位规范化为 0。
  ★ 这不是洁癖：本核**拿这个字当异常入口地址直接用**，写 `0x1234FFF7`（MODE=3）会让异常跳到
  `0x1234FFF4`（取指 `pc>>>2` 静默截断），且没有任何报错。
- **`mie`/`mip` 位序按规范**：bit3=MSIE/MSIP（软件）、bit7=MTIE/MTIP（定时器）、bit11=MEIE/MEIP（外部）。
  本核原来**外部与软件写反了**（eirq@3、sirq@11），与自家 `mcause` 的中断码（外部=11，规范）
  自相矛盾 —— 按规范对调；`mip` 的软件挂起清除位也移到 bit3。
- **`sret`(0x102) ⇒ 非法指令**（M-only 核，规范允许"实现为非法"）；
  **`wfi`(0x105) 保持合法当 NOP** —— 规范要求 WFI 必须是合法指令，只有 `mstatus.TW=1` 且
  在低权限模式执行时才非法。

### 29.4 ★ 踩到的坑（都值得记死）

1. **`csr_wr_en` 是【双重身份】，不能用它表达"不写"**：`alu.v` 里 `if (we_in && cs_wr_en)`
   用它判定"这是条 CSR 指令"，据此把读值 `cs_data` 选进 `result` 写 `rd`。做"`rs1==x0` 不写"时
   顺手清掉它 ⇒ **`csrr` 全部读回 0**。正确做法：**把 `csr_addr` 清 0** 表达"不写"
   （写 case 0 不命中、旁路也自然关掉，这正是本核既有约定：非 SYSTEM 的地址本来就是 0）。
2. **写后读旁路前递的是"意图写入值"，不等于真正锁存的值**：对 ①不锁存的地址
   （`misa`、`mcycle`/`minstret`）②写入做过规范化/masking 的地址（`mtvec`）③只取几位的地址
   （`mstatus`/`mie`/`mip`）都会**说谎**。修法：`csr_latch_w`（只有真会锁存的地址才允许旁路）+
   `csr_wr_val`（规范化后的写入值，旁路与写分支共用）。
3. **错路/停顿指令的判定不可信**（见 29.3(6)）—— 凡是"从操作数组合算出来的异常判据"，
   都必须与"这条指令真的会进流水"的条件相与。

### 29.5 验证

- `tools/archtest/` 下 **17 个程序**（新增 `csr_illegal.S`、`exc_illegal_ldst.S`），全部 `ARCH_DONE`
  且各自判据槽逐项核对（`mepc` / `mcause` / `mtval` / `be` / rd 回写 / `S` 事件条数）；
- **事件流差分**：每改一子步都与前一状态逐字节比，只有预期变更的那 1 个程序不同，其余全等；
- `bash tools/run_reg.sh` **8/8**（含两个闭环 IRQ 测试 —— 它是 `mie` 位序对调的端到端守卫）；
- **CoreMark**（xsim）CRC `e714`/`1fd7`/`8e3a`、479 字节、dcache 缺失 126、**全程 0 次异常**；
- `run.py golden` 三条基线全 `identical`（参考模型 `tools/rv32i_ref.py` 已同步改口径）。

**已知未尽**：`mie`/`mip` 只实现了这三类；`mtval` 对非法指令写 0（规范允许）；
`misa` 只报 I/M；`mcycle`/`minstret` 读写口径未动。

### 29.6 xc7a100t 批处理实现（2026-09-25，`tools/a100t_impl.tcl`）

不建工程、不动现有 `.xpr`（那是 xc7z010 的、且不在 git 里）：纯批处理读全树 RTL + 一份只含
时钟的 xdc（`tools/a100t_timing.xdc`），跑完只留报告。

| 项 | 值 |
| --- | --- |
| part | `xc7a100tfgg676-2`（封装由用户确认为 fgg676；速度等级取最常见的 -2） |
| 约束 | 只有 `create_clock -period 10.000`（100MHz）—— z010 的 38 条引脚约束对 a100t 无效，未带过来 |
| **WNS** | **+0.428 ns**（TNS 0.000，失败端点 **0** / 22049） |
| **WHS** | **+0.036 ns**（THS 0.000，失败端点 0） |
| Vivado 结论 | **All user specified timing constraints are met** |
| Slice LUTs | 10,328 / 63,400（16.29%） |
| Slice Registers | 7,424 / 126,800（5.85%） |
| Block RAM Tile | 43.5 / 135（32.22%） |
| DSP48E1 | 4 |

**最差 setup 路**：`u_cpu_top/u_post_decoder/r1_data_out_reg[3]` → **`u_cpu_top/u_lsu/exc_ldst_addr_reg[28]`**
（就是访存字节地址那一路 `r1_data_final + offset`），数据路径 **9.297ns**
（logic 2.572ns = 27.7% / **route 6.725ns = 72.3%**），14 级逻辑（CARRY4×5、LUT6×4、LUT5×2、LUT3×2）。
**最差 hold 路**：`u_io_block/u_i2c/s_rx_byte_reg[0]` → `u_i2c/rx_buf_reg_0_7_0_5/RAMA`（LUTRAM），0.196ns。

> **与 z010 对照**（§28.4，period=20ns / 50MHz）：那里 WNS 只有 +0.008ns 而 LUT 占到 65%。
> ⇒ 同一份 RTL 在 a100t 上 **100MHz 还有 0.43ns 余量**，面积只占 16%。
> 时序的杠杆仍在**布线**（72% 是 route）⇒ 降拥塞、结构性挪拍才有效，抠 LUT 级数没用
> （与 §28 的结论、以及"布线延迟记在跳数上"那条一致）。
