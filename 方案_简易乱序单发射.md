# 方案：带简易发射队列的乱序单发射（探索与规划）

> 性质：**只做探索与规划，本文件是唯一产物，RTL 一个字未改。**
> 结论来自读码与实测量（每条给 `文件:行号` 或命令），推断处已标注。
> 风格依据：`代码风格约束.md`（示例代码按该文写）。

---

## 0. 结论摘要（先给结论）

**不推荐按"完整发射队列（CAM 唤醒 + 乱序选择）"的形态实施。** 建议的最优动作是
"先测量、再决定"，而真正的拍数杠杆不在乱序（见 §5.4）。

四条支撑：

1. **可用的收益上界本来就很小，且几乎全被前端吃掉。** 本核 CoreMark 的 IPC 已经是
   **0.9155**（`逻辑说明` §26.4），即每 100 拍只有 **8.45 拍**"没有退休指令"。
   这 8.45% 是**任何调度类改造的绝对上界**，它包含取指空泡、分支误预测、各类停顿，
   而停顿之间大量重叠（不能相加）。发射队列只能碰其中"长延迟挡路 + 后面有独立 ready 的纯 ALU 项"这一类。

2. **本核在 CoreMark 里的"长延迟挡路"几乎不存在，而且紧跟在后面用它的总是分支或 load。**
   - **除法（32 拍全局冻结）在 CoreMark 稳态为 0 次**：全程序 5 条 `divu/remu` 全在
     `main` / `core_init_matrix` 里（`tools/coremark_v1/_out.lst:2275,2323,2654,2869,2876`），
     即只在**初始化**阶段执行一次。
   - **乘法**已是两级流水 + 在途旁路（`mulu.v:383-407` + `forw.v:51-55`），只在紧邻使用时停 1 拍
     （`mulu.v:373-378`）；真·动态乘法约 4.6×10³ 次/迭代（≈1.3% 拍数，推算见 §5.3）。
   - **load** 命中 1 拍应答且有 `ld_hold` 两拍旁路窗（`lsu.v:230-240`），紧邻使用停 1 拍。

3. **静态测得的乱序窗口本来就窄。** 对本份 CoreMark 反汇编逐条统计（脚本见 §5.2）：
   纯 ALU 连续段共 670 段，**均值 3.11、中位数 2、44%（293/670）长度为 1**。
   而"排空栅栏"（§2.2，保精确例外的代价）又把乱序严格限制在**一条 ALU 连续段之内**。

4. **时序上，这个核现在的最差路和这个方案想改的地方高度重叠。**
   本机最新一次实现（`project_risc_v.runs/impl_1`，**7z010**，2026-09-24 15:38，10ns）十条最差路
   **全部**是 `u_cpu_top/u_post_decoder/r1_data_out_reg[*]` → `u_cpu_top/u_lsu/{s2_addr,bus_addr_out}_reg[*]`，
   逻辑 3.9~4.4ns、**布线 6.5~7.7ns（占 60~74%）**；而且 `u_post_decoder/r1_data_out_reg` 的
   D 端就是 `forw` 的输出——发射队列要取操作数，加的正是这一条网上的负载。

**如果一定要做**：按 §3 的"**深度 2、ALU-only、排空栅栏**"最小形态，并且**必须按 §8 的阶段 0 先测量**；
拿不到测量数字之前不要动 RTL。

**必须先澄清的一件事（需你确认）**：`当前进度.md` §3.5 记的 **WNS +0.008ns / 0 失败端点**是
**7z020**（`xc7z020clg400-1`，LUT 19.48%）；而本机 `project_risc_v.xpr` 的器件是
**`xc7z010clg400-1`**，同版本 RTL 在 10ns 下是 **WNS −1.220ns / 103 失败端点 / TNS −45.586ns**
（LUT 58.73%、FF 21.00%、BRAM 72.5%）。"不让它回退"的**基线到底是哪一个**必须定下来，
否则"没回退"这个判据无法判定。

---

## 1. 现状盘点

### 1.1 关键事实：本核**没有**"谁在等谁"的表达

6 级流水（`当前进度.md` §2）：s0 `pc` → s1 `icache.inst_out` → s2 `pre_decoder` →
s3 `mid_decoder` → s4 `post_decoder`+`alu`+`bju` → s5 `wb_reg`。

**操作数在 s3→s4 的边界上就已经到齐了**：`pre_decoder` 只寄存 rs 号（`pre_decoder.v:103-109`），
regfile 在 s2 侧读出（`regfile.v:86-105`），`forw` 组合地把四条旁路源合流成
`r1_data_final/r2_data_final`（`forw.v:43-82`），`post_decoder` 在 s4 把它们**锁进自己的输出寄存器**
（`post_decoder.v:134-135,151-152,173-174,180-181`）。
也就是说：**"一条指令进了 s4"就等于"它的操作数已经到齐"**。

所以"哪条指令在等什么"这件事在本核里**没有任何 tag/记分板表达**——它被表达成**位置式的布尔量**：

| 情形 | 表达 | 位置 |
| --- | --- | --- |
| 在途 load 的 rd 撞上**停在 s3 那条**的 rs1/rs2 | `ls_use_hit` | `lsu.v:193-201` |
| 同上但撞的是 rd（WAW） | `ls_waw_hit` | `lsu.v:199` |
| **上一条乘法**的 rd 撞上 s3 的 rs1/rs2 | `stall_m` | `mulu.v:373-378` |
| 除法 FSM 在跑（32 拍） | `stall_v` | `mulu.v:364-370` |
| 车3（写回车厢）占着且没等到应答 | `s3_v & ~s3_done` | `lsu.v:205` |
| 访存指令进不了车厢 | `full_stall = mem_op & ~new_go` | `lsu.v:162` |
| 取指缺失/自举 | `icache_busy` | `icache.v:134-144`（经 `busy_q` 到 `flag_bus[0]`，`icache.v:182-185`） |
| 外部总线保持 | `bus_hold_in` → `flag_bus[4]` | `controller.v:70` |

**没有一个位带"我在等 rd=7"的信息。** 这正是"不引入 ROB 就必须另想办法"的根因：
要做发射队列，就必须先**引入本核的第一份记分板状态**（tag + ready 位）——
这是本方案真正的架构增量，也是它的主要风险来源。

### 1.2 一个"停顿"的影响范围：**整条流水线一起冻**

`controller.v:70` 把九个位一条总线广播出去：

```
flag_bus = {exec, flush_irq, flush_jump, dcache_hold, bus_hold_in, stall_m, stall_v, lsu_stall, icache_busy}
                                                                   [5]         [4]        [3]     [2]       [1]       [0]
```

而**每一个**流水级都用同样一行代码把六个停顿位**或**成自己的 `stall_w`：

- `pc.v:46`、`pre_decoder.v:76`、`mid_decoder.v:146`、`post_decoder.v:76`、`wb_reg.v:47`、
  `regfile.v:79`、`icache.v:65`（`req_valid`）。

⇒ **六位里任意一位为 1，八个流水级同一拍全冻**。`逻辑说明` §27.2 明确把这个
"八个流水级同一拍冻结"称为**单一广播不变式**并说它有价值。

**唯一的例外是 `mulu`**：它自己的冻结信号排掉了自己那两位（`mulu.v:91-92`
`pipe_stall = flag_bus[5] | flag_bus[4] | flag_bus[1] | flag_bus[0]`），因为"已发起的事务必须跑完"，
冻它只会自锁（`逻辑说明` §27.2 契约）。

**这对发射队列是好消息也是坏消息**：好消息是"停顿"只有一条广播线，加一处新冻结源的位置很集中；
坏消息是**发射队列的全部价值都建立在"停顿期还能干活"上**，而要让它干活，
必须把 pc/pre_decoder/mid_decoder 从这条广播里**摘出来**——那就正面违反了上面那个不变式（见 §4.6）。

### 1.3 三条在途通路与 `forw` 的四级优先旁路

`forw.v:43-82` 是**位置式**的四级优先 mux（注意 `loaded`/`mul_loaded` 是**独立支路**，
不能与 back2 合并——`forw.v:51-52` 有历史翻车记录）：

| 优先级 | 源 | 位置含义 | 命中判据 |
| --- | --- | --- | --- |
| 1 | `result_back1` | 正在 s4 的那条（alu 组合结果） | `r == rd_back1` |
| 2 | `ld_data` | lsu 在途 load（含 `ld_hold` 第二拍） | `loaded && r == rd_load` |
| 3 | `mul_data` | mulu 在途（乘法第二级 / 除法 d_done+0..+3 / hold） | `mul_loaded && r == rd_mul` |
| 4 | `result_back2` | 正在 s5 的那条 | `r == rd_back2` |

`rd_back1` 由 `post_decoder` 寄存器给（`post_decoder.v:37`、`post_decoder.v:141,146,154,161,169,182,189,197`），
`rd_back2` 由 `wb_reg` 给（`wb_reg.v:30`）。

**关键推论（对 §4 很重要）**：`forw` 的输出是**按当前 s3 那条指令的 rs 索引**的
（`forw.v:26-27` 的输入 `r1`/`r2` 来自 `rs1_2`/`rs2_2`）。所以发射队列**不可能**靠"重读 forw 的输出"
来给队列里的项取操作数——队列项的操作数必须在**入队那拍**就从 `r1_data_final/r2_data_final` 锁进条目。

### 1.4 必须保住的不变式清单（改造前的"不许动"列表）

| # | 不变式 | 出处 |
| --- | --- | --- |
| I1 | 八个流水级**同一拍**冻结（`stage = f(flush,stall)` 单一广播） | `逻辑说明` §27.2、`pc.v:46` 等七处 |
| I2 | `mulu` 的冻结信号**必须排掉自己**（`stall_m`/`stall_v`），否则自锁 | `mulu.v:91-92`、`逻辑说明` §27.1 |
| I3 | `lsu` 的 `full_stall` **只按车厢位置算**，不得混入上游 stall（否则与 `stall_m` 成环） | `lsu.v:162`、`逻辑说明` §5.3 |
| I4 | **在途读只许一笔**：`blank_bus = miss \| (ld_out & ~ready_in)`，blank 拍总线必须清零 | `lsu.v:156-157,181-188` |
| I5 | `mulu` 的提交沿与 `lsu` **严格同偏移** ⇒ regfile 写口 A（alu\|mul）**永不同拍** | `mulu.v:14-17`、`regfile.v:107-112` |
| I6 | 前置冲刷 `stallf` **不进 `flag_bus`**（进了要多花 0.45ns） | `bju.v:39-41,114-122`、`controller.v:64-66` |
| I7 | `wb_reg` 在 stall 拍**必须拉低写使能**（不能跟着保持） | `wb_reg.v:64-68` |
| I8 | 顶层（`cpu_top`）**零运算**、例化处不得有表达式 | `代码风格约束.md` §1.3 |

---

## 2. 正面回答：没有 ROB，怎么保证精确例外？

### 2.1 先看本核到底有哪些例外（读码，不靠 ISA 手册）

| 例外源 | 本核 | 出处 |
| --- | --- | --- |
| `ecall` / `ebreak` | 有（→ `trap`） | `post_decoder.v:212-213` |
| 中断（exti/timi） | 有（→ `irq_act` → `flush_irq`） | `csr.v:176-179`、`controller.v:68` |
| `mret` | 有 | `post_decoder.v:206`、`pc.v:93-96` |
| 分支/跳转误预测 | 有（→ `br2`/`br3`/`jalr_fail`） | `bju.v:114-122` |
| 非法指令 / 指令地址非对齐 / load-store 非对齐 | **全都没有**（已知缺口） | `当前进度.md` §6 |
| **纯 ALU 类（OP 非 M / OP-IMM / LUI / AUIPC）能产生的例外** | **空集** | 全程序只有一处 `trap` 赋值，且在 `OPCODE_SYSTEM` 分支内：`post_decoder.v:202-250` |

**这是本方案唯一的地基**：把乱序限制在"不会产生例外、不写内存、不改控制流"的纯 ALU 类
（`OPCODE_OP` 且 `func10[9:3] != 7'b0000001`、`OPCODE_OP_IMM`、`OPCODE_LUI`、`OPCODE_AUIPC`），
**指令集层面就保证了"这条指令的执行"本身不可观测**——它只写一个寄存器 rd。

### 2.2 采用的取舍：**纯 ALU 乱序 + 排空栅栏（drain barrier）**

规则（一句话）：**发射队列只装纯 ALU 类；任何非 ALU 指令要进 s4，队列必须先排空。**

```
非 ALU 指令进入 s4 的条件 = 原来的条件 且 队列本拍排空
```

**为什么这一条同时解决三件事：**

1. **精确中断 / trap**：中断受理点是"队列空"这一点。由于分派严格按程序序、队列是 FIFO，
   "队列空"⟺"已经发射的纯 ALU 项恰好是程序序的一个前缀"⇒ 中断那一刻的架构状态
   **就是**顺序前缀状态，与顺序核逐位相同。落地口在 `csr.v:179` 已有的闸门上再与一项：
   `irq_act = ... && !(iret | trap | jalr_fail | br2 | br3) && 队列空`。
   **代价：中断延迟增加"排空时间"**（上界 = 队列深度 + 队头阻塞时间）。
   FreeRTOS 的 tick 精度会受影响，需实测（见 §6 风险 R6）。
2. **分支误预测 / 冲刷**：`br2`/`br3`/`jalr_fail` 由 `bju` 在 s4 判定（`bju.v:114-122`）——
   而 s4 里能出现非 ALU 指令 **⟹ 队列已空** ⟹ **队列里没有任何可被错路冲刷的东西**。
   于是**不需要任何"队列冲刷"逻辑**（不需要 per-entry valid 的批量清零、不需要 squash 计数）。
   唯一要做的仍照 `lsu.v:72`/`mulu.v:86` 的先例，把 `stallf` 一起并进队列的入队门控
   （前置冲刷早一拍，见 §4.2）。
3. **顺序车道不需要新冒险比较**：非 ALU 指令在 s3 上等队列排空期间，它的操作数由 `forw` 每拍重算；
   等队列空时，那些老的 ALU 项已经走到 s4/s5，`forw` 的 back1/back2 回路**天然**给出正确值。
   ⇒ **`forw` 一行不用改，`lsu`/`mulu` 的 hazard 扫描一行不用改。**

### 2.3 诚实说明：这个取舍放弃了什么

| 放弃的东西 | 具体含义 |
| --- | --- |
| **跨非 ALU 指令的乱序** | 乱序窗口 = 一条 ALU 连续段（实测均值 3.11，`§5.2`）。**不能越过访存/分支/乘法/CSR** |
| **"长延迟挡路时用后续独立工作填满"** | 这是发射队列的经典卖点，而它恰好要求"跨过挡路者"；在本约束下**拿不到**（§5.3 三处热循环逐一核对） |
| **深度乱序** | 队列容量不能转化成窗口，因为窗口由"到下一个非 ALU 指令的距离"封顶 |
| **寄存器重命名** | 不重命名 ⇒ 队列里同 rd 的项必须保序（WAW），这也让 §7 的事件流判据必须改写 |
| **按需退休 / 精确 pc** | 没有"退休 pc"的概念，`mepc` 仍只能靠 `pc_addr - irq_bubble` 这套近似（`csr.v:138-139`） |

### 2.4 被否决的两条备选路线及理由

**备选 A：只对纯 ALU 乱序 + 允许非 ALU 指令在队列非空时进 s4（不做排空栅栏）**
否决。理由：a) 中断/分支冲刷会**丢弃队列中比冲刷点更老**的项（必须给队列加"按序冲刷 + squash 计数"，
复杂度和出错面立刻上一个量级）；b) 顺序车道的指令要在 s3 上等队列里**更老**的项出结果 ⇒
需要在 `stall` 路上加一条 5 位 × 深度的 CAM 比较，而 `stall` 经 `flag_bus` 全片广播
（`controller.v:64-66` 说这条广播线很贵，绕一次实测 0.45ns）；c) 收益并未因此变大（§5.3）。

**备选 B：连 load/store/mul/div 一起乱序（真 OoO）**
否决。理由：直接违反用户红线（`mulu.v`/`lsu.v` 不动）；且没有 ROB 时
"store 已写内存但撤销"无法处理，`lsu.v` 的"车3 占用即提交"语义（`lsu.v:13-15`、`逻辑说明` §5.3）
会被彻底推翻——那是重写，不是改造。

---

## 3. 发射队列（IQ）设计

### 3.1 哪些指令进队：纯 ALU 类

判据（在 `mid_decoder` 就地译出、随流水携带 1 位，不要在下游做深译码）：

```verilog
//纯 ALU 类：OP(非 M) / OP-IMM / LUI / AUIPC —— 不产生例外、不写内存、不改控制流
//M 与普通 ALU 共用 OPCODE_OP，只能靠 funct7 区分（照 post_decoder.v:140 的口径）
    always @(*) begin
        isu_c = 1'b0;
        case (inst_in[6:0])
            OPCODE_OP: begin
                isu_c = (inst_in[31:25] != 7'b0000001);
            end
            OPCODE_OP_IMM, OPCODE_LUI, OPCODE_AUIPC: begin
                isu_c = 1'b1;
            end
        endcase
    end
```

放在 `mid_decoder.v:93-138` 那个现成的组合译码块里（多一条 `case` 项，几乎不新增 LUT），
在 `mid_decoder.v:154-200` 的时序块里寄存 1 位输出 `isu_out`。
**显式排除**：`OPCODE_LOAD/STORE/BRANCH/JAL/JALR/SYSTEM` 与 `func10[9:3]==7'b0000001`。

再一条优化：**`rd == 0` 的 ALU 项直接不占队位**（`li x0,..`/`mv x0,..` 不写任何状态）。
这不影响顺序性。

### 3.2 结构：值式保留站（操作数在**分派拍**锁入），深度 **2**

选"值式"（分派时锁操作数）而不是"标签式（发射时读寄存器堆）"，理由：
- 本核**没有**可用的第二组 regfile 读口（`regfile.v:86-105` 的读是 `bypass(rs1_1)`/`bypass(rs2_1)`，
  索引来自 s2，且输出已经寄存；要让队列按自己的 rs 重读，等于加读口 + 加组合读出，代价远超收益）；
- 本核的操作数在 s3→s4 边界**本来就已经由 `forw` 组合给全了**（§1.1），"分派时锁值"是顺势而为；
- 副作用（好的）：值式天然免疫 WAR，不需要"发射时读寄存器堆会读到新值"的仲裁。

条目位宽（与 `post_decoder` 在 s4 寄存的那组**一一镜像**，便于对齐与复用）：

| 字段 | 位宽 | 说明 |
| --- | --- | --- |
| `v` | 1 | 有效 |
| `rd` | 5 | 目的寄存器 |
| `func4` | 4 | `alu_func4`（= `{func10[8], func10[2:0]}`，`post_decoder.v:146`） |
| `src1` | 32 | 分派拍锁的 `r1_data_final` |
| `src2` | 32 | 分派拍锁的 `r2_data_final` |
| `p_rd1` / `p_rd2` | 5+5 | 未到齐那一路**在等哪个 rd**（= 分派拍撞上的 `rd_load` 或 `rd_mul`） |
| `p_k1` / `p_k2` | 1+1 | 未到齐那一路的等待源（0 = load，1 = mul） |
| `r1` / `r2` | 1+1 | 该路是否已到齐 |

合计 **88 位 × 深度 2 = 176 FF**。

**深度取 2 的依据**：§5.2 实测的 ALU 连续段分布——中位数 2、均值 3.11、长度 ≤2 占 70%。
深度 2 覆盖大部分段；深度 4（352 FF）只多覆盖尾部，而**收益上界本来就只有个位数百分比**。
**深度 4 作为可选**，面积按同比例线性。

### 3.3 分派与"队列空"的定义

```
iq_disp  = isu_s3 & (rd_s3 != 5'd0) & (iq_cnt != 2'd2) & exec & ~flush_w_iq
flush_w_iq = flag_bus[7] | flag_bus[6] | stallf          // 照 lsu.v:72 / mulu.v:86 的口径
```

**"排空栅栏"要用的"队列空"不是 `iq_cnt == 0`，而是"到本拍沿为止没有比 s4 那条更老的未发射项"**：

```
iq_will_empty = (iq_cnt == 2'd0) | ((iq_cnt == 2'd1) & iq_iss)
```

`iq_iss` = 本拍有一条被选中发射（含 §3.4 的"分派即发射"直通）。
**这是整个设计最容易出 off-by-one 的地方**——`post_decoder` 的 s4 捕获门要用 `iq_will_empty`
而不是 `iq_cnt == 0`，否则每遇到一条非 ALU 指令就平白多等一拍（**净变慢**）。

### 3.4 两种"没有额外延时"的方式（二选一，需实测选）

| 方式 | 做法 | 代价 | 风险 |
| --- | --- | --- | --- |
| **直通（bypass）** | 纯 ALU 项同时"写进队列"与"当拍发射" | 队列常驻 0~1 项，稳态零延时 | 分派→发射→选择链在同拍，组合更深 |
| **快路径（不进队）** | s4 能收时照旧走 `post_decoder`（原路不变），只有 s4 收不了时才入队 | 稳态**完全不碰**原路径 | 需要"入队 vs 走老路"的二选一 mux（在 s3→s4 的控制上） |

**建议先用"快路径"**：它在稳态下把新逻辑**整个旁路掉**——对时序最友好，
而且"稳态事件流与现状逐位相同"使 §8 阶段 1 可以用**现有判据**（archtest `diff`）直接验证。

### 3.5 唤醒：**只需两条来源**，这是本设计最大的简化

为什么只有两条：分派拍 `forw` 已经把 back1/loaded/mul_loaded/back2 **四级全算过了**
（`forw.v:43-82`）。所以入队时**只有两种可能没到齐**：
1. 撞上**在途 load**：`ls_use_hit`（`lsu.v:193-201`）—— 等 `loaded`；
2. 撞上**上一条乘法**：`stall_m`（`mulu.v:373-378`）—— 等 `mul_loaded`。

除法**不需要第三条**：`mulu` 已经把 `d_pend`（`d_done+0..+3`）并进了它的组合输出口
（`mulu.v:383-407`），`mul_loaded`/`rd_mul`/`mul_data_out` 三件套**已经覆盖除法**。

```
//每条目每路
wake_r1 = ~r1 & ( (p_k1 == 1'b0 && loaded     && p_rd1 == rd_load) |
                  (p_k1 == 1'b1 && mul_loaded && p_rd1 == rd_mul ) )
```

比较器总数 = **深度 × 2 路 × 2 个 5 位比较**（深度 2 ⇒ 8 个）。

**取值**：唤醒那拍把 `ld_data_final` / `mul_data_final`（`cpu_top.v:304`）直接锁进 `src1/src2`，
即每路一个 2:1 32 位 mux（`p_k` 当选择）⇒ 深度 2 时 4 个 × 32 = **128 LUT6**。
`ld_data_final`/`mul_data_final` 都**不在**当前最差路上（最差路在 lsu 的**编址**侧、数据还没回来），
所以这一项的扇出代价可接受。

### 3.6 选择：一拍发 1 条，**最老就绪优先**

单发射 ⇒ 每拍最多发射 1 条。按程序序的优先编码（深度 2 只有 2 级）：

```verilog
    always @(*) begin
        iq_iss  = iq_v[0] & iq_rdy[0];
        iq_iss1 = (~iq_v[0] | ~iq_rdy[0]) & iq_v[1] & iq_rdy[1];
    end
```

`iq_rdy = r1 & r2`。深度 4 时是 4 级串行链（每级约 2 个 LUT6），也可改成"每项一个
AND + 优先级掩码"的并行形状换扇出。

**"越过"的准确含义**：`iq_iss1 = 1 && iq_v[0] && !iq_rdy[0]` —— 即**只越过同队列里更老的、
操作数未到齐的纯 ALU 项**。这是本设计允许的**唯一**一种乱序。

### 3.7 什么时候必须停

| 条件 | 动作 |
| --- | --- |
| `iq_cnt == 2` 且队头不就绪 | **全局停**（前端冻结）。这就是"队列满" |
| s3 是非 ALU 且 `~iq_will_empty` | 前端冻结（等排空）；`post_decoder` 的捕获门也挂这条 |
| `flush_w_iq` 或 `stallf` | 入队与发射全停（照 I6：`stallf` 直连，不进 `flag_bus`） |
| `m_pv \| d_cmt`（**mulu 正在用写口 A**） | **IQ 本拍不得发射** —— 见 §4.5，这是正确性硬要求不是性能选择 |

### 3.8 面积与扇出评估

| 项 | 量（深度 2） | 占本机 7z010 的比 |
| --- | --- | --- |
| 条目寄存器 | 176 FF | 2.4% of 7393 |
| 唤醒比较 + 取值 mux | ~170 LUT | 1.6% of 10336 |
| 发射选择 + 优先链 | ~30 LUT | 0.3% |
| alu 输入 2:1 mux（32 位 ×2 + func4/rd） | ~80 LUT | 0.8% |
| 分类位 + 控制（`iq_disp`/`iq_will_empty`/满） | ~60 LUT | 0.6% |
| **合计** | **~176 FF + ~340 LUT** | **≈ +2.4% FF / +3.3% LUT** |

**扇出是这里真正的成本，不是 LUT 数**：`r1_data_final`/`r2_data_final` 是 32 位 ×2，
现在由 `(* max_fanout = 32 *)` 约束（`forw.v:33`），而**它们就是当前十条最差路的源头网**
（`u_post_decoder/r1_data_out_reg` 的 D 端）。IQ 每拍要从这两条网上取样进 2 个条目 ×2 路
⇒ **+128 flop 负载（相对 `post_decoder` 现有的 64 个，+200%）**。
在布线占 60~74% 的路径上，这是**最需要实测**的一项（§6 R1）。

---

## 4. 与现有机制的接合（信号级）

### 4.1 不要加宽 `flag_bus`

`flag_bus` 是 9 位，12 个文件按**位号**取用（`pc.v:44-47`、`pre_decoder.v:75-77`、
`mid_decoder.v:145-147`、`post_decoder.v:75-77`、`wb_reg.v:45-47`、`regfile.v:78-80`、
`lsu.v:72-73`、`mulu.v:86-92`、`icache.v:64-65`、`bju.v:52`、`csr.v:48-49`、`controller.v:70`）。
加第 10 位 = 12 处位号改动，出错面大、收益为零。

**照 `stallf` 的先例**（`lsu.v:34`、`mulu.v:53`、`cpu_top.v:254,260,294`、
`controller.v:64-66` 明写"绕 controller 一圈会多花 0.45ns"）：
IQ 的新信号走**专用端口**，只有 pc/pre_decoder/mid_decoder/post_decoder *需要*它。

> 脚注（文档不一致，非 bug）：`lsu.v:65`、`mulu.v:79`、`post_decoder.v:70` 等处的注释仍写
> 老名字 `{..., stall_d, stall_b, stall_m, stall_v, stall_l, stall_i}`；
> **权威是 `controller.v:70`**（`{..., dcache_hold, bus_hold_in, stall_m, stall_v, lsu_stall, icache_busy}`），
> `当前进度.md` §2 与之一致。位号语义两边相同，只是注释没改名。

### 4.2 `stallf` 与两拍冲刷窗

`stallf` 是 `bju` 判定块里的**组合派生**信号（`bju.v:117`），早一拍，只喂 `lsu`/`mulu`；
两者各自把它 OR 进自己的 `flush_w`（`lsu.v:72`、`mulu.v:86`），用来覆盖
"错路指令在 c2 上停留两拍"的窗口。

**IQ 必须照抄这一手**：`flush_w_iq = flag_bus[7] | flag_bus[6] | stallf`。
否则错路指令会在 `stallf` 那一拍被**入队**（寄存器已经改不动了），
且因为它比冲刷点**更年轻**，排空栅栏管不到它 ⇒ **架构污染**。

### 4.3 `forw`：**不改内部结构**，但要处理 `rd_back1`

- **入队侧**：IQ 消费 `r1_data_final`/`r2_data_final`（`forw.v:33` 的输出来源），
  不新增源、不改 mux 树，只加负载（§3.8）。
- **消费者侧**：发射出去的项在 s4 那一拍，`forw` 的 **back1** 必须报它的 `rd`。
  但 `rd_back1` 由 `post_decoder` 寄存（`post_decoder.v:37`），发射拍它记的是**别的**指令。
  **改法（保持顶层零运算，符合 I8）**：把 IQ 的 `rd` 作为**输入**接进 `post_decoder`，
  在 `post_decoder.v` 内部把它并进 `rd_back1` 的输出表达式（该处本来就有按 opcode 分支的
  特判，见 `post_decoder.v:140-148` 对 M 指令把 `rd_back1` 清 0 的先例）：
  `rd_back1_out = iq_iss ? iq_rd : rd_back1;`
- **back2** 由 `wb_reg` 给（`wb_reg.v:30`），发射项进 s5 时自然会写进去，**不用改**。

### 4.4 `bju` 的判定延迟拍与 `pc` 重定向：**零改动**

`bju` 的判定源是 s4 的寄存器（`bju.v:29-33`、`post_decoder.v:36-38,45-49`），
判定结果寄存一拍后驱动 `flag_bus[6]`（`controller.v:69`），`pc` 在下一拍落到 `jp_target`
（`pc.v:84-87`）。因为**非 ALU 才能进 s4 ⟹ 进 s4 时队列已空**，判定与冲刷的相位
和现在**逐拍相同**。这是本方案相对其他乱序方案最大的好处：
**控制通路（重定向/预测回写/`ird_tmr`/`irq_bubble`）完全不用动。**

### 4.5 写口 A 的不变式（**必改项，不改就是静默数据错误**）

`regfile.v:107-112` 的注释把前提写得很清楚：

> 口 A = alu | mul …… **两者严格同偏移、永不同拍**，可以共用一口。

写口仲裁是 `alu > mul > ld`（`regfile.v:119-136`），且**没有保持逻辑**——
`am_we` 一旦选中 alu，`mulu` 那一拍的结果就**直接丢掉**。

现在这条不变式成立，是因为 `mulu` 的整条提交链都按 `pipe_stall` 冻结
（`mulu.v:182,212,323,334`），与 alu 的写回级同呼吸。
**发射队列一旦让 ALU 项可以在任意拍发射，ALU 写回就可能落在 `m_pv`/`d_cmt` 的拍上 ⇒
乘法/除法结果被静默丢弃。**（`当前进度.md` §3.4 的 CoreMark 基线就是靠乘法结果正确撑着的。）

修法（二选一）：
- **优先（便宜）**：给 IQ 加一条发射禁止项 `~m_pv & ~d_cmt`（`mulu.v:384-386` 的口径：
  `mul_we = m_pv | d_cmt`）。代价是偶发的 1 拍发射延迟，好处是**不动 `regfile.v`**。
- 备选（贵）：给口 A 加真正的优先级 + 保持（`mulu` 结果被压后一拍再写）——
  要改 `mulu` 的提交链，**违反红线**，不推荐。

### 4.6 `pc`/`pre_decoder`/`mid_decoder` 的推进条件（**破坏 I1 的那一处**）

这是本方案**唯一**正面违反"八级同一拍冻结"的地方，也是 §8 阶段 3 的内容。

```
原：stall_w = (flag_bus[5]|flag_bus[4]|flag_bus[3]|flag_bus[2]|flag_bus[1]|flag_bus[0]) & ~flush_w
新：stall_w = ((...同上...) & ~iq_adv) & ~flush_w
    iq_adv  = 本拍 s3 那条被吞进队列（§3.3 的 iq_disp），于是前端可以前进一格
```

三级**同一条式子**（照 `代码风格约束.md` §3.3 显式自赋值的要求，
`stall_w` 不是寄存器，这里是组合式；三级代码逐字一致，便于 `git diff -w` 复核）。

**连带效应（必须一起核）**：`mid_decoder` 的寄存器被提前推进后，它的输出
（`opcode_2`/`func10_2`/`rd_2`/`rs1_2`/`rs2_2`/`offset_*_2`）会指向**新的**那条指令，
而这些输出被 `lsu`/`mulu`/`post_decoder`/`csr` **组合**消费：

| 消费者 | 是否会因此拿到错指令 | 为什么现在安全 |
| --- | --- | --- |
| `lsu.new_in`（`lsu.v:149`） | 否 | 门里有六个 stall 位，停顿期恒 0 |
| `lsu` 的 `full_stall`/`mem_op`（`lsu.v:143,162`） | 否 | 只有纯 ALU 才满足 `iq_adv`，`mem_op=0` |
| `lsu.ls_use_hit`（`lsu.v:193-201`） | 否 | 同上，且 `s2_v` 与 `r1_post` 的组合在停顿期不产生新入队 |
| `mulu.m_push`（`mulu.v:173`） | 否 | 门里有 `!stall` |
| `post_decoder.csr_addr_pre`（`post_decoder.v:83-86`） | **是** | 停顿期 `post_decoder` 持有，`csr_data_out` 会被更新成垃圾值，但它的消费者（`alu.cs_data`）在本拍不产出有效写回 |

⇒ **必须显式加一条断言**：`iq_adv` 为 1 的那些拍，s3 那条必须是纯 ALU；
`csr_addr_pre` 的污染窗口要有 tb 覆盖（§7.3 的 `iq_barrier.S`）。

### 4.7 `wb_reg` 的门（必改，很小）

发射拍要能写回，而 `wb_reg` 在 `stall_w` 拍会**拉低 `we_out`**（`wb_reg.v:64-68`，这是 I7，
不能撤）。所以加一条超驰：

```verilog
            else if (stall_w && !iq_iss) begin
                we_out <= 1'b0;
                rd_out <= rd_out;
                rd_back2 <= rd_back2;
                result_out <= result_out;
                result_back2 <= result_back2;
            end
```

`result_back2` 必须跟着写，否则 back2 旁路会给出陈旧值。

### 4.8 `minstret` 的 `retire`（小，但会影响 FreeRTOS 测试）

`csr.v:52` 的 `retire = !(flush_w | stall | mem_inflight)` 是拍数近似。发射队列生效后，
"停顿拍但有 ALU 项退休"的拍会被漏计 ⇒ 加一项 `& ~iq_iss`（语义：这一拍确有指令退休）。

---

## 5. 收益估算（诚实版）

### 5.1 上界：IPC 预算

- 基线：CoreMark ITER=1 全程 **678,649 拍**、**IPC 0.9155**、稳态 **347,812 拍/迭代**、
  **CoreMark/MHz 2.88**（`逻辑说明` §26.4、`当前进度.md` §3.4）。
- ⇒ 每 100 拍有 **8.45 拍**没有退休指令。**这就是所有"调度类"改造的天花板。**
- 而且天花板里**大量是重叠的**：一次 dcache 缺失冻结期间落进去的分支空泡、mul-use 停顿
  是**同一拍**的重叠损失，不能相加。

### 5.2 静态窗口统计（实测，脚本在本机跑）

对 `tools/coremark_v1/_out.lst` 按助记符分类，统计"连续纯 ALU 段"的长度分布：

```
ALU ops: 2084   non-ALU ops: 1319
runs: 670    max run len: 62
mean run len: 3.11
  len  1 : 293        len  2 : 176        len  3 : 77         len  4 : 44
  len  5 : 31         len  6 : 13         len  7 : 8          len  8 : 4
  len  9 : 7          len 10 : 2          len 12 : 2          len 22 : 3
  len 23 : 1          len 51 : 1          len 53 : 3          len 60 : 1
  len 61 : 3          len 62 : 1
```

⇒ **44% 的段长为 1**（队列完全无用武之地），**70% ≤ 2**。
长段（22~62）集中在 `crcu8`（`_out.lst:175c-188c`）这类**无阻塞的直线 ALU 段**——
**直线上没有长延迟操作挡路 ⇒ 队列没有可填的空档**。这就是"窗口的形状"与"阻塞的位置"
系统性错配。

指令配比（静态）：ALU 2084 / 访存 659 / 控制 620 / RV32M 20 / **除法 0（热路径）**。

### 5.3 三处热循环逐一核对（**这一段是"不推荐"的核心证据**）

**① `matrix_mul_matrix` 内层（`_out.lst:bf0-c08`，7 条指令）**

```
bf0: lh   a4, 0(a5)
bf4: lh   a7, 0(a2)
bf8: addi a5, a5, 2
bfc: add  a2, a2, t1
c00: mul  a4, a4, a7        ← mul-use 停 1 拍（mulu.v:373-378）
c04: add  a6, a6, a4        ← 依赖 c00
c08: bne  t3, a5, bf0
```
`c00` 的 1 拍停顿里，程序序上后面的只有 `c04`（依赖它）和 `c08`（**分支**）。
⇒ 纯 ALU 队列**一个可用项都没有**，收益 **0**。

**② `matrix_mul_matrix_bitextract` 内层（`_out.lst:c90-cbc`，11 条）**

```
c90: lh   a4,0(a6)     c94: lh  a5,0(a2)     c98: addi a2,a2,2
c9c: add  a6,a6,t1     ca0: mul a5,a5,a4     ← 停 1 拍
ca4: srai a4,a5,2      ca8: srai a5,a5,5     cac: andi a4,a4,15
cb0: andi a5,a5,127    cb4: mul a5,a4,a5     ← 停 1 拍
cb8: add  a7,a7,a5     cbc: bne t3,a2,c90
```
两次 mul-use 停顿，后面的 `srai`/`andi`/`add` **全部**依赖乘法结果，
唯一的独立项是 `bne`（分支）。⇒ 收益 **0**。

**③ `core_state_transition`（`_out.lst:1270-127c`）—— load-use 的代表**

```
1270: lw   a4, 0(a0)
1274: mv   a2, a0
1278: lbu  a5, 0(a4)
127c: beqz a5, 14e8        ← load-use 停 1 拍（lsu.v:193-201）
```
紧跟在 load 后面用它的是**分支**。⇒ 收益 **0**。

**规律：本核 CoreMark 的"挡路者"几乎总是被分支或 load 立刻跟上使用，而这两类都在顺序车道上。**
要让队列吃到这些拍，就必须让**分支和 load 越过未就绪的 ALU 项**——
那就不是"只对 ALU 类生效"了，撞用户红线。

### 5.4 可恢复拍数逐项估计

| 项 | 量 | 队列能拿到吗 |
| --- | --- | --- |
| 除法 32 拍全局冻结 | CoreMark 稳态 **0 次** | 无对象 |
| mul-use 停顿 | `≈4.6×10³` 次/迭代（N=9：`matrix_mul_matrix` 729 + bitextract 1458，各跑 2 遍，加 vect/const 的 162+81 ⇒ ≈4.6×10³），**≈1.3% 拍数** | **0**（§5.3 ①②） |
| load-use 停顿 | 状态机每条字符 ≥1 次（2000 字节×2 遍）⇒ `≈4×10³`，**≈1.1%** | **0**（§5.3 ③） |
| dcache 缺失冻结 | `1,583` 次/迭代（`逻辑说明` §26.4） | **只能恢复段长那一部分**：每次缺失的窗口受"到下一个非 ALU 指令的距离"封顶（均值 3.11）⇒ 上界 `1,583×3 ≈ 4,750` 拍 ≈ **1.4%**，且要求 §4.6 的前端解冻（阶段 3，风险最高） |
| 取指空泡（预测跳转命中 1 拍） | 每个 taken 分支/jal 固定 1 拍（`icache.v:196-200`、`pc.v:60-97`，`icache.v:113` 自述"代价：多一拍空泡"） | **0**（前端问题，不是乱序问题） |

**汇总**：理论可拿 **0 ~ 1.4%**，而 §5.3 的逐循环核对把大多数情形压到 **0**；
若再算上"排空栅栏让非 ALU 指令多等"（§3.3 的 off-by-one 一不小心就是每遇到一条非 ALU
指令多一拍 ⇒ 非 ALU 占 35% ⇒ **净变慢 35%**），**风险收益比明显不利**。

**结论：不推荐。** 若目标是 CoreMark 拍数，杠杆按量级排序是：
① 预测跳转命中的 1 拍取指空泡（同量级的 8%）、② 分支预测器覆盖（64 项 BHT/BTB，
`bra_predict.v:38-43`；`ird_tmr` 只有 3 拍，`controller.v:128-132`）、
③ dcache 子字 store 的 1,468 次/迭代写失效（`逻辑说明` §26.5）。这三项都不需要动流水线结构。

---

## 6. 风险与代价

### 6.1 时序影响方向逐条

| # | 改动 | 落在哪条路 | 方向 | 依据 / 量级 |
| --- | --- | --- | --- | --- |
| R1 | IQ 从 `r1_data_final`/`r2_data_final` 取样（每拍 +128 flop 负载） | **正是当前最差路的头**：Top10 全是 `u_post_decoder/r1_data_out_reg[*] → u_lsu/{s2_addr,bus_addr_out}_reg[*]`，**布线占 60~74%** | **负（最重）** | 不是逻辑深度而是**布线长度/拥塞**；本项目已有"0.2~0.5ns 是布局噪声"的教训，必须收到 15ns 下比 |
| R2 | alu 输入的 2:1 mux（32 位 ×2 + func4/rd） | `post_decoder.r1_data_out → alu → result_back1 → forw` 这条族 | 负（+1 级 LUT，~0.25-0.4ns） | 该族在 7z010 的 Top10 里**没出现**（比最差路至少好 1.2ns）⇒ 可能不动 WNS，但 7z020 下需实测 |
| R3 | 发射优先链 + `iq_disp`/`iq_will_empty` 控制 | `wb_reg`/`post_decoder` 的 CE 路 | 负（~0.2-0.4ns） | 短链，但挂到多个寄存器的 CE |
| R4 | `pc`/`pre_decoder`/`mid_decoder` 新增 `iq_adv` 项 | 这三级的 CE；`pc` 的 CE 路已知含 flush/stall 广播 | 中负 | 并**破坏 I1**，`逻辑说明` 的逐拍表要重写 |
| R5 | `regfile` 口 A 的发射禁止（`~m_pv & ~d_cmt`） | 发射使能路 | 小负 | 只加 AND，不进 `regfile` 数据路 |
| R6 | `csr.irq_act` 增加"队列空"闸门 | `csr.v:176-184` 的组合路 → `controller.flush_irq` → 全片 | 小负 | **功能代价：中断延迟增加"排空时间"**，FreeRTOS tick 精度需实测 |
| R7 | `flag_bus` 加宽（**已否决**） | 全片 | 大负 | 12 个文件的位号都动；照 I6 走专用端口 |

**没有一处是"结构性挪拍"**。`当前进度.md` §6 记的结论是"10ns 的墙是一捆等长链，微改动推不动，
只有结构性挪拍有效"——而本方案全部是"在既有锥上加一级"。按工程经验是**净负**。

### 6.2 面积 / 拥塞

7z010 现状 LUT 58.73%、**BRAM 72.5%**（`mcu_top_utilization_placed.rpt`）。
IQ 约 +340 LUT（+3.3% 利用率），不占 BRAM。**但本核的时序墙是布线 ⇒ 加利用率就是加拥塞**，
这比 LUT 数本身更值得担心。

### 6.3 实现复杂度与验证难度

- 复杂度：**中等偏高**。真正的坑不在队列本身（值式 + 单发射 + 排空栅栏已经是最简形态），
  而在 §4.6 的连带效应（`mid_decoder` 被提前推进后，它那些被**组合**消费的输出指向了新指令）
  和 §3.3 的 `iq_will_empty` off-by-one。
- 验证难度：**高**。乱序最容易出的是**竞态类错拍错误**（唤醒同拍/晚一拍、排空与冲刷同拍），
  单跑一遍程序**很可能过**。而且：

  > **现有 `archtest` 的判据会在正确机器上误报。**
  > `cmp_events.py:61-84`（`first_diff`/`report_diff`）对每个写口做**逐事件顺序**比较。
  > 乱序发射会让 alu 口的写序列成为程序序的一个**置换** ⇒ **正确的机器也会被判 `DIVERGE`**。
  > 必须按 §7.2 改判据。

---

## 7. 验证方法

### 7.1 现有工具够不够

| 工具 | 够不够 | 说明 |
| --- | --- | --- |
| `run_reg.sh`（9 项，iverilog 快仿 ~11s） | 够（回归） | 但它 9 项里 5 项是**模块级外设 tb**，只有 `gpio_irq`/`i2c_irq` 两项过整个核 ⇒ 对流水线改造**覆盖很薄** |
| `tools/archtest/`（`diff`/`golden`） | **判据要改** | 见 §7.2；它仍是唯一能证"架构可见行为没变"的仪器 |
| CoreMark（`build_coremark_v1.sh` + `run_xsim.sh`） | 够 | 判据：**777 字符 + CRC `e714/1fd7/8e3a` + 拍数**。拍数须记基线 |
| 7z020 实现（`run_imp.tcl`） | 够 | 但**基线待确认**（§0） |

### 7.2 判据必须新增（**这是乱序改造最关键的一条**）

因为不重命名 ⇒ 同一个 rd 的多次写在**任何**正确实现里都必须**保序**（WAW），
但**不同 rd 之间允许置换**。所以：

1. **`cmp_events.py` 新增 `golden2` 模式**：对写口序列做"同 rd 保序"的等价判定
   （把事件流按 rd 分组，组内序列必须完全相同；组间顺序不判）。
2. **新增"寄存器堆快照"判据**：参考模型侧在若干里程碑各导出一份 `regs[0:31]`，
   RTL 侧用 tb 层次引用 `u_cpu_top.u_regfile.regs` 对同一里程碑取样比对
   （**这是最终状态判据，也是唯一能替代"逐事件顺序"的强判据**）。
3. **`diff` 模式（两棵树）**在阶段 1（严格队头发射）下**仍可用原判据**——
   因为那时事件流与现状逐位相同。这是阶段 1 值得单独成阶段的原因。

### 7.3 需要新增的探针（不新增 RTL 文件；用只读层次引用，照 `tb_arch.v:49-70` 的做法）

| 探针 | 用途 |
| --- | --- |
| `iq_cnt`（占用数） | 证明队列真的被用上了（否则"没收益"与"没生效"分不清） |
| `iq_iss_rd`、`iq_iss` | 统计发射率 |
| `iq_overtake_cnt` | **越过计数**：`iq_iss1` 累计。**这是"确实发生了乱序"的唯一证据** |
| `iq_disp_cnt` / `iq_full_cyc` | 背压占比 |
| `iq_stall_cyc` | 因队列满而冻结的拍数（用来抓"净变慢"） |
| **水位/时长统计** | 队头等待拍数的直方图——**直接给出"§3.6 的越过是否真的省了拍"** |

**探针纪律照 `当前进度.md` §7.3**：探针 > 20 个、沿 < 300；微测目标单一。

### 7.4 定向程序（放 `tools/archtest/prog/`，新写 4 条）

| 程序 | 覆盖 |
| --- | --- |
| `iq_win.S` | 3~4 条**互相独立**的纯 ALU，后面跟一条 dcache 缺失的 load —— 验证窗口长度与排空 |
| `iq_waw.S` | 多条写**同一个 rd** 的纯 ALU（含 `x0`）—— 验证 WAW 保序与 `rd==0` 不占队位 |
| `iq_wake.S` | 一条等 load 的项 + 后面**独立**的项 —— 验证唤醒与"越过"（期望 `iq_overtake_cnt > 0`） |
| `iq_barrier.S` | 队列非空时紧跟 `branch` / `store` / `mul` / `csrrw` —— 验证排空栅栏；**并覆盖 §4.6 的 `csr_addr_pre` 污染窗口** |

### 7.5 抗竞态的验证手法（照 §3.2 的三条教训）

1. **扫应答延迟**：`run.py`/tb 里把外设与 dcache 的应答延迟参数化，跑 `--delay 1/3/12`
   （`当前进度.md` §3.2 第 3 条："混延迟正是应答串台的触发条件，同延迟测不出来"）。
2. **扫编译优化档**：同一程序 `-O0` 与 `-O2` 各跑一遍 —— 指令序列不同 ⇒ 窗口形状不同。
3. **扫 `-DITERATIONS=1/2`** 的 CoreMark。
4. **加显式断言**（iverilog 里用 `$display` + 计数，不改 RTL 逻辑）：
   - `assert(~(s4_non_alu & iq_cnt != 0))` —— 排空栅栏违例；
   - `assert(~(flush & iq_cnt != 0))` —— 冲刷时队列非空；
   - `assert(~(iq_iss & (m_pv | d_cmt)))` —— §4.5 的写口撞车。
5. **"别把慢当卡死"**：本方案有真实的死锁风险面（§4.6 的连带效应、唤醒与排空的耦合），
   抱死时必须给 pc trace 或"零输出 + 时间在走"的证据（`当前进度.md` §7.6）。

### 7.6 时序验证

- 必跑 7z020 实现，判据同现状（`Setup : 0 Failing Endpoints`）。
- **并且**在收紧约束下比一次（照记忆里"评估时序改动必须收紧约束"的纪律），
  因为 0.2~0.5ns 的差异是布局噪声、无分辨力。
- 面积看实现后的 slice/CLB，不看综合后 LUT 数。

---

## 8. 分阶段实施顺序（每阶段可独立验证、可独立回退）

### 阶段 0 —— 先测量（**零 RTL 改动**）

目标：拿到"到底有多少拍是'被冻结且有 ready 的纯 ALU 项在后面'"。
做法：写一个**只读**探针 tb（放 `tools/`，不进 `RTL/`；照 `tb_arch.v:49-70` 的层次引用），
统计六个 stall 位各自的高拍数、以及"停顿拍 ∩ 后续 1~2 条是独立纯 ALU"的拍数。

**判据：若该拍数 < 总拍数的 3%，停止本课题。**
（依据：§5.1 的上界是 8.45%，而 §5.3 显示可恢复的部分远小于 3%。）

### 阶段 1 —— 结构落地，**语义不变**

内容：`mid_decoder` 加 `isu` 位；建 IQ（深度 2、值式、`stallf` 入冲刷窗）；
选择逻辑**只选队头（严格顺序发射）**；`regfile` 口 A 禁止发射（§4.5）；
`wb_reg` 的 `iq_iss` 门（§4.7）。
此刻它等价于一个"可深化的停顿缓冲"，**不产生任何乱序**。

判据（**全部用现有判据即可**）：
- `archtest` `diff` = **0 差异**（原判据可用，因为事件流应逐位相同）；
- CoreMark 777 字符 + CRC 全对 + **拍数逐拍一致**；
- `run_reg.sh` 9/9；
- 7z020 `0 Failing Endpoints`。

回退：删掉 IQ 的例化与 `isu` 位，`post_decoder` 恢复原捕获条件。

### 阶段 2 —— 真乱序（只放开"越过操作数未到齐的纯 ALU 项"）

内容：选择改成最老就绪优先（§3.6）；开 `iq_overtake_cnt` 探针。

判据：§7.2 的**新**判据（同 rd 保序 + 寄存器堆快照）；`iq_wake.S` 期望
`iq_overtake_cnt > 0` 且快照与参考模型一致；CoreMark 拍数（**这是第一次可能变化**）。

回退：把选择条件改回"只选队头"即**精确回到阶段 1**（这是把阶段 2 独立出来的全部意义）。

### 阶段 3 —— 解开前端冻结（**收益来源，但风险最高**）

内容：`pc`/`pre_decoder`/`mid_decoder` 的 `stall_w` 增加 `~iq_adv`（§4.6）；
处理 `csr_addr_pre` 的污染窗口（§4.6 表格末行）。

判据：同阶段 2，**追加** §7.5 的全部抗竞态手法（漏了这一步几乎一定漏 bug）；
CoreMark 拍数**必须变小**才算收益（若不变或变大，回退）。

**建议：阶段 3 单独评估、允许不做。** 它承担的时序风险（R4）与复杂度，
对应的收益上界只有约 1.4%（§5.4），而且在 7z020 上能否保住 0 失败端点**完全未知**。

---

## 附：不确定 / 需实测 / 需你确认的清单

| # | 事项 | 性质 |
| --- | --- | --- |
| U1 | **时序基线是 7z020 的 +0.008ns 还是本机 7z010 的 −1.220ns / 103 失败端点** | **需你确认**（决定"没回退"能不能判定） |
| U2 | §5.4 的 dcache 缺失恢复量（4,750 拍 ≈ 1.4%） | **需实测**（`1,583 次/迭代` 来自 `逻辑说明` §26.4，且未给出成因分解） |
| U3 | R1（`r1_data_final` 加 128 flop 负载）对 WNS 的实际影响 | **需实测**，且必须在收紧约束下比（0.2~0.5ns 是布局噪声） |
| U4 | R2（alu 输入 mux）是否会把 alu 那条族推进 Top10 | **需实测** |
| U5 | R6（中断闸门加"队列空"）对 FreeRTOS tick 精度的实际影响 | **需实测** |
| U6 | 取指空泡（每个 taken 分支 1 拍）占 CoreMark 拍数的真实比例 | **量级估算，需实测**（估 ~8%，与 8.45% 的 IPC 预算同量级 ⇒ 与停顿大量重叠） |
| U7 | 动态乘法次数 ≈4.6×10³/迭代 | **推算**（N=9 由 `core_main.c:205` 的 `size/num_algorithms` = 2000/3 = 666 与 `core_matrix.c:190-196` 的 `8i²≥blksize` 推出 ⇒ i=10, N=9） |
