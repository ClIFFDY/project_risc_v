> **⚠️ 本文是【方案稿】（2026-09-25 之前写的计划），实施时有几处偏离 —— 实际做法与验证结果以 逻辑说明 §29 为准。已知偏离：
> ① 指令地址非对齐按【规范 cause **0**】实现（本文提到任务书可能期望 4 —— 该点仍待核对）；
> ② wfi 保持【合法】当 NOP（本文建议按非法，与规范冲突）；
> ③ 访存非对齐按【访问宽度】判（半字看 bit0、字看 bit1:0），不是“只判字”；
> ④ jal 的目标由 post_decoder 就地解 immJ 算出；
> ⑤ 异常判定必须与 lsu 入口条件 new_in_pre 相与（实施时踩出的坑，本文未提）。

# 方案：机器模式异常-中断（完整异常语义 + fence）

> 2026-09-24 ｜ 基线 `9a13815`（工作区只有 `core/lsu.v` 未提交改动）｜ 约束 7z020 / 10ns
> 基线数字：WNS **+0.008ns** / 0 失败端点 / TNS 0（`当前进度.md` §3.5）
> 本文只出方案，**未改一行 RTL**。所有结论给出 `文件:行号`；推导结论与实测结论分开标注。

---

## 0. 结论速览（先看这 6 条）

1. **`trap`/`ebreak` 现在根本没有重定向 pc**：`pc.v` 的冲刷分支只认 `flush_jump_w` / `irq` / `irq_ret`
   （`core/pc.v:84-97`），`trap`/`ebreak` 不在其中。于是 ecall 那一拍 pc 原地不动、下一拍照常 `+4`
   （`core/pc.v:75`），而 `flush` 只有 1 拍宽度 ⇒ **A+4/A+8/A+12 三条指令整条丢掉、从 A+16 继续**。
   这就是"trap 生效要打一拍，否则丢指令"的真实机理（§1.2 有逐拍推演）。
   ⚠ **这是按 RTL 逐条赋值推导出来的，没有跑仿真实证**（本任务只允许写方案文档）——
   请你先用 §5.1 的 `exc_ecall.S` 微测把这一条钉死，再谈改法。
2. **`csr.v` 的 `pc_addr_in − 12`（`core/csr.v:129-130`）对中断是对的、对异常是错的**：
   中断的边界就是"最老被杀指令"= E3（`mid_decoder` 那条），`pc−12` 恰好等于它，也正是规范的 mepc 口径；
   而 `trap` 的指令在 **E4**，同一条指令在 E4 时 `pc−16`，所以 `−12` 存出来是"发生指令的**下一条**"。
   更要紧的是：**非对齐分支目标的判定点比 trap 再晚一级（E5，`pc−20`）** ⇒ 异常不能再用"pc − 常数"，
   必须改用**随指令携带的 PC 载荷**（`aux_addr`）减 4（§3.2）。
3. **非对齐目标今天不抛异常是可推导的必然**：分支预测落点 `pc_addr <= pc_addr + offset_beq2 - 4'd4`
   （`core/pc.v:63`）与 br2 落点 `jp_target = aux_addr_in + beq_off_in`（`core/bju.v:118`）的低 2 位
   = `{inst[8], 0}`（因为 `offset_beq2[1] = immB[1] = inst[8]`，见 `core/pre_decoder.v:62`），
   而取指用 `fetch_addr = pc_addr >>> 2`（`core/icache.v:116`）⇒ **静默截断到字边界**，
   落进 `.fill 2,1,0x00` 填充 ⇒ 与 `当前进度.md` §6 记的现象完全吻合。
4. **`mcause` 现在是被电平覆写的**：`core/csr.v:116-126` 只要 `MIE=1` 且有"使能且挂起"的源，**每拍**写
   `mcause`（哪怕中断没被受理）；而 `trap` 路径写的是 `32'h8000000B / 32'h80000003`
   （`core/csr.v:131`）—— **带中断位 bit31、且 11/3 是 MEI/MSI 的中断码**。异常必须写不带 bit31 的
   `0/2/3/4/6/11`。
5. **新增异常不需要新流水级、不需要新停顿**：只要每个异常源都落在**既有的判定相位**上
   （E1/E3/E4/E5，见 §3.1 的相位表），并用既有的"冲刷 + 重定向"机制交付。**硬约束**：新产生的冲刷位
   必须是**寄存器源**（不能组合地穿过 `controller`），否则 `flag_bus` 广播网会重演"stallf 绕一圈多花
   0.45ns"（`core/controller.v:64-66` 注释、`当前进度.md` §6）。
6. **岔路口必须先定案**（§7 第 1 条）：`mepc` 存"发生异常的指令"（规范）还是"回返后要执行的地址"
   （本核现状 `pc−12` 的口径）。**arch-test 的 trap handler 会给 mepc 加 4** 的那一派才吃规范口径；
   本机没有 `riscv-arch-test-v2`（`当前进度.md` §1），所以这一步**不确定，必须先把测试源码拷来读**。

---

## 1. 现状盘点（读代码得出）

### 1.1 `csr.v` 实现的 CSR 与位

| 地址 | 名字 | 实现内容 | 读（`core/csr.v:199-209`） | 写（`core/csr.v:83-113`） |
| --- | --- | --- | --- | --- |
| `0x300` | mstatus | **只有 bit3=MIE、bit7=MPIE** | `{20'd0, MPIE, 3'd0, MIE, 3'd0}` | 只取 bit3 / bit7（`85-88`） |
| `0x304` | mie | **只有 bit3=MEIE、bit7=MTIE、bit11=MSIE** | `{20'd0, MSIE, 3'd0, MTIE, 3'd0, MEIE, 3'd0}` | 只取 bit3/7/11（`90-94`） |
| `0x305` | mtvec | 整字透传，**无 MODE 位、不做 4 字节对齐规整** | 原值 | 原值（`96-99`） |
| `0x341` | mepc | 整字透传；**同时当"mret 回返地址"用** | 原值 | 原值（`101-104`） |
| `0x342` | mcause | 整字可读可写 | 原值 | 原值（`105-108`） |
| `0x344` | mip | 读：bit11=MSIP(=`sirq_pend`)、bit7=MTIP、bit3=MEIP | `{20'd0, sirq_pend, 3'd0, tirq_pend, 3'd0, eirq_pend, 3'd0}` | **只**在写值 bit11=1 时清 `sirq_pend`（`109-112`） |
| `0xB00` | mcycle | 自由计数器 | `mcycle_reg` | 不可写 |
| `0xB02` | minstret | 按 `retire` 计数 | `minstret_reg` | 不可写 |
| 其它 | — | **一律读 0、写丢弃**（`208` 的 default；`83` 的 case 无条目） | | |

两条与规范不符的地方（读码即可确认）：

- **MEIE / MSIE 的位置反了**：规范是 `MSIE=bit3`、`MTIE=bit7`、`MEIE=bit11`；本核是 `MEIE=bit3`、
  `MSIE=bit11`（`core/csr.v:90-94` 与 `199-205` 两处对称地反）。`mip` 的 MEIP/MSIP 同样反（`205`）。
  现有微测是按**本核的**位置写的（`tools/mt_csrr2.S:8-11`、`tools/gpio_test.S:46`、`tools/i2c_test.S:40`
  都写 `mie=8`）⇒ **改成规范位置会打断这三条闭环测试**，必须同步改程序（§7 第 4 条）。
- `0x344` 的写法只覆盖"清 MSIP"，**没有 set 通路**；`softi` 在 `cpu_top` 恒接 `1'b0`
  （`core/cpu_top.v:466`）⇒ **软件中断（MSI，cause 3）这条路是死的**：`sirq_pend` 除了复位
  （`core/csr.v:67`）没有任何置位者。

### 1.2 `trap` / `ebreak` / `irq_ret` 现在各做什么

**`trap` / `ebreak`**（来自 `post_decoder` 寄存器输出，`core/post_decoder.v:212-213`：`imm12==0x000` / `0x001` 且 `func3==000`）：

- `controller`：并进冲刷 —— `flush_irq = irq || irq_ret || irq_act || trap`（`core/controller.v:68`）。
- `csr`：`if (trap) begin iret_addr1/2 <= pc_addr_in - 4'd12; mcause <= ebreak ? 0x80000003 : 0x8000000B;`
  `MPIE <= MIE; MIE <= 0; irq_process <= 1; end`（`core/csr.v:128-135`）。
  ⚠ 这个 `if` 是**电平触发、没有任何一次性保护**；而 `post_decoder` 的保持分支 `else if (stall_w)`
  **没有排掉 `flush_w`**（`core/post_decoder.v:76` vs `pre_decoder.v:76` / `mid_decoder.v:146`
  / `pc.v:46` / `regfile.v:79` 都排了）⇒ 只要冲刷拍上还有任一根 stall 位（`lsu_stall` 车3 占用、
  `dcache_hold`、`icache_busy`…），`trap` 就会被**保持多拍**，`iret_addr` 被反复覆写。
- **`pc` 完全不认 `trap`** ⇒ ecall/ebreak 不跳 mtvec。

逐拍推演（稳态、无停顿；A = ecall 的地址；E1..E5 = `icache.inst_out` / `pre_decoder` / `mid_decoder`
/ `post_decoder` / `wb_reg` 五级寄存器）：

| 拍 | pc | E1 | E2 | E3 | E4 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| N | A+16 | A+12 | A+8 | A+4 | **A(ecall)** | `trap=1`，`flush_w=1`；pc 不赋值⇒保持；E2/E3/E4 在沿上清零 |
| N+1 | A+16 | instr(A+16) | 0 | 0 | 0 | `trap` 已随 E4 清零 ⇒ `flush_w=0` |
| N+2 | A+20 | instr(A+20) | instr(A+16) | 0 | 0 | 顺序流从 A+20 起恢复 |
| N+4 | A+28 | … | … | instr(A+20) | **instr(A+16)** | A+16 这条被**当成普通指令执行** |

⇒ **A+4、A+8、A+12 三条永久丢失**（它们在冲刷拍分别位于 E1/E2/E3），执行流从 A+16 续。
`iret_addr = pc−12 = A+4` 恰好是"丢掉的那三条里最早的一条"，但因为它只在 `mret` 时才被用，而 ecall
路径从来不跳 mtvec，所以这个值一直是"存着没人用"。

**`irq_ret`（mret，`core/post_decoder.v:206`：`func3==000 && imm12==0x302`）**：

- `pc.v`：`else if (irq_ret) pc_addr <= isr_ret_addr2`（`core/pc.v:93-96`）⇒ 落点 = `iret_addr2`
  （一个**在陷阱发生时**就写好、此后不再变的寄存器）⇒ **没有相位问题**，mret 是对的。
- `csr`：`if (iret) begin irq_en_reg <= irq_en_post_reg; irq_process <= 1'b0; end`（`core/csr.v:145-148`）
  —— 即 `MIE ← MPIE`；**但没有按规范把 `MPIE` 置 1**（偏差，见 §2.4）。
- `controller`：`irq_ret` 也进 `flush_irq`；`ird_tmr` **不含** irq_ret（`core/controller.v:130`），
  靠"冲刷置 `irq_bubble=4`"覆盖（`core/controller.v:124`）。

### 1.3 `flush_irq` 的合成与相位

- 合成点：`core/controller.v:67-77`，一位一网、彼此不共享逻辑（风格里明确写的）。
  `flush_irq = irq || irq_ret || irq_act || trap`，其中 `irq = irq_act`（`core/controller.v:117`）
  ⇒ `irq||irq_act` 是**冗余项**。
- 相位（关键）：三个"权威冲刷"位（`jalr_fail`/`br2`/`br3`）来自 `bra_predict` 的**寄存器**输出
  （`core/bra_predict.v:88-93`，输入是 `bju` 判定延迟拍的寄存器）；`trap`/`irq_ret` 来自
  `post_decoder` 的寄存器；`irq`/`irq_act` 来自 `csr` 的**组合**（`core/csr.v:179`，输入全是寄存器）
  ⇒ **`flag_bus` 的每一位都是寄存器源**，这是它能在 10ns 收敛的隐含前提。
- 中断的"一次性"由状态位保证：`irq_act` 里有 `!irq_process`，而 `irq_process` 在受理沿置 1、
  只在 mret 清 0（`core/csr.v:134/142/147`）⇒ 中断受理天生是单拍脉冲。**`trap` 没有这个保护**（§1.2）。
- `irq_bubble`（4bit）：复位 12；`flush_w` 拍置 4；否则每拍 +4 至 12（`core/controller.v:122-126`）。
  `flush_w` 在这里是 `flush_irq|flush_jump`（`core/controller.v:58`）——**不含新异常位**（改造时必须并进去，
  否则异常后的空窗里受理中断会读到陈旧的 bubble）。
- `ird_tmr`（2bit）：`jal | jalr_pred | br1` 且 !flush_w 时置 3，逐拍减 1，非 0 时输出 `irq_gate`
  （`core/controller.v:128-132`）；它是"预判型早跳后 3 拍不受理中断"的抑制。

### 1.4 中断从 PLIC 到 `pc` 落点的完整链路

```text
外设中断源（io_block_top.v:117 的位图：bit1=uart_rx、bit3=gpio、bit4=i2c）
  → plic：pending 锁存（plic.v:85）、优先级仲裁（plic.v:50-65）→ irq_valid
  → exti 打一拍输出（plic.v:67-70，§22.3 的时序修法）
  → io_block_top.plic_exti（io_block_top.v:118）
  → mcu_top.u_cpu_top.exti（mcu_top.v:53）
  → controller.exti（cpu_top.v:464）→ csr.exti
  → csr：eirq_pend = exti（组合，csr.v:177）；global_pend = (eirq_en&eirq_pend)|(tirq_en&tirq_pend)|(sirq_en&sirq_pend)（csr.v:178）
  → csr：irq_act = MIE && global_pend && !irq_process && !irq_gate && !(iret|trap|jalr_fail|br2|br3)（csr.v:179）
  → csr：mcause <- 0x8000000B / 0x80000007 / 0x80000003（csr.v:118-125，注：这是"电平覆写"，见 §1.6）
  → csr：iret_addr1/2 <- pc_addr_in - irq_bubble；MPIE<-MIE；MIE<-0；irq_process<-1（csr.v:137-143）
  → controller：irq = irq_act（controller.v:117）；flush_irq = irq|... （controller.v:68）
  → flag_bus[7] 广播到全核（controller.v:70）
  → pc：else if (irq) pc_addr <= isr_addr2（pc.v:89-92）；icache：inst_out 当拍打 NOP（icache.v:198）
  → 第二拍 inst_out = instr(mtvec)，第三拍交付进流水线
```

定时器中断是**核内**的：`tim_in`（`core/tim_in.v`，地址窗 `[31:24]==0 && [23:20]==1` = `0x00400000`）
例化在 `cpu_top` 内（`core/cpu_top.v:432-442`），`timi` 是**电平**（`tim_in.v:54-57` 置 1，
靠写 `+0xC` 清，`tim_in.v:82-84`）。

### 1.5 现在缺什么（逐条对 RISC-V 特权规范 M 模式）

| # | 缺的东西 | 现状证据 |
| --- | --- | --- |
| 1 | **异常不带任何 cause 语义** | `mcause` 只会被写成"中断码 + bit31"或 `0x80000003/0x8000000B`（`csr.v:118-131`），异常码（0/2/3/4/6/11）一个都没有 |
| 2 | **`trap` 不重定向 pc** | `pc.v:84-97` 无 trap 分支（§1.2 推演） |
| 3 | **无"指令地址非对齐"（cause 0）** | 全核没有一处检查目标/落点的 bit1（`pc.v:63/85`、`bju.v:101/118-120` 都直接赋值） |
| 4 | **无"非法指令"（cause 2）** | `pre/mid/post_decoder` 都靠 `case` 无 `default` 兜成 NOP（`post_decoder.v:132-251`），非法 opcode 静默流过去 |
| 5 | **无"load/store 地址非对齐"（cause 4/6）** | `lsu.v:122-136` 计算 `st_be/st_wdata/cur_addr` 时完全不看 `st_addr[1:0]` 的合法性；`sw` 的 `st_be` 恒 `4'b1111`（`lsu.v:127`） |
| 6 | **无 `mtval`** | `csr.v` 无 `0x343`（`83-113` 的 case 里没有） |
| 7 | **无 `mscratch`（0x340）** | 同上。ISR 想用 `mscratch` 保存现场会静默写丢、读回 0 |
| 8 | **无 `misa`（0x301）/`mhartid`（0xF14）** | 同上（arch-test 的启动宏常读它们，需确认） |
| 9 | **`mstatus.MPP` 恒读 0** | `csr.v:200` 把 bits[12:11] 写死 0；规范口径（只实现 M 模式时 MPP 该是什么）**未定，需确认** |
| 10 | **`mret` 不把 `MPIE` 置 1** | `csr.v:145-148` 只做 `MIE<-MPIE` |
| 11 | **无 CSR 合法性检查** | 未实现地址读 0 写丢弃（`csr.v:208`）、只读位（`csr[11:10]==11`）可写、`csrrw/rs/rc` 的 `rs1==x0` 不写语义**没做**（`post_decoder.v:215-244` 对 func3∈{2,3,6,7} 一律 `csr_wr_en<=1`；对 `0x344` 会造成"只读访问清 MSIP"的副作用） |
| 12 | **`fence`/`fence.i`** | opcode `0x0F` 在任何译码器里都没有条目 ⇒ 目前**恰好是 NOP**（`post_decoder.v:132-251` 全 case 不命中 ⇒ 全清）。没有"显式声明合法"，一旦加非法指令检查就会被误判 |
| 13 | **icache 无失效通路** | `icache.v:127` 的 way0 命中判据是常量 `tag==19'd0`、无 valid 位 ⇒ 前 16KB **物理上不可失效**；`fence.i`（自改码）无法支持（`逻辑说明` §23.10） |

### 1.6 顺带查出的既有缺陷（与本次改造耦合，必须一起决策）

1. **错路 CSR 写**：`csr.v:82` 的门是 `csr_wr_en && !stall`，而 `stall`（`csr.v:48`）**只 OR 了六根 stall 位、
   没有排 `flush_w`**。跳转冲刷拍（`br2`/`br3`/`jalr_fail`）E4 上坐的是**错路的 B+1**（这正是
   `wb_reg.v:51-55` 注释自己写的事实），若 B+1 是 `csrw`，它的 `csr_wr_en=1` 会**真的写 CSR**。
   ⇒ 新增异常判定必须**同时**把这类错路来源挡掉（§3.3 的门控表）。
2. **错路 `trap`**：同上，B+1 若为 `ecall`，冲刷拍会被"真的当成异常受理"（写 `mcause/mepc`）。
3. **`mcause` 电平覆写**（§1.1/§1.4）：`csr.v:116-126` 每拍写。
4. **`irq_bubble` 的 4/8 两档可能因"icache 不再当拍跳目标"而差一条指令**（**推导，需微测**）：
   新 pc 口径下（`pc.v:82-83` 注释：pc 落在目标上、icache 下一拍才取、一拍后交付），
   冲刷沿后第 1 拍的流水线**全空**、pc 已等于目标 T，此时"最老未提交"就是 **T**（= pc），
   而 `bubble=4` 给出 `pc−4 = T−4`。旧口径（pc 存"顺序后继"、icache 当拍取目标）下 `pc−4 = T` 是对的。
   ⇒ 影响面：只在 **br2/br3/jalr_fail 冲刷后 1~3 拍内受理中断**这一角（`trap`/异常自身不受影响，
   因为受理时 MIE 已被清 0）。**现有测试打不到这一角**（`mt_irq2.S` 只覆盖稳态 bubble=12；
   `gpio_test.S`/`i2c_test.S` 的 ISR 把 mepc 存到槽里但不判它）。**不确定，需写微测实证**。
5. **`jalr` 目标没有清 bit0**：`bju.v:101` 直接 `r1_data_in + r2_data_in`。规范要求 JALR 的
   target LSB 恒为 0（bit1 才是非对齐判据），且 BTB 会把这个值当预测地址写回（`bju.v:72` / `bra_predict.v:71-74`）。
6. **错路 store 已经被挡住（这是**好**消息，方案要用它）**：`bju.stallf` 是判定组合的**早一拍**版本
   （`bju.v:117`），直连 `lsu`/`mulu`（`cpu_top.v:260/294`），lsu 的冲刷窗因此**宽了一拍**
   （`lsu.v:72`）。推演：紧邻分支之后的那条指令在**判定组合拍**就会被 `~flush_w` 挡在车外
   ⇒ **没有错路 store 能上总线**。方案里所有新异常源都要同样并入 `stallf`（§3.7）。

---

## 2. 目标定义（M 模式异常-中断语义）

### 2.1 CSR 清单与逐位口径

| CSR | 目标实现 | 本核要不要做 | 备注 |
| --- | --- | --- | --- |
| `mstatus` `0x300` | `MIE[3]`、`MPIE[7]` 已做；**补 `MPP[12:11]`** | **做 MPP** | 只实现 M 模式，`MPP` 建议**只读 3（M）**或至少陷阱时写 3、`mret` 后按规范置最低支持的档；**规范原文口径需你确认** |
| `mie` `0x304` | **改成规范位**：`MSIE[3]`、`MTIE[7]`、`MEIE[11]` | **做（但要同步改 3 个测试程序）** | 见 §7 第 4 条 |
| `mtvec` `0x305` | 目标地址 + `MODE[1:0]` | **做最低限度**：`MODE` 读回 0（direct），**不 masking BASE** | 理由：`tools/archtest/prog/m_zicsr.S:39-47` 与 `tools/rv32i_ref.py:65` 都按**整字透传**建模/断言（写 `0x1234FFF7` 要读回 `0x1234FFF7`）⇒ 一 masking 就打断这条回归；要 masking 必须同步改测试与模型 |
| `mepc` `0x341` | 见 §2.2 的口径决策 | **做** | |
| `mcause` `0x342` | 异常 `0/2/3/4/6/11`（无 bit31）；中断 `0x8000_000B/…07/…03` | **做** | |
| `mtval` `0x343` | 非对齐：出错地址；非法指令：0（规范允许）或指令字 | **建议做**（arch-test 的 `misalign-*` 很可能查 mtval）**——但必须先核对测试源码** | 若确认不查，可先做只读 0 降风险 |
| `mip` `0x344` | 规范位 `MSIP[3]/MTIP[7]/MEIP[3→11]`，只读 | **做（同步改测试）** | 写通路按规范 M 模式只读；`sirq_pend` 的 set 通路**不做**（无 CLINT，维持现状） |
| `mscratch` `0x340` | 纯 RW 暂存 | **做**（0 逻辑，1 个寄存器） | ISR 现场保存、arch-test handler 可能用 |
| `misa` `0x301` | `MXL=01`、`I[8]`、`M[12]` | **建议做**（只读常量）**——需确认测试是否写它** | 写上按规范应抛非法指令 |
| `mhartid` `0xF14` | 只读 0 | **建议做** | |
| `mcycle`/`minstret` `0xB00/B02` | 现状保留 | **不做**（不在本次范围） | 模型数不出核的真实拍数（`archtest/README.md`） |
| `mcountinhibit`/`mcounteren`/`menvcfg`/PMP | — | **不做** | 单 hart、M-only、无 U 模式 |

### 2.2 异常清单（cause / 判定级 / mepc 口径 / mtval）

| 异常 | cause | 判定在哪一级（本项目记法） | mepc 应存 | mtval | 做不做 |
| --- | --- | --- | --- | --- | --- |
| 指令地址非对齐（分支目标） | **0** | `bju` **判定延迟拍**（与 br2/br3 同相；指令此刻在 E5） | 分支自身地址 | 目标地址 | **做（本方案的靶心）** |
| 指令地址非对齐（jalr 目标） | 0 | 同上 | jalr 自身地址 | 目标地址 | **做** |
| 指令地址非对齐（jal 目标） | 0 | **E1**（`pre_decoder` 组合，静态可判：`inst[21]`） | jal 自身地址 | 目标地址 | **做（几乎零成本）** |
| 非法指令 | 2 | E4（`post_decoder` 组合判定 + 寄存器输出） | 该指令地址 | 0 或指令字 | **做（第二优先，见 §3.4）** |
| 断点 ebreak | 3 | E4（已有 `ebreak` 信号） | 该指令地址 | 0 | **做（把现有路径修对）** |
| load 地址非对齐 | 4 | E3 判定（`lsu` 组合）→ **E4 相位交付** | 该指令地址 | 出错地址 | **做（第三优先）** |
| store 地址非对齐 | 6 | 同上 | 该指令地址 | 出错地址 | **做** |
| M 模式环境调用 ecall | 11 | E4（已有 `trap` 信号） | 该指令地址 | 0 | **做（把现有路径修对）** |
| 取指/访存访问错（1/5/7） | — | — | — | — | **不做**（无 PMP、地址全映射；未映射地址现状是"永久等应答"= 挂死，属总线侧议题，不在本次） |
| U 模式相关（8）、虚拟指令、WFI | — | — | — | — | **不做**（无 U 模式；`wfi`(0x105) 建议先按**非法指令**处理，除非测试要求它是 NOP —— **需确认**） |

> ⚠ **cause 号的确认项**：任务书写"指令地址非对齐 = 4"，但规范里 **4 是 load 地址非对齐**，
> 指令地址非对齐是 **0**（`CAUSE_MISALIGNED_FETCH = 0`）。本方案按**规范写 0**。
> 最终必须以 `riscv-arch-test` 的期望表为准 —— **本机没有该仓库，不确定，需实测**（§7 第 1 条）。

### 2.3 优先级

**同一拍上多个源**（按"程序序最老者胜"，本项目记法）：

```text
E5（bju 判定延迟拍：br/jalr 目标非对齐）
  > E4（post_decoder：ecall / ebreak / 非法指令 / CSR 非法；lsu 的 1 拍寄存故障）
    > E1（jal 目标非对齐，最年轻，可以被更老的源挤掉且不丢——见 §3.3）
```
理由：E5 的指令比 E4 的老、E4 比 E1 的老（同一拍各寄存级的位置见 §3.1 相位表）。
被挤掉的年轻源**不需要任何保护**：它的指令被冲刷掉、`mret` 后会重新取到、重新判定 ⇒ 不丢。

**异常 vs 中断**：**异常优先**。实现方式 = 把新异常源并进 `irq_act` 的门
（`core/csr.v:179` 现有的 `!(iret|trap|jalr_fail|br2|br3)`）。注意"中断不受理"只是把中断推迟到
`mret` 之后，因为被异常冲刷的指令会重新执行、异常会重新判定 —— **不会丢**。
> 规范对"同一拍上待受理中断 vs 指令自身异常"的先后表述含糊，两种做法都能自洽；
> 本方案选"异常优先"以与现有门控方向一致（`csr.v:179`/`logic说明` §6.2）。**需与 arch-test 期望一致，实测定案。**

### 2.4 `mret` 的完整语义

| 动作 | 现状 | 目标 |
| --- | --- | --- |
| `pc ← mepc`（本核口径见 §3.2） | 已做（`pc.v:93-96` 用 `iret_addr2`） | 保持，只换"源"（异常时给异常 save 的值） |
| `MIE ← MPIE` | 已做（`csr.v:146`） | 保持 |
| **`MPIE ← 1`** | **没做** | **补** |
| `MPP ← 最低支持档`（或恒 M） | 没做 | 只在实现了 MPP 后需要（**低优先**） |
| 清除"在陷阱中"的状态 | `irq_process <= 0`（`csr.v:147`） | 保持 |
| `mret` 后不立即再受理中断 | `ird_tmr` 不含 irq_ret，靠 `irq_bubble=4`（§1.6-4 有疑点） | 保持 + 把 bubble 相位问题单独定案 |

### 2.5 `fence`

| 指令 | 语义 | 本核做法 | 判据 |
| --- | --- | --- | --- |
| `fence`（`0x0F`/`funct3=000`） | 访存排序 | **当 NOP**（合规范）：单 hart、访存按序、写穿、AHB 单主机按序 ⇒ 无重排可观察 | 必须**显式声明合法**（否则新加的非法指令检查会误杀） |
| `fence.i`（`0x0F`/`funct3=001`） | 取指同步（自改码） | **两个档**：(a) 先当 NOP + 文档写明限制（CoreMark/arch-test 的静态码不触发）；(b) 真做 = **重跑自举**：置 `boot<=1`、`boot_line<=0`，并把 `valid1/valid2` 清零（way0 无 valid、只能靠重填） | 重跑自举约 4100 拍（`逻辑说明` §23.2），期间 `busy` 恒 1、全流水冻结（`icache.v:134`），天然是一个"排空 + 重取"的屏障 |
| `fence.i` 与 pc | 关闭 | 不需要保存/恢复 pc：`busy` 只冻流水、pc 停在原地，`boot` 结束自然接着取 | 需微测确认 pc 不丢 |

> ⚠ 副产物：一旦实现非法指令异常，**`.fill 2,1,0x00` 的填充字（`0x0000`）从"NOP"变成"陷阱"**。
> 这对 `misalign-*` 测试的走向**会有影响**（填充区不再被静默趟过去）—— 必须与未对齐异常**一起实测**，
> 不能只看单项。

### 2.6 明确不做 / 待确认

- **不做**：U 模式、PMP、CLINT（`mtime/mtimecmp`）、`wfi` 的低功耗语义、AHB 的 `HRESP` 错误上报、
  中断嵌套（`irq_process` 现在会挡住 handler 里 `MIE=1` 的嵌套受理 —— **这是既有行为，本次不动**，
  但要在文档里写明这是"比规范更严"的偏差）。
- **待确认**（见 §7）：mepc 口径；cause 号（0 vs 4）；mtval 是否被查；`misa`/`mhartid` 是否被读/写；
  `wfi` 期望；`mpzicsr` 里 CSR 读写位置是否被断言；`mtvec` 是否要 masking。

---

## 3. 改造方案

### 3.1 相位表（本方案的骨架）

记 E1..E5 = `icache.inst_out` / `pre_decoder` Q / `mid_decoder` Q / `post_decoder` Q / `wb_reg` Q
五级寄存器（稳态下一条指令在各级时的地址 = `pc_addr − 4/8/12/16/20`，由
`inst_out(N) = instr(pc_addr(N) − 4)`（`当前进度.md` §2）逐级推得；停顿是全流水的、冲刷是清级的，
两者都保持这个相位差）。

| 判定级 | 指令此刻位置 | 判定者（信号来自） | 可在此判定的异常 | 本拍 `pc−N` | 冲刷相位 |
| --- | --- | --- | --- | --- | --- |
| **E1** | `icache.inst_out` → `pre_decoder` 组合 | `pre_decoder`（组合） | jal 目标非对齐（`inst[21]`，静态） | −4 | 与 jal 同拍（早） |
| E2 | `pre_decoder` Q | —（不判定） | — | −8 | — |
| **E3** | `mid_decoder` Q | `lsu`（组合，`lsu.v:122-136`） | ld/st 非对齐（**必须在本拍拦住总线**） | −12 | 本拍只"拦请求"，**交付推到 E4** |
| **E4** | `post_decoder` Q | `post_decoder`（寄存器：`trap`/`ebreak`/新增非法指令）+ `lsu` 的 1 拍寄存故障 | ecall/ebreak/非法指令/CSR 非法；ld/st 非对齐（寄存后对齐到本相位） | −16 | 本拍（与 `trap` 同相） |
| **E5** | `wb_reg` Q（`bju` 判定延迟拍输出即对应这条） | `bju`（寄存器，与 `br2/br3/jalr_fail` 同相） | br/jalr 目标非对齐 | −20 | 本拍（与 `br2/br3` 同相） |

**为什么 ld/st 非对齐要"E3 拦、E4 交付"**：E3 是 lsu 唯一能看到字节地址的拍，也是它把请求摆上总线的拍
（`lsu.v:158-162` 的 `new_put`/`bus_go`，进车2 当拍就发）⇒ 判定必须在 E3、**在 `new_in` 上加一项**把
请求拦住；但"整条流水的冲刷"不能用 E3 的组合信号去驱动（那是 `forw → 地址加法 → 比较 → 广播` 的长锥），
所以在 lsu 里**寄存一拍**，让它落在 E4 相位、和 `trap` 共用同一条交付通路。

### 3.2 `mepc` 该存哪条指令 —— 用"随指令携带的 PC 载荷"，不要再算 `pc − 常数`

**先回答任务里的问题**：`csr.v:129-130` 的 `pc_addr_in − 12`

- **对中断是对的**，而且是**规范口径**：`irq_bubble` 稳态 = 12，而 `pc−12` = E3（`mid_decoder`）那条
  = **本拍冲刷要杀死的、最老的那条**（E4/E5 那两条在中断冲刷拍照常提交：`wb_reg` 的冲刷门只挂
  `flush_jump`、不含 `flush_irq`，见 `wb_reg.v:51-55` 的注释与 `wb_reg.v:69`）⇒ `mepc` = 被中断的指令
  = 回返即它自己 ⇒ **规范口径与"回返地址"口径在中断上重合**，不用改。
- **对 `trap`/异常是错的**：`trap` 的指令在 **E4** ⇒ 同一条指令在 E4 相位是 `pc−16`；`−12` 存出来是
  `trap+4`（"下一条"）。对 ecall/ebreak 恰好只差"要不要由软件再加 4"，但口径不一致，
  规范测试一定按"存发生指令"来期望。

**方案**：异常一律用 `mepc = （该判定级携带的 PC 载荷） − 4`。载体现成、零新增寄存器：

| 判定级 | 载体现状 | 出处 |
| --- | --- | --- |
| E1 | `pc.aux_addr`（`aux_addr_0`） | `cpu_top.v:106`；`pre_decoder.v:99` 无条件携带 |
| E3 | `mid_decoder.aux_addr_out`（`aux_addr_2`） | `mid_decoder.v:194` **无条件** `aux_addr_out <= aux_addr_in` |
| E4 | `post_decoder.aux_addr_out` | **需改一行**：现在 default 清 0、只给 JAL/JALR/BRANCH（`post_decoder.v:129`）；把 default 改成 `aux_addr_in`（对该模块的另一个消费者 `bju` 无副作用：`jp_target_now = aux + beq_off_in`、`br_pc_idx <= aux[8:3]` 都被 `br_flag_in`/`jalr_flag_q` 自门控，`bju.v:71/100/157`，非分支指令时 `beq_off_q2=0`、`br_flag=0`） |
| E5 | `bju` 里新加一个 32 位寄存器（名字建议 `exc_pc_q`），D 端 = `aux_addr_in`（**直连、不做运算**） | `bju.v:32` 已有 `aux_addr_in` 端口 |

载荷是"pc 约定值"（= 指令地址 + 4，见 `逻辑说明` §7/§8），所以**减 4 得到指令自身地址**。
减 4 只在**仲裁输出的公共点**做一次（一条 30 位减法，喂给 `mepc` 寄存器的 D 端 —— 不在关键路上）。

> **为什么不能继续用 `pc − 常数` 兜**：异常源的判定级跨了 E1/E3/E4/E5 四级，常数要写 4 个；
> 而且 `pc − 常数` 依赖"这 4 拍 pc 一直 +4"，在 `stallf` 造成的**局部**冲刷窗口、以及未来半乱序重构
> 引入的乱序里会失效。载荷是"跟着指令走"的，天然免疫。

### 3.3 冲刷的编码与相位

**新增一位 `flush_exc`**，进 `flag_bus`（`controller.v:70` 的 9 位 → 10 位；建议**追加**在 bit9，
即 `{flush_exc, exec, flush_irq, flush_jump, dcache_hold, bus_hold_in, stall_m, stall_v, lsu_stall, icache_busy}`
—— 追加而不是插入，避免 8 个消费模块的**位号全改**；`controller.v:70` 的注释块要同步）。

各消费点的口径（**每一位都只做一件事，逐文件一行**）：

| 模块 | 现在 | 改成 | 理由 |
| --- | --- | --- | --- |
| `controller` | `flush_irq = irq\|irq_ret\|irq_act\|trap`（`68`） | **不动**（`flag_bus[7]` 的含义不扩，异常只走新位 `flag_bus[9]`） | 一位一义：`flush_irq` 仍只表示"中断/mret/trap 类"；需要区分的地方（`pc.v` 的落点、`wb_reg` 的门）各自取位 |
| `controller` | `flush_w = flush_irq\|flush_jump`（`58`，驱动 `irq_bubble`/`ird_tmr`） | `\| flush_exc`（新输入） | 异常后的空窗必须重置 `irq_bubble`，否则空窗里受理中断会读到陈旧的 bubble |
| `pre_decoder` / `mid_decoder` / `regfile` / `pc` / `icache` | `flush_w = flag_bus[7]\|flag_bus[6]` | `\| flag_bus[9]` | 清级 |
| `post_decoder` | `flush_w = [7]\|[6]`；`stall_w` **没排 flush_w**（`76`） | `\| [9]`；另外把 `stall_w` 的保持分支改成 `else if (stall_w & ~flush_w)`（**独立改动，单独验证**，见 §7） | 清级 + 消除"trap 被 stall 保持多拍" |
| `lsu` | `flush_w = flag_bus[7]\|flag_bus[6]\|stallf`（`72`） | `\| flag_bus[9]`；**并把新异常源并进 `stallf`**（§3.7） | 拦住入口 + 早一拍挡住错路 store |
| `wb_reg` | `flush_jump_w = flag_bus[6]`（`45`） | `flush_jump_w = flag_bus[6] \| flag_bus[9]` | 异常拍 E4/E5 上坐的是**必须不提交**的指令（错路 B+1、错路的 jalr link、故障指令自身） |
| `bju` | `flush_w = [7]\|[6]`（`52`） | `\| [9]` | 清判定延迟拍 |
| `pc` | 冲刷分支只有 `flush_jump_w`/`irq`/`irq_ret`（`84-97`） | `flush_jump_w = flag_bus[6] & ~flag_bus[9]`；重定向分支改成 `else if (irq \| exc_w)` 共用 `pc_addr <= isr_addr2` | 异常**优先级高于跳转冲刷**（分支目标非对齐时 br2/br3 也在拉起，落点必须被换成 mtvec）；共用 `isr_addr2` 支路 ⇒ **不新增 mux 级** |
| `icache` | NOP 门 `(jalr_fail\|br2\|br3\|irq\|irq_ret)`（`198`） | 追加 `\| exc` | 异常拍当拍把取指输出打 NOP（错路/目标被截断的那条不许进流水线） |
| `regfile` | 写口仲裁 `we_alu && rd_alu != 0`（`120`） | 追加 `&& ~exc_kill`（**只对 alu 口**） | **晚到撤销**：jalr 的 link 写此时已在 `wb_reg` 的 Q 上（见下） |

**为什么 jalr 非对齐需要"晚到撤销"**：`bju` 的判定延迟拍对应 E5，而 jalr 的 link 值在**上一拍**
（jalr 处于 E4 时）就已经被 `wb_reg` 采走（`wb_reg.v:83-88`）⇒ 判定出来时只能"撤销"。
只对 alu 口的 `we_alu` 加一项（不动 `we_mul`），撞车时按现有仲裁顺序让给 mul（`regfile.v:119-136`）——
这也符合它自己的契约"alu/mul 永不同拍（实测 0 次）"。
> 备选（不推荐）：把非对齐判定提前到 bju 的**组合判定拍**（像 `stallf`），就能在 `wb_reg` 的
> **D 端**把它门掉、不需要撤销信号；代价是把一条 `bju` 组合输出广播到 `wb_reg`，正是
> `controller.v:64-66` 记录的那类"绕一圈多花 0.45ns"。**权衡后选"寄存器相位 + 撤销"。**

### 3.4 逐类异常的落点与代价

| # | 异常 | 判定实现（新增信号） | payload / 新增寄存器 | 优先级 | 门控（不许受理的条件） |
| --- | --- | --- | --- | --- | --- |
| A | **br 目标非对齐** | `bju` 判定组合：`success_now & beq_off_in[1]`。**关键洞察**：分支地址恒 4 对齐 ⇒ 目标 bit1 **就是立即数 bit1** = `inst[8]`（`pre_decoder.v:62` 的 `immB` 第 1 位）⇒ **零加法**，也不需要 `jp_target` 的 32 位加法 | 目标（mtval）= `jp_target_now`（这条路径上 `br2`/`br3` 恰好成立，`jp_target` 就是真目标；"预判跳且真跳"那一路 `jp_target_now=0`，需另存一份 `exc_tval_q <= jp_target_now` 按"非对齐成立"选） | 最高（E5） | `~flush_w`（更老的 `trap`/非对齐不可能同拍共存于更老级——但 `E5` 上若有更老的源则它优先，由仲裁器保证） |
| B | **jalr 目标非对齐** | `bju`：`jalr_flag_in & (jalr_low2 != 2'd0)`，其中 `jalr_low2 = r1_data_in[1:0] + r2_data_in[1:0]`（**只算 2 位**，避免把 32 位进位链挂到 `stallf` 上） | 同上；`bju.v:101` 同时补"清 bit0" | 最高 | 同上 |
| C | **jal 目标非对齐** | `pre_decoder` 组合：`inst[21]`（`immJ` 的 bit1） | `aux_addr_0 − 4` | 最低（E1，被挤掉不丢） | `~(E5 源 \| E4 源)`（仲裁器输出） |
| D | **ecall/ebreak** | 已有 `trap`/`ebreak`（`post_decoder.v:212-213`），只修 cause（去掉 bit31） | `aux_addr_2`（经 §3.2 的 default 改动后在 E4 可用） | E4 | `~flush_jump` 且"单拍脉冲"（§3.6） |
| E | **非法指令** | `post_decoder` 新增"合法性分类"组合（`case` 补 `default` + 逐类细化） | 0 或 `inst_2`（**需确认测试期望**） | E4 | 同上 |
| F | **ld/st 非对齐** | `lsu` 组合：字节永不非对齐；半字看 `addr[0]`、字看 `addr[1:0]`；本拍加进 `new_in` 的门（**同时不上车、不拉总线**） | **`lsu` 内寄存一拍**（`exc_ld_misalign_q`），对齐到 E4 相位；`mtval` = 该字节地址 | E4 | 同 D；**且任何 `flush_w` 拍都要清掉这个寄存器**（指令被更老的源冲掉时，故障必须一起丢，等 `mret` 后重新执行时再判） |

### 3.5 `csr.v` 的改造

1. **单拍脉冲**：`trap` 现在是电平（且在 stall 拍会被保持，§1.2）⇒ 在 `csr.v` 里做一个
   `trap_taken`（或用仲裁器给的 `exc_take`），**所有状态写（`mepc/mcause/mtval/MPIE/MIE/irq_process`）
   只在脉冲拍做**。
2. **异常与中断的写入分开**：
   - 异常：`mepc <= exc_pc − 4`（§3.2）、`mcause <= exc_cause`（无 bit31）、`mtval <= exc_tval`、
     `MPIE <= MIE; MIE <= 0; irq_process <= 1`。
   - 中断：**保持现状**（`iret_addr1/2 <= pc − irq_bubble`，`mcause <= 0x8000_000X`）——
     §3.2 已论证它对中断是规范口径，**不动它就把占用的风险压到最低**。
   - 删除 `mcause` 的**电平覆写块**（`csr.v:116-126`），改成"**只在中断被受理那拍**写"。
3. **`mret`**：补 `MPIE <= 1'b1`（`csr.v:145-148`）。
4. **新增 CSR**：`0x340 mscratch`、`0x343 mtval`（+ 可选 `0x301 misa`、`0xF14 mhartid`），
   读 mux 与写 case 各加一条；`mepc` 读口改为"不区分来源的那一个寄存器"（异常/中断共用同一个 mepc
   寄存器是**规范要求**：两条路都写同一个 `0x341`）。
5. **CSR 写语义**（§1.5-11）：`func3∈{2,3,6,7}`（csrrs/rc/…）在 `rs1==x0` 时**不写**；
   对只读 CSR（`csr_addr[11:10]==2'b11`）与未实现地址：**建议**发非法指令（与 D/E 项一起做），
   或者在最小版本里"读 0 写丢弃"（现状）并在文档标注。
6. `csr.v:129-130` 的 `4'd12` 字面量按风格 §4.3 改成目标位宽（同处一并修）。

### 3.6 建议新增 `core/trap_unit.v`（异常仲裁单元）

理由：与 `bju.v` 同构（"判定源来自寄存器 → 本单元仲裁/编码 → 输出给状态寄存器与冲刷网"），
把 3 个优先级的仲裁、cause 编码、payload 选择收在一处，`csr.v` 只当"架构状态 + 受理"。

```text
端口（草案）
  input  [31:0] exc_pc_e1_in, exc_pc_e3_in, exc_pc_e4_in, exc_pc_e5_in     // 四级 PC 载荷（已是 pc 约定值）
  input  [31:0] exc_tval_bju_in, exc_tval_lsu_in
  input  exc_bju_in, exc_e4_in, exc_jal_in                                  // 三个优先级的请求（都来自寄存器）
  input  [3:0]  exc_cause_e4_in                                             // E4 级的 cause（trap/ebreak/illegal/ldst）
  input  [8:0]  flag_bus
  output reg    exc
  output reg [3:0]  exc_cause
  output reg [31:0] exc_pc, exc_tval
```

风格要点（对 `代码风格约束.md`）：`localparam` 在译码块前；**译码块（`flag_bus` 拆位）是第一个逻辑块**；
仲裁用一个 `if-else` 优先级链、保持分支逐条自赋值；一行一条赋值；**块内不写注释**。

```verilog
//异常仲裁：三个源按"程序序最老者胜"取一个，输出去给 csr 当 mepc/mcause/mtval
    reg exc_bju, exc_e4, exc_jal;
    reg [3:0] exc_cause_c;
    reg [31:0] exc_pc_c, exc_tval_c;
    always @(*) begin
        exc        = exc_bju | exc_e4 | exc_jal;
        exc_cause_c = 4'd0;
        exc_pc_c    = 32'd0;
        exc_tval_c  = 32'd0;
        if (exc_bju) begin
            exc_cause_c = {2'd0, CAUSE_MISALIGN};
            exc_pc_c    = exc_pc_e5_in;
            exc_tval_c  = exc_tval_bju_in;
        end
        else if (exc_e4) begin
            exc_cause_c = exc_cause_e4_in;
            exc_pc_c    = exc_pc_e4_in;
            exc_tval_c  = exc_tval_lsu_in;
        end
        else if (exc_jal) begin
            exc_cause_c = {2'd0, CAUSE_MISALIGN};
            exc_pc_c    = exc_pc_e1_in;
            exc_tval_c  = exc_pc_e1_in;
        end
    end
```

`exc_pc` 的 `−4`（§3.2）放在**输出寄存器的 D 端**（上面 `exc_pc_c` 之后再减），
因为它只喂 `csr` 的 `mepc` 寄存器，不在链上。

### 3.7 逐模块改动清单

| 文件 | 改动 | 是否动关键路径 |
| --- | --- | --- |
| `core/flag_bus`（`controller.v:70`） | 9 位 → 10 位，追加 `flush_exc`（bit9）；`controller` 内部 `flush_w`（`:58`）并进它；各模块（§3.3 表）各自把 bit9 OR 进自己的 `flush_w` | 广播网宽 +1 位；**新位是寄存器源**（trap_unit 输出），不新增组合深度 |
| `core/pc.v` | 冲刷分支：`flush_jump_w = flag_bus[6] & ~flag_bus[9]`；新增 `exc_w`，与 `irq` 共用 `isr_addr2` 支路 | pc 的 D 端多一级 AND/OR（该链不是当前最差路径，见 §4） |
| `core/icache.v` | NOP 门追加 `exc`（`198`）；`fence.i` 若做：新增"重跑自举"入口 + 清 `valid1/valid2` | 门多一项 |
| `core/wb_reg.v` | `flush_jump_w` 追加 `flag_bus[9]` | 无 |
| `core/pre_decoder.v` | 新增 `jal_exc` 输出（`inst[21]` + `OPCODE_JAL`） | 极短 |
| `core/post_decoder.v` | (a) `aux_addr_out` 的 default 由 `32'd0` 改 `aux_addr_in`（`129`）；(b) 新增非法指令分类；(c) `stall_w` 保持分支排 `flush_w`（独立改动） | (b) 是纯译码扩展 |
| `core/bju.v` | (a) 新增 `br_misalign_now`（`success_now & beq_off_in[1]`）与 `jalr_misalign_now`（2 位加法）；(b) `jalr_target_now` 补清 bit0；(c) `stallf` 追加这两项（`117`）；(d) 判定延迟拍寄存器组新增 `exc_bju_q`/`exc_pc_q`/`exc_tval_q`/`exc_jalr_q`；(e) `jalr_flag_q` 在有异常时不要写 BTB | (a)(c) 在 `bju` 的判定组合块（**已有** s1 级深度的位置）；(b)(d) 是新的寄存器 D 端 |
| `core/lsu.v` | (a) 新增非对齐组合判定；(b) 并进 `new_in` 的门（`149`）；(c) 寄存一拍 `exc_ld_misalign_q`；(d) `flush_w`（`72`）追加 `flag_bus[9]`；(e) 任何 `flush_w` 拍清 (c) | (b) 是入口门新增一项（本地）；**(c) 是新寄存器** |
| `core/csr.v` | §3.5 全部 | 读口不动（提前一拍的寄存结构保持） |
| `core/regfile.v` | 写口仲裁 `we_alu` 追加 `~exc_kill` | 写使能多一项 AND |
| `core/trap_unit.v` | 新增（§3.6） | 全是寄存器 D 端 |
| `core/cpu_top.v` | 纯连线：`aux_addr_0`/`aux_addr_2`/`aux_addr_3`/各级异常位接到 `trap_unit`，`trap_unit` 输出接 `csr` 与 `controller`；`flag_bus` 位宽 9→10 | **零运算**（风格 §1.3） |
| `soc/mcu_top.v`、`IO_block/*` | 不动 | — |

### 3.8 样例代码（供实施时按此风格落地）

`pre_decoder` 的 jal 非对齐（静态判定；不改现有译码块，另起一个小组合块放在它后面）：

```verilog
//JAL 目标的非对齐可以静态判定：jal 的落点是 pc + immJ，pc 恒 4 字节对齐，
//故目标的 bit1 就是 immJ 的 bit1，也就是指令字的 bit21
    always @(*) begin
        jal_exc = 1'b0;
        if (!rst_q) begin
            if (inst_effective[6:0] == OPCODE_JAL) begin
                jal_exc = inst_effective[21];
            end
        end
    end
```

`bju` 的判定项与早一拍冲刷（关键：`beq_off_in[1]` 是零加法；jalr 只做 2 位加法）：

```verilog
//jalr 目标的低 2 位：只算 2 位加法，避免把 32 位进位链挂到 stallf 的锥里
    always @(*) begin
        jalr_low2 = 2'd0;
        if (jalr_flag_in) begin
            jalr_low2 = r1_data_in[1:0] + r2_data_in[1:0];
        end
    end

    always @(*) begin
        br_misalign_now   = success_now & beq_off_in[1];
        jalr_misalign_now = jalr_flag_in & (jalr_low2 != 2'd0);
        br2_now           = success_now & ~br_pred_taken_in;
        br3_now           = br_fail_now & br_pred_taken_in;
        stallf            = br2_now | br3_now | jalr_fail_now | br_misalign_now | jalr_misalign_now;
        if (br2_now) begin
            jp_target_now = aux_addr_in + beq_off_in;
        end
        else if (br3_now) begin
            jp_target_now = aux_addr_in;
        end
        else if (jalr_fail_now) begin
            jp_target_now = jalr_target_now;
        end
        else begin
            jp_target_now = 32'd0;
        end
        exc_target_now = br_misalign_now ? (aux_addr_in + beq_off_in) : jalr_target_now;
    end
```

### 3.9 与在途访存（LSU 两节车厢）的精确性论证

- **老于故障的访存必须做完**：冲刷**不碰**车厢状态机（`lsu.v:210-228` 只看 `s3_done`/`s2_move`），
  只封入口（`new_in` 里的 `~flush_w`）⇒ 车2/车3 那两笔照常推进、`ld_we` 照常写回（`lsu.v:257`）✓。
- **故障指令自己不许上总线**：非对齐在 **E3** 判定、直接进 `new_in` 的门 ⇒ 不 car、不拉总线
  （`lsu.v:160` 的 `new_put` 也不可能抬）⇒ 不写内存 ✓。
- **更老的 store 不许被吞**：若冲刷拍它正在等"补摆"（`s2_sent=0`），`s2_put_go` 被 `~flush_w` 压住
  （`lsu.v:159`），但它**留在车2**、`s2_sent` 仍为 0（`lsu.v:225` 只在新进车时按 `new_put` 赋值）
  ⇒ 之后照常补摆 ✓（这正是"不许把没发过的车当发过"那条纪律，`逻辑说明` §5.3）。
- **"在途读只许一笔"不被破坏**：冲刷拍的 `blank_bus` 语义不变；`else` 支路把总线清零
  （`lsu.v:182-188`），而外设**是电平判据、不看 `bus_valid`**（如 `plic.v:89` 只看地址窗）
  ⇒ 清零是必须的、也是够的 ✓。
- **错路的 younger store**：由 `stallf` 保证（§1.6-6）——**新异常源必须并进 `stallf`**，
  否则"预判跳且真跳且目标非对齐"那一路（此时 `br2=br3=0`）会让截断目标路径上的 store 发出去。
  这是本次改造**最容易漏**的一条。

---

## 4. 风险与代价（对 10ns 的影响方向）

> 判据纪律（照 `route-delay-is-per-hop` 与 `branch-judge-migration-constraints`）：
> **不要拿单次 WNS 之差当因果**（0.2~0.5ns 是布局噪声带宽）；看**失败端点数 + 路径结构**；
> 一次只做一个改动、每个都实测；实现至少跑两遍确认可复现。

| 改动 | 方向 | 量级估计 | 依据 |
| --- | --- | --- | --- |
| `flag_bus` 9→10 位 + 新位并进各模块 `flush_w` | ≈ | 可忽略 | 广播网只多一位；**新位是寄存器源**，不新增组合深度（这条是硬约束） |
| `pc.v` 的 `flush_jump_w & ~flag_bus[9]` + `irq\|exc_w` | −（小幅） | 一级 AND/OR | pc 的 D 端不是当前最差路径起点（最差路径在 `dcache BRAM→lsu→mulu`，见 `branch-judge-migration-constraints.md`） |
| `icache` NOP 门 +`exc` | −（极小） | 一级 OR | 该门是 `inst_out` 的 D 端，本来就有 5 项 OR |
| `bju` 判定项 +`stallf` 追加两项 | **−（最需要盯）** | 一级 AND/OR | `stallf` 是"晚到的组合广播"，`controller.v:64-66` 记录过"绕一圈多花 0.45ns"；本方案只在**本地**加项、且输入深度与现有项相当（`beq_off_in[1]` 是静态位、jalr 只用 2 位加法） |
| `bju` 新增 4 个寄存器（`exc_*_q`） | ≈ | 几十位 FF | 输入都在已有深度上（`jp_target_now` 的加法输出、`aux_addr_in` 直连） |
| `lsu` 入口门 +非对齐项 | −（小） | 一级比较 + 门 | 加法器本来就在（`lsu.v:122-136`）；新增项**不进 `stall` 网**（只进 `new_in`/`bus_go`），避免动那根广播 |
| `lsu` 新增 1 个寄存器（`exc_ld_misalign_q`） | ≈ | 1 位 FF | — |
| `post_decoder.aux_addr_out` default 改 `aux_addr_in` | ≈ | 0 | 只是 D 端常量换成信号；对 `bju` 无副作用（自门控已核） |
| `post_decoder` 非法指令分类 | −（小） | 十几~几十 LUT | 纯译码；`OPCODE_OP` 里 M/非 M 的判别已经现成（`post_decoder.v:140`） |
| `regfile` 写口 +`~exc_kill` | −（极小） | 一级 AND | 只加在 alu 口 |
| 新 CSR（mscratch/mtval/misa/mhartid） | ≈ | 少量 LUT + 32FF×2 | 读 mux 多几条 → 注意 `csr.v:186-192` 的读口是**提前一拍寄存**的（时序敏感区），新增条目要挤进同一个 `case` |
| `trap_unit.v` 新增 | ≈ | 几十 LUT + 少量 FF | 全在寄存器 D 端 |

**不加流水级、不加停顿、不加拍**（异常本身的开销 = 既有冲刷的 ~3 拍重填，与 `br2/br3` 同级；
jal 异常在 E1 判、连重填都比分支浅）。**唯一"加拍"的地方**是 `fence.i` 真做时的重跑自举（约 4100 拍，
一次性、只在 `fence.i` 时付）。

**面积**：预计 +150~300 LUT / +100~200 FF（远小于 `bju` 那次迁移的量级）。

---

## 5. 验证方法

### 5.1 微测（第一现场，纪律：探针 > 20、时钟沿 < 300，照 `tools/run_reg.sh` 的"每项独立 exe"）

必须**白盒**打这几根探针（否则只看到"结果不对"）：

1. `u_cpu_top.pc_addr` / `jp_target` —— 异常拍 pc 是否落在 **mtvec**、且**不是**被截断的目标。
2. `u_cpu_top.u_csr.mepc/mcause/mtval`（或新寄存器名）—— 三个值是否 = 规范期望。
3. `u_cpu_top.u_bju.exc_*` / `stallf` —— 早一拍冲刷是否真挡住 lsu（`bus_valid_out` 全程无脉冲）。
4. `u_cpu_top.we_5/rd_5`、`u_cpu_top.ld_we`、`mul_we` —— **故障指令与错路指令一个写口都不许动**。
5. E3 上的非对齐 load/store：`u_cpu_top.bus_valid_out` 必须**一次都不抬**（这就是"故障指令不上总线"的直接证据）。

定向程序（放 `tools/` + `project_risc_v.sim/`，照 `tools/mt_irq2.S` 的形状）：

| 程序 | 覆盖 | 判据 |
| --- | --- | --- |
| `exc_br_misalign.S` | 三路：预判不跳·真跳·目标 `&3!=0`（br2 路）、预判跳·真跳·非对齐（无冲刷路）、非对齐但**真不跳**（**不许抛**） | `mcause=0`、`mepc` = 分支地址、`mtval` = 目标、handler 之后**回到分支下一条**、目标路径一条都不执行 |
| `exc_jalr_misalign.S` | jalr 目标 `&3!=0`；顺带验 **bit0 被清**（目标是奇数时落点正确但抛异常） | 同上 + **link 寄存器未被写**（这条咬 §3.3 的"晚到撤销"） |
| `exc_jal_misalign.S` | jal 目标非对齐 | 同上 + link 未写 |
| `exc_ecall.S` / `exc_ebreak.S` | **这条专门咬"丢 3 条指令"**：ecall 前后各放 3 条"写 dtcm 不同槽"的指令，handler 之后逐槽核对 | 三条全在；`mepc` 口径与你选定的约定一致 |
| `exc_ldst_misalign.S` | `lw/lh/sh/sw` 的非对齐；含"前面有在途 load（车3 占用）时故障" | 总线流里**没有**故障请求；前面在途那笔**照常写回**；`mtval` = 出错地址 |
| `exc_priority.S` | 同拍多源：E5 非对齐 + E4 ecall；E4 ecall + 中断同时挂起 | 只受理一个、cause 按 §2.3 |
| `fence.S` | `fence`/`fence.i` 不抛非法；`fence.i` 后**能取到新码**（自改码，若做了 icache 失效） | 从"当 NOP"升级到"真生效"要走这条 |

**顺带必做的一条**（§1.6-4）：`irq_bubble` 相位微测 —— 在 **br2 冲刷后第 1/2/3 拍**拉中断，
看 `mepc` 是否 = 目标地址（而不是目标−4）。**这条现在没人测**，改异常时很容易把它带坏。

### 5.2 arch 事件流 + 参考模型（必须**连模型一起补**）

- `tools/rv32i_ref.py` 现在**明确没有**建模异常/中断（`archtest/README.md` 的"仍然不可比的两类"），
  且 `csr.v` 的 CSR 位是**照 RTL 抄的**（`rv32i_ref.py:35-70`）。补法：
  1. **异常/中断按规范建模，不照 RTL 抄**（这一块恰恰是本次要改的地方，照抄就变成自证）；
     位位置按**规范**（`MSIE=3` / `MEIE=11`），这样它还能反过来当"位置改对了没有"的裁判。
  2. 加 trap 语义：`mtvec` 跳转、`mepc/mcause/mtval` 写入、`MIE/MPIE` 交换、`mret` 回返、
     以及异常条件（非对齐、非法指令、ecall/ebreak）。
  3. `tb_arch.v` 增加 **`T <cause> <mepc> <mtval>` 事件**（现在只有 `W A/W L/W M/S`），
     让 `cmp_events.py` 能定位"第一处分叉"（这是本期定位"请求丢失"的那个仪器）。
  4. `cmp_events.py` 的 `classify()` 会按 `ins.hex` 反查写口类型（`:98-111`），新增 `T` 事件要一起处理。
- 回归口径：**先差分、后真值**（`run.py diff` 只证等价；`golden` 才证"对"），
  且 reference 的 `ecall/ebreak` 分支现在只是"最小处理"（`rv32i_ref.py:195-209`），必须补完。
- `archtest/README.md` 的"假挂死"三条（到限必须报 `ARCH_TIMEOUT`、外设逐笔应答、
  `--delay 1/3/12` 都要过）在异常测试里**同样适用**：trap handler 自己就是新的"可能挂死点"。

### 5.3 回归与长跑

| 仪器 | 命令 | 判据 |
| --- | --- | --- |
| 8 项回归 | `bash tools/run_reg.sh` | 逐项与基线**逐字一致**（含 `mt_ldfifo` 已摘除的说明，`run_reg.sh:42-45`；`gpio_irq`/`i2c_irq` 打印的 `mcause=` 栏位也要看，它现在是 `8000000b`） |
| CoreMark | `bash tools/build_coremark_v1.sh 1 -O2` → `tools/run_xsim.sh` | **777 字符 + `0xe714/0x1fd7/0x8e3a`**；拍数与基线一致（`当前进度.md` §3.4） |
| 架构集 | 官方 `I/M/privilege`（**需先把 `riscv-arch-test-v2` 拷到本机**，`当前进度.md` §8） | 目标：**11 条非对齐 signature 真错 → 0；`misalign-bgeu/bltu-01` 不再挂死**；并**新列出**因"填充字变成陷阱"而变化的其他条目 |
| 实现 | `run_imp.tcl`（7z020，10ns） | **0 失败端点**；WNS 与 +0.008 同量级（按 §4 的判据纪律，跑两遍看可复现性） |

> CoreMark 与 `run_reg.sh` 之前**必须先区分 hex**：`run_reg.sh`/`run.py build` 会覆盖
> `tools/hex/ins.hex`（`archtest/README.md` 第 3 条纪律、`当前进度.md` §3.3）。

### 5.4 "别被骗"清单（照 `当前进度.md` §3.2 与 `rtl-change-rules` 第 6 条）

- 判 RTL bug 前先自查 tb：**到限必须报 TIMEOUT**；trap handler 里写 `0x600d` 的位置要对；
  程序版本要对；`$readmemh` 路径要是本机态（`hexpath.sh local`）。
- 别把"慢"当"卡死"：说卡死必须给 pc trace。
- **异常测试的新陷阱**：handler 自己如果没写对（比如 `mtvec` 写的是 `isr` 但 `isr` 落在 itcm 之外），
  会表现成"异常一发生就永久 STALL"——先检查 handler 的地址与 `mtvec` 对齐。

---

## 6. 分阶段实施顺序（每阶段可独立验证）

| 阶段 | 内容 | 交付判据 | 回滚粒度 |
| --- | --- | --- | --- |
| **0. 定案与基线** | (1) 把 `riscv-arch-test-v2`（或至少 `misalign-*` + trap handler 头文件）拷到本机，**读 handler**：它对 mepc 加不加 4？用不用 `mscratch`？查不查 `mtval`？期望 cause 是 0 还是 4？(2) 固化现状基线：8 项回归 + CoreMark + 失败清单 | 一张"期望值表"（cause/mepc/mtval/位位置）签下来 | 无 RTL 改动 |
| **1. 状态与语义**（不动异常源） | `mcause` 去电平覆写；`trap` 改单拍脉冲；`mepc/mcause` 口径按定案修；`mret` 补 `MPIE←1`；新增 `mscratch`/`mtval`（+`misa`/`mhartid`） | `exc_ecall.S`/`exc_ebreak.S` 微测过；**8 项回归 + CoreMark 逐字不变**（中断路径宣称"零改动"，这条就是它的实证） | 单文件、可整段撤 |
| **2. 靶心：指令地址非对齐** | `flag_bus[9]`、`trap_unit.v`、`pc/icache/wb_reg/regfile` 的门与优先级、`pre_decoder` 的 jal 判定、`bju` 的 br/jalr 判定与 `stallf` 追加、`pc.v` 的 trap 重定向 | `exc_br/jalr/jal_misalign` 微测过；架构集 **13 条已知失败清零**；CoreMark 不变 | 按"源"分三小步（br→jalr→jal），每步单独验 |
| **3. 非法指令 + fence** | `post_decoder` 合法性分类（含 `fence`/`fence.i` 白名单）；CSR 合法性（只读位、`rs1==0` 不写）；可选 `fence.i` 重跑自举 | 新微测 + 架构集（**重点看"填充字变陷阱"是否改变了其他条目**）；CoreMark 不变（它没有非法指令） | 分类表可回退成现状 |
| **4. ld/st 非对齐** | `lsu` 判定 + 寄存 + 门控；`mtval` payload | `exc_ldst_misalign.S`（含"前面有在途 load"）；总线事件流里无故障请求 | 独立于阶段 2/3 |
| **5. 收尾** | 同步 `逻辑说明`（§6 重写、§7 公式表补异常行、§8 信号表补 `exc_*`）；`当前进度.md` §6 勾掉；`rv32i_ref.py` + `archtest/README.md` 的"不可比"两行销账 | 文档与 RTL 一致；README 的三件仪器都能覆盖异常 | — |

> 与 `当前进度.md` §6 的"下一步计划"对齐：那里写"`fence` 与机器模式并入紧接着的**半乱序重构**一起做"。
> 本方案的建议相反 —— **阶段 2 先单独做**：它是赛题已知失败的**唯一根因**（13 条），
> 且**不需要新流水级/新停顿**（§4），把它绑进重构会让"失败归因"变模糊。
> 半乱序重构真正需要的是**提交点**（retire）语义；本方案用"随指令携带的 PC 载荷 + 固定判定相位"
> 已经能得到精确异常，重构时只要把载荷换成 ROB 项即可平移。

---

## 7. 待确认 / 不确定清单（**不要替我拍板**）

1. **mepc 口径**（影响 arch-test handler）：存"发生异常的指令"（规范）还是"回返后要执行的地址"
   （本核现状）？**必须先读 `riscv-arch-test` 的 trap handler 源码**。本机没有该仓库，**不确定**。
2. **指令地址非对齐的 cause = 0 还是 4**？任务书写 4，规范写 0（4 是 load 非对齐）。
   本方案按规范写 0，**需以测试期望表为准**。
3. **`mtval` 是否被 arch-test 断言**？若断言，非对齐必须写目标/出错地址；若不断言，可只读 0。
4. **`mie`/`mip` 的位位置改不改**：改（规范）会打断 `tools/mt_csrr2.S:8`、`tools/gpio_test.S:46`、
   `tools/i2c_test.S:40` 与 `riscv-arch-test` 里依赖位的条目 —— 需一并改程序与模型。
5. **`mtvec` 要不要 masking/`MODE` 字段**：会打断 `tools/archtest/prog/m_zicsr.S:39-47` 与
   `rv32i_ref.py:65` 的"整字透传"口径。
6. **`misa`/`mhartid` 是否被读/写**；`wfi`(0x105) 期望是 NOP 还是非法指令。
7. **§1.6-4 的 `irq_bubble` 相位疑点**：需微测实证（br2 冲刷后 1~3 拍受理中断）。
8. **§1.6-1/2 的"错路 CSR 写/错路 trap"**：要不要在本次一并修（我建议**要**，否则新异常会被错路
   指令伪造）；但它是**独立改动**，要单独跑回归。
9. **`post_decoder.stall_w` 不排 `flush_w`**（`post_decoder.v:76`）：与其余四处的"冲刷优先于停顿"
   不一致，建议作为一个**独立 bug 修复**（有自己的微测），而不是塞进本方案。
10. **`irq_process` 挡住 handler 内嵌套中断**（比规范严）：本次**不动**，请在文档里确认这是有意为之。
