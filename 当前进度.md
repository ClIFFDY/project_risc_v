# 当前进度（长期维护）

> 更新：2026-09-24 03:40 ｜ 分支 `hfreq_opt` ｜ 远端 `origin/ClIFFDY/project_risc_v`
> 本文件放在 `RTL/`（=git 工作区）是为了**跨设备**：只有这里会跟着 commit/push 走。
> 改动 RTL 之前先读 `RTL/逻辑说明`（§5.3 车厢、§7 重定向公式表、§8 信号速查）；风格看 `RTL/代码风格约束.md`。

---

## 0. 一句话状态

RV32IM(+Zicsr) 六流水线 SoC，7z020、10ns(100MHz) 约束。**功能**：CoreMark CRC 全对、arch-test 与参考模型一致；
**频率**：上一版 7020 实现 WNS **+0.011ns**、0 失败端点、TNS 0。当前工作区有一批**未提交**的 LSU/流水线改动（见 §5）。

## 1. 目录与工具（跨设备必看）

| 东西 | 路径 | 备注 |
| --- | --- | --- |
| 工程根 | `G:/Verilog_Projects/Project_RISC_V` | 非 git 仓库 |
| RTL（git 仓库） | `RTL/`（`core/*.v` 14 个 + `逻辑说明` + `代码风格约束.md`）| 相对 `origin` 就是仓库根 |
| 测试台 | `Tools/test/archtest/` | arch 差分三件套 + 自写程序 |
| 交叉工具链 | `Tools/xpack-riscv-none-elf-gcc-15.2.0-1/bin/` | `-march=rv32im_zicsr -mabi=ilp32` |
| Python | `C:/Users/Justin/AppData/Local/Programs/Python/Python312/python.exe` | **`python` 会打到 Store 存根，别用** |
| iverilog | `D:/iverilog/bin/`（bash 里 `vvp`/`iverilog`） | 回归/微测，快（~24k 拍/s） |
| Vivado | `D:/Xilinx/Vivado/2019.1/bin/vivado.bat` | **bash 里 `vivado` 不在 PATH，要全路径** |
| 工程（实现用） | `G:/Verilog_Projects/project_risc`（`run_imp.tcl`） | 直接引用 RTL，不用同步源码 |
| riscv-arch-test 源码 | `G:/Verilog_Projects/riscv-arch-test-v2` | **在仓库外**，换机器要一起拷，或改 `run.py` 里的 `SUITE` |

**换机器第一件事**（否则仿真/综合全错）：

```bash
bash Tools/hexpath.sh local      # 把 itcm.v/dtcm.v 的 $readmemh 改成本机路径
bash Tools/hexpath.sh commit     # 只在提交前用；★ commit 态跑综合结果作废（BRAM 43 → 13）
```
（`$readmemh` 是**绝对路径**，两台机器路径不同，所以才有这个开关。）

## 2. 结构速查

**流水线**：s0 `pc`/`aux_addr` → s1 `icache.inst_out` → s2 `pre_decoder` → s3 `mid_decoder` →
s4 `post_decoder`+`alu`+`bju` → s5 `wb_reg`；恒等式 `inst_out(N) = instr(pc_addr(N) − 4)`。

**`flag_bus[8:0]`**（`controller.v:70`）：
`{exec, flush_irq, flush_jump, dcache_hold, bus_hold_in, stall_m, stall_v, lsu_stall, icache_busy}`。

**LSU = 两节车厢**（详见 `逻辑说明` §5.3）：
- 车2（发送）：ld/st 常态直接进这节，进来那拍把 addr/wdat/be 摆上总线（`s2_sent` 记"摆过没有"）；
- 车3（写回）：与 dcache 里锁住的那条**是同一条**，只等 `ready_in`；store 进车3 即算提交；
- 车3 占用 ⇒ 组合拉高**整个** stall ⇒ hazard 只扫车2；
- **在途读只许一笔**：`blank_bus = miss | (在途读 & ~ready_in)`，blank 的拍**总线上必须清零**
  （从设备是电平判据，残留地址会被当请求连答）。

## 3. 测试方法（★ 本期重点）

### 3.1 arch 差分三件套 —— 三种问题用三种仪器

```bash
PY="C:/Users/Justin/AppData/Local/Programs/Python/Python312/python.exe"; cd Tools/test/archtest

$PY run.py build I M privilege --out out_cur     # 官方集：编译→仿真→抓 signature 区
$PY run.py diff out_A out_B                      # ① 差分：两棵树 signature 是否等价（证"等价"，不证"对"）
$PY golden.py out_cur                            # ② 真值：与自研 iss.py(RV32IM+Zicsr 参考模型) 逐字比
$PY cmp_events.py <golden.txt> <rtl.txt>         # ③ 黄金事件流：按【端口】比架构事件，找第一处分叉
                                                 #    事件 = `W A/W M/W L rd= v=`（三写口，各端口内部是程序序）
                                                 #    + `J t=`（重定向落点）；错拍不算（写口落地顺序与时序相关）

$PY run.py program prog/ls_mix2.s --out out_x --delay 6   # 自写程序（定向用例）
```

- **①只能证等价**：两棵树逐位相同 ≠ 对。所以要 ②（`iss.py` 真值）兜底。
- **③是"现场仪器"**：车厢挂死/请求丢失这类问题，差分与真值只会说"不一样"，
  而总线事件流能直接指出"**少了一条读**" → 本期就是这么定位"请求丢失"的。
- `prog/` 现有：`hazard` `muldead` `ls_bb` `ls_stld` `ls_miss` `ls_waw` `ls_ext` `ls_mix2` `ls_loop`。
  `muldead.s` 本身写坏了（外设读返回 0 ⇒ `0(a2)` 谁都不应答 ⇒ 必挂），别当回归用。
- 自写程序模板：`la sp,__stack_top; ...; la t0,tohost; li t1,1; sw t1,0(t0); 1: j 1b`
  （tb 轮询 **dtcm 字0** 当"跑完"）。

**本期数字**：`I/M/privilege` 50 条与改动前 **0 差异**；`prog/` 8 条与 `iss.py` **逐字一致**；
已知 11 条非对齐 signature 真错、2 条 `misalign-bgeu/bltu-01` 挂死（都是**缺异常**，见 §6）。

### 3.2 测试台的三个"别被骗"

1. **到限没写 tohost 必须报 `ARCH_TIMEOUT`，不能打 `ARCH_DONE`**。旧 tb 到限也打 `ARCH_DONE`，
   跑分器只认这个 ⇒ 报 OK，而打出来的 signature 是**从没写过的 0** ⇒ 被误读成"某条 store 被丢"。
2. **外设模型必须逐笔应答**（移位寄存器，`ext_pend[DELAY]`）。旧的"计数器重装"会把背靠背两笔读
   **合成一个应答** ⇒ 第二条读永远等不到 ⇒ 假挂死（HEAD 也一样）。
3. **换 `--delay N` 扫一遍**（N=1/3/12 都要过）。混延迟正是"应答串台"的触发条件，同延迟测不出来。

### 3.3 仿真其它纪律

- tb 加载 hex 必须在 `#1` **之后**（否则被 `itcm.v` 自己的 `$readmemh` 覆盖）。
- 跑微测/arch 会覆盖 `Data/ins.hex` ⇒ 跑 CoreMark 前必须重建（见 3.4）。
- CoreMark 建议 `cd RTL/core && vvp <tb>.vvp`（tb 里 `../../Data/*.hex` 的相对路径按运行目录算）。

### 3.4 CoreMark

```bash
cd G:/Verilog_Projects/Project_RISC_V
ITERATIONS=1 bash Tools/coremark/Makefile.coremark     # ★ ITERATIONS 必须是【环境变量前缀】
                                                       #   写成 `bash ... ITERATIONS=1` 是位置参数，
                                                       #   会编出自动标定版 ⇒ 只打 banner 再也跑不完
cd RTL/core && vvp <coremark_tb>.vvp                   # tb 收 UART 字符，跑完打印全部字符
```

**判据**：**777 字符** + `[0]crclist : 0xe714` / `crcmatrix : 0x1fd7` / `crcstate : 0x8e3a`。
**周期基线**：最后字符 **418609 拍**（空总线改动之前 417242）。

### 3.5 实现（7z020）

```bash
cd G:/Verilog_Projects/project_risc
/d/Xilinx/Vivado/2019.1/bin/vivado.bat -mode batch -source run_imp.tcl -nojournal -log imp_7020_v3.log
```
- 判据：日志末尾 `=== RUN OK ===`（只表示流程跑完）+ `imp_timing.rpt` 的
  `Setup : 0 Failing Endpoints, Worst Slack …`。
- 器件 `xc7z020clg400-1`；`run_imp.tcl` 里固化 `place_design -directive ExtraTimingOpt` + `phys_opt AggressiveExplore`。
- **面积看实现后的 slice/CLB，不看综合后的 LUT 数**。
- 上一版结果：WNS **+0.011ns** / TNS 0 / 0 失败端点；LUT 10263(19.29%)、FF 7416(6.97%)、BRAM 43.5(31.07%)、DSP 4。
- **最新（2026-09-24，含"空总线"改动）**：WNS **+0.008ns** / Hold +0.013ns / **0 失败端点**；
  LUT **10364(19.48%)**、FF **7394(6.95%)**、BRAM 43.5(31.07%)、DSP 4 —— 与上一版基本持平。

## 4. 里程碑（带数字）

| 时间 | 提交/状态 | 内容 | 数字 |
| --- | --- | --- | --- |
| 09-20/21 | 已提交 | 频率优化四项（存档见 `Archive/_bak_20260921_stage/CHANGES.md`）| — |
| 09-23 | `37abe17` | 译码字段不再各寄存一份，改由指令字就地切片/别名 | s2+s3 寄存器 648→261 位；WNS −0.566→−0.328 |
| 09-23 | `71b7371` | 三级译码改名 `mid_decoder`/`post_decoder`；立即数选择锥移到 D 端；mulu 修背靠背 div/rem 死锁 | — |
| 09-23 | `eb86e51` | mulu 除法标志挪到发起后一拍 + 撤两处 `max_fanout` | 10ns 首次收敛：WNS −0.053→**+0.008**、失败端点 20→0 |
| 09-24 | `5bed8a0` | LSU 两节车厢 + "空总线"；mulu `stall_m` 释放；入口 JAL −4；`post_decoder` 的 `stall_w`；`retire` 排除在途访存 | CoreMark **777 字符/CRC 全对/418609 拍**；arch **0 差异**；prog 8 条与 ISS 一致 |
| 09-25 | `cd3f722` | 机器模式异常处理（trap 重定向 + 非法指令 / 三路指令地址非对齐 / 访存非对齐 / CSR 合法性，新增 `trap_unit.v`） | archtest 17/17、run_reg 8/8、CoreMark CRC 全对 + 0 异常；a100t@10ns **WNS +0.428** |
| 09-25 | `e1240d4` | lsu 共用一条地址加法器（删重复那一支与结果上的 `is_st` mux） | a100t@10ns WNS **+0.682**、LUT **−164**；事件流逐字节相同 |
| 09-25 | **基准版** | `main` = 上面两条 + AHB hresp 删除；`hfreq_opt` 继续开发 | a100t@10ns **+0.682 ✅**；@8ns **−0.929 ❌**（见 §5.1） |

## 5. 未提交 / 进行中

**已提交（2026-09-25）：`cd3f722` 机器模式异常处理 + `e1240d4` lsu 地址加法器合并。**
**设计与坑见 `逻辑说明` §29。** RTL 改 13 个文件 + 新增 1 个：

- **新增 `core/trap_unit.v`**（异常仲裁）★ 它是新建文件，而 **`.xpr` 不在 git 里**
  ⇒ **换机器必须手工把它加进工程**（Add Sources）。
- 改：`controller.v`(`flag_bus` 10 位、`exc` 独立成位) `pc.v`(trap/exc 重定向) `icache.v`(NOP 门)
  `post_decoder.v`(非法指令白名单 / jal 目标载荷 / CSR 合法性 / sret) `bju.v`(三路指令地址非对齐；
  `jp_target` 兼任 mtval) `lsu.v`(按访问宽度判非对齐 + 入口宽度门) `csr.v`(misa/mhartid、mtval、
  mtvec masking、mie/mip 位序、旁路资格) `regfile.v`(jalr link 晚到撤销) `wb_reg.v` `mid_decoder.v`
  `mulu.v` `pre_decoder.v` `cpu_top.v`，以及 `逻辑说明`(§0.3 注 + §29)。

测试侧（`tools/` 不在 git 里，仅本机）：`tools/archtest/prog/` 新增 `csr_illegal.S`、`exc_illegal_ldst.S`；
`tools/gpio_test.S`/`i2c_test.S` 的 `mie` 改到规范位 bit11；`tools/rv32i_ref.py` 同步口径；
新增 `tools/a100t_impl.tcl` + `tools/a100t_timing.xdc`（a100t 批处理实现）。

**验证**：archtest **17/17** `ARCH_DONE`，判据槽逐项核对（mepc/mcause/mtval/be/rd 回写/S 事件条数）；
每改一子步都做事件流差分 —— **只有预期的那 1 个程序变，其余逐字节全等**；
`run_reg.sh` **8/8**（含两个闭环 IRQ —— `mie` 位序的端到端守卫）；
CoreMark CRC `e714`/`1fd7`/`8e3a`、479 字节、dcache 缺失 126、**全程 0 次异常**；golden 3/3 identical。

**a100t 实现**（`tools/a100t_impl2.tcl` 参数化 + `a100t_impl_p.tcl` 带激进 directive，
不建工程、不动 `.xpr`；part `xc7a100tfgg676-2`）：

| 约束 | 结果 | 最差 setup 路 |
| --- | --- | --- |
| **10ns（100MHz）** | **WNS +0.682 / WHS +0.059，失败端点 0** ✅ | `dcache/data_reg_1` → `lsu/bus_addr_out_reg[25]/D`，9.260ns（logic 3.724 / route 5.536，10 级） |
| 8ns（125MHz）plain | **WNS −0.929 / TNS −135.5 / 241 端点** ❌ | `dcache/data_reg_3` → `bus_addr_out_reg[17]/R`，8.442ns（12 级） |
| 8ns + ExtraTimingOpt/phys_opt AggressiveExplore | **WNS −0.980 / 449 端点** ❌（更差） | `dcache/data_reg_1` → `s2_off_reg[0]/R`，8.435ns |

10ns 那一版 LUT 10,164（合并后 −164）/ FF 7,424 / BRAM 43.5 / DSP 4。明细见 `逻辑说明` §29.6。
**⇒ a100t 上 100MHz 收敛并留 0.68ns 余量；125MHz（8ns）不收敛。**

### 5.1 8ns 的墙是【一条链】，不是一捆（2026-09-25 实测）

- **m8 的 top-20 源全部是同一个 `u_dcache/data_reg_*`**（读数据 BRAM 的输出寄存器，
  `CLKBWRCLK→DOBDO` = **2.125ns**，占 8ns 预算 26%）；终点散在 `bus_addr_out` /
  `bus_data_out` / `bus_be_out` / `s2_addr` 这几组寄存器的**控制脚**上。
- RTL 链 = **load 的返回值 → forw 组合前递 → 同一拍进 lsu 入口当 rs1 → 地址加法 →
  发送态寄存器组**。触发条件：前一条 load 已过级2（在级3/dcache 侧返回），紧跟的
  load/store 当拍进 lsu 入口 —— `ls_use_hit` 只看级2 ⇒ 不停拍，全靠 forw 穿透。
- **加法器在整条链上只占 0.187ns** ⇒ 动/合并加法器没用（与上次实测 +0.254ns 一致）。
  route 占 57~60%，**收紧周期后工具拿"多两级 LUT"换"少 0.7ns 走线"** ⇒ 走线/扇出受限。
- ★ **用 `post_route.dcp` 核实了终点**（`tools/_probe3.tcl`）：
  `bus_addr_out_reg[0]` 的 `D` ← 本 bit 自己的 LUT（fo=1）、**`R` ← 一根 fo=82 的共享网
  （82 个负载全是 `/R`）**、`CE` = VCC；那根共享网由 `LUT6 INIT=64'hFFFFFFFAFFFFFFFB`
  驱动，**6 个输入里有一个就是那条数据网**（`p_15_in` = m8 路径倒数第二跳）。
  而 RTL 里这条清零函数的锥（`bus_go`/`new_in_pre`/`flush_w`/冒险比较）**不可能含数据**
  ⇒ **工具把"载荷选择/数据"压进了"清零"同一个 LUT6**，控制脚时序被数据路径污染。
- 同一份 RTL：10ns 落 `/D`、8ns 落 `/R`、激进 directive 换到 `s2_off_reg[0]/R`
  ⇒ **这一段的 slack 有很大一块由映射选择决定，不是纯结构**；工具侧手段已用尽。
- **⚠️ 网名不可信**：m8 报告里的 `u_mid_decoder/ld_data_final`、`u_mid_decoder/s2_kind_i_3`
  在 RTL 里**根本不存在**（`forw` 只在 `cpu_top.v` 例化一次，`mid_decoder.v` 里没有
  `ld_data_final` 这个信号）⇒ 别照着网名改 RTL。
- ⇒ **拿 8ns 只有一条结构路：打断"load 返回值当拍当地址用"的同拍前递**（返回值先落一拍
  再进 forw）。代价 = 相邻 load→load/store 依赖 **+1 拍**，IPC 需实测。
  （"把加法器挪上一级"不成立 —— 来的晚的恰恰是操作数本身。）

**§5 状态**：`main` 是本版的**基准分支** = `hfreq_opt` + AHB hresp 删除（`f6908b3`）。
2026-09-25 已合到本地 `main`（merge `508d961`，唯一冲突 `soc/ahb_apb_bridge.v` 按"两边都要"解：
取 hfreq_opt 的读回打一拍结构 + 按 main 的意图删 `hresp`）；**合并后 run_reg 与 `hfreq_opt`
逐拍相同**（`gpio_irq @97`、`i2c_irq @7070`、模块级项数一致）⇒ 合并行为等价。
push 必须挂代理 `127.0.0.1:7897`（VPN 只管浏览器）。

## 6. 已知问题 / 待办

> **2026-09-25 更新**：`fence` 与**机器模式异常处理已经做完**（见 §5 与 `逻辑说明` §29），
> 不再并入"半乱序重构"。下面两条待办划掉，但**留了一条要你核的**（见第 1 条）。

- [x] **缺异常/中断**（2026-09-25 做完）：非对齐分支/jalr/jal 目标现在抛**指令地址非对齐**，
      非法指令 / ecall / ebreak / 访存非对齐 / CSR 合法性也一并做了；`mtvec` 可写、`mret` 可用。
      原先"非对齐目标按字截断 ⇒ 落进 `.fill` 垃圾 ⇒ pc 每轮回退 2 字节慢慢爬"的机理已消除。
- [ ] ★★ **【需要你核对】指令地址非对齐到底是 cause 0 还是 4？**
      上面那条旧笔记写的是"**抛 4 号**"，但**规范的 cause 0 才是指令地址非对齐**，
      4 是 load 地址非对齐。本次**按规范实现成 0**（`mcause = 0`）。
      若赛题/评测表的期望是 4，则 `trap_unit.v` 里 `CAUSE_MISALIGN_INST` 改成 `4'd4` 即可（一行），
      但要同步改 `half/exc_*_misalign.S` 系列的判据槽。**这条只能你拿赛题资料确认。**
- [x] `fence` 与机器模式（2026-09-25 做完；`fence` 无需改 RTL —— itcm 无写口 ⇒ 代码物理不可变）。
- [ ] 频率：10ns 的墙是"**一捆等长链**"，微改动推不动（流程确定性、重跑逐位相同），
      只有**结构性挪拍**有效（bju 挪拍那次 87.2→93.6MHz 且面积更小）；WNS 路 70% 是布线 ⇒ 降拥塞才是杠杆。
- [x] **a100t 专项（2026-09-25）**：10ns **+0.682 ✅**（余量 0.68ns）；**8ns 不收敛 −0.929**。
      8ns 的墙是**一条链**：load 返回值当拍当地址用（详见 §5.1，含 dcp 证据）。
      下一步二选一：① 打断同拍前递（+1 拍，IPC 需实测）② 先做一次零风险的映射实验
      （把 `bus_*_out` 的 "else 清零" 改成显式写零载荷，看终点是否从 `/R` 变回 `/D`）。
      **动 RTL 前先给方案、拿点头**（纪律 1）。
- [ ] `prog/muldead.s` 本身写坏（见 3.1），要么修要么删。

## 7. 纪律（换机器接着干也照这个来）

1. **动 RTL 之前先给具体方案、拿明确点头**；"回退 X" 只覆盖 X，别顺带改别的。
2. **先读 `逻辑说明`** 再判断"是不是 bug"（§5.3 车厢、§7 重定向公式表、§8 信号速查）。
3. **结论必须来自实测**（探针 / 总线事件流 / 逐拍 diff）；别拿"数据通路看起来一致"推。
   微测目标：探针 > 20 个、沿 < 300；同一个"猜测"不要连改三版。
4. **判 RTL bug 之前先自查测试台**：贴沿零延时竞争、hex 没重编、设备模型、超时谎报、程序版本错。
5. **长跑挂后台**（CoreMark / 实现），后台任务**别 sleep 轮询**（harness 会通知；要看进度就 Read 输出文件）。
6. **别把"慢"当"卡死"**：旁边有并行仿真抢 CPU 时会慢很多；要说"卡死"必须给出 pc trace 或"零输出 + 时间在走"的证据。
7. **改一版 → 跑现场仪器 → 按现场改**（本期四个坑全是探针/事件流指出来的）。
8. RTL 源文件不加注释；`RTL/` 不放 tb/存档（tb 放 `Tools/test/`，存档放工程根 `Archive/`）。

## 8. 换台机器开工清单

1. 装/拷：工具链、iverilog、Vivado、Python 3.12、`riscv-arch-test-v2`（在仓库外！）。
2. `git clone`（或 pull）本仓库 → `bash Tools/hexpath.sh local`。
2b. ★★ **手工把 `core/trap_unit.v` 加进 Vivado 工程**（Add Sources → Add Existing Files）：
   2026-09-25 的异常处理新增了这个文件，而 **`.xpr` 不在 git 里** ⇒ 不手工加，综合会报
   "module trap_unit not found"。加一次即可，之后随工程保存。
3. 终端连 GitHub 要手动挂代理 `127.0.0.1:7897`（VPN 只管浏览器）。
4. 冒烟三连：
   - `$PY run.py build I M privilege --out out_smoke` + `$PY golden.py out_smoke`（对参考模型）
   - `ITERATIONS=1 bash Tools/coremark/Makefile.coremark` → CoreMark 出 777 字符 + `0xe714`
   - `/d/Xilinx/.../vivado.bat -mode batch -source run_imp.tcl`（要 `=== RUN OK ===`）
5. 从 §5/§6 接着干。
