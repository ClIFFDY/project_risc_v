# RTL 代码风格约束

> **适用范围**：本仓库（`RTL/`）下所有 Verilog 源文件。
> **定位**：协作文档 —— 随 RTL 仓库一起进入 GitHub，写/读代码前先看这一份。
> **配套**：`RTL/逻辑说明` 讲"为什么这么设计"，本文只讲"代码长什么样"。
> **修订**：2026-09-21 首次整理成文（见 §8 整理记录）。

---

## 1. 描述风格

**1.1** 所有逻辑一律用**行为逻辑**描述，统一采用 `if-else` 块；不用数据流表达式承载逻辑。

**1.2** `wire` / `assign` 只允许出现在两处：

- **顶层连线** —— 纯别名、端口对接（`assign a = b;`）
- **部分外设的输入 / 输出** —— 典型是 `itcm` / `dtcm` 把内部寄存的读结果直接引到输出端口
  （`assign mem_data = d_r;`）。这类不承载逻辑的别名保留，不要为"统一行为化"而改写，
  否则会动摇 BRAM 的推断形状。

**1.3** **顶层（`cpu_top`）内不允许进行行为运算。** 凡是要算的中间量，把源信号直接接到消费者模块，在**模块内部**做块内运算。

- 纯别名 / 纯连线不受此限。
- 确实必须在顶层运算的（跨模块共享、或作为多个模块的公共输入），**遇到时单独确认**，不要自行决定。

---

## 2. 写法规范

**2.1** 赋值语句允许 `=`（`<=`）上下对齐 —— **鼓励而非强制**。同一块内相邻赋值尽量对齐；左右长度差异过大、或本来就已经自然齐整时，不强行抠空格。

**2.2** `if-else` 的 `begin` / `end` 对**不允许**上下对齐。

**2.3** 嵌套 `begin-end` **逐层缩进**：内层相对外层缩进一级，`end` 与对应那层 `begin` 所在行的 `if` 对齐。

```verilog
//正例
        else if (exec) begin
            if (stall_w) begin
                r1_data_dec <= bypass(r1_q);
                r2_data_dec <= bypass(r2_q);
            end
            else begin
                r1_data_dec <= 32'd0;
                r2_data_dec <= 32'd0;
                r1_q <= r1;
                r2_q <= r2;
            end
        end
```

---

## 3. if-else 规范

**3.1** 超过单个赋值操作的块，必须使用 `begin` - 换行 - 行为 - 换行 - `end` 风格。

**3.2** 未超过单个赋值操作的，也请换行（不要写成 `if (x) y = 1;` 同一行）。

**3.3** 「停顿 / 保持」类分支统一写成**显式自赋值**，逐条列出 `x <= x;`，**不使用空的 `begin end`**。
理由：一眼看清这拍冻住了哪些信号；也避免"某条输出整个分支都没被赋值"而不声不响保持上一拍。

```verilog
//正例
            else if (stall_w) begin
                r1 <= r1;
                r2 <= r2;
                rd <= rd;
                beq_off_q1 <= beq_off_q1;
            end
```

**典型反例** —— `begin-end` 块必须提行：

```verilog
//反例：begin_end 块与行为挤在同一行
        if (we_alu && rd_alu != 5'd0)      begin am_we = 1'b1; am_rd = rd_alu; am_data = rd_data_alu; end
        else if (we_mul && rd_mul != 5'd0) begin am_we = 1'b1; am_rd = rd_mul; am_data = mul_data_mul; end
        else                               begin am_we = 1'b0; am_rd = 5'd0;   am_data = 32'd0;      end

//正例：单条赋值也换行，多条的块提行
        if (we_alu && rd_alu != 5'd0) begin
            am_we = 1'b1;
            am_rd = rd_alu;
            am_data = rd_data_alu;
        end
        else if (we_mul && rd_mul != 5'd0) begin
            am_we = 1'b1;
            am_rd = rd_mul;
            am_data = mul_data_mul;
        end
        else begin
            am_we = 1'b0;
            am_rd = 5'd0;
            am_data = 32'd0;
        end
```

---

## 4. 换行规范

**4.1** 声明：**同位宽且同性质**的信号应当合并在同一行。

**4.2** 赋值：分号后必须换行（一行一条赋值，不写 `a = 1; b = 2;`）。

**4.3** 位宽字面量写成**目标信号自身的位宽**（`pc_addr <= 32'd0;`、`r1 <= 5'd0;`）；不得出现与信号位宽不符的字面量。

```verilog
        reg exec, flush_w, stall_w;                     //可逗号合并的信号声明
        always @(*) begin                               //必须用 begin_end 块提行的行为块
            flush_w = flag_bus[3] | flag_bus[2];        //允许等于符号对齐
            stall_w = (flag_bus[1] | flag_bus[0]) & ~flush_w;
            exec    = flag_bus[4];
        end

        always @(posedge clk) begin
            if (ld_we_eff)
                regs[rd_ld] <= ld_data_ld;              //单操作 if-else 允许去掉 begin_end 对，进行换行
            if (am_we)                                  //允许 if 块条件后格式对齐
                regs[am_rd] <= am_data;
        end
```

> 单操作的 `if` 去 `begin_end` 对是允许的，但**建议在此类行为较少时保留 `begin_end` 对**。

---

## 5. 注释规范

**5.1** 字数较大的注释，一律写在相对应的 `always` 块**上方**。

**5.2** 字数较少的，可以写在 `if-else` 块的 `if` 上方或其他块上方。

**5.3** **不允许在块内行为代码之间进行注释。**

```verilog
//正例：长注释在 always 上方；短注释在块上方
//读数据进行双写口(alu/ld)旁路仲裁并输出
    always @(posedge clk) begin
        if (rst) begin
            r1_data_dec <= 32'd0;
            r2_data_dec <= 32'd0;
        end
        else if (exec) begin
            if (stall_w) begin
                r1_data_dec <= bypass(r1_q);
                r2_data_dec <= bypass(r2_q);
            end
            else begin
                r1_data_dec <= 32'd0;
                r2_data_dec <= 32'd0;
                r1_q <= r1;
                r2_q <= r2;
                if (dec) begin
                    r1_data_dec <= bypass(r1);
                    r2_data_dec <= bypass(r2);
                end
                else if (lsu) begin
                    r1_data_lsu <= bypass(r1);
                    r2_data_lsu <= bypass(r2);
                end
//mulu 那一份：与 dec/lsu 是【独立通路】，M 指令与普通 ALU 同属 OPCODE_OP（dec 也置 1），
//所以这里用独立的 if 而不是 else if —— 两份各填各的，各自只喂一个消费单元。
                if (mul) begin
                    r1_data_mul <= bypass(r1);
                    r2_data_mul <= bypass(r2);
                end
            end
        end
    end
```

**典型反例**：

```verilog
//冒险：照 lsu 的 load-use 形状
    always @(*) begin
//除法：从"div 还在 mulu 级且尚未发起"那拍起一直停到收尾后。         ##反例：注释写在 always 下方
//d_issued 置起后本项让位给 d_busy/d_done，算完就放行，指令才能离开 mulu 级。
        if (is_div && !d_issued && !bus_hold_in)
            stall = 1'b1;
        else if (d_busy || d_done)
            stall = 1'b1;
//乘法：前一条是 MUL 且当前指令要用它的 rd → 停 1 拍，结果到了就放     ##反例：注释夹在行为之间
        else if (is_mul_post && ((rd_post == r1_post) | (rd_post == r2_post)))
            stall = (mstalled && m_pv) ? 1'b0 : 1'b1;
        else
            stall = 1'b0;
    end
```

> 上例中的说明性注释应上移到 `always @(*)` 上方，或用一句话缩短后放到对应 `if` 上方。

---

## 6. 代码块顺序

```verilog
module(xxx)

localparam xxx;

reg[] / wire[] xxx;

(integer xxx)          //integer 紧邻对应操作区
initial xxx;

(genvar xxx)
function xxx / generate xxx;

//flag_bus 的信号拆分描述块
always @(*) begin
    xxx
end

//下方为主要逻辑
always @(posedge clk) begin
    xxx
end

always @(*) begin
    xxx
end

assign xxx             //如果需要
```

**6.1** 不再被引用的**死代码**（失效的 `localparam`、无人消费的信号）必须删除，不要留在文件里做"备忘"。

---

## 7. 信号声明规范

I/O规范：input在上，output reg / output在中，inout wire在下，同类型且同位宽的信号可合并，且只允许使用以上声明类型
内部信号规范：reg在上，wire在下

按照top文件和submodule文件分开讨论

top文件（部分参考cpu_top）：
模式：层次块-空行-层次块
1.层次块：有流水线层次的，按照信号首生产者的流水线位置进行上下排序
2.其他信号：无空行，按照信号首生产者所在模块的代码位置进行上下排序，单作一块
一般情况下为：流水线层次块-非流水线层次块（核内控制信号和其他信号）-总线层次块（所有的总线信号）
3.块内：按功能视情况调整上下排序，一般为addr-data-使能等其他信号,例如opcode等信号视情况自行安排
示例：

submodule文件：
模式：功能块-空行-功能块（多IO的module如decoder） 或 直接声明
功能块：即按照代码功能进行上下排序（如按照opcode的case类排列不同的addr/data/be）
直接声明：同top文件的3顺序

//由于排序很难设定统一标准，exception太多，固以上仅为参考建议

## 8. 整理记录

2026-09-21 首次把口头约定整理成文，以下条目是本次**新增/明确**的（原稿未涉及）：

| 条目 | 口径 | 出处 |
|---|---|---|
| §1.3 顶层不运算 | 顶层只做连线，带运算的中间量下放到消费者模块内部 | 本次确认 |
| §2.1 对齐力度 | "允许" = 鼓励而非强制 | 本次确认 |
| §2.3 嵌套缩进 | 逐层缩进（原稿 `regfile` 正例是贴平、`pc` 是逐层，现统一为逐层） | 本次确认 |
| §3.3 保持分支 | 显式自赋值，禁用空 `begin end` | 本次确认 |
| §4.3 位宽字面量 | 按目标信号位宽写；现存 `16'd0`/`4'd0`/`30'd0` 等错配一并修正 | 本次确认 |
| §6.1 死代码 | 失效 `localparam` 等直接删 | 本次确认 |
| §7 信号声明规范 | 已落实到 `RTL/core`（端口按 input→output 三组排序；top 声明按流水线块→非流水线块→总线块） | 负责人 |

**保留项**（原稿未写、但沿用的现状约定）：文件头的 Vivado 模板段保留；注释用中文行注释 `//`。

### 2026-09-21 全量落实（RTL/core 逐个文件）

| 项 | 落实口径 |
|---|---|
| §6 块顺序的力度 | **译码块必须是第一个"逻辑块"**；其余组合块与它服务的时序块**按功能就近**，不做全局"clk 全在上、comb 全在下"的类型分组 |
| §6 `localparam` | 必须排在**译码块之前**（`module` → `localparam` → reg/wire 声明 → … → function → 译码块 → 主要逻辑） |
| §6 reg 声明 | 一般不拆散按功能分节的声明组（如 `mulu` 的乘法/除法/输出保持/冒险判定四段） |
| §1.3 顶层不运算 | `cpu_top` 已做到**零运算符**：`d_hold_int` 下放 dcache、`stall_d` 下放 controller、`mul_freeze` 下放 mulu、读数据/ready 合流下放 lsu、`jalr_pred` 下放 pc/icache/controller、forw 的 stall 下放 forw |
| 例化处不得有表达式 | 端口连接只能是信号名或常量（`.softi(1'b0)` 可以） |
| §3.3 保持分支 | 全部改为**逐条显式自赋值**，`decoder` 那处空 `begin end` 已展开 |
| 校验手段 | 每轮 `git diff -w` 复核语义 + `regression.sh` 18/18 + CoreMark 逐位比对 |
