# 方案：双 ALU 顺序多发射（2-wide in-order）

> 定位：**探索与规划**。本文是唯一产物 —— 本轮**没有改动任何 RTL**（`core/*.v`、`soc/*.v`、
> `IO_block/*.v` 全部只读），也没有改动 `当前进度.md` / `逻辑说明` / `代码风格约束.md`。
> 基准：`hfreq_opt` 分支 + 未提交的 `lsu.v`（**只改了注释**，"两节车厢"→"两级"，语义一行未动，
> 见 `git diff core/lsu.v`）。基线数字来自 `当前进度.md`、`逻辑说明`，以及同目录的
> `方案_简易乱序单发射.md`（那篇对本机实现的实测记录，见 §9）。
> 风格：示例代码按 `代码风格约束.md` 写（顶层零运算、注释在块上方、显式自赋值、一行一赋值、
> 位宽按目标信号、`if-else` 不用空 `begin end`、例化处不得有表达式）。

---

## 0. 结论速览

1. **一拍只取一条、只译一条、只算一条**是当前核的**唯一**结构性的瓶颈：`icache` 一拍交付
   **1 条 32 位**（`icache.v:89/199`），寄存器堆 **2 读 2 写**（`regfile.v:26/114-115`），
   `post_decoder`/`alu`/`bju`/`forw`/`lsu`/`mulu` **全是单实例**（`cpu_top.v:191/233/256/290/312/351`）。
   2-wide 不是"加一条通路"，而是**把整条流水从"1 宽"改成"2 宽"**（宽度变、深度不变）。
2. **推荐方案 = lockstep 成对 + 单侧窄副路**：lane0 走**现有全部单实例单元**（一行不改），
   lane1 只允许**单周期 ALU 类（不含 M）**，走新增的 `alu2`；整对作为**一个单位**在流水里推进。
   这样 `lsu.v` / `mulu.v` / `bju.v` / `bra_predict.v` / `pc.v` 的**重定向通路**、
   `csr.v` 的**读写时序**都保持"每拍只看到一条指令"的既有语义。
3. **成对判据（硬规则）**：同一 icache 行内（`word != 31`）+ 命中 + lane0 ∈ {ALU 类非 M、LOAD、
   STORE} + lane1 ∈ {ALU 类非 M} + **对内无 RAW**。CoreMark **贪心不重叠**静态可成对率 **24.0%**
   （收紧成"两条都必须 ALU 类"掉到 19.2%；若为了省一个移位器而禁止 lane1 用移位，掉到 18.1%
   ⇒ **lane1 必须支持移位**）。
4. **收益诚实估计**：稳态每迭代 347,812 拍 → **约 268k~285k 拍**，即 **1.22×~1.30×**，
   CoreMark/MHz 2.87 → **3.5~3.7**。**这是静态统计 + 热循环抽查的外推，不是实测**；
   成对不覆盖访存/分支（占指令 36%），且现有 IPC 已经 0.9155 ⇒ **离"2 倍"很远**，
   天花板就是 `1/(1−成对率)`，而那个上限是 **1.32×**。
5. **最大三处时序风险**：① `icache` 阵列要变成**真双口** —— 本机（E:）器件是 **xc7z010**
   （BRAM 43.5/**60 = 72.5%**），**只剩 16.5 块** ⇒ "复制阵列"这条退路实际不可行，
   **TDP 推断必须成功**（失败就是 §26.3 级别的灾难）；② `pc` 的 `+8` 判据锥；
   ③ `forw` 的优先 mux 由 4 级变 6 级 —— 而**本机实测的十条最差路正是
   `u_post_decoder/r1_data_out_reg → u_lsu/s2_addr_reg`**，`forw` 的输出就是这段的 D 端入边。
6. **建议分 8 个阶段（0~7）**，阶段 1/2/3 都是"**零架构行为改动**"，判据是 **CoreMark 拍数逐位相同**；
   只有阶段 4 才真正打开成对发射。**本方案不需要新增任何流水级**（成对是宽度不是深度）——
   唯一需要新一级的是"拍内前递"，而方案明确选择**不做**（它只值 +5.5pp 成对率，见 §3.3-①）。
7. **与同目录另外两篇方案的关系**（见 §9）：`方案_简易乱序单发射.md` 的结论是"乱序不推荐"
   （收益上界只有 IPC 预算的 8.45%，且三处热循环逐条核对下来是 0）⇒ **先做 2-wide**，且两者
   **不能同时落地**。`方案_机器模式异常处理.md` 则**与本文共享同一件基础设施**
   （它也从另一头独立得出"异常不能再用 `pc − 常数`，要改成随指令携带的 PC 载荷"
   ＝ 本文的 B10）⇒ **本文阶段 3 与它可合并/互让**。

---

## 1. 现状盘点（读代码得出）

### 1.1 一拍能取几条指令

| 事实 | 证据 | 说明 |
| --- | --- | --- |
| `inst_out` 是 **32 位**、一拍交付 **1 条** | `icache.v:43`（`output reg [31:0] inst_out`）+ `icache.v:199`（`else if (rd_en) inst_out <= iram[rd_addr];`） | 一个 `rd_en` 一个地址，就是一个读口 |
| 阵列本身是 **32 位宽** | `icache.v:89`：`(* ram_style = "block" *) reg [31:0] iram [0:12287];` | 48KB = 3 路 × 128 组 × 32 字；**行内有 32 个字，但一拍只交 1 个** |
| 读地址 | `icache.v:81`（`reg [13:0] rd_addr`）+ `icache.v:168`（`rd_addr = {hit_sel, idx, word}`） | way(2)+组(7)+字(5) |
| 回填写口 | `icache.v:207-209`：`iram[{fill_way, fill_idx, fill_cnt}] <= mem_data;` | **一拍 1 个字**，`mem_data` 是 32 位（`itcm.v:35`）；backing 是 64KB 的 `itcm`（`itcm.v:41`） |
| 读/写互斥 | 写：`mem_req = (boot \|\| stage==1'b1) && !fill_end`（`icache.v:145`）⇒ 写 ⟹ `stage==1`；而 `stage==1 && !fill_end` ⟹ `busy=1`（`icache.v:135-139`）⇒ **写期间整条流水被 `icache_busy` 冻住** ⇒ `req_valid=0`（`icache.v:65`）⇒ 取指读口在写期间**空着**；`fill_end` 那拍读口只做一次 `deliver` 读（`icache.v:162-165`） | ⇒ **阵列的口在时序上"读与写不共拍"**，这是后面 TDP 方案的地基 |

**结论**：取指带宽是硬瓶颈。要么让阵列一拍给两个字（TDP），要么加取指缓冲（**但缓冲救不了稳态**：
稳态下取指就是 1 条/拍，缓冲只能抹平气泡，**不能把平均取指率提到 2**）。

### 1.2 寄存器堆：几个读口、几个写口

| 事实 | 证据 |
| --- | --- |
| **2 个读口** | `regfile.v:26`（`input [4:0] r1, r2`）、`:38`（`output reg [31:0] r1_data, r2_data`）、`:86-105`（读数据块） |
| 读地址来源是 **s2 级（pre_decoder 输出）** | `cpu_top.v:386-387`：`.r1(rs1_1), .r2(rs2_1)`（`rs1_1/rs2_1` = `pre_decoder` 输出，`cpu_top.v:151-152`） |
| 读数据是**寄存一拍**后给出 | `regfile.v:93-104`：`if (exec) … r1_data <= bypass(r1)` |
| **2 个物理写口** | 口 A = `alu｜mul`（`regfile.v:115`，仲裁 `:120-129`，优先级 `alu > mul > ld`）；口 B = `ld`（`:114`） |
| 口 A 能合并的依据 | `regfile.v:107-112` 注释：`alu` 与 `mul` 写沿都由**流水线偏移**决定、**严格同偏移永不同拍**；口 B 因 load 写沿由**总线事务长度**决定（多拍）而独占 |
| `regs` 是 **FF 阵列**不是 BRAM | 历史 `WARNING: [Synth 8-4767] … regs_reg … RAM has multiple writes via different ports in same process`（`逻辑说明:2233`）⇒ 32×32 全散成 FF |
| 增加一个写口的历史代价 | `逻辑说明:2311`：3 写口 → 2 写口省了 **1,642 LUT(−12.5%)** ⇒ **一个写口 ≈ 1,600 LUT 量级** |
| 旁路 | `regfile.v:64-71` 的 `bypass()`，与写口**同优先级**（alu > ld） |

### 1.3 执行单元：全单实例

| 单元 | 例化处 | 时序位置 | 备注 |
| --- | --- | --- | --- |
| `pre_decoder` | `cpu_top.v:141` | s2 | 输出 `inst_1 / rs1_1 / rs2_1 / aux_addr_1 / br_pred_taken_1 / jalr_pred_addr_1` |
| `mid_decoder` | `cpu_top.v:161` | s3 | 只寄存"必须跨拍"的：指令字 + 一个立即数 + rd/rs/aux/预测（其余**就地切片/别名**，`mid_decoder.v:205-216`） |
| `post_decoder` | `cpu_top.v:191` | s4 | 二级译码：操作数定型、`rd_back1`、`alu_func4`、`we`、csr_*、`success` 的判定源 |
| `bju` | `cpu_top.v:233` | s4（判定）+ 寄存一拍生效 | 判定源 = `r1_data_3/r2_data_3/alu_func4`（与 alu 同源）；`stallf` 是同一判定的组合版（`bju.v:117`） |
| `alu` | `cpu_top.v:351` | s4（**纯组合，无 clk**，`alu.v` 端口表可见） | |
| `mulu` | `cpu_top.v:290` | 与 lsu 同偏移（`mulu.v:2-27` 文件头） | 乘两级 + 除法 FSM |
| `lsu` | `cpu_top.v:256` | 发级 + 写回级（`lsu.v:88-102`） | 在途读**只许一笔**（`blank_bus`，`lsu.v:156-158`） |
| `forw` | `cpu_top.v:312` | s3（组合） | 见 1.4 |
| `regfile` | `cpu_top.v:382` | s2 读 / s5 写 | |
| `wb_reg` | `cpu_top.v:369` | s5 | |

### 1.4 旁路用的是哪一级的寄存器号

**`forw` 的 r1/r2 = `rs1_2/rs2_2`**（`cpu_top.v:316-317`）—— 即 **s3（`mid_decoder`）输出寄存器**里的
指令的源寄存器号。它把该指令的操作数（`r1_data_in/r2_data_in` = `regfile` 在 s2→s3 沿寄存出来的值）
与四个**在途生产者**比较：

| 优先级（年轻 → 老） | 比较对象 | 生产者所在级 | 证据 |
| --- | --- | --- | --- |
| 1 | `rd_back1` / `result_back1` | **s4**（`post_decoder` 寄存的 rd + `alu` 组合结果） | `forw.v:45-47` |
| 2 | `rd_load` / `ld_data`（`loaded` 门控） | `lsu` 在途 | `forw.v:48-51` |
| 3 | `rd_mul` / `mul_data`（`mul_loaded` 门控） | `mulu` 在途 | `forw.v:53-55` |
| 4 | `rd_back2` / `result_back2` | **s5**（`wb_reg` 输出） | `forw.v:56-58` |

⇒ 现有旁路窗口 = **s4 一条 + s5 一条 + 两个在途单元**。2-wide 后 s4/s5 各变成**两条**，
所以在途源由 4 个变成 **6 个**（`back1_0/back1_1/load/mul/back2_0/back2_1`）—— 见 §2/B7。

### 1.5 控制与"边界地址"

| 事实 | 证据 |
| --- | --- |
| `flag_bus[8:0]` = `{exec, flush_irq, flush_jump, dcache_hold, bus_hold_in, stall_m, stall_v, lsu_stall, icache_busy}` | `controller.v:70`（`当前进度.md:41-43` 同） |
| **逐源停顿**：每个模块各自 `\|\|` 取位，再 `& ~flush_w` | `pre_decoder.v:76`、`mid_decoder.v:146`、`post_decoder.v:76`、`wb_reg.v:47`、`regfile.v:79`、`pc.v:46`、`lsu.v:73`、`csr.v:48` |
| `stallf` **不进 flag_bus**，`bju` 组合直连 `lsu`/`mulu` | `bju.v:117` → `lsu.v:34`、`mulu.v:53`（理由见 `controller.v:65-66`：绕一圈多 0.45ns） |
| `pc` 顺序推进 **+4/拍** | `pc.v:75-76`（`pc_addr <= pc_addr + 4'd4; aux_addr <= pc_addr + 4'd4;`） |
| **中断/mret/trap 的返回地址 = `pc_addr − 字节数`**，字节数由**拍计数**折算 | `csr.v:138-139`（`iret_addr1/2 <= pc_addr_in − irq_bubble`）、`csr.v:129-130`（trap 硬编码 `−4'd12`）、`controller.v:122-126`（`irq_bubble`：复位 12、冲刷 4、否则 `+4` 到 12） |
| `ird_tmr` 是**拍计数**门控（预测型早跳后抑制受理） | `controller.v:128-132`（置 3、每拍 −1） |
| `retire` = "本拍既未冲刷也未停顿且在途访存为空"，`minstret` **每拍 +1** | `csr.v:52`、`csr.v:160` |

⇒ **`pc_addr − 12 = 最老未提交指令` 这个等式只在"一拍前进 4 字节"时成立**。这是全案**语义风险最高**的一处（§2/B10）。

### 1.6 与 2-wide 直接冲突的既有不变量（汇总）

| # | 不变量 | 出处 | 2-wide 下的后果 |
| --- | --- | --- | --- |
| I1 | `inst_out(N) = instr(pc_addr(N) − 4)` | `当前进度.md:39` | 变成"成对后一处前进 8" ⇒ 要重述 |
| I2 | `pc_addr − 12 = 最老未提交指令`（字节 = 拍 × 4） | `csr.v:129/138` | **破**（见 §2/B10） |
| I3 | `rd_back1` / `rd_back2` 各只有一条 | `post_decoder.v:37`、`wb_reg.v:30` | 各需要两条（lane0/lane1） |
| I4 | 口 A = `alu｜mul` 永不同拍 | `regfile.v:107-112`、`逻辑说明:2293` | **保得住**（见 §3.5 的推导），但前提是"mul 所在的包必是单发" |
| I5 | 口 B = `ld` 独占 | 同上 | 要被 lane1 共享 ⇒ 需要写通道让位（§3.5） |
| I6 | `lsu`/`mulu` 的冒险扫描只看**一条**指令的 rs/rd | `lsu.v:193-201`、`mulu.v:373-378` | 要补上 lane1 的 rs/rd（§3.7） |
| I7 | `minstret` 每拍 +1 | `csr.v:160` | 成对拍要 +2 |

---

## 2. 瓶颈排序（按"改起来最难 / 最容易回退时序"从高到低）

### B1 —— 取指带宽：icache 一拍一条（最难，且**必须先解决**）

- **现象**：`icache.v:89/199`，一个读口、一个字。
- **为什么难**：它是**BRAM 阵列**，而 BRAM 的口是有限的（一个 36Kb 块就 2 个口）。想一拍两个地址：
  - **做法甲（推荐）：真双口（TDP）** —— 口 A 只读（lane0），口 B **读（lane1）/ 写（回填）二选一**。
    依据见 §1.1 末行：**读与写在时序上不共拍**，所以把写入和 lane1 读塞进同一个口是**行为等价**的。
    写法必须是"同一 always 块里 `if (we) 写; else 读;`"这一种形状（照 `逻辑说明:2173-2183` §26.3 的教训），
    否则综合器会把 2 读 1 写当成 3 个口 ⇒ **复制整个阵列**。
  - **做法乙（退路）：复制阵列** —— 一份专供 lane1 读。代价 **+12 BRAM**。
    ⚠️ **本机（E: 这台）的器件是 xc7z010，BRAM 只有 60 块**，现状 43.5/60 = **72.5%**
    （见 §5 的资源表与 U1）⇒ 复制后 **55.5/60 = 92.5%**，几乎没有余量留给布局/其它模块。
    **⇒ 做法乙在本机实际上不可行；TDP（做法甲）是唯一路线。**
  - **做法丙（不推荐）：阵列加宽到 64 位** —— 解决不了"**奇数字对齐**的成对"（`{w, w+1}` 在 `w` 为奇数时
    跨两个双字对，仍要两个口），还把回填 FSM（一拍 1 个 `mem_data`）拖进来做 64 位拼装。
- **强制约束**：成对要求 **lane0 与 lane1 在同一行** ⇒ 判据加 `word != 5'd31`（1/32 = 3% 机会损失，
  `icache.v:168` 的 `word` 就是现成的）。**这一条把 lane1 的 tag/命中判定整套省掉**（同 `hit_sel`、同 `idx`，
  lane1 地址 = `{hit_sel, idx, word + 5'd1}`）—— 这是本方案在取指侧最省的一刀。
- **时序方向**：`iram` 多一个读口 → **阵列本身的读延迟不变**（BRAM 的 TDP 是原生的）；
  新增 `inst_next` 一个 32 位寄存器 + 一个 `word+1` 的 5 位加法 ⇒ **↓（有利无弊，只是多一根读地址网）**；
  但**综合推断失败 = 灾难级（§26.3 同形：16KB 掉 LUTRAM 就要 20,330 个 LUT 位点）**，48KB 更是绝无可能，
  所以这一步必须单独做、单独验（阶段 1）。

### B2 —— `pc` 的成对推进锥（新锥，可能成为新 WNS 路）

- **现象**：`pc.v:75-76` 现在是 `pc_addr + 4'd4`。成对要变成 `pc_addr + (pair_v ? 8 : 4)`。
- **为什么难**：`pair_v` 是**组合判据**，它的输入是 icache 的**输出寄存器**（`inst_out`/`inst_next`），
  锥里有：译码 opcode（1~2 级）→ 排除 M（比对 `func10[9:3]`）→ lane0→lane1 的 RAW 比较（5 位比较 ×2）
  → 与非 → 进 `pc` 加法器的**低位**。整条 ≈ **6~7 级 LUT**，且跨 `icache → pc` 一段布线。
- **为什么要注意**：`pc_addr` 同时是 `icache.fetch_addr`（`icache.v:116` `pc_addr >>> 2`）的输入 ——
  **pc ↔ icache 是一对紧耦合的短环**，这里新增的任何延迟都直接落在取指环上。
- **可选做法**：
  - (a) 把 `pair_v` 拆成"**便宜的部分先算**"：`word != 31 && cache_hit && ~busy` 这部分**只依赖 icache 内部
    寄存器**，可以在 icache 里就地生成并**寄存一拍**（`pair_fetch_ok`），与指令字同拍可用；
    真正的译码部分（ALU 类、无 RAW）留在 `pair_form` 模块。
  - (b) `pc` 的加法器只做一次：`pc_addr + (pair_v ? 32'd8 : 32'd4)`，把 mux 放在**进位链最低位**上
    （32 位进位链是 CARRY4 专用链，改 LSB 输入不影响链速度）。
  - (c) 若实测这锥超了：把 `pair_v` 的**判据收紧到"只看 opcode 主类、不做 RAW 比较"**（RAW 交给
    `forw`/停顿网去挡）—— 但那样对内 RAW 就无法排除，会错（见 §3.2 的 R2）；**不推荐**。
- **时序方向**：**↑（回退）**，幅度需实测；这是三处新锥之一。
- **旁证（本机实测）**：`方案_简易乱序单发射.md` 里对本机 `project_risc_v.runs/impl_1`（**7z010**，
  2026-09-24，10ns）的记录是：**十条最差路全部是**
  `u_cpu_top/u_post_decoder/r1_data_out_reg[*] → u_cpu_top/u_lsu/{s2_addr,bus_addr_out}_reg[*]`，
  逻辑 3.9~4.4ns、**布线 6.5~7.7ns（占 60~74%）**。它的 D 端入边就是 `forw` 的输出
  ⇒ **`forw` 的扩展正落在本机最差路的相邻段上**，这条必须按"先收紧约束（15ns）"再比。

### B3 —— 译码/发射的成对携带（工作量大，但每处都是"宽度 +1 份"）

- **现象**：s2/s3/s4/s5 每一级的寄存器都要能装两条。
- **为什么"不难但量不小"**：`09-23` 那次重构（`37abe17`，见 `当前进度.md:125`）把译码字段改成
  **"由指令字就地切片/别名"**（`mid_decoder.v:205-216`）——**这直接把 2-wide 的成本砍掉大半**：
  s3 只要多带**一个 32 位指令字**，lane1 的 `rd/func10/imm/opcode` 全部就地切片得出，**零额外寄存器**。
- **要携带的清单**（估算 ≈ **+400 个 FF**，占 7,394/106,400 的 0.4%，**面积完全不是问题**）：

| 级 | 现有 | 需要为 lane1 增加 |
| --- | --- | --- |
| s1（icache） | `inst_out`(32) | `inst_next`(32) + `pair_fetch_ok`(1) |
| s2（pre_decoder） | `inst_out, r1, r2, aux_addr_out, br_pred_taken_out, jalr_pred_addr_out` | `inst_out1`(32) + `r1_1/r2_1`(10) + `aux_addr1`(32) + `lane1_v`(1) |
| s3（mid_decoder） | 同上 + 立即数/rd/func10 | `inst_out1`(32) + `rs1_1/rs2_1`(10) + `aux_addr1`(32) + `lane1_v`(1) |
| s4（post_decoder + alu2） | 操作数/rd/we/alu_func4/csr_* | lane1 专用：操作数(64) + rd(5) + alu_func4(4) + we(1) + `rd_back1_1`(5) + `lane1_v`(1) |
| s5（wb_reg） | result/rd/we/rd_back2 | `result1`(32) + `rd1`(5) + `we1`(1) + `rd_back2_1`(5) + `lane1_v`(1) |

- **时序方向**：**—（基本持平）**。寄存器翻倍，但**每一级内部的组合逻辑不变**（lane1 走它自己的
  窄通路），最差路不会因为"多带一份"而变长；唯一变化是**扇出/拥塞**，而本设计 LUT 只用了
  **19.48%**（`当前进度.md:118`），余量极大。

### B4 —— 寄存器堆读口：2 → 4（面积最大的一处）

- **现象**：`regfile.v:26/38`，一对 rs1/rs2。2-wide 需要 **4 个读口**（两条指令各 2 个）。
- **为什么贵**：`regs` 是 **FF 阵列**（§1.2），每个读口 = 一个 32:1 的 32 位 mux（≈ 320 LUT）
  **再加一份 `bypass()` 优先 mux**（≈ 200 LUT）。两个新读口 ≈ **+1,000 LUT**。
- **可选做法**：
  - (a) 直接加 2 个读口（简单、直接、推荐首版）。
  - (b) **请求压缩**：lane1 ∈ {`OP-IMM`, `LUI`, `AUIPC`} 时只需要 rs1（rs2 是立即数），
    `LUI/AUIPC` 一个读口都不需要 ⇒ 把 4 个请求压到 3 个物理口。省 ~320 LUT，但**多一层组合与仲裁**，
    收益比不划算，**首版不做**。
- **时序方向**：**↑（回退）**。但注意：**新增的两个读口只服务 lane1，不进 lane0 的锥** ——
  lane0 的 `r1_data/r2_data` 路径一行不变 ⇒ 现有最差路不动。
- **注意**：`r1_data/r2_data` 现在是**一对**输出 + `max_fanout=32` 交给布局复制
  （`regfile.v:36-38`）；2-wide 后变成**四个**输出（`r1_data_0/r2_data_0/r1_data_1/r2_data_1`），
  按消费者拆份的做法保持一致。

### B5 —— 写口：够用，但口 B 会被 lane1 共享（**关键约束**）

- **现状**：口 A = `alu｜mul`（`regfile.v:115`），口 B = `ld`（`:114`）。
- **成对拍需要两个 ALU 结果同时落地** ⇒ 直观上要 **3 个写口**（+1,600 LUT 量级，见 §1.2）。
- **但可以不加口**，推导如下（这是本方案的一个关键发现）：
  1. **口 A 仍然够**：I4（`alu`/`mul` 同偏移）在 lockstep 下**依然成立** ——
     因为按规则 R3/R4，**`mul` 在 lane0 与 lane1 里都被排除** ⇒ 它只会出现在**单发包**里，
     于是"mul 写口那一拍，wb 级里正是 mul 自己"这一点不变，而 mul 自己 `we=0`
     （`post_decoder.v:140-143`）⇒ 口 A 那一拍没有 ALU 竞争者。（**这就是"lane0 也排除 M 类"
     的真正收益** —— 它把 I4 从"需要重新推导"变成"原样成立"。）
  2. **口 B 的另一个用户只有 `ld`**：lane1 恒为 ALU 类、需要一个写口，而口 B 原本只服务 `ld`
     ⇒ 两个来源共用一口，用写通道强制互斥（见下）。**注意 lane0 允许是 LOAD/STORE（规则 C'）**：
     它*自己*的写沿在同一个包之后 1 拍（§3.5 对账），一般不与同包的 lane1 相撞；
     真正的麻烦是**更老的、时刻不定的 load 写**。
  3. **冲突来自"时刻不定的 load 写"**：`ld_we` 由**总线事务长度**决定
     （`逻辑说明:2294`），**无法在发射时预知**（这是本方案最硬的一条物理约束）。
     最坏情形实测可发生：dcache 1 拍应答 ⇒ 两条背靠背 load 可以 **连续两拍**都 `ld_we`
     （`lsu.v:257`：`ld_we = s3_v & ~s3_kind & ready_in`，`s3_done` 让位的下一拍新 load 就能进 s3 并命中）
     ⇒ 写通道至少要 **2 深**。
- **结论（推荐）**：**口 B 由"lane1 结果"与"load 写"共享，用一个 2 深写通道仲裁**：
  - **lane1 优先**（它的结果已经到 wb 级，不能丢）；
  - load 的写被打包进通道排队，在 lane1 不用口 B 的拍落地；
  - 队列**满 ⇒ 回压**（一个停顿位，硬件强制，**不靠"实测没撞过"这种统计** ——
    `逻辑说明:2300` 明确记了"3 口合 1 靠统计、已放弃"的教训）。
- **时序方向**：口 B 多一层来源 mux（**↑ 轻微**，且落在 `bypass` 读侧要多一级比较，**↑**）；
  相比"加第三个写口"方案，**省 ~1,600 LUT**（这是选它的主要原因）。
- **备选（如果实测写通道太绕）**：老老实实加第 3 个写口，`逻辑说明:2313` 显示当年 3 写口版 WNS **反而更好**
  （+0.581 vs +0.505），但那是 20ns 下的数字，**10ns 下必须重测**。

### B6 —— 冒险：lane1 的"在途依赖"

- **现象**：`lsu.v:193-201` 的冒险只扫 `s2_rd` vs `r1_post/r2_post/rd_in`（**一条**当前指令）；
  `mulu.v:373-378` 的 `stall_m` 也只比 `r1_post/r2_post`。
- **为什么必须动它们**：lane1 可能读**在途 load/mul 的 rd**（例：`lw t0,0(a0)` 后面紧跟
  `add t1,x0,x0` 与 `sw t1,4(a1)` 之类形态）—— 现在扫不到 lane1 ⇒ **读到陈旧值**。
- **最小侵入（不重写、只加输入）**：
  - `lsu.v`：加 3 个输入 `r1_post1, r2_post1, rd_in1`，在 `ls_use_hit`/`ls_waw_hit` 各加 2~3 个比较器；
  - `mulu.v`：加 3 个输入，`stall_m` 里加 2 个比较器；
  - **两者的内部结构、车厢/流水级一行不动**。
- **注意"对内 RAW"走另一条路**：`rd(lane0) == rs(lane1)` 时**不允许成对**（在成对判据里排除，
  §3.2/R2），因为 lane0 如果是 load，它的数据要等总线，lane1 同拍取不到。
- **时序方向**：**↑（轻微）**：这几条比较器挂在 `stall`/`flag_bus[1]` 这条**全片广播**上，
  加 2~4 个 5 位比较器对深度影响很小，但广播网扇出敏感（历史：bju 的判定绕 controller 一圈要多 0.45ns，
  `controller.v:65-66`）⇒ **保持"在原地加比较器"、不要把新信号绕进 controller**。

### B7 —— 旁路：4 级优先 → 6 级优先

- **现象**：`forw.v:43-82` 的两条独立 if-else 链，各 4 个源。
- **2-wide 后**：源从 `{back1, load, mul, back2}` 变成 `{back1_1, back1_0, load, mul, back2_1, back2_0}`
  （优先级 = 年轻在前，理由见 `forw.v:42/51-52` 的注释：在途单元按程序序排在 wb 之前），
  **消费者从 2 个（r1/r2）变成 4 个（两条 lane 各 2）** ⇒ 比较器 4×6 = 24 个（现在 2×4 = 8 个）。
- **为什么敏感**：`forw` 的输出 `r1_data_final/r2_data_final` **直接喂 `alu` 的操作数**
  （`cpu_top.v:204-205`、`:362-367`）。当年把 CSR 读提前一拍的收益就是"链头整段消失"
  （`csr.v:186-189`，实测入口网 1.016ns）⇒ 这条锥是**已知的敏感区**。
- **可选做法**：
  - (a) 照现有形状扩展成 6 级链（**推荐**，逻辑直观）；
  - (b) 把 back1/back2 的"同代两条"做成一级（`{rd_0, rd_1}` 两路比较 + 一个 2:1 选择）——
    组合深度从 6 级降到 4 级 mux，但比较器数量不变；**若实测超了，这是第一顺位的手段。**
- **时序方向**：**↑（回退）**，本方案三处新锥之一。

### B8 —— 控制：`flag_bus` 九位逐源停顿的语义

- **现象**：九位是**逐源**的，每个模块自己 `\|\|` 取位（§1.5 表）。一条停顿位会**冻住所有 8 个级**。
- **2-wide 下的语义**：**"一条停顿冻住两条"正是 lockstep 想要的**（一对是一个单位），所以
  `flag_bus` 的**位定义与消费方式一行不用改**。需要检查的是"**停顿该不该同时冻两条**"：
  - `lsu_stall`：由**包里的 lane0**产生（`lsu.v:143` 的 `mem_op` 只看 `opcode`＝lane0 的；lane1 恒为 ALU
    ⇒ 永远不进 lsu）⇒ 一位停顿冻住整包，**语义不变**（B6 给 `lsu` 补的是"lane1 的 rs 参与冒险扫描"，
    不是改变停顿的产生方式）；
  - `stall_m` / `stall_v`：**M 类永不进包**（R3/R4）⇒ 完全原样；
  - `stallf`（`bju` 组合版，`bju.v:117`）：分支/jal/jalr 永不进包 ⇒ 完全原样；
  - `icache_busy`：取指侧，与宽度无关 ⇒ 原样。
- **结论**：**`flag_bus` 本身"零改动"**（位定义、消费者、`controller.v:70` 全不动），
  只是 lane1 的 rs 要补进 `lsu`/`mulu` 的冒险扫描（B6）。
  **但要写进验证清单**：加"成对拍遇 stall/stallf"的定向用例（阶段 4 的 `pair_stall.s`）。
- **时序方向**：**—**。

### B9 —— 分支/jalr 判定（`bju`）与重定向

- **现象**：判定比译码晚一拍（`bju.v:130-161`），结果与落点在那拍寄存；`jp_target` 给 `pc`
  （`cpu_top.v:99`），`br2/br3/jalr_fail` 给 `controller`（`:446-449`）与 `icache`（`:114-119`）。
- **2-wide 下的关键结论**：**因为规则 R3/R4 把控制转移从 lane0 与 lane1 里都排除了，
  分支/jal/jalr 只能出现在"单发包"里**（`lane1_v = 0`）⇒
  - `bju` 的输入 `r1_data_3/r2_data_3/alu_func4/br_flag/jalr_flag` **全部只对应 lane0** ⇒ `bju.v` **一行不改**；
  - `pc` 的四种重定向落点公式（`逻辑说明:519-529`）**一行不改**（只有"顺序"那一行要 +8）；
  - `bra_predict` 的 BHT/BTB 读写索引（`bra_predict.v:50/88-92`、`bju.v:157`）**一行不改** ——
    因为 `pc_addr`/`aux_addr` 语义上始终是**包基地址**，而分支永远是包的 lane0。
  - **`br1`（预测跳）当拍把 `inst_out` 刷成 NOP**（`icache.v:196-200`）⇒ `inst_next` 要**同样刷**，
    否则 `inst_out=0` 配上一条残留的 `inst_next` 可能被误判成对（判据里 lane0 不是 ALU 类会挡住，
    但**显式刷更安全**，且刷 NOP 不花钱）。
- **时序方向**：**↓（有利）**：lane1 不走这条通路，等于"重定向锥不加宽"。

### B10 —— 边界地址：`pc_addr − irq_bubble`（**语义风险最高**）

- **现象**：`csr.v:138-139`（`iret_addr = pc_addr_in − irq_bubble`）、`csr.v:129-130`（trap 硬编码 `−12`）、
  `controller.v:122-126`（`irq_bubble`：复位 12 / 冲刷 4 / 否则 +4 到 12）。
- **为什么破**：这个等式**只在"一拍前进 4 字节"时成立**（"最老未提交指令" = 3 拍前）。
  成对后 pc 一次前进 8 ⇒ "3 拍前"变成"3 个包前"，字节数**随取指宽度变化**。
  **并且 `ird_tmr`（`controller.v:128-132`）也是拍计数**（3 拍门控），它在新结构下"覆盖的指令数"
  变成 6 条 —— **方向上更保守，安全**（门控更严），所以它可以不动。
- **推荐做法：把这个算术彻底删掉，改成"逐级携带包基地址 + 有效位，取最老有效那一级"**
  - 新增 `pack_pc`（32 位）+ `pack_v`（1 位）随包从 s1 一路带下去（与 `aux_addr` 同形，
    但**对所有指令都有效**，不像 `aux_addr_out` 只在 JAL/JALR/BRANCH/AUIPC 才填，`pre_decoder.v:111-113`）；
  - `iret_addr` = **最深的、`pack_v` 仍有效的那个包的 `pack_pc`**（3 级优先级编码，很小）；
  - 冲刷时各级 `pack_v` 清 0 ⇒ **冲刷恢复窗口自动正确**（这就是 `irq_bubble` 的 4→8→12 斜坡在模拟的东西）。
- **时序方向**：**↓（有利）**：`pc_addr_in − irq_bubble` 这条 32 位减法链**整段消失**，
  而它的下游是 `pc` 的重定向 mux（`cpu_top.v:97` → `pc.v:93-96`）—— 这是**净收益**。
- **⚠️ 但这是全案唯一"必须逐拍证明等价"的地方**：`irq_bubble`/trap 的 `−12` 到底对应"第 3 拍"
  还是"第 3 个包"，**光看 RTL 我不敢下结论**（见 §8 不确定清单 U3）。**做法**：阶段 3 **先在 1-wide 下
  实现新机制**，用逐拍 diff 证明它与 `irq_bubble` **逐位等价**，再进阶段 4。

### B11 —— 退役计数（小）

- `csr.v:52` 的 `retire = !(flush_w | stall | mem_inflight)` 与 `csr.v:160` 的 `minstret += 1`：
  成对拍应 **+2**（用 wb 级的 `lane1_v`）。
- **影响面小**：`mcycle`（0xB00，`csr.v:206`）**不动**（它才是 CoreMark 计时用的），
  `minstret` 只在调试用；参考模型 `tools/rv32i_ref.py` 明确不建模 mcycle/minstret
  （`tools/archtest/README.md:60-64`）⇒ **不会破坏 arch 差分**。
- **时序方向**：**—**。

---

## 3. 推荐方案

### 3.1 总体：**lockstep 成对**（宽度变、深度不变）

```
               ┌──── 一个"包"(pack) = {lane0, lane1, lane1_v}，整包同拍过每一级 ────┐
s0  pc ──► s1 icache(2 读口) ──► s2 pre_decoder ──► s3 mid_decoder ──► s4 post_decoder+alu
                                     (+pair_form 判据)                     +alu2 +bju
                                                                            │
                                              s5 wb_reg(+lane1) ──► regfile 口A=lane0 口B=lane1/load
```

- **lane0 = 老的那条**，走**现有全部单实例单元**（`lsu`/`mulu`/`bju`/`post_decoder`/`alu`/csr 通路）——
  这就是"**喂 `lsu`/`mulu` 每拍最多一条**"的落实方式：**它们永远只看到 lane0**。
- **lane1 = 年轻的那条**，只允许**单周期 ALU 类（不含 M）**，走新增的 `alu2` 窄副路。
- **`lane1_v = 0` 时就是今天的机器**（逐位相同的行为）—— 这是"可独立回退"的结构基础。

**为什么是 lockstep 而不是"两条独立发射队列"**：本设计的全部停顿/冲刷语义都挂在
**一个全片广播的不变式**上（"八个流水级同一拍冻结"，`逻辑说明:2285-2287`），
`pc − 4` 恒等式（I1）、`irq_bubble` 的字节折算（I2）、`lsu` 的两级车厢（`lsu.v:88-102`）
全都建立在"各级对齐、每拍前进一位"上。**改深度（加级/解耦）会同时打破 I1/I2 与车厢时序；
改宽度只打破 I2**（已用 B10 的方法单独处理）。所以**宽度是这里唯一划算的维度**。

### 3.2 成对判据（硬规则，逐条列出、缺一不可）

| 代号 | 条件 | 理由 | 依据 |
| --- | --- | --- | --- |
| R1 | `word != 5'd31` | lane1 与 lane0 必须在**同一 icache 行**，才能共用 `hit_sel/idx`、省掉 lane1 的 tag 判定 | `icache.v:168` |
| R2 | `rd(lane0) == 0 \|\| (rd(lane0) != rs1(lane1) && rd(lane0) != rs2(lane1))` | **不做拍内前递** ⇒ 对内 RAW 不成对 | §3.3-① |
| R3 | lane0 ∈ {`OP` 非 M、`OP-IMM` 非 M、`LUI`、`AUIPC`、`LOAD`、`STORE`}（**显式排除 M、控制转移、SYSTEM**） | 排除控制转移/SYSTEM ⇒ `bju`/`bra_predict`/`pc` 重定向与 csr 时序**零改动**；**排除 M ⇒ M 类永远只出现在单发包里**，于是 `mulu` 的"写口沿与 alu 严格同偏移"（I4）与"除法 32 拍冻全流水"两条语义**一行不用重推** | §3.4-②、§2/B5-① |
| R4 | lane1 ∈ {`OP` 非 M、`OP-IMM` 非 M、`LUI`、`AUIPC`} | 副路只复刻 ALU ⇒ 一个 `alu2` 就够；lane1 **不含访存/分支/M/CSR** | 0 |
| R5 | `cache_hit && ~busy && req_valid`（成对也要在非冲刷、非停顿拍生效） | 缺失期 `deliver_ok` 那拍 `cache_hit` 是**陈旧值**（`逻辑说明:579-591` §9 那个坑）⇒ **缺失期一律不成对**，最保守 | `icache.v:130/162` |
| R6 | 允许 lane0 与 lane1 写同一个 `rd`（WAW 对内） | 口 A=lane0、口 B=lane1，靠**语句顺序**保证年轻者赢（§3.5） | `regfile.v:114-115` |
| R7 | **不必**判"M 类是否在 lane1"以外的其它类别 | R4 已排除 | — |

**为什么是这套判据（而不是别的）** —— CoreMark 静态统计（`tools/coremark_v1/_out.lst`，3,403 条，
**这是 `-O2` 那一版**，与 `tools/hex/ins.hex` 的 3,403 行逐行对应）。
**统计口径必须用「贪心不重叠配对」**（一条指令只能属于一个包），不能用"相邻对里有多少对满足条件"
（那会重复计数，把结果抬高 1.5 倍）——本文所有数字都是贪心口径，脚本见附录。

| 判据 | 静态成对率 f（贪心） | 理想加速 `1/(1−f)` |
| --- | --- | --- |
| 两条都必须 ALU 类、无 RAW | 19.2% | 1.24× |
| 两条都必须 ALU 类（**允许拍内前递**，即不做 R2） | 24.4% | 1.32× |
| **本方案：lane0 ∈ {ALU 非 M, LOAD, STORE}、lane1 ∈ ALU 类、无 RAW** | **24.0%** | **1.32×** |
| 同上但 **lane0 含 M 类** | 24.3% | 1.32× |
| 本方案 + 拍内前递 | 29.5% | 1.42× |
| lane1 ∈ ALU 类、**lane0 任意（含分支）**、无 RAW | 33.2% | 1.50× |
| lane0 ∈ ALU 类、lane1 ∈ 含访存（两条都是访存） | — | **不行**：`lsu` 一拍只能吃一条（I4 在途读只许一笔） |

静态构成（同一次统计）：`OP-IMM` 43.9%、`OP` 14.8%、`LOAD` 11.3%、`BRANCH` 9.9%、`STORE` 8.1%、
`JAL` 6.7%、`LUI` 3.3%、`JALR` 1.9%、**M 0.7%**、`SYSTEM` 0.1%。**移位指令占 11.5%** ——
若为了省一个移位器而禁止 lane1 用移位，可成对率会从 24.0% 掉到 **18.1%**（−5.9pp，代价远超收益）⇒
**lane1 必须支持移位，`alu2` 老老实实带一个 32 位移位器**。

**三个"要不要再宽一档"的判断（都读 RTL 得出）**：

- **① lane0 放开 M 类**：只多 **0.3pp**（24.0% → 24.3%），却要让 `mulu` 出现在成对包里 ——
  而 B5-①"口 A 永不同拍"的推导正是建立在"M 永远单发"上。**不值得，排除。**（M 只占指令 0.7%。）
- **② 放弃 R2（做拍内前递）**：多 **5.5pp**（24.0% → 29.5%，理想 1.32× → 1.42×），
  代价是 `alu2` 的输入锥变成"`alu` 输出 → `alu2`"（**ALU 串 ALU**）。在 10ns、本机 WNS 已经
  −1.220ns/103 失败端点（见 U1）的现状下**不可接受**；要做只能给 lane1 的 ALU 加一级，
  那又破坏 `forw` 的 back1/back2 窗口。"**不划算，明确排除**"这个结论比原以为的更硬。
- **③ lane0 放开控制转移（含分支/jal）**：多 **9.2pp**（24.0% → 33.2%，理想 1.32× → 1.50×），
  **而且它并不需要"包内半冲刷"**（我一开始的判断错了，读 RTL 后修正）：
  - `br2`/`br3`/`jalr_fail` 是**权威冲刷**：判定在 s4（`bju.v:114-122`）、寄存后在 s5 生效，
    而那时 **`wb_reg` 里正是这个包自己**（`wb_reg.v:52-75` 那段注释解释了相位）⇒
    **lane1（更年轻）被 `flush_jump` 一起门掉即可**，与今天门掉错路 B+1 是同一件事（多一行）；
  - `br1`/`jal`/`jalr_pred` 是**预判型改向、不冲刷**（`icache.v:196-200` 靠把 `inst_out` 刷成 NOP 挡住错路）
    ⇒ 只要在成对判据里加一条"**lane0 触发预判改向时 `lane1_v=0`**"（条件与 `icache.v:198` 逐字相同：
    `(jalr | br1 | jal) && req_valid`），行为就与今天等价。
  - 但：**被预测为跳的分支必须放弃 lane1**（否则错路指令会推进流水）—— 实测分支预测命中率不低，
    取/不取大约各半 ⇒ **33.2% 要打折到约 27~29%**，真正比 ① 多出来的只有 3~5pp，
    却要把 `bju`/`bra_predict`/`pc` 的重定向相位全部重新评审一遍（`br_pc_idx` 的 BHT/BTB 回写、
    `ird_tmr` 的 3 拍门控、`stallf` 的 2 拍窗）。
  - **判断：不划算，不在本轮。**（若阶段 4/5 跑顺了想要最后 4pp，再单独开一阶段做，风险可隔离。）

### 3.3 三处"不做"的取舍（每条都是时序与正确性的直接交换）

① **不做拍内前递（lane0 → lane1 同一拍）**。
  - 代价：成对率 24.0% → 29.5%（理想 1.32× → 1.42×，**只多 5.5pp**）。
  - 收益：`alu2` 的输入**不经过 lane0 的 ALU 输出** ⇒ **ALU 组合锥不加长**（现在是"取指→…→alu→写口"，
    若做拍内前递就变成"alu→alu→写口"，在 10ns、本机 WNS −1.220ns/103 失败端点的现状下**不可接受**）。
  - 要做的话只有一条路：**给 lane1 的 ALU 加一级**（结果晚一拍），但那就等于"lane1 落后一拍"，
    会破坏 `forw` 的 back1/back2 窗口与"包是一个单位"的前提 ⇒ **明确排除**。

② **不做"包内半冲刷"**（见上）。

③ **不把 lane1 的写口让位做成"统计上不撞"** —— 必须是**硬件强制**（写通道满 ⇒ 回压）。
   依据是 `逻辑说明:2299-2300` 记的教训：3 写口合 1 靠"实测没撞过"、**已放弃**。

### 3.4 模块级改动清单

**新增（2 个文件）** —— ★ 原表里的 `pair_form.v` 已按"模块按级划分"的规则**并入 `pre_decoder.v`**（E2），
不再单独成文件，见 §11。剩下的两个是真正"本级没有归属"的新单元：

| 模块 | 职责 | 位置 |
| --- | --- | --- |
| `core/alu2.v` | lane1 的 ALU。**可以直接例化第二份 `alu.v`**（`jal_flag/jalr_flag/cs_wr_en` 接常量 0，例化处不得有表达式 ⇒ 允许常量） | s4 |
| `core/wb_portb.v` | 口 B 写通道：`lane1 优先 > 上拍被让位的 load 写 > 本拍 load 写`；2 深队列；满 ⇒ 回压位 | s5 → regfile 口 B |

**改动（5 个文件，都是"加"而不是"改"）**

| 模块 | 改动 | 影响面 |
| --- | --- | --- |
| `core/icache.v` | 阵列改**真双口**（口 A 只读 lane0，口 B = lane1 读 / 回填写二选一）；新增 `inst_next`、`pair_fetch_ok`；`inst_next` 与 `inst_out` **同条件刷 NOP**（`icache.v:196-200` 那一处）；lane1 地址 = `{hit_sel, idx, word + 5'd1}` | **最敏感**，必须照 `逻辑说明:2173-2183` §26.3 的"一条写语句"形状写 |
| `core/pre_decoder.v` | 加 lane1 的 `inst_out1/r1_1/r2_1/aux_addr1/lane1_v`（照现有 `:85-132` 的 `exec/flush/stall` 三分支 + **显式自赋值**）；`jal/br_en/jalr` 的组合块**只喂 lane0**，一行不改 | 低 |
| `core/mid_decoder.v` | 加 lane1 的 `inst_out1/rs1_1/rs2_1/aux_addr1/lane1_v`；lane1 的字段**就地切片**（`inst_out1[6:0]` 等），**零额外寄存器** | 低 |
| `core/post_decoder.v` + `core/wb_reg.v` | 加 lane1 的 `rd/func10/操作数/we/result/rd_back1_1/rd_back2_1`（**同一时序块里加一组信号**，stall/flush 自赋值照抄现有形状） | 低 |
| `core/regfile.v` | 读口 2 → 4；`bypass()` 加一级（lane1 最优先）；口 B 的来源改为"写通道"（`we_portb/rd_portb/data_portb`）；**注意 `bypass` 与写口必须保持同优先级**（`regfile.v:62-63` 的老隐患注释） | 中 |
| `core/forw.v` | 4 源 → 6 源、2 消费者 → 4 消费者（§B7） | 中，**新锥之一** |
| `core/csr.v` | `iret_addr` 改用 `pack_pc`/`pack_v`（B10）；`minstret` 成对拍 +2 | 中，**语义最敏感** |
| `core/controller.v` | 写通道回压位并进停顿（加 1 个输入 + 1 个 OR 进 `flag_bus[1]` 的槽）；`irq_bubble` 在 B10 生效后可删（§6.1 要求删死代码） | 低 |
| `core/cpu_top.v` | 只做**连线与例化**（§1.3 顶层零运算）；新增的信号按 §7 声明规范分块 | 低但**易被风格评审打回** |

**一行不改（明确列出，便于评审对照）**：`bju.v`、`bra_predict.v`、`pc.v`（**唯一改动**是顺序分支的
`+4` → `+(pair_v ? 32'd8 : 32'd4)`）、`alu.v`（**只多例化一份**）、`lsu.v`（**只加 3 个输入与 2~3 个比较器**）、
`mulu.v`（**只加 3 个输入与 2 个比较器**；结构、车厢/流水级、提交链一行不动）、
`dcache.v`、`itcm.v`、`dtcm.v`、`soc/*`、`IO_block/*`。

> **`mulu.v` 为什么还要动 2 个比较器**：M 类虽然永远单发，但 **lane1 的 ALU 可以读"在途乘法"的 rd**
> （例：`mul a5,a4,a5` 后面紧跟一条用 `a5` 的成对 ALU）⇒ `stall_m`（`mulu.v:373-378`）不看 lane1
> 就会拿到陈旧值。这与 `lsu.v` 的 `ls_use_hit` 是同一个道理，同一种改法。

### 3.5 写口与 `bypass` 的语句顺序（**这一节的顺序错了就是静默丢写**）

优先级必须与"程序序"一致：**lane1（最年轻）> lane0（口 A）> 通道里的 load（最老）**。

```verilog
//口B 的写来源：lane1 结果 / 被让位下来的 load 写（通道保证二者不共拍）
//语句顺序即优先级（同一 always 块内后写者赢）：lane1 最年轻，必须最后写
    always @(posedge clk) begin
        if (we_portb_ld)   regs[rd_portb_ld]   <= data_portb_ld;
        if (am_we)         regs[am_rd]         <= am_data;
        if (we_l1)         regs[rd_l1]         <= data_l1;
    end
```

> 理由（逐条对账）：`load` 的写沿 = **它自己那个包的 wb 沿 + 1**（`逻辑说明:2294`：与流水线偏移不绑定），
> 所以与它共拍的口 A 写者**一定比它年轻** ⇒ "口 A 赢"是对的（现有代码就是 `:114` 在前、`:115` 在后）。
> `lane1` 与 `lane0` 共拍且 lane1 年轻 ⇒ **lane1 的语句必须在口 A 之后**。

`bypass()` 同步改成同一优先级（照 `regfile.v:64-71` 的形状加一级）：

```verilog
//bypass 与写口用同一优先级（lane1 > 口A(alu|mul) > load）：真撞上时"写进去的"与"前递出去的"是同一个值
    function [31:0] bypass;
        input [4:0] rx;
        begin
            if (we_l1 && rx == rd_l1)                  bypass = data_l1;
            else if (am_we && rx == am_rd)             bypass = am_data;
            else if (we_portb_ld && rx == rd_portb_ld) bypass = data_portb_ld;
            else                                       bypass = regs[rx];
        end
    endfunction
```

### 3.6 成对判据的示例实现（合规写法）

```verilog
//成对判据（缺一不可）：同行 / 命中 / lane0 非控制转移 / lane1 是单周期 ALU 类 / 对内无 RAW
    always @(*) begin
        if (word == 5'd31)         pair_ok = 1'b0;
        else if (!cache_hit)       pair_ok = 1'b0;
        else if (!lane0_issue_ok)  pair_ok = 1'b0;
        else if (!lane1_alu_ok)    pair_ok = 1'b0;
        else if (raw_in_pair)      pair_ok = 1'b0;
        else                       pair_ok = 1'b1;
    end

//对内 RAW：lane0 的写结果当拍不转发给 lane1，故 lane0 的 rd 命中 lane1 任一源寄存器即不成对
    always @(*) begin
        raw_in_pair = 1'b0;
        if (rd0 != 5'd0) begin
            if (rd0 == rs1_l1) raw_in_pair = 1'b1;
            if (rd0 == rs2_l1) raw_in_pair = 1'b1;
        end
    end
```

`pc` 的顺序推进（`pc.v` 内唯一改动）：

```verilog
//顺序取指：成对那拍一次前进 8（lane1 与 lane0 同拍进流水），单发那拍仍是 4
                else begin
                    pc_addr <= pc_addr + (pair_v ? 32'd8 : 32'd4);
                    aux_addr <= pc_addr + (pair_v ? 32'd8 : 32'd4);
                end
```

### 3.7 为什么不选别的（备选对照）

| 备选 | 否决理由 |
| --- | --- |
| **加取指缓冲（fetch queue）替代加宽取指** | 稳态取指就是 1 条/拍，缓冲**提不到 2**；它只能抹平气泡，而气泡只占现有 8.5%（IPC 0.9155）。**单独做它 ≈ 0 收益** |
| **阵列加宽到 64 位** | 解决不了奇数字对齐的成对；且要把 32 位的回填流拼成 64 位写，动 `icache.v` 的**状态机**（最敏感处） |
| **两条独立流水（真正的"双发射"）** | 打破"八级同一拍冻结"这一广播不变式（`逻辑说明:2285-2287`），`lsu` 车厢/`irq_bubble`/`forw` 窗口全部要重推 |
| **lane1 也允许访存** | `lsu` 一拍只能吃一条（在途读只许一笔，`lsu.v:156-158`）；要放宽就得重写 `lsu`，与"不改 `lsu`"的硬约束冲突 |
| **乱序** | 用户明确要求顺序 |
| **第 3 个写口**（不做写通道） | 可行但 **+1,600 LUT/25% FF 面积**（`逻辑说明:2311` 的反推）；本设计面积有余（LUT 19.5%、FF 7.0%），所以这是**并列可行的 Plan B**，只是不如写通道省 |

---

## 4. 收益估算（诚实版）

### 4.1 已知基线（全部来自文档，非本次实测）

| 量 | 值 | 出处 |
| --- | --- | --- |
| CoreMark ITER=1 拍到最后一个字符 | **418,609 拍**（空总线前 417,242） | `当前进度.md:104` |
| CoreMark 稳态每迭代 | **347,812 拍** | `逻辑说明:2200` |
| IPC | **0.9155** | `逻辑说明:2199` |
| CoreMark/MHz | **2.87~2.88** | `逻辑说明:2201`、`当前进度.md:104` |
| crclist / crcmatrix / crcstate | `0xe714 / 0x1fd7 / 0x8e3a`、777 字符 | `当前进度.md:103` |
| 分支误预测（ITER=4 那次跑） | **21,411 次 / 1,493,989 拍 ≈ 1.43%** | `project_risc_v.sim/cm_mispred.log`（★**该日志来自哪个 RTL 版本未核实**，见 U6） |

### 4.2 估算模型与结果

`IPC = 0.9155` ⇒ 每指令 1.0925 拍 ⇒ 非发射气泡占 **8.5%**（约 0.0925 拍/指令）。
成对发射**不会消掉这些气泡**（停顿与误预测照样发生），所以：

```
拍数_新 ≈ N × (1 − f) + 气泡      其中 f = 动态成对率
加速比 ≈ 1.0925 / (1 − f + 0.0925)
```

**先给动态成对率的三个独立锚点**（全部为本次读码/统计所得）：

| 锚点 | 值 | 来源 |
| --- | --- | --- |
| 静态上界（贪心、含 R1/R2 全排除） | **24.0%** | §3.2 的统计 |
| 三处热循环**逐条手算**的贪心成对率 | **14% / 25% / 25%** | `matrix_mul_matrix` 内层（`_out.lst:bf0-c08`，7 条→1 包）<br>`matrix_mul_matrix_bitextract`（`c90-cbc`，12 条→3 包）<br>`core_state_transition`（`1270-127c`，4 条→1 包） |
| 静态"ALU 连续段"的段内上界（兄弟文档独立算的） | 830/3403 = 24.4% | `方案_简易乱序单发射.md` §5.2（**与本文的解析器互为交叉校验，逐位吻合**） |

⇒ **动态成对率取 21%~25%**（静态 24.0% 略高于热循环的 14~25%，因为热循环里 load 成串、
依赖链短；用静态值当上界、热循环值当预警）。

| 动态成对率 f | 稳态每迭代 | 加速比 | CoreMark/MHz |
| --- | --- | --- | --- |
| 0.240（= 静态上界） | 268k | 1.30× | 3.7 |
| **0.21（中性）** | **275k** | **1.25×** | **3.6** |
| 0.18（保守） | 284k | 1.22× | 3.5 |

**⇒ 稳态 268k~285k 拍，1.22×~1.30×，CoreMark/MHz 2.87 → 3.5~3.7。**
（CoreMark ITER=1 的"最后字符 418,609 拍"基线会按同一比例落到 **约 322k~337k 拍**。）

### 4.3 为什么动态一定低于静态 24.0%（必须说清的打折项）

1. **循环体里的携带依赖链**（`lw → add → sw`、指针自增、循环计数器）—— CoreMark 的
   `core_list_join` / `core_matrix` 主体正是这种形状，R2（无 RAW）会把它们切掉。
   §4.2 的三处热循环手算就是这个现象的定量样子。
2. **分支/跳转（18.5%）永远单发，并且会切断其后的相邻关系**。
3. **R1（行末字，约 3%）、R5（缺失期）、写通道回压** 各再吃掉一点点。
4. **访存桩（19.4%）在 lane0 时确实能配一条 ALU**（这正是规则 C' 比"两条都 ALU"多出来的 4.8pp），
   但 `lsu` 占用/停顿拍会浪费包的宽度。

### 4.4 必须说清的"不够好"的一面

- 现有 IPC 已经 **0.9155**，说明核**没有多少浪费可捡**；2-wide 的天花板就是 `1/(1−f)`，
  **拿不到"2 倍"**，因为 36% 的指令（访存+控制转移）根本进不了 lane1。
- 想再往上，只有三条路，**三条都不便宜**：
  - **拍内前递**：+5.5pp（→ 1.42× 理想），`alu2` 的锥变成"alu→alu"，10ns 下不可接受（§3.3-①）。
  - **lane0 放开控制转移**：+9.2pp（→ 1.50× 理想），实测要打折到 ~27-29%，且要重评整条重定向相位
    （§3.3-③）。
  - **lane1 也允许访存**：**不行**，撞 I4（在途读只许一笔）。
- **结论：CoreMark 上 2-wide 的合理预期是 1.22×~1.30×**（乐观上界 1.42×），不是"翻倍"。
  **这一条建议在立项时就写进目标** —— 否则做完发现只快 25% 会很难交代。

---

## 5. 风险与代价（逐处改动 → 时序影响方向）

**资源现状 —— 两台设备的基线不一样，必须先分清（见 U1）**：

| | LUT | FF | BRAM | 备注 |
| --- | --- | --- | --- | --- |
| **本机（E:）** `project_risc_v.xpr` = **xc7z010** | 10,364 / **17,600 = 58.73%** | 7,393 / 35,200 = 21.00% | 43.5 / **60 = 72.5%** | 同版本 RTL 在 10ns 下 **WNS −1.220ns / 103 失败端点 / TNS −45.586ns**（`方案_简易乱序单发射.md` §0） |
| `当前进度.md` §3.5 记的（`G:` 那台）= xc7z020 | 10,364 / 53,200 = 19.48% | 7,394 / 106,400 = 6.95% | 43.5 / 140 = 31.07% | WNS **+0.008ns / 0 失败端点** |

⇒ **两条重要结论**：
1. **"不让 10ns 回退"的基线到底是哪一个，必须先定下来**（本机是 −1.220ns/103 端点，
   根本没有正的余量）。这与 `方案_简易乱序单发射.md` 的 U1 是同一件事。
2. **在本机（z010）上，面积不宽裕**：LUT 已 58.73%，BRAM 已 72.5%（只剩 16.5 块）。
   本文估算的 **+1,500~2,200 LUT** 会把 LUT 推到 **68%~71%**，**BRAM ±0 是硬要求**
   ⇒ 这直接判了"复制 icache 阵列"这条退路的死刑（§2/B1），也把"最长路径 / 拥塞"的风险抬高了
   （`当前进度.md:150`：10ns 的墙是"一捆等长链"，WNS 路 70% 是布线）。

| # | 改动 | 对本级组合锥 | 对 WNS（方向） | 说明 |
| --- | --- | --- | --- | --- |
| 1 | `iram` 加第二读口（TDP） | 阵列读延迟不变 | **—** | 但**推断失败 = 灾难**（掉 LUTRAM 不可能放下）⇒ 阶段 1 单独验 |
| 2 | `inst_next` + `word+1` | — | **↓** | 5 位加法 + 一个 32 位寄存器，只喂寄存器 |
| 3 | `pair_v` → `pc` 的 `+8` | **新锥 ≈ 6~7 级 LUT**，跨 icache→pc | **↑↑** | 三处新锥之一；缓解见 §2/B2(a)(b) |
| 4 | 各级多带一份（+400 FF） | 本级组合不变 | **—** | 只是扇出/拥塞；FF 只用了 21%，余量大（LUT 才是紧的） |
| 5 | regfile 加 2 个读口 | lane1 侧新增 2 个 32:1 mux + 2 份 bypass | **↑** | **lane0 的读路径一行不动** ⇒ 本机最差路（`post_decoder.r1_data_out → lsu.s2_addr`）不受它影响 |
| 6 | `bypass()` 加一级 + 口 B 加一级来源 | 读侧 +1 级优先 | **↑** | |
| 7 | 口 B 写通道（`wb_portb`） | 写侧一层 mux + 一个 2 深队列 | **↑ 轻微** | 相比"加第 3 写口"省 ~1,600 LUT |
| 8 | `forw` 4 源→6 源、2→4 消费者 | **+16 个 5 位比较器、优先链 4→6 级** | **↑↑** | 三处新锥之一；缓解见 §2/B7(b) |
| 9 | `alu2`（第二份 `alu.v`） | **与 `alu` 并列，不串** | **↑ 轻微** | 面积 +~500 LUT（含 32 位移位器） |
| 10 | `lsu.v`/`mulu.v` 各加 2~3 个比较器 | 原有的 `stall` 锥 +1 级 | **↑ 轻微** | 挂全片广播网，建议保持"原地加"，**别绕 controller**（0.45ns 的历史教训，`controller.v:65-66`） |
| 11 | `iret_addr` 改 `pack_pc`（B10） | **删掉一条 32 位减法链** | **↓（有利）** | 下游是 `pc` 重定向 mux |
| 12 | `inst_next` 同条件刷 NOP | — | **—** | 与 `inst_out` 同一个 if-else 串 |
| 13 | `minstret` 成对拍 +2 | — | **—** | 不在任何关键路上 |

**合计**：新增逻辑 ≈ **+1,500~2,200 LUT**（本机 58.73% → **约 68~71%**），FF +400（+1.1%），
**BRAM ±0（硬要求：本机只有 16.5 块余量）**，DSP 0。

**判断（分两种基线说）**：
- **若基线是 z020 那台（WNS +0.008ns、LUT 19.48%）**：三处新锥都不在"一捆等长链"那条最差路上，
  面积余量极大 ⇒ **"必然回退"不成立**，能否守住取决于三处新锥的绝对延迟。
- **若基线是本机 z010（WNS −1.220ns、103 失败端点、LUT 58.73%、BRAM 72.5%）**：
  ① 起点就没有正余量；② `forw` 的扩展落在**实测十条最差路（`u_post_decoder/r1_data_out_reg →
  u_lsu/s2_addr_reg`）的 D 端入边**上；③ LUT 会到 ~70%、BRAM 到 72.5%
  ⇒ **在这台上，"不回退"这个要求与 2-wide 是正面冲突的**，必须先降拥塞/换器件，或者接受回退。

**⇒ 这条是立项前必须与负责人对齐的第一件事**（U1）。另外，若真超了，第一顺位的手段是
**结构性挪拍**（`当前进度.md:150` 的先例：`bju` 挪拍 87.2→93.6MHz 且面积更小），
而不是给组合网加 `max_fanout`（`逻辑说明:2030`：**§22.5 的教训**）。

**哪些改动"必须新增流水级"**：**一处都没有。**
唯一天然需要新一级的是"拍内前递"，而本方案**明确不做**（§3.3-①）。
成对是"宽度"维度，深度不变 ⇒ 这是本方案相对"半乱序重构"（`当前进度.md:142`）**风险低得多的根本原因**。

---

## 6. 验证方法（用现有工具，不新增 tb 也能起步）

### 6.1 阶段 1/2/3（"零架构行为改动"）的判据

| 仪器 | 命令 | 判据 |
| --- | --- | --- |
| 微测回归 **8 项** | `bash tools/run_reg.sh` | 逐项输出与基线**逐字相同**（tim_p / plic / i2c / gpio_group / flash_spi / ahb_interconnect / gpio_irq / i2c_irq）。⚠️ `mt_ldfifo` 已被摘除（`run_reg.sh:43-46` 注释：白盒探针引用的 2 槽 FIFO 随"两节车厢"重写**已不存在**） ⇒ **成对发射后建议按新形状补一条"跨 lane 的 load-use"微测** |
| CoreMark | `bash tools/build_coremark_v1.sh 1 -O2` → `bash tools/run_xsim.sh coremark_new_tb` | **777 字符 + `0xe714/0x1fd7/0x8e3a` + 拍到最后一个字符 = 418,609**（阶段 1-3 要求**拍数逐位相同**） |
| arch 事件流差分 | `tools/archtest/run.py sim --outdir out_b` → `run.py diff out_a out_b` | **0 差异**。事件格式 `W A / W L / W M / S`（`tools/archtest/README.md:17-26`），**按端口内部程序序比对、错拍不算** ⇒ 这正是"多发射没有改变架构可见行为"的**直接证据** |
| arch 真值 | `run.py golden --outdir out_x` | 与 `tools/rv32i_ref.py` 逐事件一致（README:49-58：已覆盖 RV32IM+Zicsr） |
| 实现 | `vivado -mode batch -source run_imp.tcl` | `=== RUN OK ===` + `Setup : 0 Failing Endpoints` + **BRAM 必须仍是 43.5**（推断没掉）；**本机 z010 的 WNS 基线要用阶段 0 的实测值**（U1：别拿 z020 的 +0.008ns 当本机判据） |

⚠️ **跑 CoreMark 前必须重建 hex**：`tools/hex/ins.hex` 是**共享的**，archtest 的 `run.py build`
与回归都会覆盖它（`tools/archtest/README.md:33-35`、`reg-clobbers-shared-hex` 的既有教训）。
**当前 `tools/hex/ins.hex` 正好是 `-O2` 那一版 CoreMark（3,403 行，与 `_out.lst` 逐行对应）**。

### 6.2 阶段 4（真正打开成对）的判据

1. 上述全部 + **CoreMark 拍数必须下降**（这是收益的唯一实测口径）；
2. **`diff`（两棵树）+ `golden`（对参考模型）仍 0 差异** —— 这是"多发射没改变架构可见行为"的
   **核心证明**：成对只改变**什么时候**写，不改变**写了什么、按什么程序序写**
   （事件按端口内部程序序比、错拍不算，`tools/archtest/README.md:17-26`）；
3. 中断闭环：`gpio_irq` / `i2c_irq`（`run_reg.sh:47-53`）+ FreeRTOS 菜单（`rtos_menu_tb.v`）——
   **`irq_bubble`/`iret_addr` 这条路上唯一能咬人的就是它们**；
4. **建议新增 4 条定向程序**（放 `tools/archtest/prog/`，用现有 `run.py program` 或 `run.py run`）：
   - `pair_dep.s`：成对 × 对内 RAW（应**不成对**，事件流必须与 1-wide 一致）；
   - `pair_linend.s`：成对 × 行末字（`word==31` 交叉处）；
   - `pair_ldwb.s`：lane1 的写与 load 的写**同 rd** 相撞（验 §3.5 的语句顺序）；
   - `pair_stall.s`：成对拍遇 `lsu`/`mulu` 停顿（验"一条停顿冻两条"）。
5. **建议新增两个探针**（白盒，照 `mt_ldfifo` 的老做法，但按新结构写）：
   - "写通道滞留 > 1 拍"计数器 —— 断言 §B5 的不变式；
   - "同拍两写同口"检测器 —— 照 `逻辑说明:2307` 的 `tb_mul` 里的检测器扩到 3 个口。

---

## 7. 分阶段实施顺序（每阶段可独立验证、可独立回退）

| 阶段 | 内容 | 验证 | 回退点 |
| --- | --- | --- | --- |
| **0** | **只建基线**：记录 `run_reg.sh` 8 项输出、CoreMark 777 字符/CRC/418,609 拍、archtest `diff` 的 `out_a`、实现报告的 WNS/LUT/BRAM。**把当前未提交的 `lsu.v` 注释改动先提交**（否则回退时会混） | 三连冒烟（`当前进度.md:170-173`） | — |
| **1** | **icache 真双口 + `inst_next` + `pair_fetch_ok`**，但 `lane1_v` 恒 0（新口读出来的字**直接丢弃**） | 回归 8 项 + CoreMark **拍数与 CRC 逐位不变** + 实现：**BRAM 仍 43.5**、WNS 记录 | 单独改 `icache.v`，一个 commit 可回退。**这一阶段的意义就是把"BRAM 推断"这个灾难级风险隔离出来** |
| **2** | **口 B 写通道 `wb_portb.v`**（深度 2 + 回压），来源只有 load（lane1 尚未打开） | 同上（行为应完全不变） | 单独 commit |
| **3** | **边界地址机制替换**（B10：`pack_pc`/`pack_v` 取代 `pc_addr − irq_bubble` 与 trap 的 `−12`），**在 1-wide 下要求逐位等价** | 回归 8 项 + **中断闭环专项** + archtest diff + CoreMark 拍数不变 | 单独 commit。**若这条做不到逐位等价，方案整体要重估** |
| **4** | **打开成对发射**：先只放"**两条都是 ALU 类非 M**"（含移位；对应静态成对率 19.2%） | 回归 + CoreMark CRC + **拍数应下降** + 新增 4 条定向程序 + 事件流 diff 0 差异 | 判据表逐档回退（每档都是 1 个常量表） |
| **5** | 判据放开到**lane0 ∈ {LOAD, STORE} 也可**（对应 24.0%）；**M 类保持永远单发** | 同上 | 同上 |
| **6** | 性能收尾：跑实现，按 WNS 回退量决定是否做 B2/B7 的缓解（判据挪拍、forw 同代合并） | 实现报告 + CoreMark 拍数 | 缓解手段逐条独立 |
| **7** | 文档同步：`逻辑说明` 加一节（照第 9 条纪律）+ `当前进度.md` 更新 | — | — |

**每阶段都必须满足的纪律**（照 `当前进度.md:153-163`）：
先给方案拿点头再动 RTL；先读 `逻辑说明`；结论必须来自实测；判 bug 前先自查测试台；
长跑挂后台不 sleep 轮询；RTL 源文件**不加注释**；`git diff -w` 复核语义。

---

## 8. 不确定清单（**需实测 / 需你确认**）

| # | 事项 | 为什么不确定 | 怎么定 |
| --- | --- | --- | --- |
| **U1** | **器件型号 / 时序基线到底是哪一个** | 两台设备的实现器件不同：`当前进度.md` §3.5（`G:` 那台的 `run_imp.tcl`）是 **7z020**、WNS **+0.008ns / 0 失败端点**；而**本机（E:）`project_risc_v.xpr` 是 `xc7z010clg400-1`**，同版本 RTL 在 10ns 下是 **−1.220ns / 103 失败端点 / TNS −45.586ns**，LUT **58.73%**、BRAM **72.5%**（`方案_简易乱序单发射.md` §0 的实测）。两台的百分比与各自的容量都对得上（43.5/60=72.5% vs 43.5/140=31.07%） | **需你确认**（若本机是 z010，"不让回退"这条硬约束在立项前就要重新谈：本机没有正余量，且 BRAM 只剩 16.5 块 ⇒ 复制阵列不可行、TDP 是唯一路线） |
| **U2** | `alu`/`mul` 的"同偏移永不同拍"在 lockstep 下是否真成立 | 我按 RTL 推导成立（mul 只在单发包里 ⇒ 它写口那一拍 wb 级里正是它自己），但**这是写口 A 能不能不加口的前提** | 用 `逻辑说明:2307` 那个"alu/mul 同拍检测器"在新结构下复测，要求**0 次同拍** |
| **U3** | `irq_bubble` 的 `4/8/12` 与 trap 的 `−12` 到底对应"第 3 拍"还是"第 3 个包"；`pack_pc`/`pack_v` 方案在**冲刷恢复窗口**里是否逐位等价 | 光读 RTL 我**不敢下结论**（`csr.v:129/138` 与 `controller.v:122-126` 是"拍 × 4 字节"的算术，成对后语义变了） | 阶段 3：**先在 1-wide 下实现，与基线逐拍 diff 到逐位相同**，再进阶段 4 |
| **U4** | 写通道的"最长滞留 = 1 拍"这个不变式 | 需要"背靠背两条 load 都 `ld_we` + 两拍都有 lane1 写"三方撞车才能破（我按 `lsu.v:257` 推是可能的） | 加"滞留 > 1 拍"探针；回压位是硬件兜底，但**滞留拍数**必须实测 |
| **U5** | "在途依赖"检查放哪一级最稳 | 放发射拍（s1 判据）是**精确**的（更老的指令在 s1 时都已在流水里），放 s4（扩 `lsu`/`mulu` 扫描）能保住包不散 | 两条都留着（s1 判据 + s4 扫描），实测哪条更省 |
| **U6** | `cm_mispred.log` 的 21,411 次误预测属于**哪个 RTL 版本** | 该日志与 `cm_progress.log` 同目录，后者记的是"程序自锁 @cyc=2642936"（mul 死锁那版的现象，`逻辑说明:2273-2278` 记的坑）⇒ **日志很可能是修复前的** | 需要时重跑 `coremark_mispred_tb` 复核；**不影响 4.2 的估算**（气泡项已单独计） |
| **U7** | CoreMark 的静态统计基于 `-O2` 那一版（3,403 条，`tools/coremark_v1/_out.lst`） | 换编译选项/换 gcc 会让 24.0% 变 | 若正式立项，用**将要固化的那个编译选项**重数一遍 |
| **U8** | `icache` 的 TDP 写法能否被 Vivado 2019.1 认成"单块 BRAM 的两个口" | §26.3 的教训说明这条推断很脆 | 阶段 1 的验收就是它；**本机没有 BRAM 余量可退**（U1），所以这是**生死线** |
| **U9** | **动态成对率**（§4.2 的 21%~25%） | 我只做了**静态贪心统计 + 3 处热循环手算**；没有动态轨迹。想直接测:**本机没有现成仪器** —— `tools/archtest/tb_arch.v` 拿不到 CoreMark（它的 `tohost` = dtcm 字 0，而 CoreMark 的 `.data` 从 dtcm 0 开始，实测**开机即 `ARCH_DONE cyc=0`**；`coremark_new_tb.v` 只统计缺失/误预测） | **需实测**：阶段 4 打开成对后用 CoreMark 拍数反推 `f`；或在 `coremark_new_tb.v` 这类 tb 上加只读探针（`lane1_v` 计数）——**这是收益项唯一的实测口径** |

---

## 9. 与同目录另外两篇方案的关系（三篇是同一批改造的三个方向）

同目录下还有两篇同样"只出方案、未改 RTL"的文档：`方案_简易乱序单发射.md`（乱序单发射）
与 `方案_机器模式异常处理.md`（完整异常语义 + fence）。三篇**必须一起排期**：

### 9.1 与乱序方案：**互斥路线，本文优先**

| | 本文（2-wide 顺序） | 乱序单发射 |
| --- | --- | --- |
| 收益上界 | **1.32×**（静态成对率 24.0%）→ 实测预期 **1.22×~1.30×** | IPC 预算只有 **8.45%**，三处热循环逐条核对下来是 **0**；结论**不推荐** |
| 主要风险 | `icache` TDP 推断（本机 BRAM 无余量）、`pc`/`forw` 两处新锥 | 破坏"八级同一拍冻结"广播不变式、`csr_addr_pre` 污染窗口、判据要改写（同 rd 保序） |
| 是否需要新流水级 | **不需要** | 不需要，但需要**引入本核第一份记分板状态**（tag + ready） |
| 需要的验证改动 | 现有 `archtest` 判据**可直接用**（事件流按端口程序序比 ⇒ 顺序多发射不改程序序） | **必须改判据**（乱序会让写序列成为程序序的置换 ⇒ 正确的机器也会被判 DIVERGE） |

**为什么不建议两个一起做**：
- 乱序方案的地基是"**只有纯 ALU 类进队列，任何非 ALU 指令进 s4 前队列必须排空**"
  （它的 §2.2"排空栅栏"），而本文的推荐判据是"**lane0 允许 LOAD/STORE，lane1 是 ALU**"——
  两者对"访存指令能不能和别的指令同拍推进"的假设**正好相反**。
- 同时落地要重新推导：包与队列的交互、`stallf` 的两拍窗（两个方案都要用它）、
  `regfile` 口 A 的"永不同拍"（乱序方案为它专门加了 `~m_pv & ~d_cmt` 禁止项，
  本文靠"排除 M ⇒ M 永远单发"来保）—— 这三处会互相干扰。

**建议顺序**：**先做本文（2-wide）**——收益高一个量级（1.25× vs 0~1.4%）、
不需要新状态、现有判据能用；做完之后再评估乱序方案是否还有增量
（届时它的"上界 8.45%"里的取指空泡/误预测部分已被 2-wide 改善，收益只会更小）。

### 9.2 与机器模式异常方案：**共享同一件基础设施（本文的 B10）**

那篇有一条第 2 点结论，与本文的 **B10 是同一件事、独立得出、结论一致**，值得单独指出：

> 它的原文：**"异常不能再用『pc − 常数』，必须改用随指令携带的 PC 载荷（`aux_addr`）减 4"**，
> 因为非对齐分支目标的判定点比 trap 再晚一级（E5 ⇒ `pc−20`）。

本文从另一头（2-wide 之后 pc 一次前进 4 **或** 8 ⇒ "拍 × 4 字节"这个折算失效）得出同一个结论：
**`pc_addr − 常数` 这条算术撑不住，要换成"随包携带的 PC 载荷"**。两篇的一致结论是：
**这件基础设施应当单独成阶段、单独验证**（本文阶段 3），**谁先做谁受益**，后做的一方直接复用。

⇒ **排期建议**：如果异常/机器模式那边更早开工，本文可把阶段 3 让给它做（它的 B10 等价物），
本文只要跟一个"逐位不变"的回归即可。

### 9.3 基线不一致的第三种可能

那篇的抬头写"约束 7z020 / 10ns、WNS +0.008ns"（与 `当前进度.md` 一致），
而乱序那篇记录的是本机 `project_risc_v.xpr` = **z010**、WNS **−1.220ns**。
**⇒ 三篇里有两篇的门槛认定不同，U1 必须先钉死，否则"没回退"无法判定。**

---

## 附：本文用到的静态统计的可复现口径

```
# 数据来源（本机现成、无需重建）：tools/coremark_v1/_out.lst（3,403 条，objdump -d）
#   ★ 与 tools/hex/ins.hex 的 3,403 行逐行对应（第一条 00808117 = auipc sp,0x808）
# 分类规则：
#   ALU 类 = opcode ∈ {0110011(OP), 0010011(OP-IMM), 0110111(LUI), 0010111(AUIPC)} 且 funct7 != 0000001
#   M 类  = opcode == 0110011 且 inst[31:25] == 0000001     （与 mulu.v:164 的判据同口径）
#   控制类 = {1100011 BRANCH, 1101111 JAL, 1100111 JALR}；SYS = 1110011
#   RAW  = rd(i) != x0 且 rd(i) ∈ {rs1(i+1), rs2(i+1)}
# 关键：配对率必须用【贪心不重叠】扫一遍（配成一包就跳两步），不能用"相邻对里满足条件的对数"
#       （后者重复计数，会把结果抬高约 1.5 倍；本文初稿曾误用该口径，已全部改为贪心）
# 结果（贪心，包数/3403）：
#   lane0=ALU, lane1=ALU, 无RAW                  652  19.2%
#   lane0 ∈ {ALU,LOAD,STORE}, lane1=ALU, 无RAW    818  24.04%  ← 推荐判据（C'）
#   lane0 ∈ {ALU,LOAD,STORE,M}, lane1=ALU, 无RAW  826  24.27%
#   推荐判据但【禁 lane1 用移位】                 615  18.07%
#   lane1=ALU, lane0 任意, 无RAW                 1129  33.2%
#   （去掉 RAW 检查 = 允许拍内前递的对照）       1005  29.5%
# 交叉校验：直接数"ALU 连续段"sum(floor(L/2)) = 830（段数 670、均值 3.11）
#           ↔ 与 lane0=ALU/lane1=ALU/忽略RAW 的 830 【逐位吻合】，
#           也与 方案_简易乱序单发射.md §5.2 的独立统计（670 段、均值 3.11）吻合
# 指令构成：ALU 类 61.3% / LOAD 11.3% / STORE 8.1% / BRANCH 9.9% / JAL 6.7% / LUI 3.3% /
#           JALR 1.9% / M 0.7% / SYS 0.1%；移位指令 11.5%
```

---

## 10. 阶段 1 实测记录（2026-09-25，`dual-opt` 分支，commit `dac5eec`）

本节是**实测**，用来修正 §2/B1 的两处判断。改造只动 `core/icache.v`。

### 10.1 做了什么

`iram`（48KB = 3 路 × 128 组 × 32 字）从"一个读口 + 一个写口"改成**真双口**：

- 口 A（只读）= 原来的 lane0 取指读（`inst_out` 那条路，一行未动）；
- 口 B = **回填写 / lane1 取指读 二选一**；
- 新增 `rd_addr1 = {hit_sel, idx, word + 5'd1}` 与输出端口 `inst_next`；
- `pair_fetch_ok` **不在本阶段**（它是给成对判据挡锥用的，本阶段没有判据；且往 `inst_out`
  那个块里加赋值有"BRAM 输出寄存器吸不动"的风险 —— 那条路现在 2.45ns），挪到阶段 2/4。

`lane1_v` 恒 0 ⇒ `inst_next` 在 `cpu_top` 里**不接任何东西** ⇒ 全片行为零改动。

### 10.2 ★ 对 §2/B1 的两处修正

**① BRAM 的一个口只有一根地址总线 —— 光"读写写进同一个 always 块"不够。**

§2/B1 做法甲写的是"同一 always 块里 `if (we) 写; else 读;`"。实测：**只做到这一步仍然
Infeasible**。根因是 BRAM 的口只有**一根**地址总线、读写是这根线的时分复用；写成
`if (we) iram[写地址] <= d; else q <= iram[读地址];` 时两个地址不同，综合器会数出
**三个地址**（`rd_addr` / `rd_addr1` / 回填 `{fill_way, fill_idx, fill_cnt}`），判
`WARNING: [Synth 8-6849] Infeasible attribute ram_style = "block"` ⇒ **整个 48KB 阵列掉进
LUTRAM**（实测 `RAMB36 16 → 0`、RAMD 位点 `104 → 17,000`、`LUT 976 → 11,962`），
**而且那条告警不中断流程**。

**必须把两个来源的地址先显式合成一根**（本次的 `portb_addr`），综合器才认。

**② "TDP 是唯一路线 / 复制阵列不可行"要反过来。**

实测 TDP 的块数 = **16 块 RAMB36，与今天的 SDP 完全一样（零增量）** ⇒ 复制阵列（+16 块）
这条退路根本不需要；而"本机 z010 只剩 16.5 块"那个约束在 a100t（余量 91.5 块）上本来也不成立。

### 10.3 ★ 一个会假绿通过的坑

**全片流程里"BRAM 仍 43.5"是 vacuous 判据。** `lane1_v` 恒 0 时 `inst_next` 没有消费者 ⇒
综合把新读口与那个寄存器一起剪掉 ⇒ 阵列退回简单双口 ⇒ **BRAM 数不变、报告全绿，而真双口
根本没被实例化过**（本轮实测确认：全片 BRAM 仍是 43.5）。所以本阶段的验收必须用
**单独综合**：一个 wrapper 把 `inst_next` 引到顶层端口，直接数阵列被综合成几块
（脚本 `tools/_tdp_probe/`，不入 git）。

### 10.4 数字（单独综合：wrapper + `icache.v`，part `xc7a100tfgg676-2`）

| | RAMB36 | RAMB18 | LUTRAM 位点 | LUT | FF |
| --- | --- | --- | --- | --- | --- |
| 改前（SDP） | 16 | 0 | 104（只有 tag 阵） | 976 | 499 |
| 改后（TDP） | **16** | 0 | **104** | 995 | 524 |

**零 BRAM 增量**；LUT +19、FF +25（多一根 14 位地址与一个 32 位读寄存器）。

### 10.5 验证

| 仪器 | 结果 |
| --- | --- |
| `run_reg.sh` | **8/8**，两个闭环的 `@cyc` 与基线**逐位相同**（`gpio_irq @97`、`i2c_irq @7070`） |
| archtest 事件流差分 | **`SAME: 722 events, identical`**（改前/改后两棵树）★ 覆盖范围只到 `tools/hex/ins.hex` 当前那一个程序，**不是 17 个程序的全集** |
| 单独综合 | §10.4（16 块，零 LUTRAM） |
| 全片实现 10ns | WNS **+0.518** / WHS +0.048 / **0 失败端点** / 约束全满足；LUT 10,020（15.80%）、FF 7,390、**BRAM 43.5（不变）**、DSP 4 |

> 全片实现的对照值来自 `a100t_m10`（`hfreq_opt` + lsu 合并，**不含** hresp 删除）：WNS +0.682、
> LUT 10,164、BRAM 43.5。两者差 −0.164ns / −144 LUT：**不是同一棵树**，且幅度落在既有的
> "布局噪声"带（0.2~0.5ns）内 ⇒ 不能当"阶段 1 的回退量"读。**可以读的是**：BRAM 仍是 43.5
> （印证 §10.3 的"剪掉"），10ns 仍 0 失败端点。

### 10.6 给后续阶段的教训

1. **任何往 `iram` 加口/加地址的改动，都必须用单独综合验 BRAM 数**，别信全片报告；
2. **动 `iram` 的口之前先问"地址总线够不够"** —— 一个口一根；
3. `icache` 的 `inst_out` 那个块是"BRAM 输出寄存器吸收"的现场（2.45ns），**能不动就不动**；
4. **"能被综合剪掉"是所有铺垫阶段的通病**：凡是"新逻辑还没有真实消费者"的阶段，
   全片报告都验不出东西。阶段 2（`pair_form`）之所以值得先做，是因为它的产物
   （动态成对率）**在仿真里就能量到**；而 `wb_portb` / 携带组 / `forw` / `alu2` / `regfile`
   读口这些**必须与"打开成对"合并成一个阶段**，否则同样是假绿。

---

## 11. 模块归位（2026-09-26）：`trap_unit` / `pair_form` 并回现有模块

**起因**：这两个模块是**没有自己流水级**的纯组合单元（`trap_unit` 的输入全是 E4/E5 的寄存器源、
生效在 E5 拍；`pair_form` 在 E1→E2 边界），违反了"多级模块按级划分"（参照 `lsu`/`mulu`）。
按规则，它们应当归到**所服务的那一级的模块**里，而不是各自成文件。

| 原模块 | 并入 | 理由 | 结果 |
| --- | --- | --- | --- |
| `core/pair_form.v` | **`pre_decoder.v`**（E2）| 判据就是"把 icache 交来的两个字变成 s2 字段"的输入侧判断；§3.4 本来就让 pre_decoder 携带 lane1 的字段 | 三档输出改成 pre_decoder 的**内部 reg**；顶层三根本来悬空的 wire 消失 |
| `core/trap_unit.v` | **`controller.v`**（全片控制，内含 `csr`）| 它就是"第三类冲刷源"的产生处，与 `flush_irq`/`flush_jump` 并列；并进来后 `exc`/`exc_cause`/`exc_pc`/`exc_tval` **不必再穿顶层**（csr 就在本模块里）| `csr` 例化处**一行未改**（同名，只是从端口变内部）；controller 端口 −4、顶层 wire −4 |

**净结果**：我新增的文件 **2 → 0**；`cpu_top` 少 2 个例化、少 **7 根 wire**。级位表恢复干净：

| 级 | 模块 |
| --- | --- |
| E1 | `icache`（含 `inst_next` / `pair_fetch_ok`）|
| E2 | `pre_decoder`（**含成对判据**）|
| E3 | `mid_decoder`；`forw`（组合）|
| E4 | `post_decoder`、`alu`、`bju`（E5 生效）、`lsu`（E4 发 / E5 写回）、`mulu` |
| E5 | `wb_reg`；`regfile` 写口 |
| 全片 | `controller`（`flag_bus` + **异常仲裁** + `csr`）|

**验证（纯搬家，要求逐位不变 —— 全部通过）**：

| 仪器 | 结果 |
| --- | --- |
| `run_reg.sh` | **8/8**，`@cyc` 与搬家前逐位相同（`gpio_irq @97`、`i2c_irq @7070`）|
| CoreMark ITER=1 | 搬家前/后 `Total ticks` **都是 380,505**、CRC `e714`/`1fd7`/`8e3a`、479 字节 |
| 成对判据统计 | 与搬家前**逐个计数完全相同**（`n_cyc=402506` / `n_inst=359806` / `n_pair=65364`，含四桶归因）|
| a100t 10ns | WNS **+0.550** / WHS +0.048（搬家前 +0.518；差 0.032ns 在布局噪声带内）|

**⚠️ 记录一个操作坑（第二次踩了）**：`run_reg.sh` 会把 `tools/hex/ins.hex` 覆盖成微测程序
⇒ **跑 CoreMark 之前必须 `build_coremark_v1.sh 1 -O2` 重建**（判据：**3,403 行**、首条 `00808117`）。
本次就是因为搬家后先跑了回归、又直接跑 CoreMark 仿真，得到"收到 0 字节 / 最后字符 @cyc=0"的
**假失败**（程序其实没跑）—— 这类"看着像 RTL 坏了、其实是仪器装错了"的情况，要先核 hex。
